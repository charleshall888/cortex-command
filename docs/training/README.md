# Context Is the Game — workshop deck

The v1 presenter deck for the team talk on working well with coding agents.
Requirements: `cortex/requirements/training.md` · Story/talk track: `cortex/research/training-talk-deck-outline.md` (rev 10) · Q&A prep: `cortex/research/training-talk-qa-bank.md` · Messaging record: `cortex/research/training-talk-messaging-brief.md`.

## Running it

```bash
just training     # serves the deck and opens both windows (TRAINING_PORT=8000 by default)
```

Or by hand:

```bash
cd docs/training && python3 -m http.server 8000
```

- `localhost:8000` — the deck (SHARE this window on a call).
- `localhost:8000/presenter.html` — private notes view (KEEP FOCUS here; its arrow keys remote-control the deck over a BroadcastChannel, which is why `http://` is required — `file://` windows can't share a channel).

Controls: **→/space** one beat · **←** back one beat (from a scene's first beat, the previous scene's last) · **b** blank (valves/Q&A) · **Home/End** · URL hash `#8.4` deep-links to section 8, beat 4 (rehearsal/QA).

On a touch screen, **tap** anywhere to advance a beat and tap the **left quarter** to go back one.

Going back reloads the page at the beat before, the same way a map jump does: a beat's animations can't be stopped part-way, so undoing one in place leaves its late steps landing on the wrong beat. The replay runs about 40× fast and out of sight (`lib/warp.js`), so the slide dips and comes back on the finished beat. Deep links to a mid-scene beat land the same way. Mouse clicks are not bound on purpose — clicking the deck window to focus or share it must never skip a beat.

### The map

Press **m** anywhere in the talk (or tap the `N / 18` readout in the corner) for NIGHTLINE's pause screen: the slide sinks under the water, the dock rises out of it, and every section stands in a column over the post it builds. **← →** choose a section, **↑ ↓** choose a beat, **enter** goes, **m** or **esc** resumes. It works from `presenter.html` too, which shows what's chosen in its header. Mouse clicks on a tile go there.

- **The room sees it**, so it never spoils: the dock is drawn only as far as the talk has built it, and sections past the furthest point reached show as a dashed line until you choose one. Opening the deck cold at the title card resets that; deep links keep what was reached.
- **Resume changes nothing** — the scene kept running underneath, so you are back on the exact beat.
- **A jump reloads the page** at `#N.b` behind a fade to night. A scene's beats are async chains that can't be cancelled mid-flight, so a half-played scene you come back to in-page keeps stale pieces; a fresh page is the one landing that is always clean. Jumping to a later beat replays the beats before it, same as a deep link. Browser presentation mode survives the reload.
- Labels come from each section's `data-nav`, columns from `data-nav-group`, the caption from the scene's own headline.

### Where we are

Two small marks keep the room oriented without adding slides or beats, and both follow the map's lighting rule:

- **The act label.** The first scene of each act (the gauge, write it down, lean core, the finale) opens under its post's badge and name. It is bright on beat 0 and settles to faint on the next press, so the headline keeps the stage.
- **The corner dock.** Bottom right: three posts, the post's name, and `N / 18`. A post turns ochre on its waypoint card's beat 1, in step with the card's own dock; a ripple sits under the stretch being built; the plank draws with the finale's. Title through blueprint show the count alone, since the room hasn't met the posts yet, and the dock fades at dawn. Tap it to open the map.

Both read `data-nav-group` — a new act only needs that attribute on its first section.

### The hosted copy

GitHub Pages serves this repo's `main:/docs`, so whatever is on `main` is already live at
<https://charleshall888.github.io/cortex-command/training/> (notes view at `training/presenter.html`).
Every asset path here is relative and nothing assumes a port or scheme, so the hosted copy behaves exactly
like the local server — presenter sync included, since BroadcastChannel needs same-origin http(s) and https
qualifies. Useful for sharing the deck afterwards, or presenting from a machine without a checkout.

Two things to keep in mind:

- **It is unlisted, not private.** Both pages carry `<meta name="robots" content="noindex">`, and the deck is
  absent from `docs/sitemap.xml` and unlinked from the landing page — reachable by URL, invisible to search.
  Leave `docs/robots.txt` alone: a `Disallow` would stop crawlers reading the `noindex` that does the real
  work. Note that `presenter.html` publishes the teleprompter cues along with the deck.
- **A pasted link unfurls.** `index.html` carries Open Graph and Twitter tags pointing at `og-image.png` (1200×630,
  the title card with the three habits), so Slack, Teams and iMessage show a card instead of a bare URL. The tags use
  absolute URLs, as unfurlers require. If the title or the habits change, re-shoot the image to match. Unfurlers cache
  a card for days, so an old card can outlive the change.
- **A push to `main` redeploys mid-talk.** If you present from the hosted copy, freeze pushes for the hour.
  Otherwise run `just training` locally and keep the hosted URL as the share-afterwards link.

## The story (rev 10)

Theme: **the whole game is managing context** — every scene is one of three moves, and the three moves are the section headers, the dock posts, and the retell:

> **keep your context window lean · spawn more agents · keep your workspace clean**

One arc, full circle: the title card sets the world, the parts schematic names the pieces and shows how they connect, the cold open then poses the mystery with that vocabulary already in hand (same model, opposite results, only the context bar differs), the blueprint promises the three pillars, three acts build them (waypoint cards light a post each), and the finale replays Friday with the whole toolkit chained — interview → epic → three tickets → three parallel fresh windows — completing the dock before dawn.

20 sections: 17 scenes + 3 waypoint cards.

| # | Section | Job |
|---|---|---|
| 1 | Context Is the Game | the title card, now a scene of its own. NIGHTLINE named here; the dock arrival (cold load only) and the lobby idle — the jittered ripple and the rare silver glint — live on this scene, which is what the room actually sits and looks at while it fills up |
| 2 | The Parts | the vocabulary scene, and the deck's only schematic — deliberately **not** fishing, so the world starts at the blueprint. One left-to-right chain, each noun named in place: *what you write* (skills · docs · memory) → *the harness* (Claude Code · Cursor · Codex) → *the context window* (introduced here; §3's "started fresh" pane holds it at the same 30%) → *the model* (one machined slab). It now runs BEFORE the cold open, so nothing here is a callback — the window is introduced cold and §3 pays it off two minutes later with two of them side by side. It still builds right-to-left (`#2.1` harness, `#2.2` your files), but for a different reason than it was designed with: not because §3 already taught the window and the model, but because starting from the far end lets every new term be defined against one already on screen. The harness is drawn as a **decision diamond**. **Three ochre strands leave the sheets themselves** — one per item the sub-label names, *skills · docs · memory* — run into the diamond, and only two come out the far side, carrying their own arrowheads into the window. Three in, two out; rejection is never drawn, the count does the work and the room subtracts. Both anonymous connectors either side of the harness are gone: the strands replace the grey files→harness arrow, and the survivors replace the harness→window one, so every line into and out of this object now says which material it is. (Two earlier builds drew discards as grey curves falling from the bottom vertex, and before that a fan starting from nowhere; both were cut — the first on the presenter's call, the second because four rows arriving out of thin air never established *some of what*.) Two earlier builds drew it as a rectangle with rows inside and both failed for one reason: every other object here matches something the room already has a word for (the sheets are papers, the slab a monolith, the window a meter), while a rectangle with lines in it is the residual shape you draw when you haven't decided what the thing *is*. With no object, the choosing had to happen inside a generic frame, so it could only be shown as *state* — lit rows against dim ones — which put the whole message on brightness, the first thing a projector destroys. The diamond is a noun to this room and moves the message into geometry. It also makes the habits bracket load-bearing for free: the two survivors are the only elements that cross `x=402`, the bracket's right edge — so *some of your material leaves your half and reaches the window, and the rest never leaves it*, which is exactly what `#2.5` exists to cut. The old harness→window connector is deleted; the survivors carry their own heads, so "two of the four reached the bar" is drawn rather than implied. Then three causes, one per beat, so every shrink has exactly one author: `#2.3` **caching** (hatching stamps over the front of the window and stops at a drawn edge — reuse is a *prefix* up to a breakpoint, so the tail past it is re-read at full price even when nothing in it changed; the tag reads "cached", matching §5's wording. **This beat carries no caption at all** — the honest statement of prefix caching has too many conditions to survive as one line on a slide (breakpoints, a per-model minimum, a tail re-read at full price), so the picture and the tag carry it and the presenter says the rest. This scene makes no price claim; §5 at `#5.3` keeps the economics ("cheaper — but it still reads all of it") and reuses this same hatch), `#2.4` the model's doing (the slab grows and gains layers, the harness thins in front of it, the window between them does not move), `#2.5` yours (**your habits** close as a bracket and what you wrote gets shorter in the same beat, because they are what shortened it). The habits are drawn as a **bracket spanning *what you write* and *the harness***, labelled *how you use these* — a habit is not a stage in the pipeline and not an artifact in it, it is how you use the two parts that are actually yours, so it spans them both and belongs to neither. Two swapping slots (caption + note line, the note carrying the Opus 5 evidence at `#2.4` and the model hedge at `#2.5`); nothing reflows. 4 labeled objects + 1 bracket + 3 text lines |
| 3 | Friday, 4:00 PM | the mystery, now landing AFTER the vocabulary scene: fresh vs six-hour session, both at 4:00 PM, and the only thing that differs is the bar §2 just named — so it reads as a payoff rather than a cold puzzle. The stage is on screen from beat 0 (the title card no longer shares this scene, so every beat here is one lower than it used to be). The "whole game" thesis is spoken, off-screen |
| 4 | The Whole Game | blueprint: three pillars on three dashed posts |
| 5 | The Gauge | vessel = the bar upright; re-read; the caching beat (`#5.3` — a "this turn" sliver pours, a faster blue sweep stamps hatching over the unchanged prefix, tag "cached"; on-screen claim "cheaper — but it still reads all of it"); zones (goldilocks labeled); exit at 50% |
| 6 | The Squeeze | compaction as a gamble, anchored 2:00 PM; compacts to ~5%; the payoff is the cold open from inside ("attempted after compaction"); bars chain 95→5→58→89→91 |
| 7 | The Death Spiral | one stubborn bug, three laps; the staircase of failed attempts + corrections reddens; bar climbs 58→89 |
| 8 | The Rewind | the overnight-fishing anti-cheat story on one timeline: scope the ask → a branch tries the device clock (travelers can game it — the fork's answer is a cheap "no") → the main lane grinds on server-enforced time (spoofable anyway, 9 red segments) → the unbroken rewind take (money shot: `#8.4`) carries one note home — "single-player — cheating's allowed" → clean branch: device clock, no anti-cheat; a one-line story caption swaps per phase |
| 9 | waypoint | post 1: keep your context window lean |
| 10 | Write It Down | opens on THE CROSSING (`#10.1`–`#10.2`): the first 20% steers the rest, drawn as a night crossing seen from above — plot the bearing to tonight's spot, then two wakes run with identical timing and the unplotted one misses wide ("a small angle at the dock — a wide miss at the spot"); the sea folds away into THE INTERVIEW (`#10.3`–`#10.5`): the cloud in your head → the agent's questions → the requirements doc. The scene ends on the doc, because requirements always come first whichever road follows |
| 11 | Three Roads | "Three roads — choose how to build". The rail forks three ways at `#11.0`, top to bottom most-of-you → least-of-you: **by hand**, **tickets** `/discovery` (the middle road, and the default), **the long run** `/goal` — one chip per road, named as the `#11.6` table names it. Walked middle → bottom → top so it ends on the hand-off to The Trunk. `#11.1` tickets: the doc splits into three stamped tickets (the outer roads fold aside). `#11.2`–`#11.4` the long run, unchanged: the doc + a "done when" check list; five cheaper subagents with fresh windows slow how fast the main agent's pricey 1M window fills, but it still nears full (700k+) and auto-compacts; then the red redirect loop + rework chunks. `#11.5` by hand: the SAME instrument read differently — one window, six short turns, your pin at every turn (the long run had two), a wrong step is one red sliver caught by the next pin, the bar stops near 40%, and a dot of what you know drops into "the project" each turn. `#11.6` all three weighed: the rail steps off (the table's rows are the roads); three bar lengths only with the word on each bar (your effort high/moderate/low vs tokens low/moderate/high), a "pick it when" column, and the line "the more you're in it, the more of what's in your head reaches the project" |
| 12 | The Trunk | "Be most involved where everything else depends on it" — the avoid-slop scene, drawn as a tree: one solid silhouette (bright trunk, tapered limbs, filled green leaves) with ONE reading column on the right — part name, its examples, and at the last beat the road in the examples' slot. `#12.0` the trunk alone: **trunk** · data model · architecture · key product decisions. `#12.1` the canopy grows out of the crown and the leaves land: **limbs** · the catch log · the shop; **leaves** · empty state · sort order · button copy. `#12.2` a bad leaf reddens, drops, regrows — "a bad leaf — redo one ticket". `#12.3` a bad trunk: it reddens, the crack climbs, the red runs up the limbs, every leaf droops — "a bad trunk — everything above inherits it". `#12.4` the canopy dims under one dashed outline, the trunk lights ochre, the leaf and limb rows step back — "know the trunk — not every leaf". `#12.5` each road takes its examples' slot (trunk → by hand, or tickets; limbs → tickets, or the long run; leaves → the long run, or tickets — first-named is the default) + thesis "A healthy trunk leads to healthy branches." The "it's a guide" and leaf-becomes-a-limb caveats are spoken. Never says "lower quality" — leaves are cheap to REDO and pass the same tests |
| 13 | Fresh Eyes | prep beat, then one finding per advance: two overeager swings struck out with a plain "no" (Rust rewrite — "it's already fast enough"; cloud sync + leaderboards — "single-player, by design") and one real crack (the Brass Minnow); judge line at `#13.7` — "it will always find something, you judge what counts" |
| 14 | waypoint | post 2: spawn more agents (proverb "set more lines"; banks write-it-down · fresh-eyes — the economics/parallelism now pays off at the finale, not its own scene) |
| 15 | The Scroll and the Core | exhibit-zoned skill file ratcheting 20 → 6,000 self-contradicting lines; the rent beat (`#15.3`) — the whole file stays on screen and rides in every window; the fix as a second exhibit (the 30-line core + references/) |
| 16 | The Marker Buoys | how the harness's auto-memory fits in, drawn as lit marker buoys moored off the dock — its own notes about the project, one lesson each, one rope (the index) back to the dock; deliberately SPARE (presenter: less stuff everywhere): boxless ONE-line notes mounted in a gap in each staff ("DON'T touch catch_odds.js" — the squeeze chip's warning, re-learned and written down by the agent itself · "catch logic lives in FishManager" · "rares never reach the log"), one label in the whole picture, at most ONE stamp per buoy (✓ keep / ✎ fixed itself / ✗ strike); the §12 seam is the subtitle ("its memory, not its instructions — markers it sets itself"); lamps come up = the index loads every session (`#16.1`, purely visual — the value line is spoken); the agent re-inks its OWN marker mid-task (FishManager → CatchService, `#16.2`); the honest limit is the scene's one on-screen lesson line, landing with deliberately NO visual change ("it tends what it touches — an unread note just sits" — a wrong marker glows as bright as a right one); the added habit: an occasional fresh-eyes pass (one-line terminal, "read every marker back — still true?"; the repo-and-requirements check is spoken) that keeps the warning and cuts the Brass Minnow-bug note loose (`#16.5`; the self-fixed marker needs no second stamp); verdict "What it learns, it keeps. What it keeps, you check." |
| 17 | The Tackle Box | tooling hygiene, drawn as the dock's shared tackle box (pv-3's proverb, finally shown — no bars): your lean skill is a lure → you land a fish (prove it) → the villain staples it onto every rod, even one that "never casts" (forced, rail reddens) → the box's rule "nothing goes in till it's landed a fish" → a crewmate takes it by choice; PR-vs-opt-in fork; verdict "Rent is what you force on the crew. Tackle is what they choose." |
| 18 | waypoint | post 3: keep your workspace clean; the plank waits for Friday |
| 19 | Friday, 4:00 PM — again | the pipeline finale — now the ONLY place the economics lands; the two spent panes slide off at `f3` so folded stubs never pile up under the live trio; results = the centered before/after scorecard alone (1 session/91% red/broke the game vs 4 fresh sessions/peak 18%/3 tickets, `#19.4`) |
| 20 | Tuesday, 9:00 AM | dawn; the dock redrawn with the blueprint's spread-post geometry so the three recap columns align under their posts by construction; the log card lands in the grid cell under the fishing line (tug pays off into it); link line arrives with the recap; the closer ends the talk alone |

Rev-8 → rev-9 changes (the pattern-lens audit — the presenter's five review rounds distilled into a seven-principle lens, three audit agents run against the deck, verified findings applied): four contradictions fixed (both cold-open panes at 4:00 PM; the squeeze anchored 2:00 PM with its payoff revealed as the cold open from inside — bars now chain 95→30→58→89→91 across scenes 4→5→1; finale scorecard "without/with the habits"; scene 11 says "requirements" like scene 8) plus the should-fix list (one name for flip-through everywhere, no auth in the fiction, "wasn't in the room", the Minnow stamp gloss mirroring dawn, "planned last scene" restored, METR hedged as a bet, "instruction files", the dock's one-line intro, "The Scroll and the Core", the skill-size confession defused, the death-spiral interview callback, chart bracket "peaks ≤18%"). Declined by design: an on-screen compaction definition, trio-stagger changes, edits to the thesis or closer. Full rationale in the outline doc.

## Architecture

Hand-authored deck over a shared component library — words live with their presentation; only visuals are componentized (see the scene-schema decision in `cortex/requirements/training.md`, Open Questions).

| File | Role |
|---|---|
| `index.html` | All 20 sections, beat markup via `data-beat`/`data-beats` |
| `lib/deck.js` | Engine: beat navigation, per-scene hooks, promise-chained async beats, dock builder, intent-line flights, night-sky signature, presenter sync |
| `lib/contextbar.js` | The context bar: zone-banded gauge reused in sections 1, 4, 5, 6, 9, 11, 15 |
| `lib/vessel.js` | The context vessel: labeled pours, scan sweep, zones + exit door, pinned chips (squeeze pile, No-loop conflicts) |
| `lib/terminal.js` | Scripted mock-terminal player; `append` continues a session, `snap` lands a punchline instantly |
| `lib/filmstrip.js` | Fork/rewind timeline — deliberately built as the wave-2 Cockpit engine seed |
| `lib/notes.js` | Talk-track cues per scene per beat (feeds presenter.html) |
| `lib/deck.css` | Night edition of the landing-page design language; bundled fonts (offline-safe) |
| `presenter.html` | Synced notes + remote control + timer |

Timing knobs: `CHAR_MS`/`LINE_GAP` (terminal.js), squeeze/sweep durations (vessel.js, incl. the cached sweep's 2200ms in the `sc-gauge` hook), the rewind take (`totalMs`, filmstrip.js), the crossing's draw times (deck.css `.ps-bearing`/`.ps-wake` transitions + the `.ps-b2` reveal delay), context-bar animation `ms` per `set()` call (deck.js hooks), `flyArrow` per-call `ms`, per-hook `setTimeout`s (deck.js), the arrival choreography + lobby-idle periods (deck.css `.arrive` delays; every idle period is incommensurate on purpose), the plink/glint jitter (`schedulePlink`/`scheduleGlint`, deck.js), the dawn sunrise (deck.css `.dawn-glow` transition + `post-light` delays).

## Status

Rev-24 (2026-09-21): **the fork becomes three roads, and a new avoid-slop scene follows it.** Presenter ask: a third option beside tickets and the long run — involved back-and-forth prompting, low tokens / high effort, for unknown ground, landmines and foundations — then a slide on building the foundation by hand ("imagine a tree"). From a six-agent design fan-out (three per slide, each led with a diagnosis). (1) **Write It Down SPLIT**: the structure critic's finding was that the interview played AFTER the fork while one road was folded aside, so a cold viewer read the cloud/questions/doc as the tickets road's property — contradicting "requirements is always first". §10 now ends on the doc (`data-beats` 10→5); the new **§11 Three Roads** owns the fork. 11 beats across the two scenes vs 10 before. (2) **The third road reuses the long run's instrument** (same bar, same pins) so the room compares by eye: two check-in pins vs six, ~86% + auto-compact vs ~40%, a red loop vs one red sliver. A probe-through-unknown-water picture lost: it cannot show tokens and is forced fiction. (3) **The trade is a three-row table with three bar lengths and the word on every bar** — an ordering, not a measurement; a single you↔tokens slider lost because one axis asserts the two are exactly inverse. `.run-trade` deleted. "Less likely to extract what's in your head" is said as "it asks me less, so it guesses more" (cue) and "the more you're in it, the more of what's in your head reaches the project" (screen). "Fewest tokens" is only true if the window stays lean — the cue says so. (4) **§12 The Trunk**: a drawn tree, not the dock posts (they already mean the three habits) and not a box graph (it cannot show leaves falling and regrowing, and drops the presenter's word "trunk"); engineers already say tree/root/leaf. The word "slop" is spoken once, never printed. The finale trap is answered in the cue: "involved doesn't mean I typed it — I made its decisions and read that one line by line". **Renumbering (all sections ≥11 shift +2, 20 sections):** fresh-eyes 11→13, pv-2 12→14, scroll 13→15, buoys 14→16, tackle 15→17, pv-3 16→18, finale 17→19, dawn 18→20. Includes: deck.css `?v=68`, deck.js `?v=55`, notes.js `?v=48`. Validated in the Browser pane: every beat of §11 and §12 plus `#10.5` at 1280×720, last beats at 1024×768 with an overflow assert (0 out of frame), 0 console errors. Owed: presenter eyeball, rehearsal re-time (~+3 min).

Rev-25 (2026-09-21): **a declutter pass on §11 and §12.** Presenter: "a lot going on and spread out across the screen — the cognitive load is too high." Diagnosis: both scenes made the room read in several places at once. §11's rail held seven numbered chips (each road said twice), and its last beat showed the roads a third time as table rows under the rail. §12 had text on all four sides of the tree. Fixes: (1) **one chip per road**, named verbatim as the table names it; the "1 · 2 · 3" numbering is gone here and on §10's chip. (2) **The rail folds away at `#11.6`**, so the table stands alone; its "pick it when" cells are one line each, and the examples moved to the cue. (3) A road that stays on screen while another plays drops to faint, command included. (4) By-hand caption cut to "a wrong step costs one turn" — "the project · gets what you know" already carries the other half next to its picture. (5) **§12 reads in one column**: "you — in every decision here", the top "you don't track every leaf" line, "you can explain this yourself" and "a guide" are all cut from the picture; `#12.4` says it in one caption, and `#12.5` is three road lines plus the closing line (the leaf-becomes-a-limb caveat is spoken). The fallen leaf is swept at `#12.4`. Road line reads "→ by hand" to match the rail. Includes: deck.css `?v=71`, deck.js `?v=56`, notes.js `?v=49`. Validated headless at 1280×720 (every changed beat) and 1024×768 (last beats), 0 console errors. Owed: presenter eyeball.

Rev-26 (2026-09-21): **§12 The Trunk redrawn and cut to one reading column.** Presenter: "better, more foundational examples · way too many words · make the visuals look more professional." (1) **Examples an engineer nods at without the fiction**: trunk = data model · save format · architecture (was the catch-event record · the save format · the size and rarity rule); limbs gain examples (the catch log · the shop); leaves = empty state · sort order · button copy. (2) **Words**: the three "X — N things rest on them" labels became one-word serif part names; the six labels scattered around the tree moved into the column as one example line per part; captions are 5–7 words and only on the three beats that need one; `#12.5` swaps examples for roads in the same slot, so the end state is three names, three roads, the thesis. ~70 on-screen words → ~25. (3) **Picture**: the stick tree (trapezoid trunk, outline ellipses) is now one silhouette — a solid flared trunk, tapered filled limbs and branches, ~95 filled leaves in two greens, a ground line that fades at its ends. The canopy is generated by `lib/gentree.py` (seeded; re-run and splice to change it). State changes ride on solid fills: the bad leaf and the bad trunk turn `--zone-red`, the known trunk turns ochre with a glow. The red "rot" strokes and the × mark are gone (the × rendered as a stray tick on a rotated leaf). Includes: deck.css `?v=74`, deck.js `?v=57`, notes.js `?v=50`. Validated headless at 1280×720 (all six beats) and 1024×768 (last beat); the grow transition checked in real time in the Browser pane; 0 page console errors. Owed: presenter eyeball.

Rev-27 (2026-09-21): **§11 headline and rail.** Headline → "Three roads — choose how to build" (it is not always one doc). While an outer road plays it takes the middle row, so the rail reads as one straight line — the interview, then the road being walked — instead of a diagonal with a faint leftover "tickets" chip. Both bar diagrams drawn larger (`min(80vw, 900px)`); long-run caption cut to "subagents slow the fill — a long run still uses 700k+" (the bar's own "auto-compacted" tag carries the rest); the by-hand result circle no longer inherits the long run's red, and its label sits under it. Same round: a deck-wide pass on AI-sounding copy (gauge, fresh eyes, buoys — see the scene table). Includes: deck.css `?v=76`, deck.js `?v=58`, notes.js `?v=52`. Validated headless at 1280×720 and 1024×768.

Rev-18 (2026-07-27): **§2 rebuilt again — fishing dropped, schematic in.** The presenter's call after seeing rev 17: *"a bit too much… greatly simplify this slide to maybe not be fishing themed at all… explain simply and creatively visually the idea that the harness + models + your skills/docs all fit together."* **The reframe.** Revs 15-17 all tried to carry a vocabulary payload on a fishing object, and each one paid the acquire-the-noun tax rev 16 diagnosed. Rev 18 stops paying it: §2 is now the one plain scene in the deck. That is defensible rather than a seam — §3 is already non-fishing (two terminals and two bars), so the world properly begins at §4's dock blueprint, and §2 sits between them as "here are the pieces, plainly" before the metaphor starts. **The design.** A single left-to-right chain, because "how they fit together" is genuinely a flow and not a stack: *what you write* → *the harness* → *the context window* → *the model*. The reading order is dictated by three arrows, so nothing has to be decoded. Every object carries its own name on a common baseline, and the two named examples the presenter asked for (Claude Code · Cursor · Codex) sit under the harness box. Two moves make it cheap: (1) **it builds right-to-left** — b0 shows only the window and the model, the two objects the cold open already taught, so each later beat defines a new noun against one already on screen; (2) **the window is literally §3's bar**, same track, same green, same 30% the "started fresh" pane held, which turns a callback into free vocabulary. Texture carries the split before a word is read: your files are hand-drawn sheets with visible edges, the model is one machined slab with no seams. **The trend runs across two beats, split on purpose.** `#2.3` is the model's doing — the slab grows and gains strata, and the window in front of it does not move. `#2.4` is yours: **the habit** draws in as an arc running backward over the chain, from the window to your files, and the stack thins from three sheets to one in the same beat. The split is what makes the arc legibly the *cause* of the thinning rather than a coincidence, and it reuses the presenter's own favourite move (the model didn't change; my half did). Nothing goes to zero (the shed sheets stay faint, the deck's standing rule). **Why the habit is an arc and not a fifth box:** everything else on the slide is an artifact in a pipeline; a habit is something you do *to* the pipeline. Inline, it would be a category error. Backward, it is exactly true — you read what is actually in the window, then you go edit what you wrote — and it is the one shape that shows habits and skills interwoven: the files never maintain themselves. It also gives the thesis's word a referent on the picture instead of only in a cue, which is the rev-16 fix applied one level up. §4's blueprint still gets to be the first to *name* the three habits; §2 only shows the loop's shape. **The honest hedge is one line, not a beat** — *"better at long jobs · still priced by the token · still sharper in a clean window"* — because §5 and §6 own cost and degradation in depth, and rev 13's precedent (a whole scene cut for teaching one idea at three altitudes) applies. It points forward instead of teaching. **The thesis was overclaiming and got narrowed.** "The tools change. The habit stays." says habits are fixed; they aren't — they shift as the model improves. The durable thing is one level up: the *job* of managing what's in the window. → **"Habits shift. The game doesn't."**, which is also the cleanest possible handoff, because §4's headline is literally *The whole game is managing context*. "Habit" is still planted for the dawn closer — it now sits on the arc's label, attached to a picture, rather than being coined inside a punchline. **And the claim now has real evidence:** the second line under the picture is a swapping slot, so `#2.3` carries the hedge about the model and `#2.4` replaces it with **"Opus 5 shipped · Claude Code cut ~80% of its own prompt guidance · same job, less of it"** — the presenter's own example, and the strongest possible proof for this exact slide, since the audience uses that tool. Same slot means zero growth. **The ~80% figure is presenter-supplied and unverified here — confirm it before delivery** (it joins the existing human-verified numbers list). **Deliberately cut:** a second, fainter input to the harness ("the code · your ask") that would have prevented the wrong inference that the window holds only your files — correct, but the ask was to *greatly simplify*, so it moved into the b1 cue. **Headline** → **"However good it gets, it only sees what you send"**: the invariant is the mechanism, the takeaway, and the reason the other 15 sections exist, and unlike a trend-shaped title it stays true at every beat. **Thesis** → **"Habits shift. The game doesn't."** (see above). **Renamed** `sc-rig` → `sc-parts`, "The Rig" → "The Parts"; section numbering and all deep links elsewhere are unchanged (still 17 sections). Downstream fix: §13's callback "same as the bobbers coming off" → "that's the stack thinning". Includes: deck.css `?v=35`, deck.js `?v=38`, notes.js `?v=36` — hard-refresh once (**index.html carries no cache-buster — append `?cb=` if a stale build appears**). Validated in real Chrome: all 6 beat states plus deep links, a scripted 17-section/66-beat sweep with **0 console errors and 0 overflows**, a zero-reflow assert across all six beats (stage top and caption box pinned while both text slots swap), cue coverage 17/17. Owed: presenter eyeball, rehearsal re-time.

Rev-17 (2026-07-27): **§2 recast from anatomy to practice**, on the presenter's redirect — *"maybe you set out 3 fishing poles instead of 1, you remove the bobber, you figure out where best to drop the lines before dropping the lines."* The rev-16 waterline build was correct but inert: a labeled diagram in a talk about what to do differently. Verbs teach better than nouns here. **What made the redirect safe:** a structural review objected that §2 must stay the definition scene because 15 later sections depend on the harness/model split — verified false. "harness" appears nowhere in the deck outside §2's own headline and cues, and §2 was built the same day; every later scene was written before it existed. The definitional debt was ~zero, so the anatomy could go. **The design.** Three rods in a fan over a diagonal drop-off — the geometry itself proves deliberation, because three evenly-set rods cannot all reach a diagonal seam, so "I chose where these go" needs no explanation and no second picture. The bobbers hold every hook at one shallow depth; taking them off is what lets the hooks reach the seam at three different depths. One event therefore carries both the disposal claim and the resolution of the whole image. **What was NOT taken from the redirect, and why:** "figure out where to drop the lines" is not given its own beat or its own on-screen claim — §10 The Crossing is built around exactly that reveal (*"the miss was decided back at the dock"*), and stating the thesis 7 sections early would turn §10's discovery into a rerun. §2 *shows* deliberate placement; §10 still gets to *explain* it. **Wording.** The presenter's proposed headline "The model didn't change. The process did." was rejected on register: the deck's vocabulary for what you control is always a thing or a behavior (harness · habit · workspace · tools · memory), never an institutional abstraction, and — decisively — the dawn closer pairs rented *models* against owned *habits*, a thing against a personal behavior, so "process" (institutional register) would force the room to re-translate at the close. Replaced with **"The model didn't change. My half did."**, which reuses a phrase already in the talk track ("the half you actually control"). Thesis **"The gear comes off. The habit stays on."** → **"The rig changes. The habit stays on."** — the old line described only disposal, which covers one of the three actions; "the rig changes" covers all three. Known accepted risk: three rods nine sections before pv-2's proverb "you set more lines" may soften that line through familiarity — judged worth it for the clarity, confirm at rehearsal. Downstream fix: §13's callback "that's the leader coming off" → "same as the bobbers coming off". Includes: deck.css `?v=32`, deck.js `?v=34`, notes.js `?v=33` — hard-refresh once (**index.html carries no cache-buster — append `?cb=` if a stale build appears**). Validated in real Chrome and headless: all 4 beats, 17-section sweep, 0 console errors, 0 overflows at 1280×720, cue coverage 17/17.

Rev-16 (2026-07-27): **§2 The Rig rebuilt for comprehension** after the presenter reported it hard to follow and suspected the fishing metaphor was crowding out the message. A four-angle review (comprehension audit · cold reader · subtraction redesign · wording pass) found the metaphor was mostly a scapegoat: the audit counted **~20 distinct items** (7 gear nouns, 6 mappings, 4 claims, 4 unfamiliar notation grammars) against a slot budget of ~4-5 — a 4-5x overshoot — and apportioned only ~15% of the difficulty to fishing vocabulary. §2 also pays a tax no other scene pays: it is the first scene to build its visual vocabulary from zero, so every beat costs acquire-the-noun AND absorb-the-claim at once. Fixes: (1) **the waterline replaces the brace and the entire callout column** — the boundary already exists in the world, so defining "the harness" as everything above it costs no geometry and no notation to decode; a full-bleed ochre gradient fading upward marks the region without a word (a first pass used a flat rect and read as a UI panel). (2) **One shed, not two** — the wire leader is gone; the single hand-clipped addition is now a **float**, whose purpose is self-evident to a non-angler and whose removal *causes* the hook to settle deep, so one event delivers both the disposal claim and the relief motion. (3) **On-screen elements 14 → 6, beats 5 → 4, printed fishing nouns 6 → 0.** (4) Headline rewritten: "Everybody talks about the model. The rest of it matters just as much." was contrarian-hook cadence pointing at nothing → **"The model didn't change. The harness did."**, which lands the answer to the cold open's mystery and the new term in one breath, and offloads the claim from the picture. (5) **"the same one everybody rents" CUT** — three independent objections: a hidden fourth claim in the scene's densest beat, factually shaky as stated, and a **closer spoiler** (it rehearses the dawn closer's exact rent/own move 33 minutes early). (6) Thesis → **"The gear comes off. The habit stays on."** — keeps "habit" for the dawn payoff but mirrors what is physically happening on stage; and the habit now has a **referent**, named in the b2 cue ("every so often, look at what you're carrying, and take something off") so beat 3 points at something that just occurred rather than coining the word in its own punchline. (7) **The presenter's own "tossed about by the waves / get anchored / go deep" cut** — judged the most clichéd line in the scene, therapy-poster language in a fishing costume, and the one place the metaphor stopped being mechanism and became decoration; payload 3 survives in plain speech at the exit ("one workflow you know cold beats five you keep switching between"). (8) The b1 double-reveal (label swap + brace, five simultaneous changes across two screen regions) is gone — both names now land together, straddling the surface. (9) A standing answer added to the b1 cue for the sharpest heckle — *if the harness excludes the model, what are the next 15 sections managing?* → **"the window is the model's; what's in it is yours."** Includes: deck.css `?v=31`, deck.js `?v=32`, notes.js `?v=31` — hard-refresh once. Validated in real Chrome and headless: all 4 beats, seam into the blueprint, renumbered deep links `#14.2`/`#17.4`, 17-section sweep with 0 console errors and 0 overflows at 1280×720, cue coverage 17/17.

Rev-15 (2026-07-27): **new §2 — The Rig**, the vocabulary scene, inserted between the cold open and the blueprint on the presenter's ask for an intro slide that seeds the concepts and terms the rest of the talk uses. Three payloads, one picture. (1) *The harness matters as much as the model* — an ochre brace gathers the rod & reel ("handed to you") and the tackle ("tied on by you, tonight") and names them **the harness**; the line is labeled "the line" at b0 and re-labeled **the model** at b1, where it lights ochre and is deliberately left OUTSIDE the brace. The two textures carry the split before any label is read: the rod is one clean taper with no knots on it, the tackle is discrete pieces with visible knots. (2) *Machinery is disposable, habits are not* — flat "the harness shrinks" was rejected: it collides with the dawn closer ("everyone rents the same models. The habits are what you own") and with §10/§13/§15, all of which instruct the audience to BUILD things. The surviving claim is narrower and real: a piece of tackle exists to compensate for a specific gap, and when that gap closes it doesn't retire itself — it sits there charging rent. Shown by shedding the wire leader (`#2.2`, "for a fight you couldn't hold on bare line"). Nothing ever sheds to zero — shed gear settles faint on the bottom and stays visible, because "you need no harness" is not the claim. (3) *Get anchored, go deep* — the float comes off (`#2.3`) and, with nothing holding it up, the hook sinks from riding the chop to a still hold at the fish's own depth. This is not a metaphor laid over the picture: float rigs genuinely fish the surface and register every ripple, and dropping the float is genuinely what puts you on the bottom. The presenter's own framing ("tossed about by the waves" vs "anchored where you can go deep") is spoken over it, never printed. Thesis: *"The rig gets simpler. The habit behind it doesn't."* — which plants "habit" 33 minutes before the protected dawn closer pays it off. Chosen from a six-angle creative fan-out (two rounds): round 1 produced a three-band cross-section (honest and precise, but a generic diagram, and its banded fixed-height container visually restates the context bar and vessel), a pulley hoist (good identical-timing trick, but 8 beats and it shed to zero), and an anchor-and-chain (best closer, fake causality — "a better boat needs less anchor" is not seamanship). Round 2, briefed to be more thematic, produced the rig, a post-and-cleat (elegant blueprint-stroke handoff, but nothing ever sheds, so payload 2 had no representation), and a depth sounder (sharpest bracket, and it ruled correctly that ambient light must NEVER stand for model capability — dawn means the habits paid off, not that the model improved; adopted as a standing rule). The sounder's factory-clean-vs-hand-rigged texture contrast was borrowed into the rig. One downstream reinforcement added: §13's fix beat now carries a spoken callback ("half of what got cut was propping up something the model outgrew — that's the leader coming off"), so the habit/machinery split is used twice rather than introduced once and abandoned. **Renumbering (all sections ≥2 shift +1, 17 sections):** blueprint 2→3, gauge 3→4, squeeze 4→5, spiral 5→6, rewind 6→7, pv-1 7→8, write-it-down 8→9, fresh-eyes 9→10, pv-2 10→11, scroll 11→12, buoys 12→13, tackle 13→14, pv-3 14→15, finale 15→16, dawn 16→17 (`#4.3`→`#5.3`, `#6.3`→`#7.3`, `#7.4`→`#8.4`, `#9.1`–`#9.3`→`#10.1`–`#10.3`, `#10.7`→`#11.7`, `#12.3`→`#13.3`, `#13.1`/`#13.2`/`#13.5`→`#14.1`/`#14.2`/`#14.5`, `#16.3`/`#16.4`→`#17.3`/`#17.4`). Includes: deck.css `?v=29`, deck.js `?v=30`, notes.js `?v=30` — hard-refresh once. Validated headless at 1280×720 (real-time Chrome, fresh profile): all five beats screenshotted, per-beat state asserted, deep link `#2.4` confirmed to build the SVG, 0 console errors, no overflow. Re-time owed (~+1 min).

Rev-14 (2026-07-20): the presenter's 14-item review round. (1) Blueprint sub-line → "managing it well hangs on these three posts". (2) NEW caching beat in The Gauge (`data-beats` 4→5): a "this turn" sliver pours, then a faster BLUE sweep stamps hatching over the unchanged prefix ("cached" tag) while still walking the whole tank — on-screen claim "cheaper — but it still reads all of it"; built on vessel.js's latent `cached` sweep after a 3-angle creative fan-out converged on it; invalidation ("touch an early line, re-pay everything after") deliberately left to Q&A — a second idea, and alone it argues for append-only bloat. (3) Death Spiral's "every attempt and every 'no'" caption cut; relapse now verifiable via verbatim-identical Edit summary lines ("same edit as turn 71"). (4) TERMINAL AUDIT: every scripted terminal reduced to summary clauses — no diff hunks anywhere except the one empty-state copy line ('+ "No catches yet. The night is young."', cold-open left + finale ticket 3); cold-open bodies 19em→15em; finale task lines now carry the ticket names verbatim. (5) The Rewind retold as the overnight anti-cheat story (fork = device clock, red run = server-enforced time, note unchanged) with a swapping one-line story caption. (6) NEW crossing intro on Write It Down (`data-beats` 4→7, old beats shift +3): plot-the-bearing → two wakes, identical timing → the miss; folds away (`.railed`) before the rail appears; from a 3-angle fan-out (nav metaphor + motion choreography won; the session-track variant risked restating the filmstrip). (7) Fresh Eyes: "requirements, from an hour ago" caption cut; the two bounced findings replaced with plainly-overeager swings (Rust rewrite / cloud sync + leaderboards) struck through when the "no" lands (red ✗ stamp prefix); the separate manufactured-finding card deleted — the judge line closes the scene. (8) Buoys notes to one line each. (9) Finale: bar jitter fixed (fixed term-body heights 200px/140px so typing can't shove bars; contextbar animates from the painted value; `chains["fin"]` reset on re-entry); empty-state result card cut, scorecard centered alone; the spent finale-rail slides off at `f3` (folded stubs no longer pile up). (10) Dawn rebuilt: dock at 800×210 with blueprint post geometry (centers 1/6·1/2·5/6 of the 0–640 zone), recap columns aligned under posts by an equal-thirds grid (icons cut — blueprint's grid never had them; numbers/subs kept per the rev-10 ruling), rod owns the right bay, log card in the 1fr cell under its line, linkline moved to beat 2, closer alone at beat 3; log-at-top rejected 3-of-3 in the fan-out (detaches the card from the tug). Includes: deck.css `?v=28`, deck.js `?v=29`, notes.js `?v=29`, contextbar/vessel/filmstrip `?v=11`. Re-time owed (crossing + caching beats add ~1 min).

Rev-13 (2026-07-17): **Set More Lines (the economics chart, old §10) cut** — a zoomed-out consolidation of the "spawn more agents" pillar, which was being taught three times: §9 concept → §10 abstract economics → §17 concrete demo. The finale already proves the economics concretely (the before/after scorecard), so the abstract token/clock chart up front just repeated it. Resolution (three passes with the presenter): §9 stays a lean *concept* setup (interview → doc → window-sized tickets; its beat-5 handoff/dependency demo also trimmed), the finale keeps its full end-to-end run as the payoff — now the ONLY place economics lands. Deleted: the sc-lines section markup, its deck.js hook, and `buildEcon()`; the scene's fan-out credibility SCAR ("a fact-checker window caught a claim I'd gotten exactly backwards") rehomed to the Fresh Eyes exit. Retitle: finale "Small tasks, better results" → **"Let's try this again"** with subtitle "one tired window, or a fresh one per ticket" — a wry setup that (unlike the interim "Same model, nothing broke") doesn't spoil the payoff at beat 0; the same-model/nothing-broke bookend still lands via the closing thesis "Nothing here needed a better model". pv-2 banks "write-it-down · set-more-lines · fresh-eyes" → "write-it-down · fresh-eyes" (the proverb "set more lines" stays; parallelism pays off at the finale, not its own scene); §9 exit re-bridges straight into Fresh Eyes. Renumbering (all sections ≥10 shift −1): Fresh Eyes 10→9, pv-2 11→10, Scroll 12→11, Buoys 13→12, Tackle 14→13, pv-3 15→14, finale 16→15, dawn 17→16 (`#14.2`/`#14.5`→`#13.2`/`#13.5`, `#13.3`→`#12.3`, `#11.7`→`#10.7`, `#17.4`→`#16.4`). Dead econ CSS (`.eg-*` / `.econ-*`) stripped from deck.css too. **Finale example fully realigned to §9's catch-log epic** so one concrete example runs cold-open → §9 → finale: the standalone "make Monday's playtest real" header line is gone; both finale routes now take the *cold open's own ask* ("add an empty-state to the catch log"); and the three tickets are §9's — record catch events · flip-through log page · **empty state** (last = the cold-open callback, and the result screen "No catches yet. The night is young." is literally that ticket's output). The one-window run now breaks **biting game-wide** — the exact cold-open failure — and discovery restores it as a side-note (so dawn's "biting again by sunset" still holds). The doc keeps all three learned lessons (single-player · keepsake · never-punish-rare): never-punish-rare + keepsake fly onto the record/log tickets, single-player stays on the doc (it fixed biting, not a catch-log ticket), and the empty-state ticket stamps in place as the original ask (FIN_RED/FIN_A/FIN_C + `flyIntentLines` reworked). Includes now carry deck.css `?v=27`, deck.js `?v=28`, notes.js `?v=28` — hard-refresh once. JS syntax-checked; **not yet browser-validated** (Chrome tools unregistered this session) — owes a headless console+screenshot pass and a re-time (one scene shorter, ~2 min).

Rev-12 (2026-07-16): new scene **The Marker Buoys** (13) added in Act III between The Scroll and the Core and The Tackle Box — how the harness's persistent auto-memory fits in, plus the occasional audit. Presenter's framing rulings, both same-day: (1) EXPLAINER-FIRST — memory is a sophisticated, genuinely self-maintaining system; the audit is the *added* habit, not the headline threat (a first "audit it — nothing sweeps it for you" framing was corrected); (2) the first-build object, a dockside WHAT'S BITING chalkboard, was rejected as OFF-THEME (a chalkboard reads as classroom furniture — it echoes the room, not the world) and replaced via a second 3-angle creative fan-out that converged on **lit marker buoys moored off the dock**: notes floating on the agent's own water, a mooring rope (the index) back to the dock, no wipe affordance at all — markers are set, re-inked, or cut loose one at a time, which is the true mechanics of memory. Facts verified against current docs + literature: the model writes its own notes per project, the index loads into every session, self-maintenance is real but opportunistic (it tends what it touches; no scheduled sweep; a published benchmark has frontier models catching only ~55% of stale memories in passing). On screen: b1 lamps come up (loaded — illumination, never a wipe; purely visual, the value line is spoken) · b2 the agent re-inks its own marker (FishManager → CatchService, its ✎ the buoy's only stamp) · b3 the limit line with deliberately NO visual change (a wrong marker glows as bright as a right one — you can't tell by looking) · b4 the fresh-eyes pass (on-screen terminal: "read every marker back — still true?"; the oracle — the repo AND the requirements — is spoken, because repo-only would have *confirmed* the unfixed Brass Minnow note) · b5 ✓ keep and ✗ strike — the struck marker's rope segment cut, adrift and dark · b6 verdict "What it learns, it keeps. What it keeps, you check." A same-day declutter pass (presenter: "too noisy") cut the note placards (bare text in a staff gap instead), the second keep stamp, the on-screen value lesson line, the terminal's verb line, the ripples, and two of three picture labels. A plain-words pass followed (presenter: "AI-jargony"): "the index · loads into every session" → "picked up first, every session" (also more accurate — only the index loads at startup; note files are read on demand, which is exactly why an unread note just sits), "one job — the audit" → "now and then", "challenge every note" → "read every marker back"; the word "index" no longer appears in the scene. Downstream surfaces: blueprint + dawn pillar-3 sub now "lean instruction files · audited memory · opt-in tooling" (verbatim pair); pv-3 tech line adds "audit its memory"; pv-3 spoken cue adds "a memory you audit"; §13 exit reworked; the finale's instruction-file line untouched (deliberately distinct mechanism); Q&A bank gains #15. Renumbering: Tackle Box 13→14, waypoint 14→15, finale 15→16 (`#16.3`/`#16.4`→`#17.3`/`#17.4`), dawn 16→17. Includes now carry deck.css `?v=19`, deck.js `?v=20`, notes.js `?v=21` — hard-refresh once. Re-time at rehearsal (one scene added, ~2 min).

Rev-11 (2026-07-14), the presenter's seventh review: the three posts renamed to name the move in the reader's words — **keep your context window lean · spawn more agents · keep your workspace clean** — each now aligned with its own proverb (cut-and-retie / set-more-lines / take-care-of-your-tackle). Act II ("spawn more agents") absorbs Set More Lines + Fresh Eyes, so pv-2 closes the act like the other two waypoints ("write the direction down" is now post 2's sub-line). New scene **The Tackle Box** (13) added in Act III — tooling hygiene the scroll doesn't cover: prove a skill on your own work, then share by kind (project-specific → PR; generic → opt-in kit), never force it. Drawn as the dock's shared tackle box (the object pv-3's proverb names but the deck never drew — no bars): your skill is a lure, you land a fish to prove it, the villain staples it onto every rod (even one that never casts — the rail reddens), the box's rule is "nothing goes in till it's landed a fish," and a crewmate takes it by choice. Verdict "Rent is what you force on the crew. Tackle is what they choose." A first crew-of-five-bars draft was cut after a critique/redesign fan-out (six creative + two critical agents) found it overbuilt and restating scene 12's picture; the tackle box unifies prove-first + opt-in into one house rule and cashes pv-3's own noun. The cold-open thesis moved off-screen (spoken only). Direct fixes: squeeze pane-tag → "attempted after compaction"; the "↑ the request from the open" caption cut; empty-state no longer listed twice in the Write-It-Down handoff; pipe-rail spacing under that title relaxed. Validated headless (fresh-profile Chrome): 0 console errors, screenshots of every changed scene (no overflow). Includes now carry `?v=14` (deck.css, deck.js, notes.js) — hard-refresh once. Rehearsal-with-a-human still owed (re-time: one scene added).

Rev-10 rebuild (2026-07-13), the presenter's sixth review: anatomy card labeled as the empty-state ticket's zoom (it read as a fourth ticket); token bills grounded (~4M vs ~500k — the eighth ratio holds); compaction lands at ~5% everywhere (squeeze bar, wedge cliffs); the finale scorecard drops " by 5:58"; the dawn recap wears the blueprint's 1·2·3 numbering; all script/CSS includes carry `?v=N` cache-busters — hard-refresh (Cmd+Shift+R) once, then stale builds can't recur. Validated headless: console-clean on all 15 sections, CDP real-time end-state assertions, screenshots at 1280×720 (no overflow).

Bookend animation layer (2026-07-14), built from a 3-creative-angle + 2-research-agent fan-out: the **arrival** — on a cold load of `/`, the dock draws itself in, the line drops, and the title surfaces on the frame the ripple blooms (~7s; plays once — deep links and re-visits land on the finished card); the **lobby idle** — jittered ripple plinks, incommensurate star/moon/sway/shimmer periods, and a rare silver moon-glint across the title, so five pre-talk minutes never visibly repeat, all compositor-cheap CSS so it never fights the call encoder; the **dawn** — the 2.4s glow becomes an 8s sunrise (`linear()` curve), stars go out one by one, the posts take first light in sequence, the line tugs as the Brass Minnow's page lands, and the closer arrives by the title's exact glint gesture in gold (the first and last words of the talk lit by the same light). `prefers-reduced-motion` collapses everything to the static card via the existing global kill-switch; deck.css/deck.js are now `?v=11`. Validated headless same day: 20 assertions + all-scene console sweep, all green. Not yet done:

1. **Real-time rehearsal with a human eyeball** — notes.js was redistributed again and needs a read-aloud pass; live checks: the Death Spiral's relapse snap (`#7.3`), the rewind take (`#8.4`), the buoys' self-fix and verdict cadence (`#16.2`, `#16.5`), the finale's fold cadence and triple parallel typing (`#19.3`), and a stopwatch pass (~34.5 min on paper after cutting Set More Lines).
2. **Q&A prep dependencies** (see qa-bank): real cost numbers for the making-of story (appendix slide), a real merged-PR trail to show, DPA verification before the compliance answer, the finale-timestamps question ("is 4:19-to-5:58 realistic for three tickets?"), and the presenter's REAL skill-file line count for the scroll confession. Economics now lives only in the finale scorecard (1 session/91% red vs 4 fresh sessions/peak 18%): be ready to defend that the rent gap is window-size × turns — the figures are staged, the ratio is what daily use looks like.
3. Volatile content: the `/requirements`, `/discovery` and `/goal` chips on the Write-It-Down rail carry `<!-- volatile -->` markers; quarterly sweep per `cortex/requirements/training.md`.
