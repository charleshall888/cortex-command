# Context Is the Game — workshop deck

The v1 presenter deck for the team talk on working well with coding agents.
Requirements: `cortex/requirements/training.md` · Story/talk track: `cortex/research/training-talk-deck-outline.md` (rev 10) · Q&A prep: `cortex/research/training-talk-qa-bank.md` · Messaging record: `cortex/research/training-talk-messaging-brief.md`.

## Running it

```bash
cd docs/training && python3 -m http.server 8000
```

- `localhost:8000` — the deck (SHARE this window on a call).
- `localhost:8000/presenter.html` — private notes view (KEEP FOCUS here; its arrow keys remote-control the deck over a BroadcastChannel, which is why `http://` is required — `file://` windows can't share a channel).

Controls: **→/space** one beat · **←** previous scene · **b** blank (valves/Q&A) · **Home/End** · URL hash `#6.4` deep-links to section 6 beat 4 (rehearsal/QA).

## The story (rev 10)

Theme: **the whole game is managing context** — every scene is one of three moves, and the three moves are the section headers, the dock posts, and the retell:

> **keep your context window lean · spawn more agents · keep your workspace clean**

One arc, full circle: the cold open poses the mystery (same model, opposite results, only the context bar differs), the blueprint promises the three pillars, three acts build them (waypoint cards light a post each), and the finale replays Friday with the whole toolkit chained — interview → epic → three tickets → three parallel fresh windows — completing the dock before dawn.

16 sections: 13 scenes + 3 waypoint cards.

| # | Section | Job |
|---|---|---|
| 1 | Friday, 4:00 PM | NIGHTLINE introduced on the title card; fresh vs six-hour session, both at 4:00 PM; the bars (the "whole game" thesis is spoken now, off-screen) |
| 2 | The Whole Game | blueprint: three pillars on three dashed posts |
| 3 | The Gauge | vessel = the bar upright; re-read; the caching beat (`#3.3` — a "this turn" sliver pours, a faster blue sweep stamps hatching over the unchanged prefix, tag "cached"; on-screen claim "cheaper, not lighter — it still reads all of it"); zones (goldilocks labeled); exit at 50% |
| 4 | The Squeeze | compaction as a gamble, anchored 2:00 PM; compacts to ~5%; the payoff is the cold open from inside ("attempted after compaction"); bars chain 95→5→58→89→91 |
| 5 | The Death Spiral | one stubborn bug, three laps; the staircase of failed attempts + corrections reddens; bar climbs 58→89 |
| 6 | The Rewind | the overnight-fishing anti-cheat story on one timeline: scope the ask → a branch tries the device clock (travelers can game it — the fork's answer is a cheap "no") → the main lane grinds on server-enforced time (spoofable anyway, 9 red segments) → the unbroken rewind take (money shot: `#6.4`) carries one note home — "single-player — cheating's allowed" → clean branch: device clock, no anti-cheat; a one-line story caption swaps per phase |
| 7 | waypoint | post 1: keep your context window lean |
| 8 | Write It Down | opens on THE CROSSING (`#8.1`–`#8.3`): the first 20% steers the rest, drawn as a night crossing seen from above — plot the bearing to tonight's spot, then two wakes run with identical timing and the unplotted one misses wide ("a small angle at the dock — a wide miss at the spot"); the sea folds away into the pipeline rail, then interview → doc → 3 tickets (beats 4–7) |
| 9 | Fresh Eyes | prep beat, then one finding per advance: two overeager swings struck out with a plain "no" (Rust rewrite — "it's a keepsake, not a race"; cloud sync + leaderboards — "single-player, by design") and one real crack (the Brass Minnow); judge line at `#9.7` — "it will always find something, you judge what counts" |
| 10 | waypoint | post 2: spawn more agents (proverb "set more lines"; banks write-it-down · fresh-eyes — the economics/parallelism now pays off at the finale, not its own scene) |
| 11 | The Scroll and the Core | exhibit-zoned skill file ratcheting 20 → 6,000 self-contradicting lines; the rent beat (`#11.3`) — the whole file stays on screen and rides in every window; the fix as a second exhibit (the 30-line core + references/) |
| 12 | The Marker Buoys | how the harness's auto-memory fits in, drawn as lit marker buoys moored off the dock — its own notes about the project, one lesson each, one rope (the index) back to the dock; deliberately SPARE (presenter: less stuff everywhere): boxless ONE-line notes mounted in a gap in each staff ("DON'T touch catch_odds.js" — the squeeze chip's warning, re-learned and written down by the agent itself · "catch logic lives in FishManager" · "rares never reach the log"), one label in the whole picture, at most ONE stamp per buoy (✓ keep / ✎ fixed itself / ✗ strike); the §11 seam is the subtitle ("its memory, not its instructions — markers it sets itself"); lamps come up = the index loads every session (`#12.1`, purely visual — the value line is spoken); the agent re-inks its OWN marker mid-task (FishManager → CatchService, `#12.2`); the honest limit is the scene's one on-screen lesson line, landing with deliberately NO visual change ("it tends what it touches — an unread note just sits" — a wrong marker glows as bright as a right one); the added habit: an occasional fresh-eyes pass (one-line terminal, "read every marker back — still true?"; the repo-and-requirements check is spoken) that keeps the warning and cuts the Brass Minnow-bug note loose (`#12.5`; the self-fixed marker needs no second stamp); verdict "What it learns, it keeps. What it keeps, you check." |
| 13 | The Tackle Box | tooling hygiene, drawn as the dock's shared tackle box (pv-3's proverb, finally shown — no bars): your lean skill is a lure → you land a fish (prove it) → the villain staples it onto every rod, even one that "never casts" (forced, rail reddens) → the box's rule "nothing goes in till it's landed a fish" → a crewmate takes it by choice; PR-vs-opt-in fork; verdict "Rent is what you force on the crew. Tackle is what they choose." |
| 14 | waypoint | post 3: keep your workspace clean; the plank waits for Friday |
| 15 | Friday, 4:00 PM — again | the pipeline finale — now the ONLY place the economics lands; the two spent panes slide off at `f3` so folded stubs never pile up under the live trio; results = the centered before/after scorecard alone (1 session/91% red/broke the game vs 4 fresh sessions/peak 18%/3 tickets, `#15.4`) |
| 16 | Tuesday, 9:00 AM | dawn; the dock redrawn with the blueprint's spread-post geometry so the three recap columns align under their posts by construction; the log card lands in the grid cell under the fishing line (tug pays off into it); link line arrives with the recap; the closer ends the talk alone |

Rev-8 → rev-9 changes (the pattern-lens audit — the presenter's five review rounds distilled into a seven-principle lens, three audit agents run against the deck, verified findings applied): four contradictions fixed (both cold-open panes at 4:00 PM; the squeeze anchored 2:00 PM with its payoff revealed as the cold open from inside — bars now chain 95→30→58→89→91 across scenes 4→5→1; finale scorecard "without/with the habits"; scene 11 says "requirements" like scene 8) plus the should-fix list (one name for flip-through everywhere, no auth in the fiction, "wasn't in the room", the Minnow stamp gloss mirroring dawn, "planned last scene" restored, METR hedged as a bet, "instruction files", the dock's one-line intro, "The Scroll and the Core", the skill-size confession defused, the death-spiral interview callback, chart bracket "peaks ≤18%"). Declined by design: an on-screen compaction definition, trio-stagger changes, edits to the thesis or closer. Full rationale in the outline doc.

## Architecture

Hand-authored deck over a shared component library — words live with their presentation; only visuals are componentized (see the scene-schema decision in `cortex/requirements/training.md`, Open Questions).

| File | Role |
|---|---|
| `index.html` | All 16 sections, beat markup via `data-beat`/`data-beats` |
| `lib/deck.js` | Engine: beat navigation, per-scene hooks, promise-chained async beats, dock builder, intent-line flights, night-sky signature, presenter sync |
| `lib/contextbar.js` | The context bar: zone-banded gauge reused in sections 1, 3, 4, 5, 8, 10, 14 |
| `lib/vessel.js` | The context vessel: labeled pours, scan sweep, zones + exit door, pinned chips (squeeze pile, No-loop conflicts) |
| `lib/terminal.js` | Scripted mock-terminal player; `append` continues a session, `snap` lands a punchline instantly |
| `lib/filmstrip.js` | Fork/rewind timeline — deliberately built as the wave-2 Cockpit engine seed |
| `lib/notes.js` | Talk-track cues per scene per beat (feeds presenter.html) |
| `lib/deck.css` | Night edition of the landing-page design language; bundled fonts (offline-safe) |
| `presenter.html` | Synced notes + remote control + timer |

Timing knobs: `CHAR_MS`/`LINE_GAP` (terminal.js), squeeze/sweep durations (vessel.js, incl. the cached sweep's 2200ms in the `sc-gauge` hook), the rewind take (`totalMs`, filmstrip.js), the crossing's draw times (deck.css `.ps-bearing`/`.ps-wake` transitions + the `.ps-b2` reveal delay), context-bar animation `ms` per `set()` call (deck.js hooks), `flyArrow` per-call `ms`, per-hook `setTimeout`s (deck.js), the arrival choreography + lobby-idle periods (deck.css `.arrive` delays; every idle period is incommensurate on purpose), the plink/glint jitter (`schedulePlink`/`scheduleGlint`, deck.js), the dawn sunrise (deck.css `.dawn-glow` transition + `post-light` delays).

## Status

Rev-14 (2026-07-20): the presenter's 14-item review round. (1) Blueprint sub-line → "managing it well hangs on these three posts". (2) NEW caching beat in The Gauge (`data-beats` 4→5): a "this turn" sliver pours, then a faster BLUE sweep stamps hatching over the unchanged prefix ("cached" tag) while still walking the whole tank — on-screen claim "cheaper, not lighter — it still reads all of it"; built on vessel.js's latent `cached` sweep after a 3-angle creative fan-out converged on it; invalidation ("touch an early line, re-pay everything after") deliberately left to Q&A — a second idea, and alone it argues for append-only bloat. (3) Death Spiral's "every attempt and every 'no'" caption cut; relapse now verifiable via verbatim-identical Edit summary lines ("same edit as turn 71"). (4) TERMINAL AUDIT: every scripted terminal reduced to summary clauses — no diff hunks anywhere except the one empty-state copy line ('+ "No catches yet. The night is young."', cold-open left + finale ticket 3); cold-open bodies 19em→15em; finale task lines now carry the ticket names verbatim. (5) The Rewind retold as the overnight anti-cheat story (fork = device clock, red run = server-enforced time, note unchanged) with a swapping one-line story caption. (6) NEW crossing intro on Write It Down (`data-beats` 4→7, old beats shift +3): plot-the-bearing → two wakes, identical timing → the miss; folds away (`.railed`) before the rail appears; from a 3-angle fan-out (nav metaphor + motion choreography won; the session-track variant risked restating the filmstrip). (7) Fresh Eyes: "requirements, from an hour ago" caption cut; the two bounced findings replaced with plainly-overeager swings (Rust rewrite / cloud sync + leaderboards) struck through when the "no" lands (red ✗ stamp prefix); the separate manufactured-finding card deleted — the judge line closes the scene. (8) Buoys notes to one line each. (9) Finale: bar jitter fixed (fixed term-body heights 200px/140px so typing can't shove bars; contextbar animates from the painted value; `chains["fin"]` reset on re-entry); empty-state result card cut, scorecard centered alone; the spent finale-rail slides off at `f3` (folded stubs no longer pile up). (10) Dawn rebuilt: dock at 800×210 with blueprint post geometry (centers 1/6·1/2·5/6 of the 0–640 zone), recap columns aligned under posts by an equal-thirds grid (icons cut — blueprint's grid never had them; numbers/subs kept per the rev-10 ruling), rod owns the right bay, log card in the 1fr cell under its line, linkline moved to beat 2, closer alone at beat 3; log-at-top rejected 3-of-3 in the fan-out (detaches the card from the tug). Includes: deck.css `?v=28`, deck.js `?v=29`, notes.js `?v=29`, contextbar/vessel/filmstrip `?v=11`. Re-time owed (crossing + caching beats add ~1 min).

Rev-13 (2026-07-17): **Set More Lines (the economics chart, old §9) cut** — a zoomed-out consolidation of the "spawn more agents" pillar, which was being taught three times: §8 concept → §9 abstract economics → §16 concrete demo. The finale already proves the economics concretely (the before/after scorecard), so the abstract token/clock chart up front just repeated it. Resolution (three passes with the presenter): §8 stays a lean *concept* setup (interview → doc → window-sized tickets; its beat-5 handoff/dependency demo also trimmed), the finale keeps its full end-to-end run as the payoff — now the ONLY place economics lands. Deleted: the sc-lines section markup, its deck.js hook, and `buildEcon()`; the scene's fan-out credibility SCAR ("a fact-checker window caught a claim I'd gotten exactly backwards") rehomed to the Fresh Eyes exit. Retitle: finale "Small tasks, better results" → **"Let's try this again"** with subtitle "one tired window, or a fresh one per ticket" — a wry setup that (unlike the interim "Same model, nothing broke") doesn't spoil the payoff at beat 0; the same-model/nothing-broke bookend still lands via the closing thesis "Nothing here needed a better model". pv-2 banks "write-it-down · set-more-lines · fresh-eyes" → "write-it-down · fresh-eyes" (the proverb "set more lines" stays; parallelism pays off at the finale, not its own scene); §8 exit re-bridges straight into Fresh Eyes. Renumbering (all sections ≥10 shift −1): Fresh Eyes 10→9, pv-2 11→10, Scroll 12→11, Buoys 13→12, Tackle 14→13, pv-3 15→14, finale 16→15, dawn 17→16 (`#13.2`/`#13.5`→`#12.2`/`#12.5`, `#12.3`→`#11.3`, `#10.7`→`#9.7`, `#16.4`→`#15.4`). Dead econ CSS (`.eg-*` / `.econ-*`) stripped from deck.css too. **Finale example fully realigned to §8's catch-log epic** so one concrete example runs cold-open → §8 → finale: the standalone "make Monday's playtest real" header line is gone; both finale routes now take the *cold open's own ask* ("add an empty-state to the catch log"); and the three tickets are §8's — record catch events · flip-through log page · **empty state** (last = the cold-open callback, and the result screen "No catches yet. The night is young." is literally that ticket's output). The one-window run now breaks **biting game-wide** — the exact cold-open failure — and discovery restores it as a side-note (so dawn's "biting again by sunset" still holds). The doc keeps all three learned lessons (single-player · keepsake · never-punish-rare): never-punish-rare + keepsake fly onto the record/log tickets, single-player stays on the doc (it fixed biting, not a catch-log ticket), and the empty-state ticket stamps in place as the original ask (FIN_RED/FIN_A/FIN_C + `flyIntentLines` reworked). Includes now carry deck.css `?v=27`, deck.js `?v=28`, notes.js `?v=28` — hard-refresh once. JS syntax-checked; **not yet browser-validated** (Chrome tools unregistered this session) — owes a headless console+screenshot pass and a re-time (one scene shorter, ~2 min).

Rev-12 (2026-07-16): new scene **The Marker Buoys** (13) added in Act III between The Scroll and the Core and The Tackle Box — how the harness's persistent auto-memory fits in, plus the occasional audit. Presenter's framing rulings, both same-day: (1) EXPLAINER-FIRST — memory is a sophisticated, genuinely self-maintaining system; the audit is the *added* habit, not the headline threat (a first "audit it — nothing sweeps it for you" framing was corrected); (2) the first-build object, a dockside WHAT'S BITING chalkboard, was rejected as OFF-THEME (a chalkboard reads as classroom furniture — it echoes the room, not the world) and replaced via a second 3-angle creative fan-out that converged on **lit marker buoys moored off the dock**: notes floating on the agent's own water, a mooring rope (the index) back to the dock, no wipe affordance at all — markers are set, re-inked, or cut loose one at a time, which is the true mechanics of memory. Facts verified against current docs + literature: the model writes its own notes per project, the index loads into every session, self-maintenance is real but opportunistic (it tends what it touches; no scheduled sweep; a published benchmark has frontier models catching only ~55% of stale memories in passing). On screen: b1 lamps come up (loaded — illumination, never a wipe; purely visual, the value line is spoken) · b2 the agent re-inks its own marker (FishManager → CatchService, its ✎ the buoy's only stamp) · b3 the limit line with deliberately NO visual change (a wrong marker glows as bright as a right one — you can't tell by looking) · b4 the fresh-eyes pass (on-screen terminal: "read every marker back — still true?"; the oracle — the repo AND the requirements — is spoken, because repo-only would have *confirmed* the unfixed Brass Minnow note) · b5 ✓ keep and ✗ strike — the struck marker's rope segment cut, adrift and dark · b6 verdict "What it learns, it keeps. What it keeps, you check." A same-day declutter pass (presenter: "too noisy") cut the note placards (bare text in a staff gap instead), the second keep stamp, the on-screen value lesson line, the terminal's verb line, the ripples, and two of three picture labels. A plain-words pass followed (presenter: "AI-jargony"): "the index · loads into every session" → "picked up first, every session" (also more accurate — only the index loads at startup; note files are read on demand, which is exactly why an unread note just sits), "one job — the audit" → "now and then", "challenge every note" → "read every marker back"; the word "index" no longer appears in the scene. Downstream surfaces: blueprint + dawn pillar-3 sub now "lean instruction files · audited memory · opt-in tooling" (verbatim pair); pv-3 tech line adds "audit its memory"; pv-3 spoken cue adds "a memory you audit"; §12 exit reworked; the finale's instruction-file line untouched (deliberately distinct mechanism); Q&A bank gains #15. Renumbering: Tackle Box 13→14, waypoint 14→15, finale 15→16 (`#15.3`/`#15.4`→`#16.3`/`#16.4`), dawn 16→17. Includes now carry deck.css `?v=19`, deck.js `?v=20`, notes.js `?v=21` — hard-refresh once. Re-time at rehearsal (one scene added, ~2 min).

Rev-11 (2026-07-14), the presenter's seventh review: the three posts renamed to name the move in the reader's words — **keep your context window lean · spawn more agents · keep your workspace clean** — each now aligned with its own proverb (cut-and-retie / set-more-lines / take-care-of-your-tackle). Act II ("spawn more agents") absorbs Set More Lines + Fresh Eyes, so pv-2 closes the act like the other two waypoints ("write the direction down" is now post 2's sub-line). New scene **The Tackle Box** (13) added in Act III — tooling hygiene the scroll doesn't cover: prove a skill on your own work, then share by kind (project-specific → PR; generic → opt-in kit), never force it. Drawn as the dock's shared tackle box (the object pv-3's proverb names but the deck never drew — no bars): your skill is a lure, you land a fish to prove it, the villain staples it onto every rod (even one that never casts — the rail reddens), the box's rule is "nothing goes in till it's landed a fish," and a crewmate takes it by choice. Verdict "Rent is what you force on the crew. Tackle is what they choose." A first crew-of-five-bars draft was cut after a critique/redesign fan-out (six creative + two critical agents) found it overbuilt and restating scene 12's picture; the tackle box unifies prove-first + opt-in into one house rule and cashes pv-3's own noun. The cold-open thesis moved off-screen (spoken only). Direct fixes: squeeze pane-tag → "attempted after compaction"; the "↑ the request from the open" caption cut; empty-state no longer listed twice in the Write-It-Down handoff; pipe-rail spacing under that title relaxed. Validated headless (fresh-profile Chrome): 0 console errors, screenshots of every changed scene (no overflow). Includes now carry `?v=14` (deck.css, deck.js, notes.js) — hard-refresh once. Rehearsal-with-a-human still owed (re-time: one scene added).

Rev-10 rebuild (2026-07-13), the presenter's sixth review: anatomy card labeled as the empty-state ticket's zoom (it read as a fourth ticket); token bills grounded (~4M vs ~500k — the eighth ratio holds); compaction lands at ~5% everywhere (squeeze bar, wedge cliffs); the finale scorecard drops " by 5:58"; the dawn recap wears the blueprint's 1·2·3 numbering; all script/CSS includes carry `?v=N` cache-busters — hard-refresh (Cmd+Shift+R) once, then stale builds can't recur. Validated headless: console-clean on all 15 sections, CDP real-time end-state assertions, screenshots at 1280×720 (no overflow).

Bookend animation layer (2026-07-14), built from a 3-creative-angle + 2-research-agent fan-out: the **arrival** — on a cold load of `/`, the dock draws itself in, the line drops, and the title surfaces on the frame the ripple blooms (~7s; plays once — deep links and re-visits land on the finished card); the **lobby idle** — jittered ripple plinks, incommensurate star/moon/sway/shimmer periods, and a rare silver moon-glint across the title, so five pre-talk minutes never visibly repeat, all compositor-cheap CSS so it never fights the call encoder; the **dawn** — the 2.4s glow becomes an 8s sunrise (`linear()` curve), stars go out one by one, the posts take first light in sequence, the line tugs as the Brass Minnow's page lands, and the closer arrives by the title's exact glint gesture in gold (the first and last words of the talk lit by the same light). `prefers-reduced-motion` collapses everything to the static card via the existing global kill-switch; deck.css/deck.js are now `?v=11`. Validated headless same day: 20 assertions + all-scene console sweep, all green. Not yet done:

1. **Real-time rehearsal with a human eyeball** — notes.js was redistributed again and needs a read-aloud pass; live checks: the Death Spiral's relapse snap (`#5.3`), the rewind take (`#6.4`), the buoys' self-fix and verdict cadence (`#12.2`, `#12.5`), the finale's fold cadence and triple parallel typing (`#15.3`), and a stopwatch pass (~34.5 min on paper after cutting Set More Lines).
2. **Q&A prep dependencies** (see qa-bank): real cost numbers for the making-of story (appendix slide), a real merged-PR trail to show, DPA verification before the compliance answer, the finale-timestamps question ("is 4:19-to-5:58 realistic for three tickets?"), and the presenter's REAL skill-file line count for the scroll confession. Economics now lives only in the finale scorecard (1 session/91% red vs 4 fresh sessions/peak 18%): be ready to defend that the rent gap is window-size × turns — the figures are staged, the ratio is what daily use looks like.
3. Volatile content: the `/requirements` and `/discovery` chips on the Write-It-Down rail carry `<!-- volatile -->` markers; quarterly sweep per `cortex/requirements/training.md`.
