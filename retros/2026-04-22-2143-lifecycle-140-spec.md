# Session Retro: 2026-04-22 21:43

## Problems

**Problem**: Initial Clarify confidence assessment overreached — rated both "Scope boundedness" and "Requirements alignment" High when neither was supported. Scope didn't enumerate touchable subsystems or a fix-vs-ticket decision rule; alignment was topic-overlap with `multi-agent.md:84` / `pipeline.md`, not normative constraint. **Consequence**: Clarify critic Apply disposition had to revise both down to Medium/Partial, and the scope gap forced a second AskUserQuestion round that could have been avoided with a more honest first pass.

**Problem**: First AskUserQuestion on Problem-1 auth scope asked the user to pre-commit to which subsystems were in-scope (runner.sh / SDK wrapper / CLI flags / credentials.json) before any investigation had happened. **Consequence**: User redirected explicitly — "don't make me commit to what is in scope before we even know what is wrong. Let's dive in and investigate" — a direct correction about research-before-scope-lock.

**Problem**: Posed the shared-helper extraction question as a binary (inline-port vs extract-helper) without first examining runner.sh. The user's reply was a probe — "why is runner a bash script and not python?" — exposing that I hadn't verified my framing against the actual code. On reading runner.sh I discovered the apiKeyHelper logic is already mostly Python via a heredoc, which reshaped the decision to "extract stdlib-only module both callers import" (a third option the Tradeoffs research agent hadn't raised). **Consequence**: Re-asked the question with a corrected frame, wasting one round. The clean-extraction option only surfaced because the user pushed back, not because research anticipated it.

**Problem**: First-draft spec had four A-class defects caught by critical review: (1) R1/R2 bash-Python handoff was under-specified — no concrete shell construct, no `shlex.quote` contract, no exit-code handling under `set -euo pipefail`; (2) Edge Cases claimed runner.sh's fallthrough warnings were "preserved" but the literal warning strings at runner.sh:78,82 lived inside the block being deleted; (3) R3 placed `ensure_sdk_auth()` before `_write_pid` while `pipeline_events_path` isn't computed until daytime_pipeline.py:222 (after _write_pid at line 364) — the mandated call site could never fire the auth_bootstrap event; (4) R10 described a "MODIFIED" writer change that was a no-op, since `common.py:327-331` is already idempotent over `[X]`/`[x]`. **Consequence**: Full spec rewrite after critical review. All four defects would have shipped past the orchestrator-review checklist — they were semantic, not structural, and S1–S6 passed while the spec contained physically impossible claims (preserved strings in deleted code, events on nonexistent paths).

**Problem**: Orchestrator-review pass didn't catch the R5/R3 ordering contradiction or the R10 no-op classification despite reading the full spec against the S1–S6 rubric. The rubric checks binary-checkability and edge-case coverage, but the R5 acceptance test only tested the happy-path tempfile case and the R10 acceptance test passed on today's already-idempotent code. **Consequence**: Relying on acceptance-test presence as a quality proxy missed defects where the tests themselves were narrow enough to accept buggy implementations. Critical review caught what the rubric couldn't.

**Problem**: Tradeoffs research agent surveyed A–E alternatives for the auth fix but didn't identify the clean-extraction option (stdlib-only Python module invoked by both `runner.sh` pre-venv and `daytime_pipeline.py` post-venv). The agent treated "inline port in Python" and "shared helper requiring runner.sh rewrite" as the two axes without examining that runner.sh's auth block is already Python-via-heredoc. **Consequence**: The correct architectural option emerged only after the user's "why bash?" question forced me to re-read runner.sh. If the user hadn't probed, the lifecycle would have shipped inline-duplication as the recommended path.
