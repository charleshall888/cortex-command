# Session Retro: 2026-04-11 19:29

## Problems

**Problem**: Initial complexity/criticality assessment for backlog #071 graded the feature as simple/medium based on a surface read of the ticket. **Consequence**: The clarify-critic had to escalate to complex/high with seven concrete objections before research could begin; an honest first-pass assessment would have skipped the escalation cycle.

**Problem**: First parallel research dispatch hit the Anthropic token rate limit and all four agents failed mid-flight before producing any output. **Consequence**: Had to wait for the user's quota reset and re-dispatch the same four prompts from scratch; lost the partial state of the agents that had already done some work.

**Problem**: Did not challenge the original ticket's "agent reasons about demoability" value pitch during the lifecycle bootstrap. **Consequence**: Sunk research into a framing the adversarial pass later proved structurally untestable ("false precision"), forcing a value-vs-scope conversation with the user mid-research and a reshape from the original Approach C to Approach H.

**Problem**: First spec draft used loose `grep -c` acceptance criteria (e.g., `grep -c 'demo-' ≥ 9` for R14) that could pass on incidental matches without verifying the structural changes. **Consequence**: Orchestrator review cycle 1 flagged S1; had to tighten R3, R12, and R14 ACs in cycle 2 and add a header note explaining the AC-phrasing trade-off.

**Problem**: First spec draft omitted MoSCoW classification on R1–R14. **Consequence**: Orchestrator review cycle 1 flagged S3; had to add an explicit "all R1–R14 are Must-have" header note in cycle 2.

**Problem**: Spec critical review surfaced 23 findings that the orchestrator review had not caught — including the `core.hooksPath` back-channel through `git worktree add`, ANSI escape injection via `demo-command`, and the fact that `realpath` was the only viable resolution method (the spec's "any of four" list was a false choice). **Consequence**: Substantial spec rewrite after orchestrator review allegedly passed, suggesting orchestrator review was too narrow.

**Problem**: First plan draft (v1) bundled the entire Section 2a content into a single complex task that would have exceeded the 5–15 minute envelope. **Consequence**: Had to rewrite as plan v2 with the Section 2a content split across three tasks (skeleton → guards → active flow) before even running orchestrator review.

**Problem**: Plan v2 used prose-based "(guard clauses are added in Task 4...)" placeholder text for the Tasks 3 → 4 → 5 handoff without sentinel anchors. **Consequence**: Plan critical review caught the coordination risk (fresh subagents with no memory of Task 3 would have to infer where Task 4's content goes); had to add `<!-- SECTION-2A-PLACEHOLDER -->` and `<!-- SECTION-2A-CONTENT-INSERT -->` HTML comment sentinels and rewrite plan as v3.

**Problem**: Plan v2 verification strategy step 5 incorrectly claimed cortex-command's project-root `lifecycle.config.md` was "currently absent." **Consequence**: Critical review caught the factual error; had to verify the file exists, rewrite the live-test instructions to acknowledge the existing file, and add a non-requirement clarifying that auto-adding `demo-command` to it is a user choice not an implementation deliverable.

**Problem**: Plan v2 omitted the `integration_branch` grep that spec R5 explicitly requires, omitted the `git worktree remove ≥ 2` count that spec R8 requires, and did not specify regex syntax (BRE/ERE/glob) for Task 7's path filter. **Consequence**: Multiple per-task ACs were under-strength; an implementer could have passed all per-task verifications while missing spec requirements. Caught by plan critical review and fixed in v3.

**Problem**: Plan v2 conflated three layers (skill instruction text, agent runtime substitution, user-visible output) when describing the `{integration_branch}` and `{demo-command}` placeholders. **Consequence**: An implementer could have substituted braces at edit time and frozen example values into the skill text. Critical review caught the ambiguity; v3 added an explicit "Skill-text vs runtime-substitution convention" section.

**Problem**: The wrong Godot CLI flag (`--play res://main.tscn`, which is not a documented Godot 4 flag) propagated from the original backlog item into the discovery research, into this lifecycle's research.md, and almost into the spec before being caught by the web research agent. **Consequence**: A canonical example string in three documents had to be corrected as part of the implementation plan (Task 1 / TC12); a future reader of any of these docs would have copied a broken example.

**Problem**: Repeatedly violated the conversational length limit (≤25 words between tool calls, ≤100 words for final responses) when synthesizing critical-review findings and presenting approval surfaces. **Consequence**: Long synthesis blocks that the user had to scroll through; could have been more terse without losing substance.

**Problem**: Conducted both spec and plan critical reviews via ad-hoc parallel agent dispatches instead of invoking the `/critical-review` skill formally. **Consequence**: Skipped the skill's domain-context injection and Opus synthesizer steps; the reviews still surfaced strong findings, but the protocol was bypassed without explicit justification.

**Problem**: Did not catch the `core.hooksPath` back-channel risk (where `git worktree add` runs `post-checkout` hooks via husky/lefthook redirected directories) until the spec critical review surfaced it. **Consequence**: The spec's R10 security boundary claim ("agent never executes overnight code") was initially load-bearing for user approval but had a documented bypass; caught in time but only by an outside-context critic.
