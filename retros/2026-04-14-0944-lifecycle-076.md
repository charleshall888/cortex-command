# Session Retro: 2026-04-14 09:44

## Problems

**Problem**: The plan's shim template used `batch_result.budget_exhausted` — a non-existent field on `BatchResult` (actual fields: `global_abort_signal`, `abort_reason`, `circuit_breaker_fired`). **Consequence**: Required a full plan rewrite; this error would have crashed on every `_accumulate_result` call at runtime.

**Problem**: The plan ordered test patch migrations (Tasks 6–7) before function removal from `batch_runner.py` (Task 8), violating LEGB resolution rules. While `_apply_feature_result` still lived in `batch_runner.py`, patching `outcome_router._write_back_to_backlog` would be a silent no-op — the function resolves its internal names from `batch_runner`'s namespace, not `outcome_router`'s. **Consequence**: The original task ordering would have produced passing tests that hit real implementations instead of mocks; the error was invisible at the grep-verification level. Required full restructuring to "remove first, migrate patches second."

**Problem**: `test_no_commit_classification.py` directly imports `_classify_no_commit` from `batch_runner` and was not identified during the Caller Enumeration planning step. **Consequence**: Task 8 as originally written would have deleted `_classify_no_commit` and caused `just test` to fail with `ImportError`; caught only by the critical review reviewer who read the actual test file.

**Problem**: The plan initially placed `just test` as Task 6's verification, which would fail after patch migration but before shim collapse. The orchestrator review caught this as P4 in cycle 1 — but the fix changed the verification to grep-only without addressing the underlying ordering problem (patches migrated during LEGB no-op window). The full root cause required the critical review to surface. **Consequence**: Two rounds of plan revision needed for what was ultimately the same root-cause issue.
