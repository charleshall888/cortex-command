# Session Retro: 2026-04-13 12:12

## Problems

**Problem**: Task 11 plan originally designed the pytest as a literal duplication of `_ALLOWED_TOOLS` in the test file compared against `dispatch.py`, which does not actually verify the doc matches code — doc could silently drift. **Consequence**: Critical-review reviewer had to discover the existing `tests/test_events_contract.py` precedent (doc-is-parsed-at-runtime) and surface that Task 11 was inventing a weaker pattern. Required a significant rewrite of Task 11 after plan was otherwise complete.

**Problem**: Task 14's per-requirement verification checklist omitted Req 7 entirely and conflated it with Req 10 ("Req 10: pytest already verified in task 13" — but Req 7 IS the pytest requirement; Req 10 is "documented verbatim"). **Consequence**: A final-sweep gate would have passed without ever verifying Req 7, silently producing a plan-to-spec traceability gap.

**Problem**: Task 8's caller-enumeration grep scope was `README.md docs/ skills/` only, despite this very plan.md citing `#authentication`, `#the-execution-phase` etc., and `retros/`, `backlog/`, `lifecycle/`, `CLAUDE.md`, `requirements/`, `.github/` all containing `overnight.md#` anchor references. **Consequence**: Plan as originally written would have shipped with broken anchors across the repo. Fixed only because critical-review reviewer explicitly inventoried the caller surface.

**Problem**: Task 8 Context cited line numbers (`overnight.md:15, 263, 297, 444`) for anchor locations while Req 11 simultaneously banned line-number cross-references. Line numbers shift as sections are removed mid-edit, so the references would rot during the task itself. **Consequence**: Self-contradictory plan instruction; the author during Implement would have encountered stale pointers.

**Problem**: Task 2's outline verification was a bare heading-count (`grep -c '^#' ≥ 21`) not a per-keyword check, despite the whole point of Task 2 being to slot 21 SPECIFIC keywords into headings. **Consequence**: A file with 21 repeated `## Architecture` headings would have passed. Critical-review reviewer caught it.

**Problem**: Task 1's verification was off-by-one against its spec acceptance: `grep -c '^| ' ≥ 4` counts header + separator + 2 data rows, but spec req 8 requires "≥3 retros scanned." **Consequence**: A 2-retro log would have satisfied the task gate while violating the spec.

**Problem**: The original spec's first pass violated S1 — Reqs 4, 8, 12 used "manual review" / "PR description" / "manual" phrasing without the required `Interactive/session-dependent: [rationale]` annotation. **Consequence**: Orchestrator review flagged it and required a cycle-2 fix pass.

**Problem**: The original spec Req 12 mandated specific substrings ("post-retry triage" AND "no RETRY") at a specific paragraph depth (`awk '/^### .*brain\.py/' | head -10`). Adversarial review flagged this as brittle prose-pinning that forecloses a better framing and assumes H3 depth indefinitely. **Consequence**: Required relaxation to manual disambiguation review during critical-review apply pass.

**Problem**: The commit for refine artifacts failed the first time because sandbox blocked access to `~/.gnupg/` for GPG signing. **Consequence**: Required a retry with `dangerouslyDisableSandbox: true`; minor friction but the /commit skill's signing-check logic didn't detect the need.

**Problem**: Q3 in the first Specify interview (semantic quality gate) was worded abstractly and the user replied "I'm not really sure what you are asking here." **Consequence**: Required a follow-up question with more concrete framing ("21 grep checks can pass on 24 one-sentence stubs; want extra check?").

**Problem**: Orchestrator review on the plan flagged Task 8 sizing (~30-45 min exceeds 15-min target) but the fix was limited to Tasks 4 and 6 splits — Task 8 was left at full size with a Veto Surface rationale. **Consequence**: Plan ships with a known P1 violation; if the Implement phase overruns its budget on Task 8, the plan didn't prevent it.

**Problem**: Plan's first Task 7 edit batch had two Edits targeting the same block in sequence; the second (which tried to rewrite the whole task body) didn't match because the first had already modified it, creating a duplicate `**Complexity**: simple` line at lines 85-86. **Consequence**: Required a separate cleanup Edit to remove the duplicate.

**Problem**: During Specify, the initial Q2 response from user was prose ("One of the core principles here is progressive disclosure (we actually have some docs on this in this repo)") rather than one of the three options. Required a follow-up grep across the repo to find the progressive-disclosure reference docs and interpret the user's intent. **Consequence**: Modest delay; the interpretation ("move everything + pointer") ended up being validated by the user in a subsequent Q2-reframed question, but the original option list didn't match the user's mental model.
