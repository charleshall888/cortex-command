# Session Retro: 2026-04-07 21:54

## Problems

**Problem**: Initial Task 5 plan underspecified the exit condition semantics — "Update the exit sentence to reference `features_to_run`" was written without specifying what the updated sentence should say when `features_to_run=[]` because all pending features are in future rounds vs. when the session is truly complete. **Consequence**: A natural implementation would write "if `features_to_run` is empty, exit — session complete," which is wrong for mid-session round boundaries; required critical review to surface and fix.

**Problem**: Initial Task 5 plan did not address §2a's propagation gap — §2a's dependency gate uses `round_assigned == current_round` (independent of `features_to_run`), which would silently drop paused features from prior rounds even after §1's filter correctly included them. **Consequence**: The round filter fix would have been incomplete; paused recovery features from earlier rounds would still be excluded by §2a; required critical review to catch.

**Problem**: Initial Task 8 used a logic-copy testing approach — the test was designed to run inline Python that duplicates `count_pending()`'s logic rather than testing the actual runner.sh code. **Consequence**: The test would pass even if runner.sh had a typo or omitted `'paused'` entirely; required critical review to catch; fixed by adding a structural grep assertion.
