# Session Retro: 2026-04-21 08:53

## Problems

**Problem**: Initial plan leaned on voluntary discipline in four separate places (R14 ordering, R5 frozen-prompts, R1 "never reassign", overnight-dispatch eligibility) despite the project's "success criteria verifiable by an agent with zero prior context" rule. **Consequence**: Critical review surfaced all four as distinct high-confidence objections; ~30 minutes of Apply/Ask rework that could have been avoided by applying the project.md rule up front rather than after challenge.

**Problem**: Task 2's initial design scripted exactly one `AssistantMessage` — classic n=1 happy-path test that can't distinguish first-observed from last-observed semantics. **Consequence**: The Task 1 "never reassign" invariant would have been untested; a regression dropping the guard could silently convert the field to last-observed and ship green. Had to strengthen Task 2 in the Apply step.

**Problem**: Did not flag Task 6's operator-only nature as a dispatch-gate issue at plan-writing time despite knowing #088 is `complex + high + status=refined` — textbook `/overnight` selection material. **Consequence**: Required the critical review angle to surface that overnight dispatch would route Commit A to the session integration branch (mechanically breaking R14). The fix (marker item #099) was trivial once identified but the category — "plans with multi-day operator-driven tasks need an eligibility gate" — should have been obvious.

**Problem**: Classified Q2 (frozen-prompts automation) as an Ask based on a thin rationale ("spec Scope Boundary was approved, so reversing it requires user input"). **Consequence**: User pushed back with "think critically about this" — correctly identifying that Scope Boundary language doesn't preclude re-evaluation when critical review surfaces downstream-harm arguments the spec decision hadn't weighed. Should have applied the Apply-bar test ("is the fix unambiguous?") more carefully before defaulting to Ask.

**Problem**: Initial plan's Task 8 inherited the "fixed six-bullet Limitations" language from spec R12 without questioning whether the plan could extend it. **Consequence**: Subagent-interleaving caveat (known from spec Edge Cases L147–L149) did not land in the plan's Task 8 instructions; the review had to surface it as Apply. R12 already allowed ≥6 bullets — I read the constraint as a ceiling when it was a floor.

**Problem**: Logged events with timestamps (`2026-04-20T21:45:00Z`, `2026-04-21T09:00:00Z`) that drifted from actual local time (~08:53 at session end). **Consequence**: events.log timestamps don't match reality by ~10 minutes. Not severe but the whole point of timestamp-based attribution for R13/R14 is that they're honest — I should have used `date -u +"%Y-%m-%dT%H:%M:%SZ"` in the bash call rather than picking plausible-looking values by hand.
