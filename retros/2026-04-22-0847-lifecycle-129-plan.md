# Session Retro: 2026-04-22 08:47

## Problems

**Problem**: Wrote Task 9 around spec Req 8's backdating prescription without questioning whether the backdating served a concrete purpose. **Consequence**: User had to redirect ("why do we even care about backdating here?") after the Plan phase was well underway; forced a mid-plan spec amendment and rewrite of Task 9 + Veto Surface + Verification Strategy references. The vacuousness of spec Req 8's `--author-date-order` sort-check (which passes trivially because the command's output is already author-date-sorted) should have been caught during spec critical-review, not plan critical-review.

**Problem**: Wrote Task 4 Context with a copy-paste-ready bash one-liner embedding `import json,os,sys` and complete stderr-capture wiring, violating the Plan-phase P5 "no function bodies, no imports, no copy-paste-ready code" rule. **Consequence**: Orchestrator review flagged it on cycle 1; had to dispatch a fresh Sonnet fix agent to rewrite the Context in pattern-reference prose. Would have been caught by self-auditing against the P5 checklist before writing.

**Problem**: Chained scheduler-check commands with `&&` and `;` into a single compound Bash call (`launchctl list ... && echo --- && ls ~/Library/LaunchAgents && ... && crontab -l && ...`). **Consequence**: Sandbox permission system evaluates the full string as one unit and denied it — explicitly called out in `claude/rules/sandbox-behaviors.md` ("Compound Commands: Avoid Chaining"). Wasted a tool call and had to redo as separate Bash invocations.

**Problem**: After the compound-command denial, retried individual commands (`launchctl list`) that the sandbox also blocked without first assessing which commands would and would not work in the sandbox. **Consequence**: Two more denied tool calls before pivoting to the `~/Library/LaunchAgents` directory listing (which did succeed). Should have read the sandbox filesystem allow-rules or worked from the directory listing first.

**Problem**: Initial plan.md Task 4 asserted "`ROUND` is exported earlier in runner.sh (`runner.sh:533`) so both branches can reference `$ROUND`" without actually verifying `runner.sh:533` uses `export`. **Consequence**: Critical-review Angle A caught the false claim — `runner.sh:533` is `ROUND=$(...)`, a plain shell assignment. If an implementer had read "from the environment" literally, `os.environ['ROUND']` would have raised `KeyError`. Fixed in the Apply pass, but the original Context misled on an easily-verifiable fact.

**Problem**: Did not flag `sync-allowlist.conf:3` "`--theirs` (remote wins)" comment inaccuracy during the research phase. **Consequence**: Critical-review Angle C pursued the ambiguity all the way through a dismissal analysis before I verified via `bin/git-sync-rebase.sh:108` + git rebase semantics that the comment is misleading (not the code). Logged as a follow-up in Veto Surface, but the discrepancy should have been noted during research's "Patterns to follow" section instead of surfacing only under adversarial review.
