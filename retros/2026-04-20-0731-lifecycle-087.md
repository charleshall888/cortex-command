# Session Retro: 2026-04-20 07:31

## Problems

**Problem**: Research phase converged prematurely on "extend metrics.py" without discovering that overnight dispatches write to `lifecycle/sessions/*/pipeline-events.log`, not per-feature `lifecycle/{feature}/events.log`. All four research agents reported findings as if per-feature logs were the aggregation target; the codebase agent explicitly stated "88 completed feature files, 2,709 events" as if they were the data. **Consequence**: the first spec was built on a wrong premise about where the data lives and had to be substantially rewritten after critical review caught the mismatch.

**Problem**: When the user said "best long-term fix even if it involves some refactoring" during the Spec interview, I proposed adding `complexity`/`criticality` fields to `dispatch_complete`/`dispatch_error`/`throttle_backoff` without first checking that `dispatch_start` already emits those exact fields (dispatch.py:446-448) and fires in the same log immediately before each `dispatch_complete`. **Consequence**: wrote a full spec draft whose central refactor was redundant; the critical review had to discover this and the rewrite removed the refactor entirely.

**Problem**: During Clarify I asked the user to confirm scope "all four dimensions" (turns, cost, error_type frequency, rate-limit incidents) without first verifying that `throttle_backoff` events actually exist in the corpus. Grep would have shown `throttled_dispatch` has zero call sites and the corpus contains zero rate-limit events; a prior investigation at `lifecycle/replace-concurrency-cap-with-conflict-aware-round-scheduling/inner-task-investigation.md:57,91` already documented this. **Consequence**: the spec carried scope creep beyond DR-4 through multiple phases and the user had to question the value of the throttle system to correct it.

**Problem**: The first orchestrator review passed cycle 1 on the grounds that R4's acceptance criterion "existing dashboard test `test_parse_metrics_valid_returns_dict` still passes" was binary-checkable. The test does not exist — the real test is named `test_valid_json_returns_dict_unchanged`. `pytest -k test_parse_metrics_valid_returns_dict` selects zero tests, so the criterion is vacuously satisfied. **Consequence**: orchestrator review became a paper gate rather than a real one; the actual backcompat risk was unaddressed until critical review caught it.

**Problem**: When the user pushed back with "what is the advantage of this whole throttle system?", my initial recommendation was "drop it entirely" before I grepped for `ConcurrencyManager` vs `throttled_dispatch`. After grepping I had to correct myself: `ConcurrencyManager.acquire/release` is load-bearing in orchestrator.py and feature_executor.py; only the `throttled_dispatch()` wrapper is dead. **Consequence**: gave the user an over-broad "drop it" answer that had to be walked back in the next message, adding a round trip.

**Problem**: Asked the user several architectural follow-up questions (rate-limit dimension value, windowing strategy) after critical review instead of investigating the codebase first. Grep-verifiable facts like "is throttled_dispatch called anywhere" and "what does dispatch_start.model contain" could have been resolved before prompting the user. **Consequence**: user had to answer with additional questions of their own rather than make clean decisions from a complete picture.

**Problem**: Research agents (4 parallel) ran successfully but none of them surfaced the `lifecycle/{feature}/events.log` vs `lifecycle/sessions/*/pipeline-events.log` distinction, despite that being a grep-visible architectural fact. The codebase agent specifically looked at `discover_event_logs()` in metrics.py (which only discovers per-feature logs) but did not ask "where do the production dispatches actually write?" **Consequence**: the research artifact's convergence signal gave false confidence and the spec inherited the research's wrong scope.

**Problem**: Proposed a `AskUserQuestion` spec-approval tool call immediately after the critical-review rewrite, which the user interrupted. Likely reason: presenting a "Approve / Request revisions" prompt after a substantial rewrite doesn't give the user enough time to actually read the revised spec before being asked to approve. **Consequence**: user rejected the tool use and the spec is unreviewed/unapproved.
