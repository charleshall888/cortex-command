# Session Retro: 2026-04-02 16:29

## Problems

**Problem**: Spec Req 2 prescribed "commit message format conventions (imperative mood, max 72 chars, no trailing period)" for `global-agent-rules.md`, but these conventions don't exist in `claude/Agents.md` — they live in the project `CLAUDE.md`. **Consequence**: Internal contradiction between the content table and the "no content additions" non-requirement; required spec rewrite after critical review.

**Problem**: Spec Req 1 failure path said "proceed to Req 9 (fallback documentation)" but Req 9 is about updating ticket 006 acceptance criteria, not fallback documentation. **Consequence**: The fallback branch pointed to the wrong requirement; overnight agent following the spec would have had no valid path on failure. Required spec rewrite after critical review.

**Problem**: Plan Task 1 used `ls ~/.claude/rules/` as evidence that Req 1 was completed, and had the agent write a `req1_verified` log entry itself (self-sealing). **Consequence**: The precondition check would give a false positive for any machine that already had `~/.claude/rules/` from another source, and the log entry added no independent evidence. Required Task 1 rewrite after plan critical review.

**Problem**: Plan Task 5 Context contained a multi-line bash code block showing the exact loop structure for the new rules/ deployment, which an implementer would copy-paste verbatim. **Consequence**: Code budget violation (plans must use pattern references, not implementation code). Caught by orchestrator review before user presentation; fixed with a pattern reference to existing justfile lines 90-104.

**Problem**: Initial spec interview questions did not surface the possibility of a dedicated sandbox-behaviors file separate from the generic-rules file. The user had to suggest it themselves ("Should we add a sandboxed rules separate file that only applies to when we are sandboxed?"). **Consequence**: The spec was restructured mid-spec from a two-file to three-file split based on user input; an additional clarifying question was needed.
