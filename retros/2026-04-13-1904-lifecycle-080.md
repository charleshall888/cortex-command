# Session Retro: 2026-04-13 19:04

## Problems

**Problem**: Placed the wikilink artifact line inside the frontmatter section of `index.md` instead of in the body, requiring a second corrective edit. **Consequence**: Extra edit round-trip; the file was briefly malformed (wikilink between `---` markers instead of after the closing `---`).

**Problem**: R3's acceptance criteria grep targeted `test_lead_unit.py` but the requirement body said "Add tests to `test_brain.py`". This passed through both orchestrator review cycles without being flagged and was only caught by the critical-review agents. **Consequence**: Demonstrates that orchestrator review is not reliably catching file-target errors in ACs; the bug would have allowed a fully compliant PR to leave brain-decision coverage entirely untested.

**Problem**: R9 specified `batch_result.session_paused == True` as the assertion, but `BatchResult` has no `session_paused` field (actual: `global_abort_signal`). Also conflated `budget_exhausted` (sets `global_abort_signal`) with CIRCUIT_BREAKER_THRESHOLD consecutive pauses (sets `circuit_breaker_fired`). **Consequence**: Spec had a factually wrong field name; would have caused immediate test-authoring failure and forced the implementer to improvise without spec guidance.

**Problem**: R10 required testing cross-invocation `recovery_attempts_map` suppression ("in a subsequent `run_batch` call") without specifying the necessary `overnight_state_path` fixture round-trip. The map is a local variable that resets on each call — cross-invocation state only persists via file write-back. **Consequence**: R10 as written was unimplementable; caught by critical-review but should have been caught during spec authoring by re-checking the architecture documented in research.

**Problem**: Technical Constraints for run_batch-based tests (R7-R10) listed `execute_feature` and `merge_feature` as mock targets but named zero of the session-layer infrastructure mocks required (`parse_master_plan`, `create_worktree`, `load_state`, `load_throttle_config`, `ConcurrencyManager`). The research section explicitly listed these as required mocks. **Consequence**: Implementation gap — tests for R7-R10 would hit real git subprocess calls or load_state failures without knowing to patch them.

**Problem**: Used hardcoded zero-time ISO 8601 timestamps (`2026-04-13T00:00:00Z`, `T00:01:00Z`, etc.) in events.log entries instead of actual current timestamps. **Consequence**: events.log has fake timestamps that don't reflect when phases actually occurred, reducing its utility as an audit trail.

**Problem**: Attempted to invoke `AskUserQuestion` for spec approval without first loading its schema via `ToolSearch`, resulting in the tool call being rejected. **Consequence**: Required an extra round-trip to load the schema before the approval surface could be presented.
