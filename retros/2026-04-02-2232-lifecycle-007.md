# Session Retro: 2026-04-02 22:32

## Problems

**Problem**: The plan's detect→merge data transfer was initially designed as a shell CLI argument (`--detect-json '<detect_json>'`), which would hit ARG_MAX limits for large settings.json files and break on single quotes embedded in hook command strings. **Consequence**: The entire plan had to be rewritten after critical review to use a tempfile approach; the first planning pass was wasted work.

**Problem**: The spec's symlink discovery section contained incorrect concrete details — stated "7 utilities from bin/" when the actual count is 5, used a `cortex-*.md` glob that matches nothing in `claude/rules/` or `claude/reference/`, and the `cortex-*.sh` glob excluded `cortex-sync-permissions.py`. **Consequence**: Critical review flagged these before the spec was shown to the user, but they required a round of fixes that could have been avoided by checking the actual repo state before writing counts and globs into the spec.

**Problem**: Tasks 4 and 5 in the original plan both extended the same `merge_settings.py` file with no function interface specification — Task 4's verification step required "inspect the resulting settings.json" but the atomic write wasn't implemented until Task 5. **Consequence**: An overnight agent would have produced structurally incompatible code across the two tasks; caught by critical review and required plan rewrite.
