# Session Retro: 2026-04-20 21:35

## Problems

**Problem**: Initial Clarify confidence assessment rated all three dimensions HIGH without surfacing the concrete gaps the critic then identified (undefined schema for "per-tier", undefined "rate-limit incidents" field, unbounded sample-size range, mid-window contamination risk from in-flight #067/#068/#069, miscalibrated complexity and criticality). **Consequence**: Complexity and criticality had to be escalated post-hoc during the critic disposition step; five of six critic findings were Apply, indicating the initial assessment was systematically overconfident and the critic carried most of the calibration work.

**Problem**: The research agent claimed `ResultMessage.model` was the capture target for resolved model version, and I propagated that claim into research.md and the first draft of the spec without verifying. **Consequence**: Only during the Spec §2b Verification check did I discover `ResultMessage` has no `.model` attribute in `claude_agent_sdk==0.1.41` — `AssistantMessage.model` is the only stream-member carrying resolved model. Had to correct mid-spec and add a Technical Constraint explaining the switch.

**Problem**: First spec draft bundled the pipeline instrumentation change and the snapshot write into a single commit (R12) without recognizing the atomicity defect — that ordering is unsatisfiable because "frozen prompts during window" and "bundled commit at window_start" contradict each other. **Consequence**: Critical-review surfaced this as the top objection; had to restructure R14 into a two-commit sequence (pre-window Commit A for R1; post-measurement Commit B for snapshot) and rewrite Problem Statement accordingly.

**Problem**: First spec's R5 grep scope (`skills/ claude/reference/ CLAUDE.md`) missed at least six actual prompt surfaces: `claude/pipeline/prompts/`, `claude/overnight/prompts/`, `claude/Agents.md`, and inline string-literal system prompts in Python source (e.g., `review_dispatch.py:246`). **Consequence**: Critical-review found the "frozen prompts" invariant had zero enforcement in practice; had to expand the grep path list and add a Technical Constraint documenting the residual risk from inline prompts.

**Problem**: First spec's R10 acceptance check used subset-of-keys validation (`required <= set(d.keys())`), which is field-presence-only — typos in values (non-ISO dates, string-instead-of-int, 40-char-not-12-char SHA) would pass silently. **Consequence**: Had to tighten to a full type/regex-validating Python one-liner in the revised R11.

**Problem**: First spec's R2 test glob (`test_dispatch*.py`) excluded the actual tests that assert on `dispatch_complete` field fixtures (they live in `test_metrics.py`). The acceptance claim "`pytest test_dispatch_instrumentation.py` exits 0" was vacuous — that file contained zero occurrences of `dispatch_complete`. **Consequence**: Had to rewrite R2 to require a new, purpose-built test that patches `claude_agent_sdk.query` and asserts on the emitted event, plus a verification sweep across all test files that construct `dispatch_complete` fixtures.

**Problem**: Dispatched adversarial research agent (agent 5) and hit Anthropic rate limit mid-dispatch ("You've hit your limit · resets 4pm"). The agent returned no findings. **Consequence**: Had to wait for rate-limit reset and re-dispatch the same prompt from scratch, consuming extra tokens and delaying the synthesis step.

**Problem**: Did not ask the user about the frozen-prompts enforcement mechanism (hash-and-disqualify vs. voluntary discipline) — only asked about model-version capture, clean-round rule, unblock rule, and window-start date. Defaulted to voluntary discipline based on inference from the user's other answers. **Consequence**: Critical-review flagged "voluntary discipline has no enforcement" as a top concern; spec had to add explicit residual-risk Technical Constraint language and expand the R5 grep scope to partially compensate. User was never directly asked whether voluntary-discipline was acceptable.

**Problem**: User interrupted the `/lifecycle 88` flow at the spec-approval step to invoke `/fresh`, rather than approving or revising the spec. **Consequence**: The spec is written and internally consistent, but the approval surface was never collected; lifecycle is parked mid-Specify phase, `status: refined` write-back has not run, and the next session needs to re-enter at the approval prompt to proceed to Plan.
