# Session Retro: 2026-04-13 08:27

## Problems

**Problem**: Wrote R3's zero-merged-features guard as an additional bullet in Guard 3's existing skip clause, which is shared between both the `demo-commands:` and `demo-command:` paths per Technical Constraints — contradicting the "UNCHANGED: demo-command: single-string path" guarantee in Changes to Existing Behavior. **Consequence**: Required a post-critical-review fix to add path-conditional qualification ("applies only when `demo-commands:` path is active"); would have silently modified legacy path behavior if shipped undetected.

**Problem**: Specified 7 parsing rules for the `demo-commands:` block list in R2 but wrote greppable ACs for only 4 of them. Rules 2 (block boundary — stop at first non-indented line), 5 (empty command value → discard), and 6 (fallthrough to `demo-command:` when no valid entries remain) had no verifiable acceptance criteria despite being the most load-bearing structural rules. **Consequence**: Critical review caught the gap; 3 additional ACs added. Would have left the structural parsing rules unverifiable during overnight implementation.
