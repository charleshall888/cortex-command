# Session Retro: 2026-04-21 21:21

## Problems

**Problem**: Created the lifecycle directory at `lifecycle/114-cortex-cli-skeleton/` using the user's invocation argument literally, before checking the project convention (derive from backlog title via slugify). **Consequence**: had to mid-flight rename the directory, rewrite `index.md`, rewrite `events.log` with the corrected `feature` field, and re-run `update-item lifecycle_slug=` — burned five tool calls correcting a decision that a one-line precedent check (`ls lifecycle/ | grep 112-`) would have avoided.

**Problem**: `clarify_critic` assessment cited epic research DR-1 as a basis for Intent clarity, but the ticket body actually cites DR-4 and DR-5. Imported external context silently and credited it to the ticket's own clarity. **Consequence**: the critic agent flagged this as "importing external context and then crediting the ticket for clarity it does not explicitly contain," requiring an Apply fix to strip the DR-1 attribution. Revealed a pattern of conflating background-context support with ticket-body support when grading confidence.

**Problem**: First-draft spec's acceptance criteria had multiple empirically-broken checks — `find ... -maxdepth 2` with an "exit 0" criterion (find returns 0 regardless of match count, verifies nothing); argparse subparser listing regex `'^  (overnight|...)'` with two-space indent (argparse default is four-space); grep patterns missing `import claude.X` (148 sites), `unittest.mock.patch("claude.X.Y")` (286 sites across 28 files), and uvicorn colon-syntax (`uvicorn claude.dashboard.app:app`). **Consequence**: if the critical-review step had been skipped, the implementation phase would have treated structurally-false ACs as passing signals, leaving live references to `claude.*` on disk while the rename was marked "done."

**Problem**: First-draft spec's `uv sync` edge case described the install as a "non-editable build" when uv docs explicitly document it as `source = { editable = "." }`. **Consequence**: would have misinformed the implementation about what the mitigation was guarding against. The author had not verified uv behavior before writing the mitigation.

**Problem**: First-draft spec included `[tool.uv] package = false` as a contingent fallback without recognizing that it would nullify the `[build-system]` work's effect outside of one-shot `uv build` — the fallback would leave 114 as effectively a no-op packaging-wise and force all work to ticket 115. **Consequence**: a logically self-defeating mitigation shipped in the first spec draft; removing it required explicit trade-off reasoning the spec had not performed upfront.

**Problem**: Ran AskUserQuestion with the "Rename now in this ticket" option labeled only with "scope expands ~292 file edits" without naming the full scale of indirect touchpoints (mock-patch strings, uvicorn colon-syntax, settings.json permission rules, orchestrator prompt text, in-flight runner `.venv` transition risk). **Consequence**: user made the correct scope call but without full visibility into the commit-atomic-land requirement that only surfaced later during critical review.

**Problem**: Dispatched 4 adversarial reviewers with abbreviated spec excerpts rather than the full artifact; Agent 4 needed to check whether `[build-system]` was already present in `pyproject.toml` — a check the full spec would have obviated. **Consequence**: reviewer spent tool calls rediscovering context the spec already contained; findings quality was still high but the dispatch prompts could have been tighter.
