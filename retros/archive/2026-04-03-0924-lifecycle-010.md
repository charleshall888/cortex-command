# Session Retro: 2026-04-03 09:24

## Problems

**Problem**: The initial research assumed paths in SKILL.md resolve relative to the skill's own directory (`~/.claude/skills/{skill}/`), but they actually resolve against the CWD. **Consequence**: The first spec was built around a wrong mechanism, requiring the entire approach to be replaced with `${CLAUDE_SKILL_DIR}` after the user asked about cross-repo behavior.

**Problem**: The initial "compliant" assessment of `refine/SKILL.md` (using `skills/lifecycle/references/clarify.md`) was wrong — repo-relative paths are also broken from other repos. **Consequence**: `refine` was omitted from the initial fix scope and had to be added after the mechanism was clarified.

**Problem**: The first spec prohibited `${CLAUDE_SKILL_DIR}` as a Non-Requirement ("the repo-relative convention is the established pattern"), which was the correct fix. **Consequence**: The spec actively blocked the right approach; had to be reversed on the next revision.

**Problem**: The claim "sub-files are never loaded directly by other repos" in the backlog item was accepted without verification during Clarify. **Consequence**: It took the user asking a direct question about wild-light for the flaw to surface; Clarify should have flagged this as an unsupported assertion.

**Problem**: The research-to-spec transition produced a spec with two conflicting grep commands for verification (Requirement 1 and Technical Constraints) without noticing the contradiction. **Consequence**: Required a critical-review cycle to catch and fix.

**Problem**: Sub-file-to-sub-file references (e.g., `clarify.md` → `clarify-critic.md`) were outside the initial audit scope (`skills/*/SKILL.md` only), requiring the user to explicitly ask whether to expand scope. **Consequence**: The bug was only half-fixed in the first version of the plan; the user had to initiate the scope expansion.
