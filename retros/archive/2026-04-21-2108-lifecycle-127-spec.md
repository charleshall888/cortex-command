# Session Retro: 2026-04-21 21:08

## Problems

**Problem**: Initial Clarify confidence assessment rated criticality "high" when ticket frontmatter declared `priority: critical` and the research feasibility table named the fix "Critical". **Consequence**: Critic had to flag the unilateral downgrade; required rework to revise the assessment and re-log the clarify_critic event with an applied fix.

**Problem**: Initial Clarify rated complexity "simple" on the premise that the rename was mechanical, without auditing whether `{plan_path}` appeared in multiple session-level locations or whether the instruction block was a new pattern. **Consequence**: Critic escalated to "complex"; research had to be re-scoped for complex+critical dispatch count (5 agents instead of 3-4).

**Problem**: Spec's initial R4 hardcoded `lifecycle/<feature.slug>/plan.md` as "authoritative" based on the earlier interview answer "matches what the executor already reads" — without first checking whether any other consumers read the state field. **Consequence**: Critical review later revealed the path is hardcoded across 7+ sites (`feature_executor.py` at 238/518/686/755, `report.py:734`, `daytime_pipeline.py:208/325`, `dashboard/seed.py:101`) AND that `plan.py:404` supports a `BacklogItem.plan` override that is already broken pipeline-wide. The Spec interview question presented the user with incomplete context, and the decision was revisited post-critical-review.

**Problem**: Spec's initial delimiter choice was angle-bracket `<feature.X>` per research recommendation, without weighing Claude's training prior against it. **Consequence**: Critical review caught that `<X>...</X>` is trained as structural XML and `{{double_brackets}}` is Anthropic's documented template convention — the choice inverted the signals most specific to Claude. Required reverting to double-brace syntax.

**Problem**: Initial Edge Case claim "mis-substituted angle-bracket tokens would also fail plan-path checks the same way literal `{plan_path}` would" was factually wrong. **Consequence**: Critic caught that literal `<` and `>` on disk cascades differently through shell interpolation and regex hooks than a plausible absolute path; the edge case had to be rewritten.

**Problem**: Initial R6 acceptance criteria were string-presence only (grep for certain substrings), permitting a compliant-but-weak descriptive block. **Consequence**: Critic demonstrated that "pre-filled tokens are not something you substitute" would satisfy the check while reading as taxonomy, not prohibition. Had to strengthen R6 to require imperative markers (`MUST NOT`, `YOU substitute`, `CRITICAL:`/`IMPORTANT:`/`YOU MUST:` opener).

**Problem**: Initial R7 test-harness spec allowed extracting the Python body of `fill_prompt()` into a helper module as an alternative to shelling out to the real function. **Consequence**: Critic caught that the helper would be a copy of substitution logic and could drift silently from the real `runner.sh`. Had to narrow R7 to require real `fill_prompt()` invocation via subprocess.

**Problem**: Initial R8 "single-commit deploy" claim over-sold atomicity; didn't acknowledge that `runner.sh` is bash-loaded once and a mid-session `git pull` could leave the in-memory `fill_prompt()` pointing at the old token name while the on-disk prompt has the new name. **Consequence**: Critic caught the bash-semantics gap; had to add R8c explicitly documenting the residual race and mandating an operational no-active-runner check (R8b consulting `~/.local/share/overnight-sessions/active-session.json`).

**Problem**: Spec Interview question 1 (executor reconciliation) framed "Pin to literal path" as "Matches what the executor already reads. Minimal scope creep" without surfacing that `state.features[<slug>].plan_path` had NO production reader at all and that the `BacklogItem.plan` override feature was silently broken pipeline-wide. **Consequence**: User's initial answer was made on incomplete information. Critical review exposed the pipeline-wide breakage, and the decision was revisited with the user choosing "best long-term fix" — reverting R4 to state-driven at the prompt layer.

**Problem**: Initial clarify §3 requirements alignment claim "Aligned with requirements/multi-agent.md" waved at subsystem coverage without citing a specific functional requirement. **Consequence**: Critic flagged the evidence-free alignment assertion. Was eventually dismissed with rationale (prompt substitution is implementation detail within a documented functional area), but the assertion should have been evidence-backed from the start.

**Problem**: Discovery Bootstrap used the backlog item's `discovery_source` field correctly to locate epic research, but did not surface to the user that research/spec would be delegated to `/refine` — the user had to infer from the subsequent skill invocations. **Consequence**: Minor opacity in the phase-entry handoff; not a correctness issue but a communication gap.

**Problem**: Invoked `/refine` without first reading `skills/refine/SKILL.md` verbatim in some earlier iterations of this session's approach — caught and corrected, but still a deviation from the lifecycle SKILL.md's explicit instruction "Read `skills/refine/SKILL.md` verbatim. Do not paraphrase or reconstruct `/refine`'s protocol from training context." **Consequence**: Recovered without issue because the protocol was read after the skill invocation began, but the ordering violated the lifecycle's documented handoff sequence.
