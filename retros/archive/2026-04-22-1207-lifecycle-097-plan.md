# Session Retro: 2026-04-22 12:07

## Problems

**Problem**: Wrote the plan's phase-detection verification command as `detect_lifecycle_phase("lifecycle/...")` — passed a `str` to a function whose signature is `(feature_dir: Path) -> str`. **Consequence**: The command raises `TypeError` at `claude/common.py:113` before reaching the assertion; critical-review caught it, but I had shipped the same bug to the plan because I never executed the command against the real tree during plan writing.

**Problem**: Wrote the R19 pre-merge historical sweep as `grep -rln 'dispatch_complete.*"mode": *"worktree"' lifecycle/*/events.log` — the `mode` field lives on `implementation_dispatch` events, not on `dispatch_complete` events, in the real `devils-advocate-smart-feedback-application/events.log`. Also used plain `grep` (no `-E`) so the `*` meta is literal. **Consequence**: The tripwire returns zero hits against its own intended target; scope-expansion gating described in Veto Surface is inoperative until the grep is fixed. Only caught by the Verification-coverage reviewer running the grep against the real file.

**Problem**: Wrote the Task 7 `feature_complete` event with space-after-colon NDJSON (`{"ts": "...", "event": "..."}`), inconsistent with the file's 12 prior lines which use no-space (`{"ts":"...","event":"..."}`). **Consequence**: Format drift from file convention; spec R19's own grep (no-space) would not match the Task-7-written event; caught by critical-review cross-check against sibling events.logs.

**Problem**: Set Task 2's `Depends on: none` when it should have been `Depends on: [7]` — Task 2 deletes the Worktree-Aware Phase Detection override that currently masks devils-advocate's missing `feature_complete`, and Task 7 adds that event. **Consequence**: Feature-branch HEAD between the two commits would have misclassified devils-advocate as `implement 0/4` for any consumer reading mid-PR. Caught by the atomicity reviewer; fix was to invert the dependency edge.

**Problem**: Left the Verification Strategy as prose-only below the Tasks list, assuming the implementer would remember to run the six cross-cutting checks. **Consequence**: Three of the six checks (R17 writer sweep, R19 pre-merge sweep, `just check-symlinks`) had no per-task coverage; `implement.md` §4's auto-proceed would transition to review/complete the moment Task 8 flipped `[x]`, silently skipping the cross-cutting gate. Caught by the "Verification Strategy not gated" reviewer; fix was to add Task 9 with `Depends on: [1-8]` as an executable gate.

**Problem**: Replicated spec-level verification bugs (the TypeError and the wrong-event grep) into the plan by trusting the spec's acceptance criteria verbatim rather than running them. **Consequence**: Spec R19 acceptance (spec.md:55) and Technical Constraints (spec.md:112) still carry the same bugs; plan's Veto Surface now flags them and marks the spec amendment as deferred, but the spec artifact itself is stale against its own executable criteria.

**Problem**: Ran a compound `ls` with `&& cat` / `|| echo` that exited non-zero (`.dispatching` missing), which cancelled two parallel Read tool calls dispatched in the same batch. **Consequence**: Lost a round of parallel file reads; had to retry each call sequentially with individual `test -f` guards.

**Problem**: `/commit` failed with GPG signing error mid-session despite the repo's `excludedCommands` config claiming `git` children bypass the sandbox. **Consequence**: Had to retry with `dangerouslyDisableSandbox: true` to land the plan commit. Not a correctness issue I caused, but it is friction that suggests `sandbox-behaviors.md`'s "git children are fully unsandboxed" claim may not hold reliably.
