# Session Retro: 2026-04-21 21:08

## Problems

**Problem**: Codebase research agent (Agent 1) produced a confident "verified mechanism" explaining the bug as cross-repo misrouting at runner.sh:1180-1198, but did not check the actual state.json of historical sessions. All 5 sessions had `integration_worktrees={}` and took the single-repo branch — Agent 1's proposed fix would not have committed any of the failed sessions. **Consequence**: I built the initial spec framing on a wrong diagnosis; the adversarial agent had to catch it and I had to revalidate from scratch, then substantially rewrite research.md and spec.md.

**Problem**: I did not cross-check Agent 1's mechanism claim against the single data point already in my context — session 2026-04-01-2112 committed successfully with an empty `integration_worktrees` too, which contradicts the cross-repo misrouting theory. **Consequence**: Agent 1's framing survived into the first draft of research.md and its rebuttal came only from the adversarial agent, not from earlier verification.

**Problem**: Initial spec introduced `morning_report_commit_result` as a new event without checking the events.py EVENT_TYPES registry. log_event raises ValueError on unknown event names. **Consequence**: As written, R2's acceptance test could never pass because the runner would crash on first emit. Critical review caught it; had to add Req 2 (register constants) and rewrite Req 3/4.

**Problem**: Initial spec consolidated research.md's richer `morning_report_generate_result` event (with per-path sha256/path/size fields) into a leaner commit_result event with only `{status, commit_sha, staged_files}`. The dropped fields were exactly what would discriminate wrong-path writes from legitimate no-ops. **Consequence**: Spec was strictly weaker instrumentation than research prescribed; critical review had to force it back in.

**Problem**: Initial spec's backfill acceptance (R6) only checked commit count, subject marker, and date sort — not blob content. The sync-allowlist `--theirs` auto-resolution could silently discard backfill content while all shape-level acceptance passes. **Consequence**: Had to add sha256 byte-equality check and harden preflight to scheduler-disable during full PR lifecycle.

**Problem**: I used the phrase "critical-criticality, complex-tier" in a fix-agent prompt during orchestrator cycle 1 when the actual criticality is HIGH. The fix agent propagated "This ticket has critical criticality and complex tier" into spec.md line 11. **Consequence**: Cosmetic factual inaccuracy in the approved spec; not load-bearing but a durable artifact of a sloppy prompt.

**Problem**: I moved through Clarify without re-verifying the ticket's mechanism claim against disk state — I trusted the ticket's "every session has failed to commit" framing until halfway through Research when I discovered commit 85e87aa existed. **Consequence**: Clarify's intent statement and scope were built on a wrong premise; required mid-Research reframing of the spec direction.

**Problem**: I dropped research.md's recommendation to audit consumers of the target-worktree copy (`claude/dashboard/data.py:942` per adversarial agent) and relegated it to a spec-phase concern. It still isn't resolved — spec's Non-Requirements say cross-repo branch isn't changed, but whether dropping the dead worktree write breaks any consumer remains open. **Consequence**: If the implementation actually removes any worktree write path, a downstream consumer could silently regress.

**Problem**: I didn't bundle research.md's open question Q3 (surface `|| echo "Warning..."` via structured event, not just stdout) into the initial spec. Critical review had to explicitly surface it. **Consequence**: First-draft spec would have preserved the silent-to-morning-report-reader failure mode for exception-path failures.

**Problem**: My Clarify §3a critic agent correctly flagged that I was citing requirements/pipeline.md:24 for the morning-report commit criterion without proving it. I had read the file but my initial attribution was ambiguous between that line and docs/overnight-operations.md:413 (same sentence). I dismissed with rationale, but the ambiguity persisted into research.md's Requirements section and the spec. **Consequence**: Minor citation sloppiness that survived multiple phase transitions; adversarial agent 4 (docs partial-reconcile) latched onto the same issue and forced clarification.
