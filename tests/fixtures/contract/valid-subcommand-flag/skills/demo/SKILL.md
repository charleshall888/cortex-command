---
name: demo
description: Valid subcommand-flag fixture — known subcommand with all required flags must produce zero violations
---

Emit a research sizing event with all required flags:

```bash
cortex-discovery emit-research-sizing --topic my-feature --complexity simple --criticality medium
```
