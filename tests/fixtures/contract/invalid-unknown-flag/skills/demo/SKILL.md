---
name: demo
description: Unknown flag fixture for contract lint
---

Create a backlog item with an unknown flag:

```bash
cortex-create-backlog-item --title "My feature" --status backlog --type feature --bogus-flag value
```
