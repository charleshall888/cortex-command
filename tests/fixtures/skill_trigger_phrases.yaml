# Trigger-phrase corpus for canonical SKILL.md description regression gate.
#
# Each phrase listed under a skill's `must_contain` array must appear as a
# case-sensitive substring of the corresponding canonical
# `skills/<skill>/SKILL.md` `description` frontmatter field. The phrases are
# the high-signal user-facing utterances that route to the skill — silently
# dropping them from the description breaks routing for users who say them.
#
# Sourced from the post-#178 SKILL.md description text on main at the time
# this file was authored (#181 sub-task last). To add a new skill or phrase,
# edit this file via standard PR review — `tests/test_skill_descriptions.py`
# enforces substring presence at CI time.
skills:
  backlog:
    must_contain:
    - /cortex-backlog:backlog
    - add to backlog
    - pick a backlog item
  backlog-author:
    must_contain:
    - write a ticket body
    - author a backlog item
  commit:
    must_contain:
    - /cortex-core:commit
    - make a commit
    - commit these changes
  critical-review:
    must_contain:
    - critical review
    - pressure test
    - adversarial review
    - challenge from multiple angles
  dev:
    must_contain:
    - /cortex-core:dev
    - what should I work on
    - where do I start
  discovery:
    must_contain:
    - discover this
    - decompose into backlog
    - break this down into tickets
  interview:
    must_contain:
    - thinking-partner interview
    - General-purpose priming interview
    - grilling
    - help you think through a topic
  lifecycle:
    must_contain:
    - start a lifecycle
    - lifecycle research/specify/plan/implement/review/complete
    - start a feature lifecycle
    - structured phases
  morning-review:
    must_contain:
    - /morning-review
    - morning review
    - what happened overnight
  overnight:
    must_contain:
    - /overnight
    - start overnight session
    - launch overnight
  pr:
    must_contain:
    - /cortex-core:pr
    - create a pr
    - open a pull request
  refine:
    must_contain:
    - refine backlog item
    - prepare for overnight
    - prepare feature for execution
    - "Clarify \u2192 Research \u2192 Spec"
  requirements:
    must_contain:
    - /cortex-core:requirements
    - gather requirements
    - define project scope
  requirements-gather:
    must_contain:
    - Interview-only sub-skill
    - produces a structured Q&A markdown block
  requirements-write:
    must_contain:
    - Synthesize-only sub-skill
    - v2-compliant
    - never conducts an interview itself
  research:
    must_contain:
    - /cortex-core:research
    - research this topic
    - investigate this feature
