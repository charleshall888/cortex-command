# Example

The `cortex-pr-review` plugin ships review skills. R5(c) excludes plugin
names from candidate evaluation, so this should produce no violation even
though no `bin/cortex-pr-review` is deployed.
