---
name: example
description: Table-cell-with-narrative wiring fixture (R6 boundary)
---

Run `cortex-foo` to invoke the primary helper, and `cortex-bar` for the sibling.

| script | purpose |
| --- | --- |
| `cortex-foo` | example helper |
| `cortex-bar` | sibling helper |
