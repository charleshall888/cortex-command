# landing-page/_imports/

Staging area for Claude Design handoff exports.

## Current contents

- **`Cortex Command v9.html`** — design source for the landing page, exported from Claude Design on 2026-05-15. This is the read-only canonical artifact that `docs/index.html` is derived from (with edit-mode scaffold stripped, mobile/a11y/reduced-motion fixes applied, the creative additions detailed in the implementation plan).

## Workflow

Files here are imports, not products. Edit `docs/index.html` directly (or the future multi-file `src/` rebuild — see the rebuild backlog ticket). When Claude Design produces a `v10.html`, drop it here, diff against `docs/index.html`, and land changes section by section.

Do not edit files in this directory.
