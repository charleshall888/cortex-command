# Plan: Artifact template cleanups — D1 §3 Architectural Pattern with enum annotation, D3 index.md body-trim via wikilink-append removal at 4 sites, D4 ## Open Decisions optional in specify.md §3

## Overview

Apply three independent canonical-source template edits under `skills/` — D1 inserts an optional `**Architectural Pattern**` field with an explicit-conditional+enum annotation into the default plan.md §3 template (additive: ~2 lines of template prose per future non-critical plan, balanced against per-plan authoring discipline), D3 stops the 4 phase-write sites from appending per-artifact wikilinks to `lifecycle/{feature}/index.md` (subtractive; the SKILL.md template body already emits the target B1+ shape so D3a is verification-only), D4 annotates `## Open Decisions` in specify.md §3 as optional (subtractive: opens ~71 latent ceremonial sections to omission going forward), then run the dual-source mirror regeneration and verify all eleven spec acceptance criteria. D1 is the only additive item in an otherwise subtractive ticket — Task 1's Context names this asymmetry so it is not framed as homogeneous with D3/D4. The three edit tasks are decomposed by spec-requirement family (D1 / D3 / D4), each independent at the Implement-phase batching layer (all three carry `Depends on: none` per the worktree-merge mechanics of non-overlapping string-anchored edits), with Task 4 gating the feature on the structural mirror-drift check that the pre-commit hook also enforces AND on the strict-sequencing prerequisite that #183's Gate 2 prose has been deleted from `skills/lifecycle/SKILL.md` before Task 3's specify.md edit lands.

## Tasks

### Task 1: D1 — Add optional Architectural Pattern field with enum annotation to plan.md §3 default template
- **Files**: `skills/lifecycle/references/plan.md`
- **What**: Insert a `**Architectural Pattern**: {category}` line plus its inline enum annotation into the §3 default plan template's Overview block (currently L137-173, with `## Overview` at L140 and `## Tasks` at L143), preserving the §1b critical-tier prompt and the orchestrator-review P8 row untouched.
- **Depends on**: none
- **Complexity**: simple
- **Context**: Anchor the edit on the string `## Overview` heading at L140 and the placeholder line `[1-2 sentence summary of the implementation approach and key architectural decisions]` at L141 — insert the new field directly after the Overview placeholder so it renders before `## Tasks`. The annotation must match the exact phrasing required by Req 1's second acceptance grep: *"Include only when the implementation commits to one of: event-driven, pipeline, layered, shared-state, plug-in. Omit otherwise."* (the closed enum mirrors `plan.md` L32, L52-53, L73 for cross-tier consistency). Pre-edit baseline: `grep -c "Architectural Pattern" skills/lifecycle/references/plan.md` returns the §1b critical-tier count (3); post-edit must return 4. Do NOT touch §1b (L26-123) or `skills/lifecycle/references/orchestrator-review.md` L165 (P8 row). The annotation deliberately uses explicit-conditional+enum form (per spec edge case "Soft-MUST drift in template prose" and research adversarial mitigation #6) — do not soften to "when load-bearing" phrasing. Edit by string anchor (the `## Overview` heading and placeholder text), not by absolute line number — line numbers are advisory and may drift if other edits land first.
- **Verification**: Three greps must all pass on the post-edit file: (i) `grep -c "Architectural Pattern" skills/lifecycle/references/plan.md` returns `4`, (ii) `grep -c "Include only when the implementation commits to one of: event-driven, pipeline, layered, shared-state, plug-in" skills/lifecycle/references/plan.md` returns ≥ `1`, (iii) `grep -c "Architectural Pattern" skills/lifecycle/references/orchestrator-review.md` returns the same count as on `main` (verified unchanged via `git show main:skills/lifecycle/references/orchestrator-review.md | grep -c "Architectural Pattern"`). Pass when all three conditions hold.
- **Status**: [x] complete — landed at commit 4c4229d

### Task 2: D3b-D3e — Remove "Add wikilink" instructions at the 4 index.md append sites
- **Files**: `skills/lifecycle/references/plan.md`, `skills/lifecycle/references/review.md`, `skills/refine/SKILL.md`
- **What**: Delete the four `- Add wikilink: - {Phase}: [[…]]` instruction lines so that phase-write index.md updates no longer regenerate per-artifact wikilink bodies after the SKILL.md template (which already omits them) is honored.
- **Depends on**: none
- **Complexity**: simple
- **Context**: Four anchor lines to delete, all matching the pattern `- Add wikilink: - {Phase}: [[{lifecycle-slug}/{file}|{file}.md]]`. Current locations (verified): `skills/lifecycle/references/plan.md` L240 (`- Plan:`), `skills/lifecycle/references/review.md` L150 (`- Review:`), `skills/refine/SKILL.md` L142 (`- Research:`), `skills/refine/SKILL.md` L171 (`- Spec:`). Each site sits inside an `If "{phase}" is already in the artifacts array...` block; the `artifacts` array append and `updated` field update must remain — only the wikilink-append bullet is deleted. After deletion, the four update-step blocks each become three steps (skip-if-present, append-to-array, update `updated`, rewrite atomically) instead of four. Edit by string anchor (the `Add wikilink:` prefix), not by absolute line number — Task 1's edit at L140-141 of `plan.md` does not conflict with Task 2's edit at L240 of the same file (non-overlapping content-anchored hunks ~99 lines apart merge cleanly under git 3-way merge), and the implementer's grep-based anchor verification re-grounds the L240 location independently of upstream changes.
- **Verification**: Four greps must all return `0` on the post-edit files: `grep -A 10 "If \"plan\" is already in the" skills/lifecycle/references/plan.md | grep -c "Add wikilink"`, `grep -A 10 "If \"review\" is already in the" skills/lifecycle/references/review.md | grep -c "Add wikilink"`, `grep -A 10 "If \"research\" is already in the" skills/refine/SKILL.md | grep -c "Add wikilink"`, `grep -A 10 "If \"spec\" is already in the" skills/refine/SKILL.md | grep -c "Add wikilink"`. Pass when all four counts are `0`.
- **Status**: [x] complete — landed at commit 85cd4de

### Task 3: D4 — Mark `## Open Decisions` optional in specify.md §3 template
- **Files**: `skills/lifecycle/references/specify.md`
- **What**: Annotate the §3 template's `## Open Decisions` heading (currently L139-140) so authors may omit the section entirely when empty, while the §2b Pre-Write "Open Decision Resolution" prose remains in force.
- **Depends on**: none
- **Complexity**: simple
- **Context**: Anchor on the string `## Open Decisions` heading at L139 and the existing placeholder bullet at L140. Add an annotation containing the exact substring `"Optional — omit when empty"` (required by Req 8's first acceptance grep) plus the operative author-facing phrasing *"omit when empty; include with items when uncertain at spec time"* — pattern style follows `critical-review/SKILL.md:147` (explicit conditional, no normative-judgment phrases like "when load-bearing"). The §2b prose at L91-97 (`**Open Decision Resolution**: Before adding any item to ## Open Decisions, attempt to resolve it using this order...`) MUST remain untouched — Req 8's second acceptance grep checks that the `Open Decision Resolution` heading count is unchanged from baseline. **Strict-sequencing contract** (per spec frontmatter `blocked-by: [183]` and research §S1, user's clarify-phase choice): the plan delivers D4's canonical-source edit in this branch, but the PR does not ship to `main` until #183's Gate 2 prose has been deleted from `skills/lifecycle/SKILL.md` and that deletion has reached `main`. The enforcement is wired in Task 4's verification (a Gate 2 prose absence grep on the branch's local `skills/lifecycle/SKILL.md` is a hard precondition for Task 3's commit). If the branch is rebased on a `main` that does not yet contain #183's Gate 2 deletion, Task 4's check fails closed and Task 3's edit cannot be committed — eliminating the "Gate 2 transient-window" doc-inconsistency the spec's Edge Cases section names.
- **Verification**: Two greps must both pass on the post-edit file: (i) `grep -c "Optional — omit when empty" skills/lifecycle/references/specify.md` returns ≥ `1`, (ii) `grep -c "Open Decision Resolution" skills/lifecycle/references/specify.md` returns the same count as on `main` (verified unchanged via `git show main:skills/lifecycle/references/specify.md | grep -c "Open Decision Resolution"`). Pass when both conditions hold.
- **Status**: [-] deferred — Gate 2 prose still present on main (#183 not merged); strict-sequencing per plan Veto Surface item 1. To be landed in a #180-followup ticket after #183 merges its Gate 2 deletion to main. Deferral logged in events.log batch 0.

### Task 4: Verify D2/D3a no-op-state + Reqs 9/10/11 acceptance + Gate 2 prose absence + run mirror regeneration
- **Files**: `skills/lifecycle/SKILL.md`, `skills/lifecycle/references/plan.md`, `backlog/180-artifact-template-cleanups-architectural-pattern-scope-boundaries-indexmd.md`, `plugins/cortex-core/skills/lifecycle/SKILL.md`, `plugins/cortex-core/skills/lifecycle/references/plan.md`, `plugins/cortex-core/skills/lifecycle/references/review.md`, `plugins/cortex-core/skills/lifecycle/references/specify.md`, `plugins/cortex-core/skills/refine/SKILL.md`
- **What**: Run the six remaining spec acceptance checks (Reqs 2, 3, 9, 10, 11), wire the Gate 2 prose absence check as the strict-sequencing precondition for Task 3's commit, and trigger the dual-source mirror regeneration confirming no canonical-mirror drift after Tasks 1-3 land.
- **Depends on**: [1, 2, 3]
- **Complexity**: simple
- **Context**: Each sub-check has its own pass/fail line so the multi-Req verification has explicit per-Requirement attestation. **Req 2** (D2 no-op): `grep -c "Scope Boundaries" skills/lifecycle/references/plan.md` returns the same count as `git show main:skills/lifecycle/references/plan.md | grep -c "Scope Boundaries"`. **Req 3** (D3a): `grep -c "Feature lifecycle for" skills/lifecycle/SKILL.md` returns ≥ 1 (passes on current state at L150) AND the "Create index.md" template block contains no per-artifact wikilink patterns — anchor the scan on heading text rather than absolute line numbers so the check survives upstream line drift, via `sed -n '/^### Create index.md/,/^### Backlog Write-Back/p' skills/lifecycle/SKILL.md | grep -cE '- (Research|Spec|Plan|Review): \[\['` returns `0`. **Req 9** (blocked-by:[183]): `grep -E "^blocked-by:" backlog/180-artifact-template-cleanups-architectural-pattern-scope-boundaries-indexmd.md` matches a line containing `[183]`. **Req 10** (mirror regeneration): `just build-plugin && git diff --exit-code plugins/cortex-core/skills/` exits 0 — the pre-commit hook will also run this automatically, but the explicit invocation verifies drift-free state independent of commit timing. **Req 11** (47.1% documentation hygiene): `grep -c "47.1%" lifecycle/artifact-template-cleanups-architectural-pattern-optional-indexmd-body-trim-frontmatter-preserved-d4-open-decisions-optional/spec.md` returns ≥ 1 (passes on current state) AND `git diff main backlog/180-*.md backlog/172-*.md | grep -E '^[+-]' | grep -v '^[+-]{3}'` shows only frontmatter-line diffs — no body-line edits. **Strict-sequencing prerequisite for Task 3** (the Gate 2 prose absence check that wires `blocked-by:[183]` to a runtime gate rather than a frontmatter-existence-only check): `sed -n '/^### 6\\./,/^##/p' skills/lifecycle/SKILL.md | grep -cE "Gate 2|Specify\\s*[→-]\\s*Plan complexity escalation check"` returns `0` — meaning Step 6's Gate 2 prose (currently at L270-274 on `main`) has been deleted (i.e., #183's Gate 2 deletion has reached the branch's base). If this returns nonzero, Task 3's specify.md edit MUST NOT be committed; the operator either waits for #183 to merge and rebases, or splits Task 3 into a follow-up ticket. **Regression checks**: all four Task 2 wikilink-removal greps and all three Task 1 architectural-pattern greps still pass post-Task-4. Wiring Co-Location: no new `bin/cortex-*` scripts; D3 documentation-side edits are honored at write-back sites whose consumers are already in place (the SKILL.md template, P-checklist 4 of orchestrator-review which already does not reference per-artifact wikilinks).
- **Verification**: Seven checks pass: (a) Req 2 grep count equals `main` baseline; (b) Req 3 both conditions hold (heading-anchored sed returns `0`); (c) Req 9 grep matches `[183]`; (d) Req 10 — `just build-plugin && git diff --exit-code plugins/cortex-core/skills/` exits with code 0; (e) Req 11 grep returns ≥ 1 AND backlog body diff shows only frontmatter changes; (f) Gate 2 prose absence check returns `0` (else: Task 3 commit blocked, surface blocker to operator); (g) all four Task 2 wikilink-removal greps and all three Task 1 architectural-pattern greps still pass (regression). Pass when all seven conditions hold; fail when any single condition fails.
- **Status**: [ ] pending

## Verification Strategy

End-to-end verification runs the eleven-grep acceptance battery from spec.md against the canonical sources AND the regenerated mirrors. The pre-commit hook (`.githooks/pre-commit` Phase 3, L168-174) re-runs `just build-plugin` on every commit and fails with stderr surfaced if the mirror regeneration produces drift — so any commit landing through the standard hook chain implicitly verifies Req 10. Task 4's explicit `just build-plugin` invocation is the belt-and-braces check independent of commit timing, and its acceptance command `git diff --exit-code plugins/cortex-core/skills/` is the same drift check the hook performs in Phase 4. No new test files are written; the spec's eleven `grep` / `git show` / `git diff` commands ARE the test surface. The current state of D3a (already-target B1+ shape) and the spec's 47.1% citation (already present in spec.md) are verification-only requirements that pass on entry — Task 4 confirms they remain passing after Tasks 1-3 commit. **Strict-sequencing enforcement**: Task 4's Gate 2 prose absence check is the runtime gate that ties Task 3's commit to #183's actual delivery, not to the structural existence of the `blocked-by:[183]` frontmatter field. If the branch is rebased on a `main` lacking #183's Gate 2 deletion, Task 4 fails closed and Task 3's specify.md edit is blocked — the operator either waits-and-rebases or splits Task 3 to a follow-up ticket. End-to-end smoke check after merge: create a fresh `lifecycle/{slug}/index.md` via the SKILL.md "Create index.md" path and confirm it contains H1 wikilink + intro line + frontmatter only (no per-artifact wikilinks), AND create a fresh non-critical plan.md via the §3 default template and confirm the `**Architectural Pattern**: {category}` line + enum annotation render before `## Tasks`. Critical-tier plan emission (§1b) and the orchestrator-review P8 row are not exercised by this ticket — both remain unchanged and their existing tests/usage are the regression net.

## Veto Surface

- **#183 sequencing (committed position)**: the plan implements all eleven spec requirements and wires `blocked-by:[183]` to a runtime check in Task 4 (Gate 2 prose absence on the branch's local `skills/lifecycle/SKILL.md` must hold before Task 3's specify.md edit is committed). This implements research §S1's strict-sequencing decision faithfully — D4's canonical-source edit appears in this branch but the PR holds for #183 to merge first. The user could veto this and instead split Task 3 into a follow-up ticket (landing #180 with 10 of 11 requirements, then a #180-followup with D4 only after #183 merges). The current committed plan trades simpler ticket scope (all 11 in one PR) for a tighter merge-order constraint (this PR holds for #183); the alternative trades two PRs for parallel mergeability. Either is structurally sound.
- **D3a is verification-only, folded into Task 4.** The spec lists D3a (Req 3) as a SKILL.md edit, but the current `skills/lifecycle/SKILL.md` "Create index.md" template body (L145-151) already emits exactly the B1+ shape the spec specifies (H1 wikilink + intro line, no per-artifact wikilinks). No edit to the SKILL.md template is needed; D3a's acceptance grep already passes. Task 4 includes the heading-anchored verification (line-number-anchored grep was rejected to survive line drift per the spec's Technical Constraint at L74). The user could veto and request an explicit "verify SKILL.md template is unchanged" sub-task with its own status checkbox — current plan folds the verification into Task 4 for compactness.
- **Architectural Pattern enum annotation placement (Task 1).** The enum-required field is placed in the Overview block (between the placeholder and `## Tasks`). Alternative placements considered: (a) above the Overview placeholder as a header-level field; (b) at the top of `## Tasks`; (c) in a new standalone `### Architecture` subsection. Option (a) was selected because it co-locates with the existing one-line Overview prose, matches the §1b critical-tier emission shape, and avoids creating a new section. **Decision available**: any of (a)/(b)/(c) is structurally valid.

## Scope Boundaries

- This plan does NOT edit `## Scope Boundaries` in `skills/lifecycle/references/plan.md` (D2 no-op per spec Req 2; #182 owns the deletion via Outline replacement).
- This plan does NOT edit `skills/lifecycle/references/plan.md` §1b critical-tier prompt (L26-123) or `skills/lifecycle/references/orchestrator-review.md` P8 row (L165) — both already correctly gated on `criticality = critical`.
- This plan does NOT edit `skills/lifecycle/SKILL.md` Step 6 Gate 2 prose (L270-274) — #183 owns Gate 2 deletion; Task 4's Gate 2 prose absence check verifies that deletion has reached the branch's base before Task 3 commits.
- This plan does NOT retroactively compress existing `lifecycle/archive/*/index.md` files (~138 files). The SKILL.md L124 guard preserves them; mixed-format coexistence is benign per spec Edge Cases.
- This plan does NOT mutate the body of `backlog/180-*.md` or `backlog/172-*.md` to correct the inverted Scope Boundaries 47%/53% framing. The 47.1% correction lives only in this lifecycle's `spec.md` and `plan.md`; backlog-body grooming is a separate doc-hygiene pass.
- This plan does NOT add MUST/CRITICAL/REQUIRED language anywhere (all four sub-items are de-escalations: required→optional or no-op). CLAUDE.md MUST-escalation policy is not applicable.
- This plan does NOT update `.obsidian/` configuration or install Obsidian community plugins (no Dataview, no frontmatter-links). The B1+ shape (H1 + intro line preserved) is the chosen mitigation for the Obsidian graph-view regression.
- This plan does NOT introduce new `bin/cortex-*` scripts, new hooks, or new skills. All edits are to existing canonical-source template files under `skills/`.
- This plan does NOT add a permanent regression test (e.g., a `tests/test_index_md_template_shape.py`) for the SKILL.md "Create index.md" template body invariants. The Task 4 grep is point-in-time; a structural regression test is a follow-up if warranted.
