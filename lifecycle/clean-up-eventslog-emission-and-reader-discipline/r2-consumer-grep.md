# R2 Consumer-Grep Verification

Fresh repo-wide consumer-grep for each DELETE-row event from spec R1, run from the repo root on 2026-05-11.

Scope (per spec R2): `cortex_command/`, `bin/`, `hooks/`, `claude/`, `tests/`, `skills/`, `plugins/cortex-core/skills/`, `cortex_command/overnight/prompts/`.

Patterns:
- name-only: `grep -rn '"<event_name>"' <scope>`
- skill-prompt schema form: `grep -rn '"event": "<event_name>"' <scope>` (run against skill-prompt scopes)

Hit-categorization notes:
- Emitter sites are skill-prompt JSON examples instructing the agent to write an event line. They are not consumers.
- The canonical skill source lives under `skills/`; `plugins/cortex-core/skills/` is the auto-generated mirror regenerated by the pre-commit hook. Both are listed; both are emitter sites.
- Tests-only hits do not block deletion (per task spec) but are enumerated.
- The Python identifier `_check_task_completed` in `cortex_command/overnight/feature_executor.py` is a substring match against `task_complete`; the function actually consumes the `task_idempotency_complete` event (see lines 320-321), so it is **not** a consumer of the `task_complete` event. Listed as not-a-consumer.
- `skills/morning-review/references/walkthrough.md:303-305` describes a `requirements_updated` consumer scan — this is documented in spec R1 as "DELETE consumer scan" and is being removed by the same wave, so it is not a blocking external consumer for R2 purposes (the consumer is being deleted alongside the emitter under R1).

## Per-event results

| event_name | grep_pattern | scope | hit_count | classification (DELETE / RECLASSIFY: <reason>) |
|---|---|---|---|---|
| `task_complete` | `"task_complete"` and `"event": "task_complete"` | cortex_command/, bin/, hooks/, claude/, tests/, skills/, plugins/cortex-core/skills/, cortex_command/overnight/prompts/ | 3 total: 2 emitter sites (`skills/lifecycle/references/implement.md:187`, `plugins/cortex-core/skills/lifecycle/references/implement.md:187`); 1 tests-only (`tests/test_common_utils.py:115`). Substring-only false positive `_check_task_completed` in `cortex_command/overnight/feature_executor.py` consumes a different event (`task_idempotency_complete`) and is not a consumer. | DELETE |
| `confidence_check` | `"confidence_check"` and `"event": "confidence_check"` | (same scopes) | 4 emitter sites only: `skills/lifecycle/references/specify.md:65,76` and mirror `plugins/cortex-core/skills/lifecycle/references/specify.md:65,76`. No non-test consumers. | DELETE |
| `decompose_flag` | `"decompose_flag"` and `"event": "decompose_flag"` | (same scopes) | 3 total: 2 emitter sites (`skills/discovery/references/decompose.md:49`, mirror at `plugins/cortex-core/skills/discovery/references/decompose.md:49`); 1 tests-only (`tests/test_decompose_rules.py:195`). | DELETE |
| `decompose_ack` | `"decompose_ack"` and `"event": "decompose_ack"` | (same scopes) | 3 total: 2 emitter sites (`skills/discovery/references/decompose.md:50`, mirror at `plugins/cortex-core/skills/discovery/references/decompose.md:50`); 1 tests-only (`tests/test_decompose_rules.py:202`). | DELETE |
| `decompose_drop` | `"decompose_drop"` and `"event": "decompose_drop"` | (same scopes) | 3 total: 2 emitter sites (`skills/discovery/references/decompose.md:51`, mirror at `plugins/cortex-core/skills/discovery/references/decompose.md:51`); 1 tests-only (`tests/test_decompose_rules.py:209`). | DELETE |
| `discovery_reference` | `"discovery_reference"` and `"event": "discovery_reference"` | (same scopes) | 2 emitter sites only: `skills/lifecycle/SKILL.md:220` and mirror `plugins/cortex-core/skills/lifecycle/SKILL.md:220`. No non-test consumers. | DELETE |
| `implementation_dispatch` | `"implementation_dispatch"` and `"event": "implementation_dispatch"` | (same scopes) | 2 emitter sites only: `skills/lifecycle/references/implement.md:107` and mirror `plugins/cortex-core/skills/lifecycle/references/implement.md:107`. No non-test consumers. | DELETE |
| `orchestrator_review` | `"orchestrator_review"` and `"event": "orchestrator_review"` | (same scopes) | 4 emitter sites only: `skills/discovery/references/orchestrator-review.md:27`, `skills/lifecycle/references/orchestrator-review.md:42`, and mirrors at `plugins/cortex-core/skills/discovery/references/orchestrator-review.md:27`, `plugins/cortex-core/skills/lifecycle/references/orchestrator-review.md:42`. No non-test consumers. | DELETE |
| `orchestrator_dispatch_fix` | `"orchestrator_dispatch_fix"` and `"event": "orchestrator_dispatch_fix"` | (same scopes) | 4 emitter sites only: `skills/discovery/references/orchestrator-review.md:55`, `skills/lifecycle/references/orchestrator-review.md:72`, and mirrors at `plugins/cortex-core/skills/discovery/references/orchestrator-review.md:55`, `plugins/cortex-core/skills/lifecycle/references/orchestrator-review.md:72`. No non-test consumers. | DELETE |
| `orchestrator_escalate` | `"orchestrator_escalate"` and `"event": "orchestrator_escalate"` | (same scopes) | 4 emitter sites only: `skills/discovery/references/orchestrator-review.md:98`, `skills/lifecycle/references/orchestrator-review.md:120`, and mirrors at `plugins/cortex-core/skills/discovery/references/orchestrator-review.md:98`, `plugins/cortex-core/skills/lifecycle/references/orchestrator-review.md:120`. No non-test consumers. | DELETE |
| `requirements_updated` | `"requirements_updated"` and `"event": "requirements_updated"` | (same scopes) | 2 emitter sites: `skills/lifecycle/references/review.md:182` and mirror `plugins/cortex-core/skills/lifecycle/references/review.md:182`. Plus 1 in-wave consumer scan at `skills/morning-review/references/walkthrough.md:303-305` (and its mirror) — this is the consumer scan that R1 already lists for deletion in the same wave. No external consumers. | DELETE |

## Summary

All 11 DELETE-row events from spec R1 are confirmed dead: zero real non-test, non-emitter, non-legacy-tolerance consumers across the audited scope. No reclassifications required. The R1 deletion plan stands without revision.
