# Plan: lifecycleconfig-template-ships-dormant-skip-specify

## Overview

Comment the four dormant keys out of both parity-gated `lifecycle.config.md` copies using claim-free enum hints, pin the invariant with a positive-assertion test, correct the prose that claims the keys work, and quiet the four seeded configs on this machine. Key decisions: tasks are decomposed **by file** so each builder's write set is unambiguous and every claim site has exactly one owner — not because of the sub-task same-worktree hazard (`plan.md`'s co-scheduling rule applies to letter-suffixed siblings like `3a`/`3b`, which this plan has none of; integer tasks each get their own worktree). The consequence of by-file scoping is that a claim site missed by the census can be fixed by **no one**, since builders may not touch files outside their Files list — so the census below was re-derived by repo-wide grep rather than inherited from the spec. Verification follows Task 2's precedent throughout: **assert the corrected claim is present**, never merely that the false phrase is absent.

## Outline

### Phase 1: Template consistency (tasks: 1, 2)
**Goal**: both shipped templates carry all four dormant keys commented with claim-free hints, the mirror is regenerated, and a test pins the invariant so it cannot silently return.
**Checkpoint**: `.venv/bin/pytest tests/test_lifecycle_config_parity.py tests/test_lifecycle_config.py tests/test_lifecycle_config_dormant_template.py -q` green, and a fresh-init config emits zero dormant-key warnings.

### Phase 2: Documentation truth-up (tasks: 3, 4, 5, 6, 9)
**Goal**: no live prose in the repo claims the four keys are honored, and none claims CI enforces the parity gate.
**Checkpoint**: `grep -rIn 'CI parity gate\|CI gates merge' --include='*.md' --include='*.py' . | grep -v '^./cortex/lifecycle/'` returns nothing, AND each task's positive assertion holds. (The `cortex/lifecycle/` exclusion covers this feature's own artifacts plus the archived `reconcile-lifecycleconfigmd-*` lifecycle, which are historical records — preserve-don't-rewrite, same convention as `cortex/research/archive/`.)

### Phase 3: Seeded-config sweep (tasks: 7, 8)
**Goal**: all four seeded configs stop warning; external-repo edits land as focused single-file commits in their own repos.
**Checkpoint**: the four-repo warning count reads 0 / 0 / 0 / 0 (baseline 2 / 2 / 2 / 4), and each config carries the keys commented, not deleted.

## Tasks

### Task 1: Comment the four dormant keys out of both templates and regenerate the mirror
- **Files**: `cortex_command/init/templates/cortex/lifecycle.config.md`, `skills/lifecycle/assets/lifecycle.config.md`, `plugins/cortex-core/skills/lifecycle/assets/lifecycle.config.md`
- **What**: Replaces lines 10–13 of both hand-maintained copies with four claim-free commented keys, then regenerates the rsync mirror. This is the ticket's actual fix.
- **Depends on**: none
- **Complexity**: simple
- **Context**: Both copies currently read `# default-tier:           # simple | complex (override auto-assessment)` / `# default-criticality:    # low | medium | high | critical` / `skip-specify: false` / `skip-review: false` at lines 10–13. Target state (spec R1), byte-identical in both, trailing hint aligned at column 26 to match the existing `# default-criticality:` line:
  ```
  # default-tier:           # simple | complex
  # default-criticality:    # low | medium | high | critical
  # skip-specify:           # true | false
  # skip-review:            # true | false
  ```
  Four constraints: (1) **no `---` in any hint** — `tests/test_lifecycle_config_parity.py:56` does `raw.split(b"---", 2)`, a naive byte split, so a `---` truncates the compared region and misreports as `missing required option line(s): ['# backend: jira', …]`; use the em-dash if a dash is needed. (2) The two copies must be **byte-identical between the `---` delimiters** (ADR-0017; `test_frontmatter_byte_parity`). (3) The hints assert no dormancy status by design — that lives in `_DORMANT_KEYS` (#372's "no third hand-maintained copy" boundary). (4) **`just build-plugin` writes far more than this task's Files.** It loops every `BUILD_OUTPUT_PLUGINS` entry and rsyncs **14 skills** for `cortex-core` (`commit pr lifecycle backlog-author requirements … interview`), 2 for `cortex-overnight`, 1 for `cortex-backlog`, plus `rm -f plugins/$p/hooks/cortex-*.sh` and re-rsyncs of hooks and `bin/cortex-*` (`justfile:594-627`). **Precondition**: `git status --porcelain plugins/` must be clean before running it — otherwise the recipe sweeps unrelated pre-existing drift into this task's commit, outside its Files list. If it is not clean, stop and report rather than committing the sweep. Never hand-edit the mirror; the pre-commit Phase 2–4 drift loop detects-and-blocks but does not regenerate.
- **Verification**: `.venv/bin/python3 -c "import yaml,pathlib; from cortex_command.lifecycle_config import _DORMANT_KEYS,_extract_frontmatter_text; print([sorted(set(yaml.safe_load(_extract_frontmatter_text(pathlib.Path(p).read_text())) or {}) & _DORMANT_KEYS) for p in ['cortex_command/init/templates/cortex/lifecycle.config.md','skills/lifecycle/assets/lifecycle.config.md']])"` prints `[[], []]` (baseline `[['skip-review','skip-specify'], ['skip-review','skip-specify']]`); AND for each of the two hand-maintained copies, `grep -c '^# skip-specify:'` = 1, `grep -c '^# skip-review:'` = 1, `grep -c '^# default-tier:'` = 1, `grep -c '^# default-criticality:'` = 1 (positive: present-and-commented, not deleted); AND `grep -c -- '---' cortex_command/init/templates/cortex/lifecycle.config.md` = 2; AND `grep -c 'override auto-assessment' cortex_command/init/templates/cortex/lifecycle.config.md` = 0; AND `.venv/bin/pytest tests/test_lifecycle_config_parity.py -q` passes 4/4; AND `diff skills/lifecycle/assets/lifecycle.config.md plugins/cortex-core/skills/lifecycle/assets/lifecycle.config.md` is empty; AND `git status --porcelain plugins/ | grep -v 'skills/lifecycle/assets/lifecycle.config.md'` is empty (no unrelated sweep).
- **Status**: [ ] pending

### Task 2: Add the positive-assertion dormancy pin
- **Files**: `tests/test_lifecycle_config_dormant_template.py`
- **What**: Creates the regression test that stops a dormant key ever shipping live again, asserting positively so it also fails on deletion, on an emptied frontmatter, and on activation day.
- **Depends on**: [1]
- **Complexity**: simple
- **Context**: New file; pytest auto-collects (`justfile:527` runs `.venv/bin/pytest tests/ -q`), no registration needed. Take the key inventory from the source of truth rather than restating it — the module `cortex_command.lifecycle_config` exposes `_DORMANT_KEYS` (a frozenset, `:46-48`) and the `_extract_frontmatter_text` helper; private-symbol access from tests is established precedent (`tests/test_lifecycle_config.py:185,201,214` reaches into `lc._WARNED_KEYS` the same way). Parametrize over three paths: the init template, `skills/lifecycle/assets/…`, and the `plugins/cortex-core/…` mirror (include the mirror — the drift hook is procedural and `--no-verify`-bypassable, not a content invariant). Per path assert all three clauses from spec R5: (a) frontmatter region non-empty; (b) `set(parsed) & _DORMANT_KEYS == set()`; (c) **every** `_DORMANT_KEYS` member appears as a commented line in the region. Clause (a) is not optional and (c) is load-bearing — verified fail-open without them: `_extract_frontmatter_text('---\n---\n')` → `''` → `yaml.safe_load('')` → `None` → `set(None or {}) & _DORMANT_KEYS` → `set()` → passes on a gutted template. Clause (c) is what makes activation day loud: when a key leaves `_DORMANT_KEYS`, an intersection-only pin drops coverage exactly when `lifecycle_config.py:41`'s "activation must be loud and deliberate" guard needs it. Add four negative-case sentinels over synthetic in-memory frontmatter (mirroring the sentinel pattern in `tests/test_lifecycle_config_parity.py:102-140`): live key **fails**, commented key **passes**, empty region **fails**, absent key **fails**. Note `yaml.safe_load(None)` raises `AttributeError`, so a missing file/None region errors loudly rather than passing — that edge needs no extra guard.
- **Verification**: `.venv/bin/pytest tests/test_lifecycle_config_dormant_template.py -v` shows 3 parametrized path cases plus 4 sentinel cases, all passing. The four sentinels ARE the discrimination proof — they assert the predicate rejects a live key, an empty region, and an absent key while accepting a commented one, all over synthetic in-memory frontmatter, so no shipped template is mutated to prove the pin bites.
- **Status**: [ ] pending

### Task 3: Correct both false claims in docs/overnight.md
- **Files**: `docs/overnight.md`
- **What**: Removes the false `skip-specify`/`skip-review` bullet at `:57` AND corrects the false "CI parity gate" claim at `:48` — the second site the spec's census missed, in the same file.
- **Depends on**: none
- **Complexity**: simple
- **Context**: Two edits, both in this file — and this task is the **only** one permitted to touch it, so both must land here. (1) `:57` reads `- **`skip-specify`** / **`skip-review`**: Skip lifecycle phases when they don't add value for this repo` — false; nothing reads them. It sits in an "Other fields:" list (`:54-58`) beside accurate entries for `type` (live-in-prose) and `commit-artifacts` (live-in-code) — **preserve those two bullets**. Delete rather than reword: `docs/overnight-operations.md:728` states "do not enumerate the scaffolded fields in more than one doc", and `:15` already defers mechanics to that doc. (2) `:48` reads "kept byte-identical to what `cortex init` scaffolds by a **CI parity gate** — see ADR-0017". False for the same reasons as tasks 4–6, and **incoherent after Task 6** — it points at the ADR that Task 6 rewrites to deny CI enforcement. Say the asset's frontmatter is kept byte-identical to the `cortex init` template by a parity test run under `just test`. Keep the ADR-0017 back-pointer.
- **Verification**: `grep -cE 'skip-specify|skip-review' docs/overnight.md` = 0 (baseline 1 — the grep must name **both** keys; naming only one lets a rewrite under the other key's name pass) AND `grep -c 'Skip lifecycle phases' docs/overnight.md` = 0 AND `grep -c 'CI parity gate' docs/overnight.md` = 0 (baseline 1) AND `grep -c 'ADR-0017' docs/overnight.md` ≥ 1 (positive: the back-pointer survives) AND `grep -c '^- \*\*`type`\*\*' docs/overnight.md` = 1 AND `grep -c 'commit-artifacts' docs/overnight.md` ≥ 1 (positive: the two accurate bullets were not swept away).
- **Status**: [ ] pending

### Task 4: Correct the dormant-key and CI-gate claims in docs/overnight-operations.md
- **Files**: `docs/overnight-operations.md`
- **What**: Fixes the false "specify/plan reads optional defaults" consumer claim, marks the four keys dormant in the scaffolded-field list, and corrects the false "CI parity gate" claim — three edits, each independently verified.
- **Depends on**: none
- **Complexity**: simple
- **Context**: This doc **owns** config-consumer claims per `docs/policies.md:35-37`, so corrections land here and other docs point at it — meaning the `### cortex/lifecycle.config.md consumers and absence behavior` section (`:704-728`) must **survive**, not be deleted. Three edits: (1) `:715` — `- **lifecycle specify/plan**: reads optional defaults (default-tier, default-criticality, skip-specify, skip-review) and falls back to skill-level defaults when absent.` is **false**; no consumer reads any of the four. State they are scaffolded but dormant and that specify/plan always run their own auto-assessment. (2) `:706` — "Scaffolded fields include `type`, `test-command`, … `default-tier`, `default-criticality`, `skip-specify`, `skip-review`, …" flattens live and dormant into one list; mark the four dormant explicitly. This is the only edit that adds dormancy information to the owning doc — do not skip it. (3) `:706` also carries "a CI parity gate keeps its frontmatter byte-identical" — false: `validate.yml`'s blocking allowlist excludes `test_lifecycle_config_parity.py`, and `main` has no branch protection (`GET /branches/main/protection` → 404; ruleset = `deletion` + `non_fast_forward`), so nothing gates merge. Say a parity test checks byte-identity under developer-run `just test`. Back-point to ADR-0017 rather than re-deriving rationale (`cortex/adr/README.md:51-56`).
- **Verification**: `grep -c 'reads optional defaults' docs/overnight-operations.md` = 0 (baseline 1) AND `grep -c 'CI parity gate' docs/overnight-operations.md` = 0 (baseline 1) AND `grep -c 'consumers and absence behavior' docs/overnight-operations.md` = 1 (positive: the owning section survives) AND `grep -c 'always run' docs/overnight-operations.md` ≥ 1 (positive: edit 1's replacement claim is present) AND `grep -c 'dormant' docs/overnight-operations.md` ≥ 1 (positive: edit 2 added dormancy information) AND `grep -c 'do not enumerate the scaffolded fields in more than one doc' docs/overnight-operations.md` = 1 (positive: `:728`'s own rule survives).
- **Status**: [ ] pending

### Task 5: Correct the dormant-key and CI-gate claims in docs/setup.md
- **Files**: `docs/setup.md`
- **What**: Splits `type` (genuinely prose-read) from the four dormant keys, drops the claim that dormant keys are read as overrides, and corrects the false CI-gate claim two lines above it.
- **Depends on**: none
- **Complexity**: simple
- **Context**: Two edits. (1) `:120` — "The remaining keys — `type`, `skip-specify`, `skip-review`, `default-tier`, `default-criticality` — are advisory or read only as optional overrides." Two defects: it lumps `type` (in `_LIVE_PROSE_KEYS` at `cortex_command/lifecycle_config.py:45`, genuinely read by skill prose) with four keys nothing reads, and "advisory"/"read only as optional overrides" both assert the dormant four ARE consulted. **Deleting the trailing clause is not sufficient** — "are advisory" alone preserves both defects. Use the three-way taxonomy the code already defines: `_LIVE_CODE_KEYS` / `_LIVE_PROSE_KEYS` / `_DORMANT_KEYS` (`lifecycle_config.py:42-49`) — `type` is prose-read; the four are dormant and read by nothing. (2) `:118` — carries the same false "CI parity gate" claim as tasks 3/4/6; correct it the same way. Note `:118` already says the asset is "the single place to read the scaffolded schema rather than re-listing it here" and `:120` then re-lists it — prefer shortening toward that stated intent over expanding the enumeration.
- **Verification**: `grep -c 'advisory' docs/setup.md` = 0 (the weasel word IS the defect; baseline ≥1) AND `grep -c 'CI parity gate' docs/setup.md` = 0 (baseline 1) AND `grep -c 'dormant' docs/setup.md` ≥ 1 (positive: the taxonomy landed) AND no single line names both `type` and any of `skip-specify`/`skip-review`/`default-tier`/`default-criticality`: `grep -nE '\`type\`' docs/setup.md | grep -cE 'skip-specify|skip-review|default-tier|default-criticality'` = 0 (positive: the lumping is gone).
- **Status**: [ ] pending

### Task 6: Correct ADR-0017's enforcement claim
- **Files**: `cortex/adr/0017-reconcile-and-gate-lifecycle-config-sources.md`
- **What**: Fixes the Consequences line asserting CI-time enforcement that does not exist.
- **Depends on**: none
- **Complexity**: simple
- **Context**: Line 72 reads "Enforcement is CI-time (`just test` / CI gates merge), not commit-time — a …". **Both halves are false**: no workflow runs `just test` (`validate.yml` invokes specific `pytest tests/…` files; `release.yml`/`auto-release.yml` run neither), and `main` is unprotected so nothing gates merge. **Deleting only the parenthetical is not sufficient** — "Enforcement is CI-time, not commit-time" is still false and merely removes the evidence for the error. The headline claim must go: state that the parity test runs under **developer-run `just test`** and is **not currently CI-blocking**. Preserve the line's surrounding trade-off rationale (why test-time rather than commit-time was chosen) — that decision is unchanged. This is a **factual correction to a Consequences line, not a new decision**, so no new ADR and no status change (`cortex/adr/README.md:23`). Do not restate the dormant-key story here. The CI/merge-gating gap goes to a follow-up ticket.
- **Verification**: `grep -c 'CI gates merge' cortex/adr/0017-reconcile-and-gate-lifecycle-config-sources.md` = 0 (baseline 1) AND `grep -c 'Enforcement is CI-time' cortex/adr/0017-reconcile-and-gate-lifecycle-config-sources.md` = 0 (positive: the headline claim is gone, not just its parenthetical) AND `grep -ci 'not currently CI-blocking\|developer-run' cortex/adr/0017-reconcile-and-gate-lifecycle-config-sources.md` ≥ 1 (positive: the corrected claim is present) AND `grep -c 'commit-time' cortex/adr/0017-reconcile-and-gate-lifecycle-config-sources.md` ≥ 1 (positive: the trade-off rationale survives).
- **Status**: [ ] pending

### Task 9: Correct the CI claim in the parity test's docstring
- **Files**: `tests/test_lifecycle_config_parity.py`
- **What**: Fixes the fifth and last live CI-enforcement claim — a docstring asserting the test gates CI, in the very file the rest of this plan cites as its evidence base.
- **Depends on**: none
- **Complexity**: simple
- **Context**: Line 8 reads "This test fails ``just test`` / CI if their **frontmatter regions** diverge, or". The `just test` half is true; the `/ CI` half is false — this file is not in `validate.yml`'s blocking allowlist, so CI never runs it. The repo-wide census missed this site initially because its wording differs from the other four ("`just test` / CI", not "CI parity gate"). Edit the docstring only — **do not touch any assertion, constant, or helper**; the test's behavior is relied on by Task 1's verification and must not change. Keep the docstring's rationale about why byte-comparison bypasses the production parser (CRLF sensitivity) intact.
- **Verification**: `grep -c '/ CI' tests/test_lifecycle_config_parity.py` = 0 (baseline 1) AND `grep -c 'just test' tests/test_lifecycle_config_parity.py` ≥ 1 (positive: the true half survives) AND `.venv/bin/pytest tests/test_lifecycle_config_parity.py -q` still passes 4/4 (positive: no assertion was disturbed) AND `git diff --stat tests/test_lifecycle_config_parity.py` shows only docstring lines changed.
- **Status**: [ ] pending

### Task 7: Quiet this repo's own seeded config
- **Files**: `cortex/lifecycle.config.md`
- **What**: Comments out `skip-specify` / `skip-review` in cortex-command's own live config so the repo stops warning on every CLI call. Rides this feature's PR normally — it is this repo.
- **Depends on**: none
- **Complexity**: simple
- **Context**: Lines 4–5 read `skip-specify: false` / `skip-review: false`; `default-tier`/`default-criticality` are absent here, so only two lines change. Use the same claim-free commented form as Task 1 — **comment, do not delete**: the warning count alone cannot tell the two apart, and the spec's no-delete rule is scoped to templates, so this task's positive grep is the only thing preventing deletion here. Already inventoried by #372 (`cortex/backlog/372-*.md:46`, "dormant fields set in this repo's live config"). This is a live config, not a template — NOT parity-gated and NOT an init-hash input (`scaffold.py`'s `_HASH_INPUT_TEMPLATES` entry resolves through `_TEMPLATE_ROOT` to the template, not repo-root), so it stands alone. Do not reformat unrelated keys.
- **Verification**: `.venv/bin/python3 -c "import pathlib; from cortex_command.lifecycle_config import read_branch_mode; read_branch_mode(pathlib.Path('.'))" 2>&1 | grep -c 'no effect'` = 0 (baseline 2) AND `grep -c '^# skip-specify:' cortex/lifecycle.config.md` = 1 AND `grep -c '^# skip-review:' cortex/lifecycle.config.md` = 1 (positive: commented, not deleted) AND `grep -c '^commit-artifacts: true' cortex/lifecycle.config.md` = 1 (positive: unrelated keys untouched).
- **Status**: [ ] pending

### Task 8: Quiet the three external seeded configs, one commit each
- **Files**: `/Users/charliehall/Workspaces/wild-light/cortex/lifecycle.config.md`, `/Users/charliehall/Workspaces/gaggimate-barista/cortex/lifecycle.config.md`, `/Users/charliehall/Workspaces/Team-Builder-Bot/cortex/lifecycle.config.md`
- **What**: Comments the dormant keys out of the three other cortex-using repos on this machine and commits each edit in its own repo, so the change is durable rather than orphaned dirt.
- **Depends on**: [7]
- **Complexity**: simple
- **Context**: **Prerequisites, both operator-granted and both verified before this task can run**: (a) the session sandbox must permit writes to these paths and their `.git/` directories — under the default sandbox all six are `operation not permitted`, and `~/.claude/settings.local.json` allowlists only `cortex-command/cortex/` and `wild-light/cortex/`; the operator disabled the sandbox for this work (2026-07-16). Re-probe before editing; if denied, stop and report — do not attempt to re-enable or work around it. (b) This task runs `git commit` directly, which `CLAUDE.md:26` otherwise forbids ("never run `git commit` manually"); `/cortex-core:commit` is cwd-bound (no `git -C`/`--work-tree`) and structurally cannot target a foreign repo. The operator explicitly authorized this exception for this task only — it does not generalize.
  Per-repo state and treatment: **wild-light** has all four keys live (`default-tier: simple`, `default-criticality: medium`, `skip-specify: false`, `skip-review: false`), authored in `bbe598337` (2026-03-21, `Co-Authored-By: Claude Sonnet 4.6`) — eleven days before cortex-command's first template (`428e54ea`), so never seeded; comment all four, preserving intent in-file per the operator's decision. It is **trunk-based** (its CLAUDE.md) and ~22 ahead of origin, so a direct `main` commit is native there. **gaggimate-barista** and **Team-Builder-Bot** have two keys each. **Team-Builder-Bot is PR-flow** (merge commits from `feature/*` for PRs #3–#6) and sits clean at 0-ahead/0-behind — a direct `main` commit diverges from its observed workflow and leaves an unpushed local commit; flag this to the operator when reporting rather than silently normalizing it.
  Hooks: none of the three has `.git/hooks`, `core.hooksPath`, or `.githooks/` — a hook check discovers nothing. Their commit conventions live in log culture (all three: imperative, capitalized, no trailing period) — match that; do **not** import cortex-command's `[release-type:]` marker, which those repos have no pipeline for. wild-light has an **uninstalled** `.pre-commit-config.yaml` carrying unfiltered `trailing-whitespace`/`end-of-file-fixer` auto-fixers; if it has since been installed, expect the staged file to be reformatted and a possible stash conflict against its dirty tree — stop and report if so.
  Staging: each commit stages `cortex/lifecycle.config.md` **only**; never `git add -A`; never touch a pre-existing dirty file. All three target configs are currently clean, so a single-file add stages only this edit. **Record each resulting SHA in the report** — `feature_complete` cannot reference them (`stage_artifacts.py` runs `git add` with `cwd=<this repo>`), so the report is the only undo path.
- **Verification**: `Interactive/session-dependent: for each of the three repos — `git -C <repo> show --stat HEAD` lists exactly one file (`cortex/lifecycle.config.md`); `grep -c '^# skip-specify:' <repo>/cortex/lifecycle.config.md` = 1 and `grep -c '^# skip-review:' <repo>/cortex/lifecycle.config.md` = 1 (wild-light additionally `^# default-tier:` = 1 and `^# default-criticality:` = 1) proving commented-not-deleted; every pre-existing dirty entry from the pre-task `git status --short` snapshot is still present and unstaged; and spec R10's four-repo loop reports 0 for each. Capture the pre-task status snapshot per repo before editing — the dirty set drifts (wild-light moved from 9 to 7/10 entries during planning, and one entry is no longer under `cortex/`).`
- **Status**: [ ] pending

## Risks

- **Task 8 writes history in three repos this lifecycle does not govern, under two operator-granted exceptions** (sandbox disabled; `git commit` run directly against CLAUDE.md:26). Both were explicitly authorized for this task. Neither generalizes, and the plan re-probes before acting.
- **Team-Builder-Bot is PR-flow and pristine.** A direct `main` commit there is the one treatment that clashes with an observed convention. Flagged rather than normalized — say so when reporting, and it can be reverted with a single `git reset --hard HEAD~1` since it sits 0-ahead.
- **The three external commits are unreferenceable by this feature.** `feature_complete` has no external field and Complete cannot stage a foreign repo. The report's recorded SHAs are the only undo path; "Done" here does not mean those repos are done.
- **A one-time drift report follows the template edit.** `cortex-command` and `wild-light` each print a `.cortex-init` drift notice ending `Overwrite all with shipped: cortex init --force` on their next lifecycle entry. **Do not run `--force`** — in wild-light it would overwrite a customized `cortex/requirements/project.md`. Anticipated by ADR-0017:75-76.
- **Task 6 and Task 9 edit an ADR and a test docstring.** Both are factual corrections, not behavior changes, but both touch files whose purpose is to be authoritative.
- **The CI enforcement gap stays open.** Per the operator's call, this ticket corrects the docs downward instead of wiring CI. The real problem — `validate.yml` red since 2026-07-13, `main` unprotected, blocking steps skipped — goes to a follow-up ticket and is strictly larger than #380.
- **Phase headings do not sequence anything.** The dispatcher batches by `Depends on` alone; batch 0 is `{1, 3, 4, 5, 6, 9, 7}` and batch 1 is `{2, 8}`. The Outline's phases are for reading, not for execution order.

## Acceptance

A fresh `cortex init` seeds a `lifecycle.config.md` that emits **zero** dormant-key warnings (baseline 2), and the four seeded repos on this machine report 0 / 0 / 0 / 0 (baseline 2 / 2 / 2 / 4), with every key **commented rather than deleted** in each. No live prose in the repo claims the four keys are honored or that CI enforces the parity gate — verified repo-wide (`grep -rIn 'CI parity gate\|CI gates merge' --include='*.md' --include='*.py' .` returns only this feature's own artifacts and the archived `reconcile-lifecycleconfigmd-*` lifecycle, which are historical records). `tests/test_lifecycle_config_dormant_template.py` fails if any shipped template ever seeds a dormant key live, deletes one, or empties its frontmatter — and fails loudly on activation day, when a key leaves `_DORMANT_KEYS` while still commented in the template.
