# Review: open-critical-review-agent-count

**Stage**: 1 (spec compliance) — tier `moderate`, criticality `high`. Stage 2 (code
quality) does not apply; this change touches no code, only prose.

**Cycle**: 1

## Per-requirement ratings

### R1 — Reviewer width is 1–2 with no escalation tier: **PASS**
`sed -n '/## Step 2:/,/## Step 3:/p' skills/critical-review/SKILL.md | grep -n "3–4\|3-4"`
returns no output (verified, exit 1 / no match). Step 2 reads "Choose **1 or 2** — a
hard ceiling, not a starting point," with no clause admitting 3+.

### R2 — Criticality weighting survives as a single clause, not a matrix: **PASS**
Step 2 carries exactly one sentence: "Weight toward 2 when criticality is
`high`/`critical`; that's a preference, not an escalation path." No table, no
per-cell mapping. `docs/internals/sdk.md` is untouched (confirmed via
`git diff c80d4e4e..HEAD --stat` — not in the changed-file list), so its line 144
ruling ("reviewer count … is what criticality buys") is honored, not restated.

### R3 — Over-subscription has a stated rule: **PASS**
Step 2's final sentence: "More weaknesses than slots → highest-severity wins, don't
widen past 2." Both required patterns present:
`grep -Ec "highest-severity|highest severity"` = 1,
`grep -Ec "not widen|don't widen|does not widen"` = 1 (matches "don't widen").
Sits directly adjacent to the angle-distinctness rule that creates the pressure, per
the plan's placement note.

### R4 — Total-failure fallback reconciled to the new range: **PASS**
Step 4 now reads "one general-purpose agent derives 1–2 angles itself... skip
synthesis" — no "3–4" anywhere. `grep -c "3–4" skills/critical-review/SKILL.md` = 0.

### R5 — Width-1 does not leave unreachable prose: **PASS** (judgment call, per spec's own framing)
Step 4: "One reviewer failing **at width 2** → synthesize from the survivor,
prefixed '1 of 2 reviewer angles completed.' ... Total failure — both at 2, or **the
lone angle at 1** — → one general-purpose agent derives 1–2 angles itself..." The
partial-coverage branch is explicitly scoped to width 2 ("at width 2"), so it makes
no claim at width 1; width 1's only failure mode (the lone reviewer) is routed
through the total-failure branch, which is reachable and correct at both widths. No
sentence asserts a state that cannot occur.

### R6 — L1 surface stays truthful about parallelism, all trigger phrases survive: **PASS**
`description` now opens "Adversarial review — dispatches 1–2 reviewer agents..."
(dropped "Parallel"); `when_to_use` reads "...critical-review dispatches 1–2
sub-agents..." (dropped "parallel"). Neither asserts unconditional parallel
dispatch. All four `must_contain` phrases from
`tests/fixtures/skill_trigger_phrases.yaml` ("critical review", "pressure test",
"adversarial review", "challenge from multiple angles") appear verbatim.
`uv run pytest tests/test_skill_routing_disambiguation.py tests/test_l1_surface_ratchet.py`
→ 36 passed. L1 byte sum measured at 679 (desc 329 + when_to_use 350), within the
795 budget.

### R7 — `docs/skills-reference.md:110` matches the shipped rule: **PASS**
Line now reads "1-2 reviewers, weighted toward 2 on high/critical criticality or
novel claims" — no "3-4"/"3–4" in the entry
(`grep -n "3-4\|3–4" docs/skills-reference.md` empty). "A synthesis agent merges the
parallel findings into a single coherent challenge" sentence is left intact
(`grep -c` = 1).

### R8 — `cortex/requirements/project.md:38` amended: **PASS**
Line: "Reviewer width is 1–2, weighted toward 2 when criticality is high/critical.
Supersedes #383." `grep -c "Critical-review gates at spec only.*1–2"` = 1,
`grep -c "Opus synthesizer"` = 0, `grep -c "Supersedes #383"` = 1. The dead
"routed to Sonnet with an Opus synthesizer" clause is gone; the "gates at spec
only" and plan-phase-no-gate statements are preserved.

### R9 — Net-negative on prose bytes: **PASS**
`git show 5430bb66^:skills/critical-review/SKILL.md | wc -c` = 6201;
`wc -c skills/critical-review/SKILL.md` = 6188. Lower, as required.

### R10 — Mirror and ratchet hygiene: **PARTIAL**
Command-level acceptance is fully met:
`uv run pytest tests/test_dual_source_reference_parity.py tests/test_reference_size_ratchet.py tests/test_skill_size_budget.py`
→ 44 passed, and `plugins/cortex-core/skills/critical-review/SKILL.md` is
byte-identical to canonical (git history shows the mirror path present across
`5430bb66` + `77dc91c4`). But the requirement's prose condition — "regenerated by
the pre-commit hook rather than hand-edited" — was not met in practice. Confirmed
by reading both the hook and the resulting commit:
- `.githooks/pre-commit` Phase 3 (`.githooks/pre-commit:563-619`) rebuilds the
  mirror set into a temp checkout via `just build-plugin`, then reconciles by
  **content hash** (`git hash-object` vs. the staged blob) — that comparison logic
  is sound.
- The bug is one layer upstream: `just build-plugin`'s `rsync -a` calls
  (`justfile:669,674,678`) carry no `--checksum`. `git checkout-index` into the
  hook's temp tree stamps both the canonical source and the stale mirror with
  checkout-time mtimes in the same instant; Task 1's edit swapped "parallel" for
  "reviewer" — both 8 bytes — so size *and* mtime matched between source and
  destination inside that temp tree, and rsync's quick-check skipped the copy.
  `reconciled=0` at exit 0, no error surfaced.
- Commit `77dc91c4` ("Resync cortex-core mirror for critical-review SKILL.md")
  hand-stages the mirror directly — exactly the "never stage the mirror by hand"
  action CLAUDE.md and the plan (Task 4 context) both forbid — because the
  automated path silently produced nothing to reconcile. The commit message states
  the root cause plainly and the plan's Task 4 note discloses it; nothing here was
  hidden.

Net effect: the *artifact* R10 asks for (byte-correct mirror, tests green, mirror
path in the commit history) exists. The *mechanism* R10 specifies (hook-regenerated,
not hand-edited) was bypassed because the mechanism itself has a latent hole this
ticket exposed but did not introduce and is explicitly out of scope to fix here.
Rating this PARTIAL rather than FAIL because the deviation was forced, disclosed,
and left the repo in the state the requirement cares about; rating it PARTIAL
rather than PASS because "rather than hand-edited" is a literal clause in R10 and
it was violated.

## Requirements Drift

**State**: detected

**Findings**:
- The `rsync -a` same-byte-length blind spot in `just build-plugin` (justfile:669,
  674, 678) means the pre-commit hook's Phase 3 mirror reconciliation
  (`.githooks/pre-commit:563-619`) can silently reconcile 0 files — exit 0, no
  warning — for a canonical-source edit that preserves file size under a
  same-second staged-tree checkout. This is a correctness gap in the mechanism
  that `cortex/requirements/project.md`'s "Enforcement gates carry named evidence"
  bullet describes as fixed (the entry credits the hash-comparison rewrite with
  making concurrent sessions safe; it does not anticipate the rsync layer beneath
  that comparison silently no-oping). Reproduced and root-caused today
  (2026-08-04, commit `77dc91c4`) on `skills/critical-review/SKILL.md`; the same
  gap affects every mirrored skill/hook/bin file whenever an edit doesn't change
  byte count. No fix is proposed or made here — Task 4's plan note explicitly
  scopes it out — but the requirements doc's existing claim about this gate is now
  stale until either the gap is closed or documented as a known limitation.
- Checked `cortex/requirements/multi-agent.md` per the orchestrator's warning: it
  governs the overnight runner's agent spawning/concurrency/worktree isolation (a
  different subsystem — `ConcurrencyManager`, ASGI dispatch, ADR-0005 worktree
  placement) and contains no claim about critical-review's reviewer count. The
  governing clause is `project.md`'s "Critical-review gates at spec only" bullet,
  which Task 3 already amended. No drift against `multi-agent.md`.
- The Step 4 fallback-message reword ("parallel dispatch failed" →
  "reviewer dispatch failed") is **not** treated as drift: it falls within the
  spec's own "Changes to Existing Behavior" bullet ("partial-coverage branch made
  width-1-coherent") — at width 1 there is no parallel dispatch to describe as
  failed, so the wording correction is required by R5's coherence demand, not an
  unscoped addition.

**Update needed**: `cortex/requirements/project.md`

## Suggested Requirements Update

**File**: `cortex/requirements/project.md`
**Section**: Architectural Constraints
**Content**:
```
- **Mirror rebuild has a same-byte-length blind spot**: `just build-plugin`'s `rsync -a` calls (justfile:669,674,678) carry no `--checksum`; a staged-tree checkout stamps source and mirror with matching mtimes in the same instant, so a canonical-source edit that preserves file size is invisible to rsync's quick check and `.githooks/pre-commit` Phase 3 reconciles 0 files at exit 0. Reproduced 2026-08-04 (commit `77dc91c4`, `open-critical-review-agent-count`); affects every mirrored skill/hook/bin file, not just this one. Unfixed — out of scope where discovered; the mitigation is `--checksum` on the `rsync -a` calls.
```

## Verdict

```json
{"verdict": "APPROVED", "cycle": 1, "issues": ["R10 PARTIAL: mirror byte-parity and tests are correct, but the mirror was hand-staged (commit 77dc91c4) rather than hook-regenerated, because just build-plugin's rsync -a silently no-ops on same-byte-length edits (root cause confirmed in justfile:669,674,678 + .githooks/pre-commit:563-619) — non-blocking here, but the underlying gap is real and undocumented"], "requirements_drift": "detected"}
```
