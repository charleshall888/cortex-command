# Review: pre-commit-gates-silently-skip-deep

## Stage 1: Spec Compliance

### Requirement R1: Shared membership matcher (`cortex_command/lint/_globs.py`, stdlib-only, 3.12-safe, `**`=zero-or-more-segments, specified directly not pinned to `root.glob`)
- **Expected**: New module exposing `matches_any_glob(rel_path, globs) -> bool`; `**` treated as zero-or-more segments; identical on 3.12 and 3.13+; no `PurePath.full_match` / `glob.translate` at runtime. Acceptance: `pytest tests/test_lint_globs.py -q` exits 0 asserting all nine truth-table rows; `grep -c "full_match\|glob\.translate" cortex_command/lint/_globs.py` = 0.
- **Actual**: Module present with `_glob_to_regex` (hand-rolled `re` translation: trailing `/**` → `^{prefix}/.+$`; no-`**` → anchored exact with `*`→`[^/]*`; `**/`-containing → `(?:[^/]+/)*`-joined anchored parts) and `matches_any_glob`. `grep` = 0. `pytest tests/test_lint_globs.py -q` = 31 passed (parametrized over a 14-case `TRUTH_TABLE` covering all nine Edge Case shapes plus any-/empty-glob cases). On Python 3.14 (this env) the 3.13+ `glob.translate` oracle cross-check also ran green; 3.12-safety is verified structurally (no 3.13-only API at runtime, confirmed by `test_no_py313_only_api`-style grep). Independently confirmed the old `Path.match` semantics return `False` for the depth-1 and depth-≥3 in-scope cases the matcher now correctly admits (`True`), and that the matcher correctly rejects `sub/CLAUDE.md` vs `CLAUDE.md` (anchored, not tail-match) where old `Path.match` wrongly returned `True`.
- **Verdict**: PASS
- **Notes**: `_glob_to_regex` is `functools.lru_cache`-wrapped (preserves contract.py's former import-time pre-compile amortization); `matches_any_glob` annotated `Sequence[str]` (re-iterable; guards against one-shot generator silently returning False). Membership is specified directly per Technical Constraints — not pinned to the version-split `root.glob`.

### Requirement R2: Three importable checkers route `--staged` membership through the helper, old calls deleted
- **Expected**: `parity_check.py`, `prescriptive_prose.py`, `bare_python_import.py` consult `matches_any_glob` for `--staged` membership (preserving each `SCAN_GLOBS`/fast-path); old `Path.match(glob)` / `full_match(glob)` calls removed. Acceptance: the three `grep -c "\.match(glob)\|full_match"` = 0; R5 behavioral tests pass (proving the helper is actually consulted, not merely imported).
- **Actual**: All three `_matches_scan_glob` bodies are now `return matches_any_glob(rel_path, <THIS_CHECKER_GLOBS>)`. `parity_check.py` imports `from cortex_command.lint._globs import matches_any_glob` (absolute, correct — it lives outside `lint/`); the two `lint/` siblings use the relative `from ._globs import matches_any_glob`. All three deletion greps = 0. R5 behavioral tests pass (see R5). `bare_python_import.py`'s now-obsolete `full_match` explanatory comment is replaced with a docstring explaining the 3.12 crash fix.
- **Verdict**: PASS
- **Notes**: Signatures unchanged, so in-file callers (`_gather_staged_inscope_texts`/`_staged_paths`) need no edits. The `--audit`/`root.glob` enumeration paths are untouched (out of scope per Non-Requirements). The secondary bug — `bare_python_import` raising `AttributeError` on 3.12 via `full_match` — is genuinely fixed: no `full_match` reference remains anywhere in `cortex_command/` or the events-registry script.

### Requirement R3: events-registry inline vendor + full-truth-table parity test
- **Expected**: `bin/cortex-check-events-registry` carries an inline matcher copy (replacing `Path.match`), exposing a two-arg `matches_any_glob`; a parity test pins it to the shared helper over all nine Edge Case shapes, loaded via `SourceFileLoader`+`spec_from_loader` (NOT `spec_from_file_location`) with the module-load `cortex-log-invocation` subprocess neutralized. Acceptance: `pytest tests/test_events_registry_glob_parity.py -q` exits 0 (nine shapes); `grep -c "\.match(glob)" bin/cortex-check-events-registry` = 0.
- **Actual**: Inline `_glob_to_regex` + two-arg `matches_any_glob` vendored (logically identical to `_globs.py` — same `_esc_segment`, trailing-`/**`, no-`**`, and `(?:[^/]+/)*`-join branches; `functools`, `re`, `collections.abc.Sequence` all imported), with `_matches_scan_glob` delegating `matches_any_glob(rel_path, SCAN_GLOBS)`. `grep -c "\.match(glob)"` = 0. The parity test loads via `importlib.machinery.SourceFileLoader` + `importlib.util.spec_from_loader`, monkeypatches `shutil.which` → `None` before `exec_module`, resolves `loaded.matches_any_glob` (the two-arg symbol), and asserts `inline == shared` AND `inline is expected` over the same 14-case corpus covering all nine shapes (including the single-`*` over-scan rejection, directory-prefix discrimination, and hidden-file case). A separate `test_inline_exports_two_arg_matcher` guards the export contract. `pytest tests/test_events_registry_glob_parity.py -q` exits 0.
- **Verdict**: PASS
- **Notes**: Loading mechanism matches the spec exactly (no `spec_from_file_location`; no env-guard alternative). Parity targets the two-arg symbol, not the single-arg `_matches_scan_glob` that closes over the script's own `SCAN_GLOBS` — correct per the plan.

### Requirement R4: Dual-source mirror regenerated
- **Expected**: After editing `bin/cortex-check-events-registry`, its `plugins/cortex-core/bin/` mirror is regenerated (not hand-edited); `git diff --quiet plugins/cortex-core/bin/cortex-check-events-registry` exits 0.
- **Actual**: `git diff --no-index` between canonical and mirror exits 0 (byte-identical). Both files appear in the diff stat with matching `49 ++++++++-` line counts. Mirror is in sync.
- **Verdict**: PASS
- **Notes**: The `.githooks/pre-commit` drift hook fails closed on divergence; in-sync state confirms the regenerated (not hand-edited) mirror.

### Requirement R5: Behavioral regression lock — the bug-fix proof (four checkers, real `--staged`, non-zero exit AND deep-file path)
- **Expected**: For each of the four checkers, a real temp git repo stages an in-scope file with a known violation, runs in `--staged` mode WITHOUT `--root`, and asserts non-zero exit AND that the deep file's path appears in the violation. Depth-≥3 mandatory for all four; depth-1 mandatory for every checker whose globs contain a `**` glob. Parity must use a staged-blob-only divergence (worktree restored clean) so it goes RED with the bug present. Acceptance: the four test files exit 0.
- **Actual**: All four tests present, each asserting BOTH a non-zero exit AND both the depth-≥3 (`skills/lifecycle/references/deep_probe.md`) and depth-1 (`skills/depth1_probe.md`) paths plus the checker-specific marker:
  - **parity** (`test_check_parity.py:159`): staged-blob-only divergence implemented exactly per plan — commit clean, stage the orphan `cortex-*` reference, then restore worktree to clean (index=violation, worktree=clean). Asserts non-zero, `E002`, deep + shallow paths. Orphan token names are assembled from string fragments at runtime to avoid self-tripping the parity gate on this test file (`tests/**/*.py`).
  - **prose** (`test_check_prescriptive_prose.py:233`): simple staging (no working-tree fallback), `## Role` prescriptive-prose violation, asserts non-zero, `PRESCRIPTIVE_PROSE`, both paths.
  - **bare_python** (`test_bare_python_import_lint.py:321`): simple staging, `import cortex_command` block, asserts non-zero, `L201`, both paths.
  - **events-registry** (`test_check_events_registry.py:312`): materializes a valid `bin/.events-registry.md` so execution reaches the staged scan, stages an unregistered-event emission, invokes the real extensionless script as a subprocess, asserts non-zero, `UNREGISTERED_EVENT`, `MISSING_REGISTRY not in out`, and both paths.
  - Bug-sensitivity independently confirmed: old `Path.match` returns `False` for both the depth-≥3 and depth-1 probe paths against `skills/**/*.md`, so under the old semantics the deep files would be dropped from the staged scan, the checker would exit 0, and each test would go RED.
  - `pytest` over all four files exits 0.
- **Verdict**: PASS
- **Notes**: The parity test correctly addresses the working-tree-fallback false-lock risk (parity overlays staged blobs onto a `root.glob` enumeration that gates only the overlay). Test helpers are hermetic: `core.hooksPath` redirected to an empty `.no-hooks` dir, `commit.gpgsign false`, and `PYTHONPATH` prepends the repo root so the working-tree `cortex_command` is exercised (not a stale wheel).

### Requirement R6: No collateral damage on the current repo
- **Expected**: The corrected matcher surfaces no latent findings in this repo's deep files. Acceptance: `just test` exits 0 AND the pre-commit gates pass on the actual #282 commit.
- **Actual**: `just test` ran 1792 passed, 27 skipped, 1 xfailed, **1 failed**: `tests/test_mcp_subprocess_contract.py::test_plugin_path_mismatch_exits_nonzero`. That file is NOT touched by the #282 diff (no MCP/subprocess code changed), and the test PASSES both in isolation and when run with its file's neighbors — it fails only inside the full-suite run, i.e. a pre-existing cross-test ordering/state-pollution issue unrelated to the glob-matcher change. The corrected checkers' own behavioral and parity tests (the surfaces this change touches) all pass; the parity default-lint over the working tree produces zero findings once `__pycache__/*.pyc` environment artifacts are filtered (the only non-zero noise was stale `.pyc` files in `tests/__pycache__`, not repo content). The #282 commit series (`7817280b`..`d8b320eb`) landed on `main` through the commit path, so its gates passed.
- **Verdict**: PASS
- **Notes**: The lone `just test` failure is a pre-existing, change-unrelated test-isolation flake, not collateral damage from the matcher. Treating it as outside R6's "corrected matcher surfaces latent findings" scope. Worth a separate housekeeping ticket for the MCP-contract test's ordering sensitivity, but it does not gate this verdict. (The plan's Phase-1 checkpoint phrased R6 as a per-checker `--audit`-path scan; note `prescriptive_prose` has no `--audit` flag and `parity_check` silently ignores `--audit` — the authoritative R6 criterion is the spec's `just test` + commit-gate clause, which is what was evaluated.)

### Requirement R7: contract.py consolidation (gated on #279 complete)
- **Expected**: `contract.py` migrates its `--staged` membership to import/use `matches_any_glob`, removing its private matcher. Acceptance: `grep -c "...matches_any_glob..." cortex_command/lint/contract.py` ≥ 1; `grep -c "rel_path\.match(glob)"` = 0; `pytest tests/test_check_contract.py -q` exits 0 (incl. #279's depth tests).
- **Actual**: `from ._globs import matches_any_glob` added; private `_glob_to_regex` and `_SCAN_GLOB_PATTERNS` deleted; `_in_scan_scope(rel)` reduced to a thin `return matches_any_glob(rel, _SCAN_GLOBS)` wrapper (sole caller at line 1577 unchanged); `_SCAN_GLOBS` retained (helper is glob-agnostic). `grep` for the helper = 2 (import + use); `rel_path.match(glob)` = 0. `pytest tests/test_check_contract.py -q` passes (included in the 140-test green run). #279 is terminal per the plan (commit `6e524a0e`).
- **Verdict**: PASS
- **Notes**: All five `--staged` membership sites now route through the single canonical implementation (events-registry via the parity-pinned inline copy). Full 5-site unification achieved as the Should-have intended.

## Requirements Drift

**State**: none
**Findings**:
- None. The change aligns with project.md's "Two-mode gate pattern" Optional note (which already names `_in_scan_scope` and "a recursive-glob matcher safe on Python 3.12+" as the membership mechanism) and the "SKILL.md-to-bin parity enforcement" / dual-source constraints. The matcher's stdlib-only, 3.12-safe, directly-specified-membership design matches the documented architectural intent exactly.
**Update needed**: None

## Stage 2: Code Quality

- **Naming conventions**: Consistent with the codebase — `matches_any_glob` (public predicate), `_glob_to_regex` (private, cached), `_matches_scan_glob`/`_in_scan_scope` (per-checker thin wrappers preserving existing call-sites). Test functions follow the existing `test_staged_deep_nested_file_flags_via_real_git` / truth-table-row-id pattern. The `Sequence[str]` annotation (over `Iterable[str]`) is the right choice and is documented in-code as guarding re-iterability — module-level `tuple[str, ...]` constants are what all callers pass.
- **Error handling**: The matcher is total (anchored `re.fullmatch`-equivalent via `^...$` + `.match`); empty glob set returns False (explicitly tested). The `_staged_paths` subprocess wrappers retain their existing `(CalledProcessError, FileNotFoundError)` → `[]` fallback. The events-registry R5 test correctly defends against the MISSING_REGISTRY false-lock by materializing a valid registry and asserting `MISSING_REGISTRY not in out`.
- **Test coverage**: Strong and bug-sensitive. R1 unit test asserts all nine shapes plus any-/empty-glob; R3 parity test asserts inline==shared==expected over the same nine shapes with the correct extensionless-loader + `shutil.which` neutralization; the four R5 tests each assert non-zero exit AND the deep file's path (not a weak grep), with the parity test using the staged-blob-only divergence that goes RED under the old `Path.match`. I independently verified the old-semantics divergence on the exact probe paths. Test helpers are hermetic (gpgsign disabled, hooks redirected, `PYTHONPATH` forcing the working-tree package). The parity test's runtime-assembled `cortex-*` token fragments are a sound way to avoid the test file self-tripping the parity gate it itself is in scope for.
- **Pattern consistency**: The layered shared-matcher-beneath-five-checkers design matches the plan's stated architecture and the existing two-mode gate pattern. The inline-vendor + parity-test treatment for the standalone script (rather than promoting it to a module) respects the documented Non-Requirement and the dual-source byte-mirror constraint. The `lru_cache` is a deliberate, justified amortization addition (replaces contract.py's removed import-time pre-compile). One observation, not a defect: the `just test` run surfaced a pre-existing, change-unrelated test-isolation failure in `test_mcp_subprocess_contract.py` that warrants a separate housekeeping ticket.

## Verdict

```json
{"verdict": "APPROVED", "cycle": 1, "issues": [], "requirements_drift": "none"}
```
