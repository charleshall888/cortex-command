# Plan: Collapse byte-identical refine/references files (orchestrator-review.md + specify.md → lifecycle canonical)

## Overview

Apply three localized edits to `skills/refine/SKILL.md` Step 5 (path redirection + two adaptation-list updates), delete the two near-byte-identical refine canonical reference files, regenerate the plugin mirror via `just build-plugin`, run the verification gates (parity test + scoped grep), apply a one-line tweak to the backlog body's labeling error, and ship as a single commit. Architectural decision: refine reads lifecycle's specify.md directly via `${CLAUDE_SKILL_DIR}/../lifecycle/references/specify.md` (substitution applied at refine's invocation time, walking up to the plugin's `skills/` parent then back into lifecycle's references); refine no longer maintains a private canonical copy.

## Tasks

### Task 1: Apply the four Step 5 edits to refine SKILL.md
- **Files**: `skills/refine/SKILL.md`
- **What**: (a) Widen Step 5's introductory scope clause from `(§1–§4)` to `(its full protocol)` so the adaptation list can legitimately address sections outside §1–§4 (the new R5 row targets `## Hard Gate`, which is a top-level heading at the same level as `## Protocol` and outside `(§1–§4)` by literal reading) — without this widening, the post-Opus-4.7 literal-following regime can read `(§1–§4)` as scoping out anything beyond §4 and skip Hard Gate evaluation, making R5 dead text. (b) Redirect Step 5's specify.md read to lifecycle canonical (R3). (c) Tighten the §5 adaptation row to forbid spurious `phase_transition` event emission and `/cortex-core:commit` invocation (R4). (d) Append a new `## Hard Gate` adaptation row (R5) with wording that acknowledges the Thought/Reality table's interaction with refine's existing `**§4 (User Approval) — Complexity/value gate**` adaptation. All four edits land in the same Step 5 block (lines ~177–183), making this one-file, multi-edit task atomic.
- **Depends on**: none
- **Complexity**: simple
- **Context**:
  - Current line 177 reads: `Read \`references/specify.md\` and follow its full protocol (§1–§4) with these adaptations:` — apply two edits in this single line: (i) replace `references/specify.md` (inside the existing backticks) with `${CLAUDE_SKILL_DIR}/../lifecycle/references/specify.md`; (ii) replace `(§1–§4)` with `(its full protocol)`. Both edits must be content-addressed (search for the literal tokens `references/specify.md` and `(§1–§4)` within Step 5), not position-addressed, so they survive line shifts from any concurrent ticket-173 edit.
  - Current line 183 §5 row reads: `**§5 (Transition)**: Skip — /cortex-core:refine does not log phase transitions. The caller handles transition events if applicable.` — replace the trailing sentence with the R4 expansion: `Do NOT emit the \`phase_transition\` JSON template embedded in this section, and do NOT run \`/cortex-core:commit\` from within /cortex-core:refine; the caller (/cortex-core:lifecycle) owns phase-transition logging and commit-artifacts.`
  - Append the new R5 bullet immediately after the §5 row (still inside the bullet list). Use this wording, which both follows the existing `**§N (Name)**` style as closely as the §-less heading allows and acknowledges the §4 interaction the synthesizer flagged: `**\`## Hard Gate\`**: Applies — refine's spec phase has the same no-implementation-code rule. The Hard Gate's Thought/Reality table carries through, with one caveat: the row "I'll add this to Open Decisions since I'm not sure" interacts with refine's existing **§4 (User Approval) — Complexity/value gate** adaptation above (which already routes simple specs and "drop entirely" alternatives through an explicit user-presented surface) — when both fire, follow §4's surface flow rather than a separate Open Decisions deferral.`
  - Pattern reference: existing adaptation bullets use `**§N (Name)**: ...` style. R5's row addresses a `##`-level heading rather than a §-numbered section; the divergent backtick-wrapped form `**\`## Hard Gate\`**:` is intentional (it preserves the heading's literal `##` so a future reader can match the row to its source) and is the established way to address `##`-headings in adaptation lists.
- **Verification**: Five independent grep checks (commands run from repo root):
  - `grep -c '${CLAUDE_SKILL_DIR}/../lifecycle/references/specify.md' skills/refine/SKILL.md` returns ≥ 2 (one for R3 in the Step 5 invocation, one inside the R5 bullet); `grep -c 'Read \`references/specify.md\`' skills/refine/SKILL.md` returns 0 — pass if both.
  - `grep -c '(§1–§4)' skills/refine/SKILL.md` returns 0; `grep -c '(its full protocol)' skills/refine/SKILL.md` returns ≥ 1 — pass if both.
  - `grep -F 'Do NOT emit the \`phase_transition\` JSON template' skills/refine/SKILL.md` returns 1 — pass if exact count.
  - `grep -F 'Hard Gate' skills/refine/SKILL.md | grep -F 'Applies'` returns ≥ 1 — pass if non-empty.
  - `grep -F '§4 (User Approval) — Complexity/value gate' skills/refine/SKILL.md | grep -c 'interacts with'` returns ≥ 1 — pass if non-empty (confirms R5 wording acknowledges the §4 interaction, not a "carries through unchanged" overclaim).
- **Status**: [x] complete

### Task 2: Delete the two refine canonical reference files
- **Files**: `skills/refine/references/orchestrator-review.md` (delete), `skills/refine/references/specify.md` (delete)
- **What**: Remove both canonical files via `git rm`. R1 + R2.
- **Depends on**: none
- **Complexity**: simple
- **Context**:
  - Both files are tracked in the repo; use `git rm <path>` so the deletion is staged.
  - The dual-source parity glob in `tests/test_dual_source_reference_parity.py` discovers pairs via `skills/*/references/*.md` — deleted canonical files automatically drop from the parametrized count, so no test edit is needed.
  - `skills/refine/references/clarify.md` and `skills/refine/references/clarify-critic.md` are out of scope (covered by tickets 175, 176).
- **Verification**: `test ! -f skills/refine/references/orchestrator-review.md && test ! -f skills/refine/references/specify.md` — pass if exit 0.
- **Status**: [x] complete

### Task 3: Regenerate plugin mirror via `just build-plugin` and stage the prune
- **Files**: `plugins/cortex-core/skills/refine/references/orchestrator-review.md` (deleted), `plugins/cortex-core/skills/refine/references/specify.md` (deleted)
- **What**: Run `just build-plugin` to regenerate the mirror tree from canonical sources; rsync `-a --delete` prunes the orphaned mirror copies. Then explicitly `git rm` the two mirror files (symmetric tooling with Task 2's canonical deletions) so the index contains the deletions before commit. R6.
- **Depends on**: [1, 2]
- **Complexity**: simple
- **Context**:
  - Recipe: `justfile` → `build-plugin` runs `rsync -a --delete skills/$s/ plugins/$p/skills/$s/` for each skill. The `--delete` flag is the load-bearing piece.
  - Pre-commit drift hook structure (`.githooks/pre-commit`): Phase 2 detects whether canonical sources changed (BUILD_NEEDED=1 fires when any path matching `^(skills/|bin/cortex-|hooks/cortex-|claude/hooks/cortex-)` is staged); Phase 3 runs `just build-plugin` itself when BUILD_NEEDED=1, regenerating the mirror in the worktree at commit time; Phase 4 runs `git diff --quiet -- plugins/$p/` (worktree-vs-index, no `--cached`) and blocks if the worktree (post-Phase-3 rebuild) differs from the index. Task 3's load-bearing role is to **pre-stage the deletions so the index matches the worktree state Phase 3 will produce on commit** — not "stage so the hook skips." The hook always runs both phases when BUILD_NEEDED=1 (Task 1 stages skills/refine/SKILL.md, so it will).
  - Use `git rm` with explicit file paths (mirroring Task 2): `git rm plugins/cortex-core/skills/refine/references/orchestrator-review.md plugins/cortex-core/skills/refine/references/specify.md`. This is symmetric with Task 2's canonical-side `git rm` and avoids the directory-pathspec deletion-staging behavior that depends on git ≥ 2.0 semantics.
- **Verification**:
  - `test ! -f plugins/cortex-core/skills/refine/references/orchestrator-review.md && test ! -f plugins/cortex-core/skills/refine/references/specify.md` — pass if exit 0 (R6 acceptance).
  - `git diff --cached --name-status -- plugins/cortex-core/skills/refine/references/` shows both files prefixed `D` (deleted) — pass if both lines start with `D`.
- **Status**: [x] complete

### Task 4: Run dual-source parity test, scoped grep gates, and synthetic substitution check
- **Files**: (no changes; verification only — uses pre-existing test file `tests/test_dual_source_reference_parity.py`)
- **What**: Run `python -m pytest tests/test_dual_source_reference_parity.py` (R7); run the scoped grep gate (R10) and mirror grep (R11) to confirm no live source dirs reference the deleted paths; run R8a's static cross-skill resolution check; AND run a new synthetic substitution check that simulates the `${CLAUDE_SKILL_DIR}` substitution Claude Code applies at refine's invocation time and asserts every resulting path is readable on disk. The synthetic check is a pre-merge runtime gate for claim (a) — the explicit `${CLAUDE_SKILL_DIR}/..` walk — and would catch path typos, parent-walk arithmetic errors, and structural mismatches with the plugin layout.
- **Depends on**: [3]
- **Complexity**: simple
- **Context**:
  - Parity test: `tests/test_dual_source_reference_parity.py` is parametrized over the `skills/*/references/*.md` glob; deleted canonical files automatically drop from the parametrized count.
  - R10 pathspec: `git grep -F "skills/refine/references/orchestrator-review.md" -- 'skills/*/SKILL.md' 'skills/*/references/*.md' 'hooks/' 'claude/' 'plugins/' 'bin/' 'tests/' 'docs/' '*.justfile'` and the same form for `skills/refine/references/specify.md`. The list intentionally omits `backlog/`, `research/`, `lifecycle/`, `CHANGELOG.md`, and other historical-record dirs.
  - R11: `git grep -F "skills/refine/references/<file>" -- 'plugins/'` for both files.
  - R8a: `test -r skills/lifecycle/references/specify.md && test -r skills/lifecycle/references/orchestrator-review.md`.
  - **Synthetic substitution check (new)**: parse every `${CLAUDE_SKILL_DIR}/<rest>` token out of `skills/refine/SKILL.md`, substitute the literal directory `skills/refine` for `${CLAUDE_SKILL_DIR}` (this is what Claude Code does at refine's SKILL.md invocation time), and assert each resulting path is a readable file. The check exercises the path expression itself, not just the destination file. One-liner shape: `python3 -c "import re,os,sys; t=open('skills/refine/SKILL.md').read(); paths=set(re.findall(r'\\\${CLAUDE_SKILL_DIR}/([^\` \n]+)', t)); bad=[p for p in paths if not os.path.isfile(os.path.normpath(os.path.join('skills/refine', p)))]; sys.exit(1 if bad else 0); print(bad)"` (or an equivalent bash pipeline). The check covers the repo-development layout; plugin-cache layout verification is a noted residual gap (see Veto Surface — claim (b) item).
- **Verification**:
  - `python -m pytest tests/test_dual_source_reference_parity.py` — pass if exit 0 (R7 acceptance).
  - `git grep -F "skills/refine/references/orchestrator-review.md" -- 'skills/*/SKILL.md' 'skills/*/references/*.md' 'hooks/' 'claude/' 'plugins/' 'bin/' 'tests/' 'docs/' '*.justfile'` returns no matches AND `git grep -F "skills/refine/references/specify.md" -- 'skills/*/SKILL.md' 'skills/*/references/*.md' 'hooks/' 'claude/' 'plugins/' 'bin/' 'tests/' 'docs/' '*.justfile'` returns no matches — pass if both empty (R10 acceptance).
  - `git grep -F "skills/refine/references/orchestrator-review.md" -- 'plugins/'` returns no matches AND `git grep -F "skills/refine/references/specify.md" -- 'plugins/'` returns no matches — pass if both empty (R11 acceptance).
  - `test -r skills/lifecycle/references/specify.md && test -r skills/lifecycle/references/orchestrator-review.md` — pass if exit 0 (R8a acceptance).
  - Synthetic substitution check (the python one-liner above): pass if exit 0; on failure, the printed list of unreadable paths identifies the broken substitution token. This gate did not exist in the spec — it is a plan-level hardening over R8a (which only checks destination existence) added in response to critical review.
- **Status**: [x] complete

### Task 5: Apply the one-line ticket-body labeling tweak
- **Files**: `backlog/174-collapse-byte-identical-refine-references-files-orchestrator-review-and-specify.md`
- **What**: Per the spec's in-scope-only-as-tweak directive (spec.md "Non-Requirements" trailing entry), correct the ticket body's "Update SKILL.md Step 4 (orchestrator-review invocation) and Step 5 (specify invocation) ..." labeling error to accurately describe the actual edit envelope: a single string-addressed Step 5 update that picks up orchestrator-review transitively. No structural change to the ticket — one-line tweak for future readers.
- **Depends on**: none
- **Complexity**: simple
- **Context**:
  - Current line 41 of the backlog file reads: `Update \`skills/refine/SKILL.md\` Step 4 (orchestrator-review invocation) and Step 5 (specify invocation) to read from \`skills/lifecycle/references/orchestrator-review.md\` and \`skills/lifecycle/references/specify.md\` respectively`.
  - Replacement framing should: (a) drop the false "Step 4 (orchestrator-review invocation)" claim (refine SKILL.md Step 4 is the Research phase and has no orchestrator-review invocation), (b) note that orchestrator-review is reached transitively via lifecycle's specify.md, and (c) name the actual SKILL.md edit envelope as a content-addressed Step 5 string replacement against `\`references/specify.md\``.
  - Suggested replacement (one bullet): `Update \`skills/refine/SKILL.md\` Step 5 (specify invocation) to read from lifecycle canonical (\`${CLAUDE_SKILL_DIR}/../lifecycle/references/specify.md\`); orchestrator-review is reached transitively via lifecycle's specify.md (no separate refine SKILL.md edit needed for orchestrator-review).`
  - Edit is content-addressed against the literal `Update \`skills/refine/SKILL.md\` Step 4 (orchestrator-review invocation) and Step 5 (specify invocation)` prefix.
- **Verification**: `grep -F "Step 4 (orchestrator-review invocation)" backlog/174-collapse-byte-identical-refine-references-files-orchestrator-review-and-specify.md` returns no matches — pass if empty.
- **Status**: [x] complete

### Task 6: Update lifecycle index.md and commit via `/cortex-core:commit`
- **Files**: `lifecycle/collapse-byte-identical-refine-references-files-orchestrator-reviewmd-specifymd-lifecycle-canonical/index.md`, plus all previously-staged changes from Tasks 1–5.
- **What**: (a) Update `lifecycle/{slug}/index.md` to append `"plan"` to the `artifacts` inline array, add the `- Plan: [[{slug}/plan|plan.md]]` wikilink, and bump `updated` to today's date. (b) Stage all working-tree changes from Tasks 1–5 plus index.md. (c) Invoke `/cortex-core:commit` to create the commit; the pre-commit hook's Phase 4 drift loop runs and (per Task 3's prior staging) does not block. R9.
- **Depends on**: [1, 2, 3, 4, 5]
- **Complexity**: simple
- **Context**:
  - Index.md current state has `artifacts: ["research", "spec"]` — append `"plan"` inline (never block notation per the lifecycle skill's index.md conventions).
  - Wikilink format: `- Plan: [[collapse-byte-identical-refine-references-files-orchestrator-reviewmd-specifymd-lifecycle-canonical/plan|plan.md]]`.
  - `updated:` field bumps to today's date.
  - Use `/cortex-core:commit` (NOT manual `git commit`) per project convention; the skill drafts the message and validates it through the shared commit hook.
  - Commit must include all canonical edits (Tasks 1, 2, 5), the staged mirror prune (Task 3), the new `lifecycle/{slug}/plan.md` artifact, and the index.md update — one cohesive commit, not multiple.
- **Verification**:
  - After `/cortex-core:commit` succeeds: `git log -1 --name-only` shows `skills/refine/SKILL.md` (modified), `skills/refine/references/orchestrator-review.md` (deleted), `skills/refine/references/specify.md` (deleted), `plugins/cortex-core/skills/refine/references/orchestrator-review.md` (deleted), `plugins/cortex-core/skills/refine/references/specify.md` (deleted), `backlog/174-...md` (modified), `lifecycle/{slug}/plan.md` (new), `lifecycle/{slug}/index.md` (modified) — pass if all eight paths appear.
  - `git diff --quiet plugins/` exits 0 (no drift remaining anywhere in the plugin tree post-commit) — pass if exit 0 (R9 acceptance, scope widened from `plugins/cortex-core/skills/refine/` to `plugins/` to match the pre-commit hook's actual diff scope, which is the entire build-output plugin tree).
- **Status**: [x] complete

## Verification Strategy

End-to-end gate after all tasks complete:

1. **Mechanical state**: Run the four R-acceptance commands from the spec sequentially and confirm all return their expected outputs:
   - `test ! -f skills/refine/references/orchestrator-review.md` (R1, exit 0)
   - `test ! -f skills/refine/references/specify.md` (R2, exit 0)
   - `test ! -f plugins/cortex-core/skills/refine/references/orchestrator-review.md` (R6, exit 0)
   - `test ! -f plugins/cortex-core/skills/refine/references/specify.md` (R6, exit 0)
2. **SKILL.md edit shape**: Five grep checks per Task 1 verification (R3 path redirect, scope-clause widening from `(§1–§4)` to `(its full protocol)`, R4 phase_transition forbid, R5 Hard Gate apply, R5 §4-interaction acknowledgment).
3. **Test gate**: `python -m pytest tests/test_dual_source_reference_parity.py` exits 0 (R7).
4. **Static cross-skill resolution**: `test -r skills/lifecycle/references/specify.md && test -r skills/lifecycle/references/orchestrator-review.md` exits 0 (R8a).
5. **Synthetic substitution check (new, plan-level hardening over R8a)**: the python one-liner in Task 4 — parse `${CLAUDE_SKILL_DIR}/<rest>` tokens out of `skills/refine/SKILL.md`, substitute `skills/refine`, assert each resulting path is readable — exits 0. This exercises the path expression itself and catches typos / parent-walk arithmetic errors / future structural mismatches pre-merge.
6. **Drift hook**: post-commit `git diff --quiet plugins/` exits 0 (R9, widened scope to match the pre-commit hook's actual diff scope).
7. **Source-dir grep gate**: per Task 4's R10 + R11 commands; both empty.
8. **Manual runtime smoke (R8b, deferred)**: at the next genuine `/cortex-core:refine` invocation post-merge, the implementer attests in the PR description (or follow-up comment) that refine's Step 5 reads the lifecycle canonical and produces a complete spec.md. To raise the resolution of R8b above the bare "no resolution-error message" signal — which research §F4 documents as insufficient because `head -100` truncation and partial-content recovery do not surface as "file not found" errors — the attestation must include an objective grep check against the produced `lifecycle/{slug}/spec.md`: confirm that the spec contains content that is verifiably from lifecycle canonical and not from any stale cached refine copy. Concretely: assert that the produced spec includes the literal phrase `## Hard Gate` somewhere in its trace OR that the events.log for the verifying session contains an `orchestrator_review` event with `phase: "specify"` (proving lifecycle/specify.md's §3a transitive hop fired). The R8b attestation pastes the verifying lifecycle slug AND the grep/log evidence into the PR description (or follow-up comment). R8b remains deferred for orchestrator-review skip-rule exercise (synthetic fixtures cannot reliably set tier+criticality under refine standalone, per the spec), but it is no longer the sole detection signal for path-resolution correctness — the synthetic substitution check in Task 4 is now the pre-merge gate for claim (a).

## Veto Surface

- **Two distinct cross-skill resolution claims, only one of which now has a pre-merge runtime gate**. Critical review surfaced that the plan's correctness rests on two structurally different resolution claims, conflated in the spec's Veto Surface and Non-Requirements:
  - **Claim (a) — explicit substitution-then-walk**: refine SKILL.md's `${CLAUDE_SKILL_DIR}/../lifecycle/references/specify.md` substitutes at refine's invocation time and resolves to a real file. **Pre-merge gate (new)**: Task 4's synthetic substitution check exercises the substituted path expression against on-disk files; covers repo-development layout. Plugin-cache layout (`~/.claude/plugins/cache/...`) is a residual gap — see ASK item below if you want to add a plugin-cache fixture.
  - **Claim (b) — implicit transitive bare-relative resolution**: when refine reads lifecycle/specify.md and Claude encounters the bare `Read \`references/orchestrator-review.md\`` token at line 145 of that file, the resolver must pick `skills/lifecycle/references/orchestrator-review.md` (not refine's now-deleted copy). **No pre-merge gate exists for claim (b)**. The spec's affirmative argument is "lifecycle's own use of bare `references/orchestrator-review.md` has worked for the entire history of the lifecycle skill" — but lifecycle is the *invoker* in that case, so its CWD/SKILL.md anchor lands correctly; refine-as-invoker is a structurally different context with no documented Anthropic guarantee. The post-merge R8b attestation may not exercise this hop reliably (see "State inconsistency to call out" below). If you want claim (b) verified pre-merge, the cleanest defense is the workspace-rooted M1 variant — see ASK item below.

- **ASK 1 — workspace-rooted M1 re-introduction**: should we add a one-line edit to `skills/lifecycle/references/specify.md:145` to path-qualify the orchestrator-review reference as `Read \`skills/lifecycle/references/orchestrator-review.md\`` (workspace-rooted, not `${CLAUDE_SKILL_DIR}`-using)? The spec rejected M1 narrowly on grounds that `${CLAUDE_SKILL_DIR}` does not re-substitute in raw read-text — which only invalidates the substitution-using M1 variant. The workspace-rooted variant is not subject to that objection: it makes the path absolute-relative-to-repo-root, which Claude's Read tool resolves uniformly regardless of the invoking skill. **Trade-off**: edits one line in lifecycle's owned reference file (slightly wider blast radius — every lifecycle invocation now reads the qualified form too, but byte-equivalent in behavior); eliminates claim (b) entirely; trivially verifiable by inspection. **Recommendation**: YES — the trade-off is small and the gate-coverage gain is large.

- **ASK 2 — plugin-cache install verification**: end-user plugin-cache install (`~/.claude/plugins/cache/<mkt>/cortex-core/<ver>/skills/refine/`) is the dominant deployment target but neither pre-merge gates nor R8b's post-merge attestation will run there (R8b runs in the implementer's repo-development environment because the implementer is the merger). Should we add a synthetic plugin-cache fixture to Task 4 that simulates the substitution under a cache-style directory layout (e.g., copy `skills/` to a tempdir nested under `cache/<mkt>/<ver>/`, then run the synthetic substitution check there)? **Trade-off**: adds ~10 lines of test scaffolding to Task 4; covers the dominant deployment target's substitution behavior pre-merge. **Recommendation**: defer to operator — plugin-cache layout is structurally identical to repo-dev (just a longer absolute path), and the substitution arithmetic is the same; the marginal coverage gain is debatable.

- **ASK 3 — adaptation list exhaustiveness over `### `-level subsections**: refine's Step 5 adaptation list, after Task 1, addresses §1, §2a, §3b, §4, §5, and `## Hard Gate` — covering all top-level `## ` headings of lifecycle/specify.md. Critical review flagged that two `### `-level subsections of `## Protocol` are inherited unannotated: `### 2b. Pre-Write Checks` and `### 3a. Orchestrator Review`. §3a is the load-bearing transitive orchestrator-review hop the entire ticket pivots on (refine reads lifecycle/specify.md, hits §3a, follows the bare reference to orchestrator-review.md). Should Task 1 add explicit rows for these subsections — e.g., a `**§2b (Pre-Write Checks)**` row noting that "Research cross-check" overlaps with refine's existing §1 adaptation, and a `**§3a (Orchestrator Review)**` row stating that refine inherits the hop unchanged but its Applicability skip rule may evaluate undefined under refine standalone (since refine never emits `lifecycle_start`)? **Trade-off**: adds two more bullets to the adaptation list (mechanical) and broadens R5's "exhaustive over top-level sections" framing to "exhaustive over Protocol subsections + top-level sections" (load-bearing forward-coupling cost rises slightly). **Recommendation**: add the §3a row (it documents the load-bearing transitive hop, which is implicit today); skip the §2b row (research cross-check is genuinely covered by §1 and a duplicate row is documentation noise).

- **Forward-coupling cost on lifecycle/specify.md** (research F7): every change to lifecycle/specify.md after this lands becomes a behavior change for two skills (lifecycle + refine). Refine SKILL.md's Step 5 adaptation list becomes the only defense against unwanted propagation. No automated re-audit owner is created here — ticket 182 (vertical-planning adoption) inherits responsibility for re-auditing the adaptation list when adding new sections to lifecycle/specify.md. If you want a stronger defense, file a small follow-up ticket to add an automated test that asserts lifecycle/specify.md's top-level `## ` headings are exhaustively enumerated by refine SKILL.md's Step 5 adaptation list.
- **R8b runtime smoke is post-merge and operator-attested, not pre-merge** — but it is no longer the sole detection signal. Task 4's new synthetic substitution check is the pre-merge gate for claim (a)'s `${CLAUDE_SKILL_DIR}/..` resolution; R8b's role is now the post-merge runtime confirmation (with strengthened evidence requirements per Verification Strategy gate 7 — the attestation must include either the `## Hard Gate` literal in the produced spec or an `orchestrator_review` event with `phase: "specify"` in events.log). The original spec framing ("only synthetic path requires either modifying refine or hand-crafting an events.log fixture") was a false dichotomy — that argument concerned the orchestrator-review skip-rule evaluation, not path-substitution correctness, and the synthetic check in Task 4 closes the path-substitution gap without either of those measures.
- **One commit covers both canonical edits and lifecycle artifacts**: Task 6 commits the SKILL.md edits, deletions, mirror prune, backlog tweak, plan.md, and index.md update together. If you'd rather split (e.g., one commit for the canonical change + one for the lifecycle artifacts), the plan can be re-decomposed — but the pre-commit drift hook makes a single commit the simpler shape.
- **State inconsistency to call out**: `events.log` lacks a `lifecycle_start` event (refine did not emit one). Per the lifecycle skill's Step 2 detection, this defaults tier=simple and criticality=medium for any future resume — which contradicts the backlog frontmatter (complexity:complex, criticality:high). The plan above proceeds at complex+high based on the backlog (and the spec's complexity/criticality rationale), but the formal events.log state is anomalous. **Recommendation**: append a `lifecycle_start` event to events.log now (`tier: complex, criticality: high`) so subsequent resumes detect correctly. This is not a plan task — it's a one-line state correction the operator can approve before plan execution begins.

## Scope Boundaries

Maps to spec.md "Non-Requirements" — the following are explicitly excluded from this feature:

- Adjacent reference collapses (tickets 175, 176): `clarify.md` and `clarify-critic.md` remain unchanged.
- Z-stream copy-paste bug fix (ticket 173): the duplicate Alignment-Considerations block at refine SKILL.md:117–157 and the `update_item.py` rename at 231–232 are independent. Either ordering is mechanically safe given Task 1's content-addressed edits.
- Cross-skill handoff integration test (ticket 181): R8b's manual smoke is the runtime interim verification; ticket 181 codifies automated coverage when its blocker (178) clears.
- Vertical-planning section additions (ticket 182): stream F sections (`## Phases`, P9/S7 gates) land in lifecycle/specify.md after this ticket.
- Path-style consistency for refine SKILL.md `references/clarify.md` references (lines 38, 65, 86): bare-relative intra-skill reads are reliable; only the cross-skill case introduced by R3 needs the `${CLAUDE_SKILL_DIR}/..` form.
- Editing `skills/lifecycle/references/specify.md:145` to path-qualify the orchestrator-review pointer: the substitution-using variant (`${CLAUDE_SKILL_DIR}/...`) is rejected — `${CLAUDE_SKILL_DIR}` does not re-substitute in raw read-text. The workspace-rooted variant (e.g., `Read \`skills/lifecycle/references/orchestrator-review.md\``) is **NOT** in this scope-boundaries list — see Veto Surface ASK 1, where re-introducing M1's workspace-rooted form is offered as an open scope decision before plan execution begins.
- Adding an automated grep gate for dangling SKILL.md references: ticket 181's job.
- Programmatically enforcing the "refine never reaches plan phase" invariant: documented in the SKILL.md adaptation list; no new test or assertion.
- Refine emitting a synthetic `lifecycle_start` event during clarify exit: out of scope; would close the synthetic-fixture gap for R8b but is a behavior change to refine's contract.
