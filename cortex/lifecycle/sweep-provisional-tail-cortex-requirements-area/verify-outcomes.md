# Verify outcomes — #358 provisional-tail sweep (cortex/requirements)

Assembled from the five per-file Phase-1 verification passes (Tasks 2a–2e), one row per `(file, id)` — the composite key is unique (ledger `id` alone is non-unique: 32 candidates share 12 ids). This file is the `(file,id)`-keyed delta.

**Schema** (per row): `| file | id | heading | anchor_token | verdict | signal | reason | applied_in_commit |`
- `verdict` ∈ {`survives`, `refuted`, `deferred`}
- `signal` names its Req 2 type — (a) structural-substitution, (b) informative-only, (c) preserved-elsewhere — and for (a)/(c) a checkable location. A bare self-authored "looks safe" reason does not satisfy the gate.
- `applied_in_commit` = `pending` for survivors until the per-file apply commit backfills its hash (Task 10); `—` for deferred/refuted.

**Outcome tally**: 32 rows = 31 `survives` + 1 `deferred` (observability s14) + 0 `refuted`. Applied set per file: backlog s2/s3/s4/s5/s6/s7/s8/s9/s10/s13/s14; pipeline s3/s4/s5/s7/s13/s14; observability s6/s7/s8/s9/s13 (s14 deferred); remote-access s2/s4/s5/s6/file-compress; multi-agent s2/s4/s6/s9.

**OQ3 (ledger write)**: operator resolved OQ3 to **direct-write** at plan approval (`oq3_decision=direct-write` on the `plan_approved` event). Task 11 therefore writes this batch's own `(file,id)` rows to `master_candidates.json` (safe — `(file,id)` unique, files disjoint) rather than deferring. `dup_groups.json` is not touched.

**Four coordinated same-text clusters (apply each as ONE Phase-2 edit, not sequential trims — see per-file sections for detail):**
- **backlog** s3/s5/s6/s7 — s3/s5 dedupe into Architectural Constraints; **s6's three-behavior enum stays at FR ~L52** and **s7's two decisions stay at FR ~L60 / OQ ~L129** (NOT the AC sink). Plus the s7↔s14 coupling (shared persist-ticket-ref bullet).
- **pipeline** s13 — folds State-file-locking / Repair-cap / Branch-persistence restatements into `## Architectural Constraints` (Branch-persistence overlaps s3's span).
- **observability** s9↔s13 — the arrays-replace-not-merge gotcha is stated three times (AC criterion, Sandbox-Socket Note, Edge-Case); collapse to one canonical statement.
- **remote-access** s2↔s6↔file-compress — the tmux tool-agnostic framing and the macOS/Ghostty constraint are each triplicated; one canonical home per fact.

**Phase-2 citer notes (from verification; scope `cortex_command/**`+`docs/**`):**
- pipeline: `cortex_command/lifecycle_event.py:15-16` docstring cites ``pipeline.md`` L143/146/151 (L-prefix) — refresh in the pipeline commit.
- multi-agent: `cortex_command/overnight/fill_prompt.py:7` docstring cites `multi-agent.md:50` (content actually at ~51-52, pre-existing off-by-one) — fix in the multi-agent commit. `docs/overnight-operations.md:655` cites `multi-agent.md:77` — recompute after s9's trim. (Path correction: `fill_prompt.py` is under `cortex_command/overnight/`; `test_fill_prompt.py` is at repo-root `tests/` and pins the prompt, not the doc line — no change.)
- remote-access: only one file-level citer (`docs/overnight-operations.md:615`, no line number) — no recompute needed.
- backlog / observability: no `cortex_command/**`+`docs/**` line-anchored citers surfaced in verification; Task 4's broad baseline enumeration confirms completeness.

---


## Section: backlog.md

# Verify outcomes — cortex/requirements/backlog.md (Task 2a, whole-file recall-first pass)

Read whole of `cortex/requirements/backlog.md` (all 132 lines, current as of this pass) before judging any candidate — no span-isolated verification.

**Cluster note (s3/s5/s6/s7 — apply as ONE coordinated Phase-2 edit, not four sequential trims):** s3 (→ AC L101-102/104) and s5 (→ AC L103) genuinely dedupe into Architectural Constraints. **s6's kept three-behavior enum lives at FR `### Consumer backend routing (skill layer)` (~L52, the Outputs line: "local-engine call, LLM best-effort against an external tracker, or skip"), NOT the AC sink** — s6 keeps that FR line and only cuts the AC-duplicative framing sentence. **s7 is COMPRESS-in-place: its two kept decisions live at FR `### External backend via LLM best-effort` (~L60, compose-via-backlog-author) and at Open Questions (~L129, re-resolution-not-persisted-ID-map), NOT the AC sink.** A naive "fold all four candidates into AC" would silently drop s6's enum and both of s7's decisions. Phase 2 must verify s6's enum survives at FR L52 and s7's decisions survive at FR L60 / OQ L129 after the coordinated edit.

**Cross-requirement coupling (s7 ↔ s14):** s7's kept re-resolution decision (surviving at Open Questions ~L129: "Whether to persist the external ticket ref ... Recommendation: re-resolution for v1; revisit if it proves flaky.") is the *same* persist-ticket-ref bullet that s14 trims to a one-line decision record. Phase 2 must sequence these so s14's trim does not delete the last surviving copy of the re-resolution decision before/without confirming it is preserved at s7's FR home (~L60's round-trip acceptance criteria already state the decision independently, so the OQ bullet is redundant, not sole-source — but the coordination must be re-checked at apply time, not assumed here).

**Named must-keep spans confirmed present and untouched by any candidate below:** the ADR-0016 back-pointer (AC ~L106) and ADR-0019 back-pointer (AC ~L107) both remain outside every candidate's span; the "backend is named `cortex-backlog`, not `local`" naming rule (AC ~L105) is likewise untouched by any of the 11 candidates' claims.

| file | id | heading | anchor_token | verdict | signal | reason | applied_in_commit |
|---|---|---|---|---|---|---|---|
| cortex/requirements/backlog.md | s2 | ## Overview | "The backlog becomes an **optional, backend-configurable** capability" | survives | c (preserved-elsewhere) | COMPRESS. Confirmed: ADR-0016 exists at cortex/adr/0016-configurable-backlog-backend-and-llm-as-adapter.md and carries the LLM-as-adapter rationale; AC L101 (engine-stays-in-wheel), L102 (backlog-author-stays), L104 (optional-plugin pattern) restate the scope boundary from Overview paras 2-3; NFR "Zero per-tool maintenance" bullet (~L93) restates the "zero per-tool maintenance" motivation verbatim from Overview para 2. AC L106 itself instructs back-pointing rather than restating. Whole-file read confirms no unique content in paras 2-3 that isn't captured by these three locations. | 9618653d |
| cortex/requirements/backlog.md | s3 | ### Optional `cortex-backlog` plugin packaging | "Only `skills/backlog` moves" | survives | a (structural-substitution) + c (preserved-elsewhere) | MERGE_DEDUP. Confirmed structural: justfile:575 `BUILD_OUTPUT_PLUGINS := "cortex-core cortex-overnight cortex-backlog"` already registers the plugin; tests/test_dual_source_reference_parity.py is the dual-source drift gate enforcing the mirror. Confirmed preserved-elsewhere: AC L101 (engine-stays-in-wheel), L102 (backlog-author-stays-in-core), L104 (register in justfile BUILD_OUTPUT_PLUGINS / dual-source mirror / docs/setup.md) already restate this FR's three acceptance criteria near-verbatim. Part of the s3/s5/s6/s7 coordinated cluster (see preamble). | 9618653d |
| cortex/requirements/backlog.md | s4 | ### Configurable backend in the cortex init config | "gains a `backlog:` block declaring the active backend" | survives | a (structural-substitution) | COMPRESS. Confirmed: cortex_command/init/templates/cortex/lifecycle.config.md is the shipped scaffold template carrying the `backlog:` block; tests/test_init_backlog_scaffold.py (test_scaffolded_template_resolves_to_cortex_backlog) proves the scaffold resolves via resolve_backlog_backend; tests/test_lifecycle_config_backlog_backend.py covers absent/scalar/null/empty/malformed backend-value resolution. The Inputs/Outputs template rows restate what the schema itself (now code+test-carried) already specifies; the enum/default/github-issues example are the non-derivable unique content to keep. | 9618653d |
| cortex/requirements/backlog.md | s5 | ### Backend resolution (config-authoritative) | "not by detecting whether the plugin is installed" | survives | a (structural-substitution) + c (preserved-elsewhere) | MERGE_DEDUP. Confirmed: cortex_command/lifecycle_config.py:97 defines `resolve_backlog_backend`, enforced by tests/test_lifecycle_config_backlog_backend.py (default-to-cortex-backlog on absent/null/empty/malformed cases). Confirmed preserved-elsewhere: AC L103 ("The config field is the source of truth for the active backend, not plugin-install detection — the wheel cannot reliably introspect installed Claude Code plugins...") already states the identical rule with rationale. Part of the s3/s5/s6/s7 coordinated cluster (see preamble). | 9618653d |
| cortex/requirements/backlog.md | s6 | ### Consumer backend routing (skill layer) | "Backend branching lives in the consumer skills, not in the CLI tools." | survives | c (preserved-elsewhere) | MERGE_DEDUP, with a correction to the naive framing. AC L100 ("Backend routing lives at the skill/consumer layer; the `cortex-*` CLI tools remain cortex-backlog-only") already states the routing-in-skills/CLI-backend-blind rule this FR's Description restates; ADR-0019 (cortex/adr/0019-skill-helper-verb-backend-structural-guard.md) plus AC L107 already carry the refined exception; skills/lifecycle/references/discovery-bootstrap.md:13 independently cites "backend-blind." **Correction per cluster note: the three-behavior enum ("local-engine call, LLM best-effort... or skip") at FR ~L52 is the sole surviving copy of that enum — it is NOT duplicated in AC and must be kept, not merged away.** Only the Description/Inputs framing sentences (which duplicate AC L100) are the actual redundant content. | 9618653d |
| cortex/requirements/backlog.md | s7 | ### External backend via LLM best-effort (create + round-trip) | "no typed client" | survives | c (preserved-elsewhere) | COMPRESS-in-place (not a merge into the AC sink). AC L102 ("...discovery and morning-review compose ticket bodies through it on the external-tracker create path...") duplicates the compose-via-backlog-author decision; Open Questions ~L129 ("Whether to persist the external ticket ref... Recommendation: re-resolution for v1...") duplicates the round-trip-by-re-resolution decision already stated in this FR's acceptance criteria (~L67). The Inputs/Outputs template rows are pure restatement of the Description and were confirmed to add no unique content on whole-file read. **s7↔s14 coupling applies here (see preamble) — do not let s14's trim of the OQ L129 bullet remove the only surviving copy without first confirming the FR home (~L60) still states the decision.** | 9618653d |
| cortex/requirements/backlog.md | s8 | ### Overnight requires `cortex-backlog` | "The unattended overnight runner only supports the local backend." | survives | a (structural-substitution) + c (preserved-elsewhere), corrected scope | MERGE_DEDUP, with the drifted "three equivalent instances" claim corrected. Confirmed code/test-enforced: tests/test_overnight_backlog_backend_guard.py (test_handle_prepare_refuses_non_local_backend, test_handle_launch_refuses_non_local_backend) and cortex_command/overnight/cli_handler.py implement the fail-closed guard. Confirmed near-verbatim duplication between this FR's acceptance criteria (~L76-78: "...overnight refuses with a message naming the configured backend and stating that overnight requires cortex-backlog...") and Edge Cases (~L119: "...overnight refuses with a clear message naming the configured backend and requiring cortex-backlog") — these two are a true collapse candidate. **Correction (Req 4b): the third cited instance, NFR Safety (~L95: "External-tracker writes occur only on interactive... paths... never on the unattended overnight path with --dangerously-skip-permissions"), is a weaker thematic match, not a verbatim restatement — it states a broader safety principle (writes are interactive-only) rather than the overnight-refusal message itself. Confirm all three instances are truly equivalent before collapse; preserve any distinct NFR nuance rather than erasing it.** | 9618653d |
| cortex/requirements/backlog.md | s9 | ### `none` backend behavior | "A repo can opt out of all cortex ticket interaction, including LLM best-effort." | survives | a (structural-substitution) + c (preserved-elsewhere) | COMPRESS. Confirmed structural: commit history shows #317 shipped none-routing across all 5 interactive consumers (commits 239227ab discovery, 8c9b3983 dev, 92802781 refine, 9635ae2f morning-review, plus lifecycle write-back), so the routing behavior is code-shipped, not solely prose-specified. Confirmed preserved-elsewhere: Edge Cases (~L124: "`none` + discovery: composed tickets are surfaced inline / in the research artifact, not treated as an error") duplicates this FR's Outputs line (~L85: "Incidental consumers skip; discovery surfaces its output instead of erroring"). Template Inputs/Outputs rows are restatement. | 9618653d |
| cortex/requirements/backlog.md | s10 | ## Non-Functional Requirements | "Zero per-tool maintenance" | survives | a (structural-substitution) + c (preserved-elsewhere) | COMPRESS. Confirmed: tests/test_overnight_backlog_backend_guard.py code-enforces the Safety bullet's overnight-only-local claim (structural). Confirmed preserved-elsewhere: "Zero per-tool maintenance" bullet duplicates Overview para 2's driving-motivation language (~L11: "...zero per-tool maintenance..."); "Backward compatibility" bullet duplicates the Backend-resolution FR's acceptance criteria (~L44: "With no backlog: block present, consumers behave exactly as they do today"); "Safety" bullet duplicates the Overnight-requires-cortex-backlog FR (~L76-78) and Edge Cases (~L119), same instances discussed under s8. "Honest support claims" is the unique bullet to keep — no duplicate found on whole-file read. | 9618653d |
| cortex/requirements/backlog.md | s13 | ## Edge Cases | "Round-trip ambiguity (multiple tracker items match a title/slug)" | survives | b (informative-only) + c (preserved-elsewhere) | COMPRESS. Three bullets confirmed preserved-elsewhere (c): "External backend configured but overnight is run" duplicates FR (~L76-78) and NFR Safety (~L95) — same triplication discussed under s8; "Existing lifecycle.config.md with no backlog: block" duplicates the Backend-resolution FR acceptance criteria (~L44); "`none` + discovery" duplicates FR s9's Outputs (~L85) and Edge Cases self-consistency already covered under s9. Three bullets confirmed informative-only (b), no shall/must or new behavior-determining statement beyond what a frontier model derives: "Round-trip ambiguity... the LLM disambiguates or asks the user rather than guessing"; "Empty instructions for an external backend... falls back to general knowledge of the named tool"; "Backend names a tool the LLM can't drive... best-effort fails gracefully and surfaces the error." | 9618653d |
| cortex/requirements/backlog.md | s14 | ## Open Questions | "The Jira best-effort path is unverified" | survives | a (structural-substitution) + c (preserved-elsewhere), corrected scope | COMPRESS, with the drifted "three of four" claim corrected per Req 4(a). Whole-file read of the Open Questions section (~L127-131) confirms only **2 of the 4 bullets** carry a baked-in `Recommendation:` clause: bullet 2 ("Whether to persist the external ticket ref... Recommendation: re-resolution for v1; revisit if it proves flaky") and bullet 3 ("Whether `cortex init` should interactively prompt for a backend... Recommendation: scaffold-with-default + comments, no interactive prompt in v1"). Both recommendations are confirmed shipped: bullet 2's decision is independently stated in FR s7's round-trip acceptance criteria (~L67, preserved-elsewhere, ties to the s7↔s14 coupling in the preamble); bullet 3's decision is structurally confirmed by cortex_command/init/templates/cortex/lifecycle.config.md (scaffold-with-default, no interactive prompt) and tests/test_init_backlog_scaffold.py. **The other 2 bullets are genuinely open and must NOT be touched: bullet 1 ("The Jira best-effort path is unverified... Deferred until a real Jira user drives it") and bullet 4 ("Whether building any concrete external adapter... is ever warranted... deferred to a future area decision driven by real demand") carry no Recommendation and no shipped resolution — trim only the 2 recommended bullets; do NOT touch the 2 genuinely-open bullets; never write a decision record for an unsettled question.** | 9618653d |

**Summary**: 11/11 candidates verified whole-file, recall-first. All 11 survive (no candidate was refuted — no span was found load-bearing in a way that contradicts its claim). Two corrections applied per Req 4: s14's scope narrowed from "3 of 4" to "2 of 4" Open-Questions bullets; s8's "three equivalent instances" claim corrected to flag the NFR (~L95) instance as a weaker thematic match requiring nuance-preservation, not a straight collapse target. All pin locations (justfile:575, tests/test_dual_source_reference_parity.py, cortex_command/lifecycle_config.py:97 resolve_backlog_backend, tests/test_lifecycle_config_backlog_backend.py, tests/test_init_backlog_scaffold.py, tests/test_overnight_backlog_backend_guard.py, cortex_command/overnight/cli_handler.py, cortex/adr/0016-*.md, cortex/adr/0019-*.md, skills/lifecycle/references/discovery-bootstrap.md:13) were confirmed to exist and to substantiate their cited claim by direct inspection during this pass.

## Section: pipeline.md

# Verify-outcomes fragment: cortex/requirements/pipeline.md (Task 2b)

> Whole-file recall-first verification of the 6 `cortex/requirements/pipeline.md` candidates
> (s3, s4, s5, s7, s13, s14) from `cortex/research/skill-value-scorecard/master_candidates.json`.
> This is a Phase 1 (verify & capture) fragment — no requirements file was edited to produce it.
> `start_line`/`end_line` in the ledger are stale by 0–2 lines relative to the current file
> (confirmed by direct comparison; e.g. ledger `s3.start_line=11` vs. actual heading line 13,
> `s13.start_line=149` vs. actual heading line 149 — exact only by coincidence). All rows below
> anchor by heading + pinned token per Req 3, never by stored line number.

**Preamble note — s13 cluster (coordinate with s3 in Phase 2):** s13 is a MERGE_DEDUP that folds
three restated constraints — State-file-locking (≈ Non-Functional "Concurrency safety" bullet),
Repair-cap (restated in both the Conflict Resolution AC and the Post-Merge Test Failure Recovery
AC), and Branch-persistence (restated in the Session Orchestration AC, "Integration branches
(`overnight/{session_id}`) persist after session completion...") — into the canonical
`## Architectural Constraints` section, where all three already have a canonical bullet
(**State file locking**, **Repair attempt cap**, **Integration branch persistence**). Apply as
ONE coordinated Phase-2 edit (not three independent trims), because the Branch-persistence
restatement being cut lives inside s3's own declared span (Session Orchestration). s3's actual
targeted text (the dry-run and cancel/runner.pid bullets) does not overlap s13's Branch-persistence
line, but both edits touch the same H2/H3 region in the same file — sequence s13's Branch-persistence
cut and s3's compress in the same coordinated pass so neither clobbers line offsets the other
depends on.

**Preamble note — Phase 2 anchor-refresh flag:** `cortex_command/lifecycle_event.py:15-16` is a
code docstring reading `` (see `cortex/requirements/pipeline.md` L143/146/151 for the atomicity,
audit-trail, and locking constraints this satisfies) `` — an L-prefix line-anchor citation of the
Non-Functional Requirements / Architectural Constraints region s13 rewrites. Confirmed present
verbatim at `cortex_command/lifecycle_event.py:15`. This citer must be refreshed (recomputed line
numbers or converted to a heading reference) in the same Phase-2 commit that applies s13, per Req 7's
L-prefix-form enumeration requirement.

## Outcomes

| file | id | heading | anchor_token | verdict | signal | reason | applied_in_commit |
|---|---|---|---|---|---|---|---|
| cortex/requirements/pipeline.md | s3 | ### Session Orchestration | "`cortex overnight start --dry-run` is a supported test-affordance mode" | survives | (a) `tests/test_runner_pr_gating.py::test_dry_run_stdout_byte_identical` (confirmed present, byte-identical stdout snapshot vs. `tests/fixtures/dry_run_reference.txt`) enforces the dry-run mechanics independently of this bullet's narration; (c) the cancel/runner.pid verification detail ("magic + start_time (±2s via psutil)") is preserved verbatim-in-substance at this file's own `## Dependencies` section (runner.pid bullet: "cancel verifies magic + start_time (±2s via psutil) before signalling") and at `docs/overnight-operations.md` "Runner concurrency guard" section (confirmed lines ~210–227: "magic header, schema version, session id, OS pid, and `start_time` that `verify_runner_pid` cross-checks against `psutil.Process.create_time()`") | COMPRESS the two mega-bullets (dry-run test-fixture narration; cancel/runner.pid verification detail) to contract-level statements. Live citer `docs/overnight-operations.md:212` line-anchors `pipeline.md:28` (confirmed present verbatim) and must be refreshed/repointed in the same Phase-2 edit (Req 7). Does not touch the must-keep "Session phases transition forward-only" state-transition bullet (separate line, untouched). | e82344d5 |
| cortex/requirements/pipeline.md | s4 | ### Feature Execution and Failure Handling | "Intra-feature task ordering is preserved" | survives | (a) structural-substitution, stronger than the ledger's own "none found" framing: `cortex_command/common.py::compute_dependency_batches` (confirmed) raises `ValueError(f"Unresolvable dependency reference(s) {dangling}: no task with that id exists...")` for exactly the "dependency reference that resolves to no declared task" case, tested in `cortex_command/tests/test_common.py::TestComputeDependencyBatches` and `TestSubTaskBatching` (confirmed, incl. `test_subtask_serial_chain_3a_3b`, `test_parallel_subtask_siblings_coschedule`); `### Task Na` sub-task heading parseability is owned/tested by `cortex_command/pipeline/parser.py` + `cortex_command/pipeline/tests/test_parser.py` (confirmed `test_subtask_heading_colon_parses_to_task_id` etc.); the `task_id`-not-`.number` identity design is documented in depth by `cortex/adr/0010-task-id-is-task-identity-not-number.md` (confirmed) | COMPRESS the ~1.17KB implementation-narration bullet (task_id grammar, `### Task Na` parseability detail) to a contract-level statement; the behavior it describes is independently enforced and tested at the code layer, not merely asserted by this doc. Does not touch the separate feature-status-transition bullet (must-keep, untouched). | e82344d5 |
| cortex/requirements/pipeline.md | s5 | ### Overnight Runner Supervision | "This is distinct from and strictly inside" (appears near-verbatim in both the Description and the Acceptance-criteria bullet — confirms the claimed duplication) | survives | (c) preserved-elsewhere/intra-doc dedup: after compress, one canonical statement of the watchdog-inside-guardian distinction survives at this same heading (the AC bullet, keeping the three non-derivable constants 1800/14400/2700); the constants themselves are independently pinned in code — confirmed `STALL_TIMEOUT_SECONDS` (`cortex_command/overnight/runner.py:87`), `ABSOLUTE_CEILING_SECONDS` (`cortex_command/overnight/runner_primitives.py:46`), `WEDGED_STALENESS_SECONDS` (`cortex_command/overnight/recovery.py:98`, with an in-code `assert WEDGED_STALENESS_SECONDS > STALL_TIMEOUT_SECONDS`) — and in `cortex/adr/0011-out-of-process-overnight-runner-supervision.md` and `docs/overnight-operations.md:43` | COMPRESS: state the watchdog/guardian distinction once instead of twice within the same H3; no external doc/test/ADR pins this heading's *prose* (only the constant values, which are retained per the claim) | e82344d5 |
| cortex/requirements/pipeline.md | s7 | ### Post-Merge Review | "On a substantive non-APPROVED outcome" | survives | (c) preserved-elsewhere: `cortex/adr/0015-review-could-not-run-vs-dispatch-crash-split.md:60` cites "`(Post-Merge Review)`" by name (confirmed present verbatim), and `docs/internals/pipeline.md:170-182` designates it "the contract in requirements/pipeline.md (Post-Merge Review)" (confirmed present verbatim, "the contract" language literally present) — both carry the rationale/emphasis prose being cut, so compressing to contract facts (revert-vs-preserve split, could_not_run discriminator, aggregate breaker counting, uniform per-site gating) loses no unique information | COMPRESS the body only — the `### Post-Merge Review` **heading is a named must-keep and is not touched** (both citer docs reference the section by name, which G1's heading-set gate protects structurally). This is the file's single largest paragraph (~2.36KB); only rationale/emphasis prose already recorded in ADR-0015 + docs/internals/pipeline.md is cut, the contract facts stay. | e82344d5 |
| cortex/requirements/pipeline.md | s13 | ## Architectural Constraints | "**State file locking**" | survives | (a)+(c) the canonical statements already live in this template-required H2 (confirmed present: "State file locking", "Repair attempt cap", "Integration branch persistence" bullets) — this IS the surviving canonical location, so the compress target is the *other* three restatements: Non-Functional "Concurrency safety" bullet (state-file-locking restatement, confirmed present), the Conflict Resolution + Post-Merge Test Failure Recovery AC bullets (repair-cap restated twice, confirmed present as "Repair attempt cap is a fixed architectural constraint" in both sections), and the Session Orchestration AC's "Integration branches...persist after session completion" bullet (branch-persistence restatement, confirmed present). `## Architectural Constraints` heading itself is structurally required by `skills/requirements-write/SKILL.md:53` and enforced by `tests/test_requirements_skill_e2e.py` (confirmed H2 list includes it) — G1 protects the heading; only the redundant restatements elsewhere are cut to pointers. | MERGE_DEDUP: three constraints restated 2–3× across the file collapse to their one canonical statement, already resident in this fixed-architectural-constraint section (a named must-keep, left intact). See preamble note: Branch-persistence's restatement sits inside s3's declared span — coordinate the two edits in the same Phase-2 pass. Live citer `cortex_command/lifecycle_event.py:15` (L-prefix form, confirmed present) needs anchor refresh — see preamble note. | e82344d5 |
| cortex/requirements/pipeline.md | s14 | ## Dependencies | "`runner.pid` — per-session IPC contract" | survives | (c) preserved-elsewhere across owning docs, confirmed by direct inspection: runner.pid schema/verification detail → `docs/overnight-operations.md` "Runner concurrency guard" (lines ~210–227, confirmed verbatim-depth: magic header, schema version, `verify_runner_pid`/`psutil.Process.create_time()`); scheduled-launches sidecar+lock → `docs/overnight-operations.md` (lines ~233–252, confirmed, `schedule()` GC pass and one-shot-via-teardown mechanics); MCP tool list + install-guard carve-outs + `runner-bootstrap.log` → `docs/mcp-server.md` (confirmed: tool list near top, `runner-bootstrap.log` diagnostics section, `CORTEX_ALLOW_INSTALL_DURING_RUN` bypass section all present verbatim-depth); sandbox-settings lifecycle → `docs/internals/pipeline.md` lines ~115–131 (confirmed, "Allowed write paths" subsection); active-session pointer → `docs/overnight-operations.md:307`, `docs/internals/mcp-contract.md`, `docs/internals/pipeline.md:132` (confirmed present, though the "phase: starting transient window" nuance is doc-unique to pipeline.md's Dependencies bullet — flagged, not a blocker since this is a compress-to-pointer not a full deletion). `## Dependencies` heading required by `skills/requirements-gather/SKILL.md:60`/`skills/requirements-write/SKILL.md:53` + `tests/test_requirements_skill_e2e.py` (confirmed) — heading only, not contents. | MERGE_DEDUP: collapse the eight operator-manual bullets to one-line "path — role (see owning doc)" entries; every bullet's detail is independently carried, at equal or greater depth, by the owning doc the repo's own doc-ownership convention (CLAUDE.md `## Conventions`) already designates. No test or code pins these bullet contents (confirmed: `mech_pins` are all path/token co-occurrence references from downstream consumers of the *paths themselves*, not readers of this doc's prose). | e82344d5 |

## Section: observability.md

# Verify outcomes — cortex/requirements/observability.md (Task 2c, whole-file recall-first pass)

Read whole of `cortex/requirements/observability.md` (all 161 lines, current as of this pass) before judging any candidate — no span-isolated verification. Confirmed the file carries **8** `## ` headings (Overview, Functional Requirements, Non-Functional Requirements, Architectural Constraints, Dependencies, Edge Cases, Install-mutation invocations, Open Questions) — one more than `skills/requirements-write/SKILL.md`'s 7-section template, because "Install-mutation invocations" is a legitimate extra H2 (named must-keep, see s14 row below). G1 (H2-set byte-identical pre/post) therefore must preserve all 8, not 7.

**Cluster note (s9 ↔ s13 — apply as ONE coordinated Phase-2 edit, not two independent trims):** the arrays-replace-not-merge gotcha is stated **three** times in this file: Sandbox Socket Access's acceptance criterion (L92, "arrays replace, not merge"), Sandbox Socket Access's Note (L96, "arrays replace (not merge with)... write a self-contained array"), and the Edge Cases bullet "`settings.local.json` array clobber" (L128, "setup recipe uses deep merge to preserve sibling keys"). s9's claim targets the first two instances (AC↔Note) as an in-section dedupe; s13's claim independently targets the third instance (Edge Case) as a dedupe against the Note. Both claims are correct in isolation, but they converge on the **same** Note (L96) as the survivor — Phase 2 must apply this as one coordinated edit that leaves exactly one canonical statement (most likely the Note, since it carries both halves of the gotcha: the replace-semantics rule and the setup-recipe mitigation) and repoints both the AC bullet and the Edge Case bullet at it, rather than three independent edits that could each assume a different survivor.

**Named must-keep confirmed present and untouched by any applied candidate below:** the `## Install-mutation invocations` H2 (L131-157) is the observability.md maintainer contract (pre-install in-flight guard audit-classification procedure) named must-keep in the spec's Technical Constraints. No applied candidate (s6/s7/s8/s9/s13) touches this section; s14 (below) targets it directly but is deferred, not applied — the section is untouched by this batch either way.

| file | id | heading | anchor_token | verdict | signal | reason | applied_in_commit |
|---|---|---|---|---|---|---|---|
| cortex/requirements/observability.md | s6 | ### Overnight Kill/Stall Telemetry | "the feedback signal for tuning the (currently guessed) ceiling value" | survives | a (structural-substitution) | COMPRESS confirmed. Whole-file read shows the Description (L55) and the acceptance criterion (L57-59) are near-verbatim duplicates, both ending in the identical clause used as this row's anchor token. Structural pins confirmed by direct inspection: `cortex_command/overnight/runner_primitives.py` lines 44/82/90 define and pin the `WatchdogContext.stall_reason` field and its `"inactivity"`/`"ceiling"` values; `tests/test_runner_threading.py` asserts `stall_reason == "inactivity"`, `stall_reason == "ceiling"`, and `stall_reason == ""` (no-kill case) directly against the code, not against this prose. The field/value contract is code+test-enforced — safe to collapse Description and AC into one statement. | 8d7a95db |
| cortex/requirements/observability.md | s7 | ### Runtime Adoption Telemetry | "closes the runtime-adoption-failure detection gap that DR-5 cannot reach" | survives | c (preserved-elsewhere) | COMPRESS confirmed. Description's closing clause ("Composed with DR-5 static parity lint (ticket 102)... DR-5 catches missing wiring; runtime telemetry catches wired-but-never-invoked scripts.") and the Priority line (this row's anchor token) restate the identical DR-5-complementarity rationale. Confirmed the pinned anchors this claim depends on survive independent of which copy is cut: verb names `bin/cortex-log-invocation`/`bin/cortex-invocation-report` are stated in Description's first two (untouched) sentences; the per-session JSONL path and error-breadcrumb path are stated verbatim in Inputs/Outputs (L65-66, untouched); the archived-spec R1-R18 pointer is stated in Acceptance criteria (L67, untouched). Pin files confirmed to exist on disk: `tests/test_log_invocation_branch_order.py`, `tests/test_path_self_test_enumeration.py`, `cortex/backlog/103-add-runtime-adoption-telemetry-via-pretooluse-bash-hook-matcher-dr-7.md`. | 8d7a95db |
| cortex/requirements/observability.md | s8 | ### In-Session Status CLI | "`_dispatch_overnight_status`" | survives | a (structural-substitution) + c (preserved-elsewhere); one sub-span reclassified to b (informative-only) — see correction | COMPRESS confirmed, with one correction to the claim's stated justification. Description (L72) carries three trimmable sub-spans: (1) the dispatch-chain narration (this row's anchor token → `handle_status`); (2) install-location detail (`~/.local/bin/cortex`); (3) the legacy-shim history note ("The legacy `bin/overnight-status` bash shim is retired (see `pipeline.md:28`)"). All acceptance criteria (L76-82) and the read-only NFR are structurally pinned and untouched by the trim: `tests/test_cli_overnight_recover.py:6` literally cites `observability.md:93/99`'s read-only-status contract in its own docstring; `cortex/adr/0011-out-of-process-overnight-runner-supervision.md:31` independently states "`status` is a read-only observability surface (`observability.md`)" — both pin the read-only characteristic (AC/NFR), not the dispatch narration, confirming sub-spans (1)/(2) are safe to cut (signal a). **Correction:** the claim asserts sub-span (3) is "owned by `pipeline.md:28`" (structural-substitution) — verified FALSE by direct inspection: `docs/internals/pipeline.md:28` is a `metrics.py` table row with no shim/retirement content, and `git log --all -p -- docs/internals/pipeline.md` returns zero hits for "shim" — pipeline.md has never documented shim retirement, at any point in its history. Reclassified sub-span (3) to signal **b (informative-only)** instead: it is a historical note with no `shall`/`must` and no enum/constant, and the underlying fact remains independently verifiable (`bin/overnight-status` confirmed absent from `bin/`), so removing it loses no enforcement even though its self-citation is stale. **Flag:** the stale `pipeline.md:28` self-pointer is a pre-existing data-quality defect in observability.md's own prose, not introduced by this trim; the true owning location for "the shim is retired" was never pipeline.md and is not otherwise restated anywhere in the repo — this is a low-value historical-fact loss, not a normative regression, but it is not the clean structural-substitution the original claim asserted. | 8d7a95db |
| cortex/requirements/observability.md | s9 | ### Sandbox Socket Access | "self-contained array containing all required sockets" | survives | c (preserved-elsewhere) — canonical survivor to be fixed at Phase-2 apply time, coordinated with s13 | COMPRESS confirmed. Whole-file read confirms the arrays-replace-not-merge gotcha is stated three times (see preamble cluster note: AC L92, Note L96 [this row's anchor], Edge Case L128). No external file's `mech_pins` cite the gotcha by observability.md line/phrasing (ledger `pins: []`; `mech_pins` reference the generic symbols `allowUnixSockets`/`sandbox.filesystem.allowWrite`/`settings.json`, not this specific statement), so no live citer depends on all three copies existing verbatim. Consolidating to one canonical statement (Phase 2, coordinated with s13 per the cluster note) preserves the full contract per signal (c), contingent on the surviving statement literally carrying both halves of the gotcha (array-replace semantics AND the setup-recipe self-contained-array mitigation) — final survivor location is a Phase-2 apply-time decision, not resolved by this row alone. | 8d7a95db |
| cortex/requirements/observability.md | s13 | ## Edge Cases | "resets event offset to 0 on session ID change" | survives | c (preserved-elsewhere) | MERGE_DEDUP confirmed. Whole-file read confirms the claimed 4-kept/3-dropped split: kept unique bullets are "No active session" (L122), "jq unavailable" (L124), "Stale PID in `.runner.lock`" (L126), "tmux socket grants broad access" (L129) = 4, matching the claim exactly. Dropped-as-duplicate bullets confirmed preserved-elsewhere: "Session directory rotation" (L123, this row's anchor token) duplicates Dashboard's AC (L38, "Session change... resets event offset and re-reads from the beginning"); "Corrupt `overnight-state.json`" (L127) duplicates In-Session-Status-CLI's AC (L82, "Handles corrupt `overnight-state.json` gracefully... falls back to events-only"); "`settings.local.json` array clobber" (L128) duplicates the Sandbox-Socket Note (L96) — this is the third instance of the s9 gotcha (see preamble cluster note), and its removal here must be sequenced with s9's Phase-2 consolidation so the canonical statement that survives is the Note's successor, not silently dropped along with this bullet. Pin `skills/requirements-write/SKILL.md:54` confirmed by direct inspection: item 6 of the 7-section template is `## Edge Cases`, so the H2 is template-required — only bullets are trimmed, the H2 itself stays (G1-safe). | 8d7a95db |
| cortex/requirements/observability.md | s14 | ## Install-mutation invocations | "is opt-in by callers — not invoked at package import" | deferred | — | deferred: out-of-editorial-scope (Req 9). s14 is a LAZY_REF proposing relocation of the entire "Install-mutation invocations" H2 (L131-157, ~960 tokens, 25% of the file) to `docs/internals/auto-update.md`. This is a cross-file move outside the 5-file editorial scope (spec Req 9 / OQ2) and targets a section the spec's Technical Constraints names must-keep ("observability.md 'Install-mutation invocations' maintainer contract"). Not applied and not refuted — the relocation may still be valid work, just not this batch's scope; a follow-up backlog item to evaluate it as a real lifecycle change is filed separately per Req 9's acceptance criteria (ticket creation is downstream of this fragment, not performed by this verification task). Observability.md's applied set for this batch is therefore s6/s7/s8/s9/s13 only — 5 of 6 candidates, not 6. | — |

**Summary**: 6/6 candidates verified whole-file, recall-first. 5 survive (s6, s7, s8, s9 as COMPRESS; s13 as MERGE_DEDUP), 0 refuted, 1 deferred (s14, per Req 9 — out-of-editorial-scope, not a content judgment). One correction applied during verification: s8's claim that the legacy-shim history note is "owned by `pipeline.md:28`" does not hold (confirmed by direct inspection and full git history search that pipeline.md never documented shim retirement); the sub-span is reclassified from signal (a) to signal (b) informative-only rather than blocking the trim, since the underlying fact (shim absence) is independently verifiable and the note carries no normative content. One cross-candidate coordination flagged: s9 and s13 both target the same third instance of the arrays-replace-not-merge gotcha (Edge Case L128 vs. Sandbox-Socket Note L96) and must be applied as one Phase-2 edit fixing a single survivor, not two independent trims. All pin locations checked by direct inspection: `cortex_command/overnight/runner_primitives.py:44,82,90`, `tests/test_runner_threading.py`, `tests/test_log_invocation_branch_order.py`, `tests/test_path_self_test_enumeration.py`, `cortex/backlog/103-*.md`, `tests/test_cli_overnight_recover.py:6`, `cortex/adr/0011-*.md:31`, `skills/requirements-write/SKILL.md:54` all confirmed to exist and substantiate their cited claim; `docs/internals/pipeline.md:28` checked and found to NOT substantiate its cited claim (see s8 correction above).

## Section: remote-access.md

# Verify outcomes — cortex/requirements/remote-access.md (fragment, Task 2d)

Whole-file, recall-first verification of the 5 `master_candidates.json` rows with
`file == "cortex/requirements/remote-access.md"`, `status == "unverified"`, no
`overlaps_ticket`, no `reproposal_of` (ids: `file-compress`, s2, s4, s5, s6 —
confirmed against the ledger; `file-compress` is MERGE_DEDUP over the tail-section
aggregate, s2/s4/s5/s6 are COMPRESS). Keep-guidance is taken only from each
candidate's ledger `claim` field, per spec Req 2. This is a transient Phase-1
input; Task 3 folds it into the assembled `verify-outcomes.md`. No requirements
file is edited by this fragment.

**s2 ↔ s6 ↔ file-compress cluster (spec Req 5b)**: whole-file reading confirms
two facts are each triplicated across this doc, not just duplicated:
- **tmux tool-agnostic framing** — Overview L9 ("current implementation uses
  tmux… subject to change"), Dependencies L49 ("tmux (current implementation,
  **subject to change**)"), and Open Questions L58-60 ("tool currently providing
  session persistence (tmux skill) is under review… must be preserved regardless
  of which tool provides it") all state the same fact. Open Questions is the
  Overview-adjacent duplicate named by s2's claim; the Dependencies echo is a
  third instance surfaced by this verification pass, inside `file-compress`'s span.
- **macOS/Ghostty constraint** — NFR L41 ("macOS is the primary and only
  supported platform… Ghostty dependency"), Architectural Constraints L45
  ("depends on a macOS terminal that supports persistent container processes
  (currently Ghostty)"), and Dependencies L49 ("Ghostty terminal (macOS)") all
  state the same fact. NFR is s6's span; Architectural Constraints and the
  Ghostty half of Dependencies are inside `file-compress`'s span.

All three candidates individually survive verification (below), but per spec
Req 5(b) Phase 2 must apply them as **one coordinated edit** with one canonical
home per fact — not three independent trims that could each leave a different
"last" copy standing, or that could jointly delete every copy of a fact.

Named must-keep confirmed intact and untouched by all 5 candidates' spans: the
**Failure transparency** NFR bullet (L39) is independently load-bearing —
`docs/overnight-operations.md:615` cites it by file reference ("per
`cortex/requirements/remote-access.md`, notification and session-management
failures are silent by design"). s6's claim already keeps this bullet; verified
correct and flagged here as a hard constraint on Phase 2, not just a nice-to-have.

**Citer scan (spec Req 7 scope, `cortex_command/**` + `docs/**`)**: the only live
citer of this file anywhere in that scope is `docs/overnight-operations.md:615`,
and it is a **file-level reference with no line number** (no `remote-access.md:N`,
no L-prefix form, no range form found by grep across `cortex_command/`, `docs/`,
`tests/`, `skills/`, `hooks/`, `claude/`). It cites the Failure-transparency fact,
which survives unmodified, so no post-trim recompute is needed for this file
regardless of which of the 5 candidates are applied in Phase 2.

| file | id | heading | anchor_token | verdict | signal | reason | applied_in_commit |
|---|---|---|---|---|---|---|---|
| cortex/requirements/remote-access.md | s2 | ## Overview | "subject to change" | survives | c (preserved-elsewhere) | The Overview's tool-agnostic framing ("The current implementation uses tmux for session persistence, but the requirement is defined at the capability level: the specific tool providing persistence is subject to change") is preserved-elsewhere: near-verbatim at Open Questions L58-60 ("The tool currently providing session persistence (tmux skill) is under review. The requirements above describe the capability that must be preserved regardless of which tool provides it.") and echoed a third time at Dependencies L49 ("tmux (current implementation, subject to change)"). Confirmed by direct read of current file (no drift from the claim). Part of the s2↔s6↔file-compress cluster (Req 5b) — Phase 2 must pick one canonical home (Overview is the natural one, per the claim's own framing) and point the other two at it, not delete all three or leave duplicates. | 7295b21d |
| cortex/requirements/remote-access.md | s4 | ### Session Persistence | "developer initiates session move or reconnection" | survives | b (informative-only) + c (preserved-elsewhere) | Inputs ("Active Claude Code session; developer initiates session move or reconnection") and Outputs ("New terminal window with session running inside a persistent container; original session continues unaffected") are template-filler scene-setting with no independent `shall/must` content — quoted spans, signal b. The two AC bullets this candidate proposes cutting — "detached from its client window without interrupting the active conversation" and "reattached from a different terminal window or device" — restate Description L15 ("must persist independently of the client connection, surviving network interruptions, terminal closures, and device switches"), signal c, preserved at L15. Confirmed no code/test/hook in this repo implements or checks tmux session-persistence behavior (grepped `tests/`, `cortex_command/`, `hooks/`, `skills/`); `docs/setup.md:7` confirms tmux/terminal session mechanics are machine-config-repo territory, not this repo's, so there is no structural-substitution (signal a) available or needed. The three untouched AC bullets (session identity preserved, enumerable, name/ID-addressable) carry the only non-derivable capability contract and are correctly kept per the claim. | 7295b21d |
| cortex/requirements/remote-access.md | s5 | ### Remote Session Reattachment | "roaming between networks" | survives | b (informative-only) + c (preserved-elsewhere) | Inputs/Outputs are template filler, same pattern as s4 (signal b, quoted). The AC bullet "mosh connection survives IP address changes and roaming between networks" states mosh's well-known generic design property (mosh was built specifically for this), not a project-specific decision — confirmed no test/hook/doc in the repo encodes IP-roaming behavior (grepped `tests/`, `cortex_command/`, `hooks/`, `skills/`, `docs/` for "mosh"/"IP address"/"roaming" — only generic tool-name mentions found, e.g. `docs/internals/sdk.md:240`, `skills/morning-review/references/walkthrough.md:127,658`, none describing IP-roaming semantics); it is derivable from the tool choice already recorded in Dependencies L50 and Description L28 (signal b). The genuinely project-specific decisions this candidate keeps — Tailscale+mosh stack choice, no-port-forwarding constraint, should-have priority — are independently confirmed load-bearing: `docs/internals/sdk.md:240` cites "Tailscale + mosh + tmux handles remote access" for `RemoteTrigger`, but does not restate the no-port-forwarding nuance anywhere, so that AC line (L34) must survive untouched, consistent with the claim. | 7295b21d |
| cortex/requirements/remote-access.md | s6 | ## Non-Functional Requirements | "Failure transparency" | survives | b (informative-only) + c (preserved-elsewhere) | The Failure-transparency bullet (L39) is confirmed independently load-bearing — `docs/overnight-operations.md:615` cites it by file reference — and the claim correctly keeps it untouched; verified as a hard constraint, not just a claim to trust. "Timeout: Session reattachment depends on network latency" (L40) is a vacuous, non-actionable restatement with no `shall/must` content — no timeout value, config, or check exists anywhere in `cortex_command/` or `hooks/` (grepped) — signal b, quoted. The Platform/Ghostty bullet (L41, "macOS is the primary and only supported platform for session persistence (Ghostty dependency). Linux/Windows are not supported.") is confirmed triplicated in substance at Architectural Constraints L45 and Dependencies L49 — signal c. Part of the s2↔s6↔file-compress cluster (Req 5b); Phase 2 must consolidate to one canonical Platform/Ghostty statement (NFR is the natural home, since it also carries the "Linux/Windows are not supported" negative-scope statement the other two copies lack) rather than trimming independently. | 7295b21d |
| cortex/requirements/remote-access.md | file-compress | ## Architectural Constraints / ## Dependencies / ## Edge Cases / ## Open Questions (aggregate) | "persistent container processes" · "Local notifications" · "Ghostty not installed" · "tmux skill" | survives | b (informative-only) + c (preserved-elsewhere) | Verified sub-span by sub-span (whole-file, recall-first): (1) Architectural Constraints' sole bullet ("depends on a macOS terminal that supports persistent container processes (currently Ghostty)") is preserved-elsewhere at NFR L41 and Dependencies L49 — signal c; same macOS/Ghostty triplication also touched by s6 (Req 5b cluster). (2) Dependencies' "Local notifications: `terminal-notifier` (macOS), Ghostty (for click-to-activate)" line is preserved-elsewhere in substance at `observability.md:116` ("Notifications (macOS): `terminal-notifier` (installed via `brew install terminal-notifier`); Ghostty terminal") — signal c; confirmed vestigial via `git show 373ca304 --stat`, which independently confirms that commit's subject/body ("Remove cortex-notify-remote hook… strip references from… observability/remote-access requirements") removed the companion remote-notification hook this "Local notifications" line used to pair with, leaving it an orphaned duplicate of observability.md's copy. Dependencies' other two bullets ("Session persistence: tmux…" and "Remote connection: Tailscale…") are each independently preserved-elsewhere too (Overview L9 and s5 Description L28 respectively, signal c) but are **not** claimed for deletion by this candidate — the claim's "roughly half" framing and MERGE_DEDUP category mean Phase 2 condenses/points, it does not blank the whole Dependencies section; flagging this so Phase 2 doesn't over-delete. (3) Edge Cases' two bullets ("Ghostty not installed → fails at window creation, error suggests installation"; "Tailscale/mosh not installed → fails at client") are mechanically derivable "dependency missing → that leg fails" statements — no test/hook implements or checks these specific error messages (grepped `tests/`, `hooks/`, `cortex_command/`), and the kept NFR Failure-transparency bullet already establishes failures are silent-by-design, which is in tension with the aspirational "error message suggests installation" clause — signal b, quoted. (4) Open Questions' tmux-under-review bullet is preserved-elsewhere at Overview L9 (near-verbatim) — signal c; third leg of the tool-agnostic-framing triplication (Overview / Dependencies "subject to change" / Open Questions), same s2↔s6↔file-compress cluster requiring one coordinated Phase-2 edit. | 7295b21d |

## Notes for Phase 2 (citer refresh, spec Req 7)

- Only one live citer of this file exists in the `cortex_command/**` + `docs/**`
  scope: `docs/overnight-operations.md:615`, a **file-level reference with no
  line number** ("per `cortex/requirements/remote-access.md`"). No recompute is
  needed post-trim as long as the Failure-transparency fact it cites (kept
  untouched by s6) survives at some canonical location — which it does under
  every candidate's verdict above.
- No L-prefix or range-form citers found for this file anywhere in the repo.

## Verification self-check

- Row count: 5 (`grep -c '^| cortex/requirements/remote-access.md ' verify-outcomes-remote-access.md` = 5).
- All 5 rows are `survives`; every signal cell is non-empty and typed (b/c, no
  bare "looks safe" reasons — each cites a quoted span, a specific surviving
  location, or a specific grep/`git show` confirmation).
- No candidate refuted in this fragment.
- Self-sealing check: every signal is backed by an artifact independently
  re-checkable by a third party (a specific file+line quoted or grepped, or a
  `git show <hash> --stat` commit-message excerpt) rather than by this agent's
  own unaided judgment. The one soft spot: the s2/s6/file-compress "preserved
  elsewhere" calls describe content as semantically equivalent, not always
  byte-identical, across the three triplicated copies — flagged explicitly in
  the cluster note above so Phase 2 treats consolidation as a coordinated
  single edit (per spec Req 5b) rather than three independent trims that could
  each assume a different copy is "the" canonical one.

## Section: multi-agent.md

# Verify outcomes — cortex/requirements/multi-agent.md (fragment, Task 2e)

Whole-file, recall-first verification of the 4 `master_candidates.json` rows with
`file == "cortex/requirements/multi-agent.md"`, `status == "unverified"`, no
`overlaps_ticket`, no `reproposal_of` (ids: s2, s4, s6, s9). Keep-guidance is taken
only from each candidate's ledger `claim` field, per spec Req 2. This is a transient
Phase-1 input; Task 3 folds it into the assembled `verify-outcomes.md`. No
requirements file is edited by this fragment.

Named must-keep confirmed intact and untouched by all 4 candidates' spans: the
cited GitHub issue `anthropics/claude-code#39886` (Edge Cases, lines 87-94) — none
of s2/s4/s6/s9's spans (Overview 7-10; Agent Spawning 13-25; Parallel Dispatch
39-53; Architectural Constraints 73-79) overlap the Edge Cases section, so this
must-keep is unaffected by this fragment's verdicts.

| file | id | heading | anchor_token | verdict | signal | reason | applied_in_commit |
|---|---|---|---|---|---|---|---|
| cortex/requirements/multi-agent.md | s2 | ## Overview | "The multi-agent area covers how the system spawns, isolates, and coordinates" | survives | b (informative-only); a (structural-substitution, corroborating) | The Overview body is descriptive framing that restates the four Functional-Requirements headings that follow (go/no-go criteria for parallel dispatch → Parallel Dispatch; per-feature worktree isolation → Worktree Isolation; model selection based on task characteristics → Model Selection Matrix; error recovery escalation → Model Selection Matrix's escalation ladder) with no `shall/must` or behavior-determining statement — quoted span: "The multi-agent area covers how the system spawns, isolates, and coordinates multiple Claude agents working in parallel. This includes the go/no-go criteria for parallel dispatch, per-feature worktree isolation, model selection based on task characteristics, and error recovery escalation. These capabilities underpin the overnight runner's ability to work on multiple features simultaneously without interference." Corroborating structural-substitution signal: only the `## Overview` heading itself is schema-pinned — `tests/test_requirements_skill_e2e.py:61` (`AREA_TEMPLATE_H2S` tuple, "these are anchors ... downstream consumers grep them by name") — and file selection is routed by path/trigger-phrase match, not Overview content — `tests/test_load_requirements_protocol.py:249` (fixture: "Working on agent spawning, parallel dispatch, worktrees → cortex/requirements/multi-agent.md", matched by tag substring, independent of the file's own prose). Both pins verified present and accurate at the cited lines. | 553d4de7 |
| cortex/requirements/multi-agent.md | s4 | ### Agent Spawning | "Per-spawn OS-kernel sandbox enforcement is layered under `bypassPermissions`" | survives | a (structural-substitution) | The per-spawn sandbox paragraph's behavior — `CORTEX_SANDBOX_SOFT_FAIL` downgrading `failIfUnavailable`, and its unconditional morning-report surfacing — is pinned by tests against code, not against this doc's prose: `tests/test_runner_sandbox.py` (`test_soft_fail_killswitch_set` and the surrounding "Req 4: CORTEX_SANDBOX_SOFT_FAIL kill-switch" block, confirmed present ~L523-534) exercises `sandbox_settings.read_soft_fail_env()`/`build_sandbox_settings_dict()`, and `tests/test_morning_report.py` (confirmed header block L1-26) exercises `cortex_command.overnight.report.render_soft_fail_header()` against a fixture `ReportData`. `docs/overnight-operations.md` "### Per-spawn sandbox enforcement" (confirmed heading at L629) is the CLAUDE.md-designated owning doc ("docs/overnight-operations.md owns the round loop and orchestrator behavior") that the compressed line already points to, so replacing the ~700B paragraph with a one-line summary + that pointer is safe. | 553d4de7 |
| cortex/requirements/multi-agent.md | s6 | ### Parallel Dispatch | "Orchestrator dispatch-template substitution contract" | survives | a (structural-substitution); b (informative-only, for the cut rationale/footnote clauses) | The `<substitution_contract>` XML block's presence in the rendered prompt is pinned by `tests/test_fill_prompt.py::test_fill_prompt_contains_substitution_contract` (confirmed L62-67, asserts `"<substitution_contract>" in out`), not by this doc's prose — the doc's role here is human-readable explanation, which is compressible. The specific clauses this candidate proposes cutting — "to defeat lexical priming" (rationale for the visual-distinction choice) and "Enforced by `tests/test_fill_prompt.py` at the shell layer; agent-layer substitution is a convention and not independently validated" / "Operator discipline only; no automated gate today" (both self-describing footnotes) — are informative-only, no `shall/must` content, quoted directly from the current doc. Per the task's explicit keep-guidance, retained verbatim (not cut, confirmed present in current text): the two token-tier names (session-level single-brace `` {token} `` pre-filled by `fill_prompt()`, per-feature double-brace `` {{feature_X}} `` substituted by the orchestrator agent), the single-layer-prompt exception (`batch-brain.md`, `repair-agent.md`, pipeline prompts remain single-brace), and the `active-session.json` predicate (absent, or `phase` not `running`, or PID not alive). | 553d4de7 |
| cortex/requirements/multi-agent.md | s9 | ## Architectural Constraints | "Worktrees for the default repo are created at" | survives | c (preserved-elsewhere) | `cortex/adr/0005-repo-relative-worktree-placement.md` (Context + Decision sections, confirmed by direct read) already carries, as the canonical location, the same three things this candidate proposes cutting from multi-agent.md L77: the placement rationale ("aligns with Anthropic's own native worktree convention (`agent-a<hex>/` directories under `.claude/`)", ADR Trade-offs), the `.mcp.json` filename-scoping explainer ("the deny is filename-scoped and blocks agent writes; it does not block `git worktree add` from creating the worktree directory or checking out other files", ADR Context), and the superseded env-prefix history (ADR Context: "`$TMPDIR/cortex-worktrees/` based on the (empirically false) premise ... This ticket (#260) reverts to the repo-relative placement after an empirical probe (2026-05-20)"; corroborated in git history at commit `b149b4bc` "Land Phase 4 of restore-worktree-root-env-prefix: doc sweep + R7 retirement", confirmed to exist). The path facts and the `resolve_worktree_root()` chokepoint citation (`cortex_command/pipeline/worktree.py`) are retained per the claim, not cut. | 553d4de7 |

## Notes for Phase 2 (citer refresh, spec Req 7)

- `cortex_command/overnight/fill_prompt.py:7` docstring cites `` cortex/requirements/multi-agent.md:50 `` but the substitution-contract content it describes is actually at line 51 (confirmed: current L50 is `- **Priority**: must-have`; L51 begins `- **Orchestrator dispatch-template substitution contract**: ...`). This is a pre-existing off-by-one, independent of this batch's trims — fix it to the correct post-trim line in the same Phase-2 commit that applies s6.
- `docs/overnight-operations.md:655` cites `` cortex/requirements/multi-agent.md:77 `` and is currently accurate (confirmed: current L77 is the worktree-placement rationale bullet s9 compresses). s9's trim is within the `## Architectural Constraints` span (~L73-79); recompute this citation to the post-trim line in the same Phase-2 commit that applies s9.
- The task brief's third citer note names `cortex_command/test_fill_prompt.py:62-66`. **Flagging a path discrepancy**: no file exists at that path — the file that exists is `tests/test_fill_prompt.py` (repo-root `tests/`, not under `cortex_command/`), and its lines 62-66 are confirmed to be `test_fill_prompt_contains_substitution_contract`, which pins the rendered *prompt* text (the `<substitution_contract>` block), not this doc's line — no change needed there regardless of this batch's trims. Phase 2's citer enumeration should search under the correct `tests/` path.

## Verification self-check
- Row count: 4 (`grep -c '^| cortex/requirements/multi-agent.md ' verify-outcomes-multi-agent.md` = 4).
- All 4 rows are `survives`; every signal cell is non-empty and typed (a/b/c).
- No candidate refuted in this fragment.
