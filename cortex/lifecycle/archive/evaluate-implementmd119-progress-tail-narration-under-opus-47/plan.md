# Plan: evaluate-implementmd119-progress-tail-narration-under-opus-47

## Overview

Single atomic edit: delete the `(b) Progress tail` bullet at `skills/lifecycle/references/implement.md:119` and renumber the inter-iteration sleep bullet from `(c)` to `(b)`. Regenerate the plugin mirror via `just build-plugin` so canonical and mirror commit together (the pre-commit drift hook enforces this). Verification runs the six grep counters from the spec plus the `test_skill_contracts` invariant test.

## Tasks

### Task 1: Delete `(b) Progress tail` and renumber `(c) → (b)` in canonical and plugin mirror

- **Files**:
  - `skills/lifecycle/references/implement.md` (canonical edit)
  - `plugins/cortex-interactive/skills/lifecycle/references/implement.md` (regenerated by `just build-plugin`)
- **What**: Delete line 119 (the entire `(b) Progress tail` bullet, including the `tail -n 5` Bash call and the "capped at 5 (not 20)" rationale) from the canonical `implement.md`. Renumber the next bullet's label from `(c) Inter-iteration sleep` to `(b) Inter-iteration sleep`. Run `just build-plugin` to regenerate the mirror via `rsync -a --delete skills/lifecycle/ plugins/cortex-interactive/skills/lifecycle/`. Commit both files together using `/cortex-interactive:commit`.
- **Depends on**: none
- **Complexity**: simple
- **Context**:
  - Target text at canonical line 119 (verbatim, verified 2026-05-04): `- (b) Progress tail: \`tail -n 5 lifecycle/{feature}/events.log\` and surface a brief summary of the 5 most recent events to the user. The tail is capped at 5 (not 20) to limit context accumulation over long runs.`
  - Target text at canonical line 120 (relabel only): `- (c) Inter-iteration sleep: \`sleep 120\` Bash call with \`timeout: 130000\` (130 seconds — ample margin over the 120-second sleep).` → relabel `(c)` to `(b)`; preserve the rest verbatim.
  - Surrounding (a) bullet at line 118 must remain unchanged: `- (a) Liveness: \`kill -0 $pid 2>/dev/null\`. Non-zero exit means the process has exited — break out of the polling loop and proceed to result surfacing.`
  - `kill -0 $pid 2>/dev/null` also occurs at canonical line 60 (§1a.ii double-dispatch guard) — out of scope; do not touch.
  - Plugin mirror regen recipe: `justfile:476` (`just build-plugin`); pre-commit drift hook at `.githooks/pre-commit:174–202` blocks commits where canonical and mirror disagree.
  - Commit per CLAUDE.md convention: invoke `/cortex-interactive:commit`, never `git commit` directly.
  - No callers reference the `(b) Progress tail` wording or the `(c) Inter-iteration sleep` label outside the two implement.md files (canonical + mirror); spec/research/backlog mentions are documentation only.
- **Verification**: Run the six spec acceptance grep counters and the contract test as a single compound check. Pass when ALL of:
  - `grep -c "Progress tail" skills/lifecycle/references/implement.md` outputs `0`
  - `grep -c "tail -n 5" skills/lifecycle/references/implement.md` outputs `0`
  - `grep -c "surface a brief summary of the 5 most recent events" skills/lifecycle/references/implement.md` outputs `0`
  - `grep -c "(c) Inter-iteration sleep" skills/lifecycle/references/implement.md` outputs `0`
  - `grep -c "(b) Inter-iteration sleep" skills/lifecycle/references/implement.md` outputs `1`
  - `grep -c "^- (a) Liveness:" skills/lifecycle/references/implement.md` outputs `1`
  - `grep -cF 'kill -0 $pid 2>/dev/null' skills/lifecycle/references/implement.md` outputs `2`
  - `grep -c "^- (b) Inter-iteration sleep:" skills/lifecycle/references/implement.md` outputs `1`
  - `grep -cF 'sleep 120' skills/lifecycle/references/implement.md` outputs `1`
  - `diff skills/lifecycle/references/implement.md plugins/cortex-interactive/skills/lifecycle/references/implement.md` exits `0`
  - `grep -c "Progress tail" plugins/cortex-interactive/skills/lifecycle/references/implement.md` outputs `0`
  - `pytest tests/test_daytime_preflight.py::test_skill_contracts -q` exits `0`
- **Status**: [x] complete

## Verification Strategy

The 11 grep counters and 1 pytest invocation listed in Task 1's Verification field are the spec's acceptance criteria translated literally — passing them is the end-to-end verification. No additional integration testing is needed because (a) the change is a prompt-text deletion in a skill reference file, not executable code, and (b) the existing `test_skill_contracts` invariants assert on structural §1b properties that this change must preserve. End-to-end behavioral verification (does the daytime polling loop actually narrate well under 4.7?) is explicitly out of scope per the spec's Non-Requirements — Anthropic's step-2 observation is deferred to future ad-hoc daytime dispatches, not gated in this lifecycle.

## Veto Surface

- **Single-task decomposition**: This plan has one task. The spec requires a single coordinated atomic change (canonical edit + mirror regen + verification) that the pre-commit drift hook forces into one commit. Splitting into "edit canonical" + "regen mirror" + "verify" tasks would create intermediate states the pre-commit hook rejects, forcing `--no-verify` between sub-tasks. The user may prefer split tasks if the implement-phase sub-task dispatch model benefits from explicit verification staging — flag if so and re-decompose.
- **Verification field is dense**: 12 individual checks in one task. The implementer agent must run all of them to mark complete; partial passes are a fail. Acceptable per spec, but if the implement-phase dispatch agent struggles with compound verification, splitting into a dedicated verification task is a fallback.

## Scope Boundaries

Per spec's Non-Requirements:
- No migration to Claude Code's Monitor tool — polling architecture (§1b vi/vii) unchanged.
- No edits to adjacent narration directives elsewhere in implement.md (lines ~47, 122, 134, 138, 140, 217, 244, 291).
- No replacement narration mechanism (event-change-triggered, structural-event-triggered, or otherwise).
- No stall/hang detector to compensate for the removed implicit hang signal.
- No execution of Anthropic's step 2 (cross-dispatch observation) or step 3 (conditional shaped-example reinstatement).
- No new tests asserting on the deletion — the grep acceptance criteria + existing `test_skill_contracts` are the regression coverage.
- No rename of ticket #133's title (`evaluate-implementmd180-...`) to reflect the current line-119 target.
