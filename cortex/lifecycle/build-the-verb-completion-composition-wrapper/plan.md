# Plan: build-the-verb-completion-composition-wrapper

## Overview

Move the four multi-emission decision clusters (plan approval, review verdict, spec approval, implement transitions) into wrapper-verb console scripts whose function bodies ARE the ordering invariant; convert the kept-pauses inventory to marker-generated output with exact set-equality parity; and point the overnight orchestrator at the shared `reduce_lifecycle_state` reducer. Phasing is load-bearing: test harness first, marker generation before prose rewrites (Task 3's markers are added while the old line-anchored parity net is still live — its ±35 tolerance absorbs the marker line-shifts until Task 5 retires the scheme), then verbs+prose, then the overnight read.

**Architectural Pattern**: layered — a verb composition layer (function bodies own event ordering) sits between the skill-prose routing layer (routes on JSON discriminants) and the append-only `events.log` primitives (`log_event` / `_append_event_atomic`).

## Outline

### Phase 1: Test-harness foundation (tasks: 1, 2)
**Goal**: Close the `just test` coverage blind spot and add environment-fixture guard tests, so later pin retirements trade into enforced tests.
**Checkpoint**: `just test` runs the lifecycle/backlog/`cortex_command` test trees and is green, including the new arm-f fixture tests.

### Phase 2: Marker-generated kept-pauses (tasks: 3, 4, 5)
**Goal**: Markers at every current pause site (census re-audited exhaustively before the AskUserQuestion site-scan retires), a pause data file + generator, and parity collapsed to exact marker-set equality + freshness + marker-anchored semantic checks (including AskUserQuestion-proximity for the consent kinds).
**Checkpoint**: `kept-pauses.md` is generated output; the rewritten parity test is green locally and wired into CI; the line-tolerance scheme is gone; the doc supersession lands atomically with the flip.

### Phase 3: Wrapper verbs and prose routing (tasks: 6, 7a, 7b, 7c, 7d, 8, 9, 10, 11, 12)
**Goal**: The four verbs ship with write-side order tests; the four cluster prose files route on verb discriminants; FILE_EVENTS retires to per-file zero sweeps; registry producers re-point to the verbs.
**Checkpoint**: The four cluster files contain zero raw-emission occurrences of any form (`cortex-lifecycle-event` typed/`log`, `cortex_command.lifecycle_event`, `log_event(`); `review→implement-rework` has a single emitter at every commit boundary; verb deployment + order tests green; `cortex-check-events-registry --audit` passes.

### Phase 4: Shared overnight reducer (tasks: 13, 14)
**Goal**: The overnight prompt reads criticality through the in-process shared reducer with documented fallbacks (single-agent + morning-report surface, never defer — per the project's failure-handling philosophy); the orphaned heading-citation pin retires.
**Checkpoint**: `grep -c '_read_criticality' orchestrator-round.md` = 0; `test_skill_section_citations.py` green with the 1a pin removed.

## Tasks

### Task 1: Extend the `just test` recipe to the lifecycle/backlog/`cortex_command` test trees
- **Files**: `justfile`
- **What**: Add pytest invocations for `cortex_command/lifecycle/tests/`, `cortex_command/tests/`, and `cortex_command/backlog/tests/` to the `test` recipe so their coverage is enforced. This is a blocker for every FILE_EVENTS/heading-pin retirement downstream.
- **Depends on**: none
- **Complexity**: simple
- **Context**: The `test` recipe is a bash recipe at `justfile:501-534` using a `run_test` wrapper (lines 520-528). It currently runs `test-pipeline`, `test-overnight`, `test-init` (which runs `pytest cortex_command/init/tests/`), `tests/test_install.sh`, `pytest tests/ -q` (repo-root `tests/` only), and the dashboard suite. None of the three target dirs are currently invoked (all three exist: lifecycle/tests=15 files, cortex_command/tests=6, backlog/tests=3). Add `run_test "..." .venv/bin/pytest cortex_command/lifecycle/tests/ -q` style lines in the recipe body around lines 524-528, matching the existing `run_test` idiom.
- **Verification**: (a) `just test` exits 0 and its output shows collections from all three dirs; `grep -c 'cortex_command/lifecycle/tests' justfile` ≥ 1 AND `grep -c 'cortex_command/backlog/tests' justfile` ≥ 1 AND `grep -c 'cortex_command/tests/' justfile` ≥ 1.
- **Status**: [x] done (a5e8a445 — `just test` overall exit-1 is a pre-existing network-sandbox failure in `test_mcp_subprocess_contract.py`, unrelated; new group + grep checks pass)

### Task 2: Environment-fixture guard tests (arm f)
- **Files**: `cortex_command/lifecycle/tests/test_prepare_worktree_fixtures.py` (new), and/or `tests/test_interactive_lock.py`
- **What**: Add tests that stage real on-disk fixtures — `interactive.pid` variants (live-owned-by-self, live-owned-by-other, stale-ESRCH, start-time-mismatch) and an overnight `runner.pid`/session-pointer pair — and drive `prepare_worktree()` / `acquire_lock()` without stubbing the composed primitives (seam-level patches only for `os.kill`/psutil liveness).
- **Depends on**: [1]
- **Complexity**: complex
- **Context**: Current seams: `tests/test_interactive_lock.py` is unit-level (in-memory lock dicts, monkeypatched `os.kill`/psutil, 8 liveness rows); `cortex_command/lifecycle/tests/test_prepare_worktree.py` stubs every composed primitive on the module namespace. No test drives `prepare_worktree()` end-to-end against a real `interactive.pid` or `runner.pid`/session-pointer fixture — that is the gap. Adversarial caution (research §Guard-verb fixtures): full end-to-end process fixtures against real `os.kill`/psutil risk pid-reuse races and sandbox flakiness — prefer seam-level monkeypatching of liveness plus a narrow set of on-disk `tmp_path` fixture tests. Use the `_scaffold`/`monkeypatch.chdir` pattern from `test_prepare_worktree.py`.
- **Verification**: (a) named tests exist in `cortex_command/lifecycle/tests/` and/or `tests/test_interactive_lock.py` and pass: `just test` exits 0 and shows the new test names.
- **Status**: [x] done (0218cc07 — 8 real-fixture tests in `test_prepare_worktree_fixtures.py`; one documented `ipc.ACTIVE_SESSION_PATH` path-redirect seam, no guard logic stubbed)

### Task 3: Pause markers at every current pause site (census re-audited)
- **Files**: `skills/lifecycle/SKILL.md`, `skills/refine/references/clarify.md`, `skills/refine/references/specify.md`, `skills/lifecycle/references/plan.md`, `skills/lifecycle/references/implement.md`, `skills/lifecycle/references/backlog-writeback.md`, `skills/lifecycle/references/complete-first-run.md`, `skills/lifecycle/references/concurrent-sessions.md`, `skills/lifecycle/references/complete.md`, `skills/refine/SKILL.md` (the last two pending the §census re-audit)
- **What**: **First, re-audit the census exhaustively** — grep both `AskUserQuestion` literals AND prose-"ask" phrasings (`ask the user`, `ask which`, `prompt the user`, `re-invoke`) across `skills/lifecycle` and `skills/refine`, and reconcile the result against the 10 currently inventoried sites. The seed set is the 10 inventoried + at least these un-inventoried prose-ask sites: `implement.md:76` (retry/skip/abort), `complete-first-run.md:11` (test-command ask), `concurrent-sessions.md:5` (resume pick), `complete.md:23` (orphan_ambiguous "ask which to use"), `backlog-writeback.md:9` (Exit-2 "ask the user to re-invoke" — note `backlog-writeback.md` hosts TWO distinct pauses: `:7` AskUserQuestion + `:9` prose-ask), and audit `specify.md:60` and `refine/SKILL.md:21`. Also confirm whether `SKILL.md:34` hosts one or two distinct AskUserQuestion affordances (the `empty`-lifecycle offer and the `ambiguous-backlog` prompt) and mark each. Then insert an HTML-comment marker with a stable semantic-slug ID and a kind token (e.g. `<!-- pause: plan-approval relayed-consent -->`) immediately above each confirmed site. This exhaustive re-audit is load-bearing: Task 5 retires the `AskUserQuestion` site-scan (`_askuserquestion_sites`), so the seed census becomes the permanent, otherwise-unguarded source of truth — an omission here is not caught later.
- **Depends on**: none
- **Complexity**: simple
- **Context**: HTML-comment markers are lint-safe — the existing sentinels (`skill-path-lint:ignore-next`, `bare-python-lint:ignore-next`, `size-budget-exception:`) are prefix-scoped consume-next-line patterns with no generic comment scanning; the events-registry checker matches only `"event": "..."` / `--event` literals; reference files are not size-budget-counted. The 10 inventoried sites are enumerated in `skills/lifecycle/references/kept-pauses.md:5-14` (SKILL.md:34, clarify.md:40, specify.md:23/48/135/137, plan.md:120, implement.md:21, backlog-writeback.md:7, complete-first-run.md:36). Note `complete-first-run.md:11` (test-command ask) is a DIFFERENT site than the already-inventoried `complete-first-run.md:36` (phase-exit). **Cited line numbers are indicative navigation aids only** — marker placement is semantic and Task 5's parity is line-agnostic; the `specify.md` anchors inherited from the inventory are already drifted (actual AskUserQuestion sites ≈ 133/135, not the inventory's 135/137), so locate each pause by its prose, not the cited line. Kinds map to the four-kind vocabulary decided in Task 4 (question / phase-exit-wait / config-conditional / relayed-consent); the two highest-stakes consent pauses (plan approval, spec approval) take `relayed-consent`. Semantic slugs (e.g. `plan-approval`, `implement-batch-failure`, `resume-feature-pick`) are the recommendation. Line numbers shift as markers are added; downstream Task 5's marker-anchored parity absorbs this (the old line-tolerance test tolerates ±35 until Task 5 retires it).
- **Verification**: (b) every confirmed census site carries a marker: `grep -rc '<!-- pause:' skills/lifecycle skills/refine | awk -F: '{s+=$2} END{print s}'` equals the re-audited census count (a landing-time completeness check only — MUST NOT persist as a count assertion; the durable invariant is Task 5's set-equality). The re-audit's prose-ask grep returns no site lacking a marker.
- **Status**: [x] done (d655d841 — 18-marker census; `SKILL.md:34` split into two sites; MUST-escalation halts excluded per original inventory scope — see Task 4/5 note below)
- **Census note (for Tasks 4/5)**: The 18-marker census excludes MUST-escalation halts (`review.md:75/101`, `orchestrator-review.md:32` — "await direction" / "gather input for rework") as a distinct category from kept question-pauses, consistent with the original inventory's scope. Task 4's data file mirrors the 18 markers; if a later decision folds escalation-halts into the pause taxonomy, add them as markers + data rows together.

### Task 4: Pause data file + generator console script
- **Files**: `skills/lifecycle/references/kept-pauses-data.toml` (new), `cortex_command/lifecycle/generate_kept_pauses.py` (new), `pyproject.toml` (`[project.scripts]`)
- **What**: Author a declarative pause data file carrying, per marker: **kind** (question / phase-exit-wait / config-conditional / relayed-consent), an optional **suppressed-by** attribute (a `lifecycle.config.md` key, or `judgment` for model-conditional rendering — orthogonal to kind), and a rationale. Add a generator (pure `generate_md(entries)`-shape function + `main()`, console script `cortex-generate-kept-pauses`) that renders `kept-pauses.md` with a "generated — don't hand-edit, re-run <generator>" header. Do NOT regenerate/commit `kept-pauses.md` and do NOT touch the requirements docs in this task — both are deferred to Task 5's atomic flip (the old line-anchored parity test still owns `kept-pauses.md` until then, and the doc rewrites name the rewritten parity test + generated output, which don't exist until Task 5; superseding the docs here would leave them describing enforcement that doesn't yet exist).
- **Depends on**: [3]
- **Complexity**: complex
- **Context**: Generator shape precedent: `cortex_command/backlog/generate_index.py` — `generate_md()` (pure function over parsed data) at :220, `main()` at :321. TOML is recommended for the data file (stdlib `tomllib`, diffable, rides the `skills/` rsync mirror since `cortex/` is not mirrored by `just build-plugin`). Kind vocabulary rationale: `complete-first-run.md:11` is a question suppressed by the `test-command` config key; `specify.md:135` is a question rendered on model judgment — so suppressed-by is orthogonal to kind. Add the `[project.scripts]` row `cortex-generate-kept-pauses = "cortex_command.lifecycle.generate_kept_pauses:main"`. The generator commits without being run against the committed `kept-pauses.md` (no freshness gate exists until Task 5), so this task's commit is internally consistent — the data file and generator exist but nothing consumes them yet.
- **Verification**: (a) `python3 -m cortex_command.lifecycle.generate_kept_pauses` (or `CORTEX_COMMAND_FORCE_SOURCE=1 cortex-generate-kept-pauses`) run twice is idempotent (identical stdout/output both runs); (b) `grep -c 'cortex-generate-kept-pauses' pyproject.toml` ≥ 1 and the data file parses (`python3 -c "import tomllib; tomllib.load(open('skills/lifecycle/references/kept-pauses-data.toml','rb'))"` exits 0).
- **Status**: [x] done (7cfdea9c — 18 `[[pause]]` rows set-equal to the markers; generator idempotent; added `just kept-pauses` + a W003 allowlist row for Task 5 to prune)

### Task 5: Atomic flip — regenerate kept-pauses.md, rewrite the parity test (set-equality + freshness + semantic sub-checks incl. consent-proximity), supersede the docs, wire into CI
- **Files**: `tests/test_lifecycle_kept_pauses_parity.py`, `skills/lifecycle/references/kept-pauses.md` (regenerated output — run `cortex-generate-kept-pauses --write` or `just kept-pauses`), `.github/workflows/validate.yml`, `cortex/requirements/project.md`, `docs/policies.md`, `bin/.parity-exceptions.md` (prune the `cortex-generate-kept-pauses` W003 allowlist row — see note)
- **W003→W005 handoff (from Task 4)**: Task 4 added a `bin/.parity-exceptions.md` allowlist row for `cortex-generate-kept-pauses` (the console-script had no in-scope wiring reference yet). Once THIS task references the command from a `tests/`-scoped file (the freshness gate calls `generate_md`/the generator) or `kept-pauses.md`, that allowlist row becomes W005 (allowlist-superfluous) and MUST be pruned in the same commit. The `just kept-pauses` recipe Task 4 added stays.
- **What**: In ONE atomic commit: (1) regenerate `kept-pauses.md` from the Task-4 data file; (2) rewrite the parity test to assert — (a) exact set-equality between prose marker IDs and data-file IDs (orphans and missing both fail); (b) a regenerate-and-diff freshness check of the committed `kept-pauses.md`; (c) marker-anchored semantic sub-checks for **every** kind — `phase-exit-wait` entries validate against a step/section heading near the marker, config-`suppressed-by` entries validate the suppression wiring token near the marker, **AND `question`/`relayed-consent` entries validate that an `AskUserQuestion` literal (or, for a relayed-consent prose site, the approval-surface / verb-call token) sits within a small window of the marker** — this restores the retired line-anchored test's invariant #1 (every consent pause points to a real interaction site) for the two highest-stakes kinds, which set-equality alone does not cover; (3) retire `LINE_TOLERANCE`/the line-anchor scheme; (4) supersede the requirements docs — rewrite `cortex/requirements/project.md:27` from "two kinds" to the kind + suppressed-by model naming the data file + generator + this rewritten parity test as the enforcement pair, and re-target `docs/policies.md:9`'s "update both together" instruction to the data file (deferred here from Task 4 so the docs never describe enforcement before it exists); (5) add this test (or a wrapper) to CI in `validate.yml`.
- **Depends on**: [3, 4]
- **Complexity**: complex
- **Context**: Current scheme (all retiring): `_INVENTORY_BULLET` regex (`:32-35`), literal-`AskUserQuestion` site scan `_askuserquestion_sites()` (`:90-111`), `LINE_TOLERANCE = 35` (`:28`), `_CONDITIONAL_PAUSE_TAG = "conditional pause"` (`:42`) + `_CONDITIONAL_PAUSE_MARKER` (`:43`), `_PHASE_EXIT_PAUSE_TAG` (`:41`) + `_STEP_HEADING` (`:39`). It reads `KEPT_PAUSES_MD` (`:27`). New markers are parsed from prose via a `<!-- pause: <id> <kind> -->` regex over `skills/lifecycle` + `skills/refine` `*.md`. The consent-proximity check (c) reuses the retired test's `_askuserquestion_sites()` scan logic but anchors on the marker's line rather than an inventory bullet — do not delete that scan wholesale, repurpose it. `project.md:27` currently reads "**Kept user pauses come in two kinds**: (a) AskUserQuestion-site pauses; (b) phase-exit pauses". CI target: `.github/workflows/validate.yml` has one blocking pytest step (`pytest tests/test_check_contract.py`, `:38-39`) and a dashboard smoke test (`:54`); add a step invoking `pytest tests/test_lifecycle_kept_pauses_parity.py` right after `:38-39`. No new pre-commit phase (the dual-source gate already blocks every commit on plugin-tree cleanliness; freshness rides pytest + CI). Committing docs + regenerated output + rewritten test together keeps the repo internally consistent at every commit boundary.
- **Verification**: (a) `just test` runs the rewritten parity test green; negative-control assertions fail on a marker without a data row, a data row without a marker, a stale committed doc, a config-suppressed entry whose wiring prose was deleted, **and a `relayed-consent` marker with no nearby AskUserQuestion/approval-surface token**; `grep -c 'test_lifecycle_kept_pauses_parity' .github/workflows/validate.yml` ≥ 1; (b) `grep -c 'two kinds' cortex/requirements/project.md` = 0 and the replacement text contains `kept-pauses-data`.
- **Status**: [x] done (81732ffd — atomic flip: 18-row regen, set-equality + freshness + per-kind semantic anchors (consent-proximity `_CONSENT_TOKENS`), docs superseded, CI wired, W003 row pruned; 10 tests incl. 6 negative controls)

### Task 6: Additive event schema — typed `lifecycle-cancelled` subcommand + `spec_approved` consent field
- **Files**: `cortex_command/lifecycle_event.py`, `bin/.events-registry.md`, `tests/test_lifecycle_event_roundtrip.py`
- **What**: Add a typed `lifecycle-cancelled` subcommand to `_EVENT_SUBCOMMANDS` (makes the event typed and pinnable — today it is emitted only via the generic `log` escape hatch), and add an optional enum field **`decision`** (enum value `approved` this wave) to the `spec-approved` subcommand's field specs. Register both in `bin/.events-registry.md`: a typed row for `lifecycle_cancelled`, and a field-additive note on `spec_approved`'s row (the field-additive section pattern at registry `:147-178`).
- **Depends on**: [1]
- **Complexity**: simple
- **Context**: `_EVENT_SUBCOMMANDS` is a registry-driven dict at `lifecycle_event.py:246-294`, value shape `(event_name, [FieldSpec...])` where `FieldSpec = (flag, emit_key, kind, required, choices)`; parser build (`:336-352`) and dispatch (`:386-406`) are registry-driven, so a new key needs no other code change. Subcommand-name convention is event name with `_`→`-` (`:234`). Existing keys include `spec-approved`, `plan-approved`, `feature-paused`, `phase-transition`, `review-verdict`, `batch-dispatch`, `drift-protocol-breach`. Consent-field precedent: `plan_approved`'s optional `dispatch_choice` enum (registry `:23`); keep consent enum/identifier-only (events.log has no redaction path). The single-value `approved` enum is intentional — it makes `spec_approved` self-describing as a durable consent record (problem statement's "durable consent for the two highest-stakes pauses"), enum-typed so future values are additive. Registry column order (`:5`): `event_name | target | scan_coverage | producers | consumers | category | added_date | deprecation_date | rationale | owner`.
- **Verification**: (a) `python3 -m cortex_command.lifecycle_event lifecycle-cancelled --feature x` writes a `{"event":"lifecycle_cancelled",...}` row; `python3 -m cortex_command.lifecycle_event spec-approved --feature x --decision approved` writes a row carrying `"decision":"approved"`; (a) `just test` runs `test_lifecycle_event_roundtrip.py` green.
- **Status**: [x] done (b2fad8c9 — `lifecycle-cancelled` typed subcommand (empty FieldSpec, matches log-form) + `spec-approved --decision` enum{approved}; registry upgraded; 38+4 roundtrip tests green)

### Task 7a: Plan-decision verb (`cortex-lifecycle-plan-decision`)
- **Files**: `cortex_command/lifecycle/plan_decision.py` (new), `cortex_command/lifecycle/tests/test_plan_decision.py` (new)
- **What**: A verb returning discriminants `{branch-mode-approved, wait-approved, cancelled, revise}`. Ordered emissions per arm: branch-mode → `plan_approved{dispatch_choice}` then `phase_transition plan→implement`; wait → `plan_approved{dispatch_choice: wait}` then `feature_paused`, NO transition; cancelled → `lifecycle_cancelled`; revise → no emission. Ship the module + write-side order test. **The `bin/` wrapper, `[project.scripts]` row, and deployment-test row all land together in Task 8** — NOT here — because a committed `bin/cortex-*` wrapper is "deployed" per the parity checker (`gather_deployed` = bin exec files ∪ project.scripts keys) and would trip the W003 pre-commit gate ("deployed but not referenced") until Task 8's deployment-test row supplies the wiring signal. A bare module under `cortex_command/` is NOT "deployed", so this task commits cleanly.
- **Depends on**: [6]
- **Complexity**: complex
- **Context**: Verb template = `cortex_command/lifecycle/finalize.py` — `KNOWN_STATES` tuple (`:87`); pure composable function + `main(argv)` (`:238-252`) that never tracebacks (`_telemetry.log_invocation` first, JSON envelope via `json.dumps(..., separators=(",",":"))`, exit 0); `# (a)/(b)/(c)` ordered-side-effect comments (canonical in `wontfix_cli.py:196-198`), each re-asserting its postcondition via a presence-check helper (`_feature_complete_exists` pattern, `finalize.py:96-120` — parsed `event`-field match, never substring); short-circuit arms (`revise`, `cancelled`) return before the first mutating step. Emit via `log_event(event=..., feature=..., fields=[(kind,key,value)])` (`lifecycle_event.py:142-173`) — never hand-rolled `json.dumps` (scan_lifecycle substring-matches exact spacing). Add a slug-shape guard rejecting path separators/`..` in `--feature` before any filesystem access. Caller-passed args only (ADR-0019); document CWD-only root resolution (`_resolve_user_project_root_from_cwd`, `common.py:123`), matching `lifecycle_event`/`finalize`. Test template = `cortex_command/lifecycle/tests/test_finalize.py`: `_scaffold` (delenv `CORTEX_REPO_ROOT`, create lifecycle dir, `monkeypatch.chdir`), `_events_rows` parsed-JSON assertions, per-arm ordered-emission sequences from parsed rows, a **no-duplicate assertion**, a double-invocation idempotency test, a substring-false-positive negative control, `KNOWN_STATES` exhaustiveness, never-crash CLI test (drives `main(argv)` in-process — no bin wrapper needed).
- **Verification**: (a) `just test` runs `test_plan_decision.py` green (asserting per-arm emission sequences incl. the no-duplicate and double-invocation idempotency tests); `CORTEX_COMMAND_FORCE_SOURCE=1 python3 -m cortex_command.lifecycle.plan_decision --feature x --decision revise` exits 0 with a JSON envelope and no events.log write.
- **Status**: [x] done (5b8b3729 — 23 tests; `phase_transition` guard matches event+from/to)

### Task 7b: Review-verdict verb (`cortex-lifecycle-review-verdict`)
- **Files**: `cortex_command/lifecycle/review_verdict.py` (new), `cortex_command/lifecycle/tests/test_review_verdict.py` (new) — bin wrapper + wiring deferred to Task 8 (W003, see Task 7a)
- **What**: A verb owning the verdict×cycle routing: `APPROVED`→complete; `CHANGES_REQUESTED` cycle 1→implement-rework; cycle ≥2 or `REJECTED`→escalated. Ordered emissions: `review_verdict` → optional `drift_protocol_breach` (post-hoc `--breach --retries` caller args) → routed `phase_transition`. It is the **single owner** of `phase_transition review→implement-rework` and presence-checks the transition before emitting.
- **Depends on**: [6]
- **Complexity**: complex
- **Context**: Same verb-body/test conventions as Task 7a (finalize/wontfix template). The §4a drift-apply judgment loop stays PROSE and completes before the verb is called; the verb owns only the truncated `review_verdict → (breach) → transition` tail (research §Skill-Prose: "the prose calls the verb after the drift loop with post-hoc breach/retry args"). Current prose emissions being absorbed: `review.md:80-82` (`review_verdict`), `review.md:91` (`drift_protocol_breach`), `review.md:99/100/101` (the three `phase_transition` arms). The single-ownership of `review→implement-rework` is established by the ordering of the prose tasks: implement.md's duplicate §3 emission is removed FIRST (Task 10), then review.md §5 is routed to this verb (Task 11) — so this verb becomes the sole emitter with no intervening commit in which two callsites both emit. The verb still presence-checks the transition. Order test covers all three routes; assert re-invocation after the rework transition emits no duplicate row.
- **Verification**: (a) `just test` runs `test_review_verdict.py` green (all three routes + a no-duplicate re-invocation test); `CORTEX_COMMAND_FORCE_SOURCE=1 cortex-lifecycle-review-verdict --feature x --verdict APPROVED --cycle 1` exits 0 with a JSON envelope.
- **Status**: [x] done (2fe8f87a — 23 tests; single owner of review→implement-rework; review_verdict guard on event+cycle; documented cross-cycle-breach residual)

### Task 7c: Spec-approve verb (`cortex-lifecycle-spec-approve`)
- **Files**: `cortex_command/lifecycle/spec_approve.py` (new), `cortex_command/lifecycle/tests/test_spec_approve.py` (new) — bin wrapper + wiring deferred to Task 8 (W003, see Task 7a)
- **What**: A verb returning `{approved, cancelled, revise}`. On `approved`: emit `spec_approved` (with the optional `decision` consent field); then — when the caller passes `--emit-transition` — `phase_transition specify→plan`; then the backend-gated write-back (`--backend`, `--backlog-file`, `--spec-path`, `--areas`) via in-process `update_item`, with `--areas` preserve-on-omit (clearing requires an explicit empty flag). On `cancelled`: emit `lifecycle_cancelled` (typed) and nothing else. On `revise`: no emission. Each emission presence-checks independently (in-session retry + manual mid-verb-crash recovery are safe). Record the ADR-0019 "taken knowingly" scope-crossing in the docstring plus a one-line amendment note on ADR-0019's precedent list.
- **Depends on**: [6]
- **Complexity**: complex
- **Context**: Backend-gated write-back precedent = `finalize.py:123-160` (`_apply_backlog_writeback`: `none`→skip, external→best-effort, `cortex-backlog`→`resolve` + in-process `update_item`, ambiguous→`_Exit2`→exit 2). Reuse the `_Exit2` carve-out (`finalize.py:92-93,247-248`). `--areas` preserve-on-omit: omission is the common case, not a clear. Context→flag mapping: lifecycle-wrapped refine passes `--emit-transition`; standalone refine passes `--no-emit-transition` (preserving today's standalone behavior of emitting NO specify→plan row) — both flag states have production callers, so the order test covers both. Same verb-body/test conventions as 7a. Order tests cover: all three discriminants × both flag states × backend arms (cortex-backlog / none / external / not-found), the `_Exit2` ambiguous-slug carve-out, `--areas` preserve-on-omit, and a mid-sequence-crash repair test (delete the transition row, re-run, assert single rows).
- **Verification**: (a) `just test` runs `test_spec_approve.py` green (all listed matrix cells incl. the crash-repair test); `CORTEX_COMMAND_FORCE_SOURCE=1 cortex-lifecycle-spec-approve --feature x --decision revise --backend none --backlog-file "" --spec-path p --no-emit-transition` exits 0 with a JSON envelope and no write.
- **Status**: [x] done (190db58c — 33 tests; backend matrix × both flag states, _Exit2, areas preserve/clear, crash-repair; ADR-0019 amended)

### Task 7d: Implement-transition verb (`cortex-lifecycle-implement-transition`)
- **Files**: `cortex_command/lifecycle/implement_transition.py` (new), `cortex_command/lifecycle/tests/test_implement_transition.py` (new) — bin wrapper + wiring deferred to Task 8 (W003, see Task 7a)
- **What**: A verb covering the implement cluster's mechanical emissions: `batch_dispatch{batch,tasks}`, and the §4 exit transition that reads criticality/tier via `common.reduce_lifecycle_state` (never raw events.log parsing) and emits `phase_transition implement→{review|complete} --tier`. The routing rule ("review when criticality ∈ {high,critical} OR tier = complex") lives in the function body; the corrupted-state arm follows the criticality-matrix rule (route to the review-requiring arm).
- **Depends on**: [6]
- **Complexity**: complex
- **Context**: `reduce_lifecycle_state(events_path: Path)` (`common.py:762-816`) returns a `LifecycleStateReduction` NamedTuple: `.state: dict[str,str]` (keys `"criticality"`,`"tier"`), `.skipped_lines`, and a computed `.corrupted` property (`:664-678`) — there is NO top-level `criticality`/`tier`/`corrupted` dict key; read `.state.get("criticality")` / `.state.get("tier")` / `.corrupted`. Same verb-body/test conventions as 7a. Order test drives both routes (review vs complete) plus the corrupted-state arm (this interactive verb routes corrupted → the review-requiring arm per criticality-matrix.md:26; the overnight path deliberately differs — see Task 13). The prose/code drift on the routing rule is closed in Task 10 (criticality-matrix.md rewritten to defer to the verb).
- **Verification**: (a) `just test` runs `test_implement_transition.py` green (both routes + corrupted-state arm); `CORTEX_COMMAND_FORCE_SOURCE=1 cortex-lifecycle-implement-transition --feature x --batch 1 --tasks '[1,2]'` exits 0 with a JSON envelope.
- **Status**: [x] done (640c2624 — 27 tests; routing table criticality×tier; corrupted→review/complex; batch_dispatch guard on event+batch; `--mode` inferred)

### Task 8: Verb wiring — four `bin/` wrappers + `[project.scripts]` rows + deployment-test rows
- **Files**: `bin/cortex-lifecycle-plan-decision` (new), `bin/cortex-lifecycle-review-verdict` (new), `bin/cortex-lifecycle-spec-approve` (new), `bin/cortex-lifecycle-implement-transition` (new), `pyproject.toml` (`[project.scripts]`), `tests/test_lifecycle_verb_deployment.py`
- **What**: Create the four dual-channel `bin/` wrappers, add the four `[project.scripts]` rows (`cortex-lifecycle-plan-decision`, `-review-verdict`, `-spec-approve`, `-implement-transition` → their module `:main`), and append the four `(console_script, entry_point, bin_rel)` tuples to the `VERBS` list in the deployment test — **all in ONE commit** so every deployed script (bin wrapper + console script) gains its wiring signal (the deployment-test `bin_rel`/console-script literals) in the same commit, satisfying Requirement 7 (W003 wiring, no allowlist rows). Each bin wrapper: copy `bin/cortex-lifecycle-finalize` verbatim, swapping the module path to the verb's module (4-branch: FORCE_SOURCE / wheel-probe / working-tree / exit-2); `chmod +x`. The wrappers auto-mirror into `plugins/cortex-core/bin/` via the pre-commit hook.
- **Depends on**: [7a, 7b, 7c, 7d]
- **Complexity**: simple
- **Context**: `bin/cortex-lifecycle-finalize` is the wrapper template. `[project.scripts]` lifecycle block is at `pyproject.toml:53-72`. Deployment test `VERBS` list at `tests/test_lifecycle_verb_deployment.py:31-47`; its three parametrized assertions check the `[project.scripts]` row, the executable `bin/` wrapper, and that the module string appears in the wrapper. Its docstring warns: do NOT add a `VERBS` row before its pyproject row AND `bin/` wrapper both exist (else E002) — this task creates all three together so the invariant holds within the commit. `bin_rel` must be a contiguous `"bin/cortex-..."` literal. Parity note (`gather_deployed` = bin exec files ∪ project.scripts keys): committing the wrappers + pyproject rows + the referencing deployment-test rows together means no script is ever "deployed but not referenced" at a commit boundary — no allowlist row needed.
- **Verification**: (a) `just test` runs `test_lifecycle_verb_deployment.py` green for all four new rows; `just check-parity` (or `bin/cortex-check-parity --staged`) passes with no W003/E002 for the four verbs; `python3 -c "import tomllib; d=tomllib.load(open('pyproject.toml','rb'))['project']['scripts']; print(all(k in d for k in ['cortex-lifecycle-plan-decision','cortex-lifecycle-review-verdict','cortex-lifecycle-spec-approve','cortex-lifecycle-implement-transition']))"` prints `True`; all four `bin/cortex-lifecycle-*` wrappers are executable.
- **Status**: [x] done (ee458197 — 4 bin wrappers + 4 project.scripts + 4 VERBS rows, one commit; 21 deployment tests; parity clean, no allowlist)

### Task 9: Plan cluster — plan.md prose rewrite + FILE_EVENTS zero-sweep + registry re-point
- **Files**: `skills/lifecycle/references/plan.md`, `tests/test_lifecycle_event_roundtrip.py`, `bin/.events-registry.md`
- **What**: Rewrite plan.md §4/§5 to route on the plan-decision verb's JSON discriminant (complete.md Step-7 pattern: "act on the verdict; do not re-derive it"); judgment content (option assembly, the `AskUserQuestion` approval surface) stays prose; keep the warning that overnight eligibility does not honor `feature_paused`; add a "command not found → halt and instruct install; do NOT emit events by hand" arm **that names the VERB (`cortex-lifecycle-plan-decision`), never the event logger** (see the halt-arm convention below). Retire plan.md's FILE_EVENTS entry to a **per-file zero-sweep** asserting zero raw-emission occurrences of ANY form. Re-point the registry producer columns for the plan-cluster events to the verb and flip their `scan_coverage` from `gate-enforced` to `manual`. Establish the shared zero-sweep helper (reused by Tasks 10-12) plus negative-control tests asserting the sweep catches each covered form.
- **Depends on**: [8]
- **Complexity**: complex
- **Context**: Current emissions: `plan.md:122` (`plan_approved` branch-mode), `:125-126` (`plan_approved` wait + `feature_paused`), `:128` (`lifecycle_cancelled`), `:132` (`phase_transition plan→implement`). FILE_EVENTS is `dict[str, dict[str,int]]` at `test_lifecycle_event_roundtrip.py:123-154`; the plan.md row (`:124-128`) is `{plan_approved:2, feature_paused:1, phase_transition:1}`. The existing parser `_SUBCMD_RE` (`:78`) has a `(?!log\b)` lookahead that skips `log`-form — the zero-sweep must be a NEW check that counts ALL raw-emission forms and asserts zero, because a per-pair zero row would be blind to `log`-form reintroductions. **Sweep breadth (load-bearing):** match all three raw-emission surfaces the prose could regress into — `cortex-lifecycle-event` (typed + `log`), the module-invocation form `cortex_command.lifecycle_event` (which L201/`cortex-check-bare-python-import` does NOT catch — it flags `import` statements, not `python3 -m module` invocations), and a bare `log_event(` call. The L201 lint covers the `import cortex_command.lifecycle_event` path; the sweep + L201 together close all raw-emission forms in skill prose. **Halt-arm convention (resolves the sweep/prose tension):** the command-not-found halt arm names the cluster VERB and the install remedy; it MUST NOT contain `cortex-lifecycle-event`, `cortex_command.lifecycle_event`, or `log_event(` — otherwise it self-trips the sweep. State this convention inline where the arm is authored. Registry rows: `plan_approved` (`:23`), `feature_paused` (`:31`), `phase_transition` (`:11`), `lifecycle_cancelled` (`:24`). `scan_coverage` flips because the commit-time gate scans only skill prose/overnight prompts, not Python emission sites; the rationale column notes the replacement enforcement (write-side order tests + this sweep). No new MUST/CRITICAL language absent an evidence artifact (docs/policies.md).
- **Verification**: (a) `just test` runs `test_lifecycle_event_roundtrip.py` green with the plan.md zero-sweep + its three negative controls (one per covered form); `grep -cE 'cortex-lifecycle-event|cortex_command\.lifecycle_event|log_event\(' skills/lifecycle/references/plan.md` = 0; (a) `bin/cortex-check-events-registry --audit` passes.
- **Status**: [x] done (cafa7d85 — reusable `count_raw_emissions`/`ZERO_SWEEP_FILES` helper + 3 negative controls; plan.md routes on the verb; plan_approved/feature_paused→manual, phase_transition/lifecycle_cancelled kept gate-enforced (still multi-producer). CAVEAT: `cortex-check-events-registry --audit` has 12 PRE-EXISTING STALE_DEPRECATION rows unrelated to this feature — separate cleanup)

### Task 10: Implement cluster — implement.md rewrite (incl. §3 duplicate removal, ordered BEFORE the review cluster) + criticality-matrix defer + zero-sweep + registry re-point
- **Files**: `skills/lifecycle/references/implement.md`, `skills/lifecycle/references/criticality-matrix.md`, `tests/test_lifecycle_event_roundtrip.py`, `bin/.events-registry.md`
- **What**: Rewrite implement.md §2b (`batch_dispatch`) and §4 (exit transition) to route on the implement-transition verb; **remove implement.md §3's duplicate `phase_transition review→implement-rework` emission** (the review-verdict verb from Task 7b is the single owner — Requirement 9); add the command-not-found halt arm (names the verb, never the logger — see Task 9's halt-arm convention). Rewrite the criticality-matrix.md routing-rule line to DEFER to the verb (no prose/code drift pair). Retire implement.md's FILE_EVENTS entry to a per-file zero-sweep. Re-point registry producer columns for `batch_dispatch` and the implement `phase_transition` arms to the verb and flip `scan_coverage` to `manual`. **Ordering (load-bearing — closes the double-emission window):** this implement-cluster task runs BEFORE the review-cluster task (Task 11). Today `review→implement-rework` is emitted by BOTH implement.md §3 AND review.md §5 (a pre-existing duplicate). Removing implement.md §3 here leaves review.md §5 as the SOLE emitter; Task 11 then routes review.md §5 to the verb (still a single emitter). So at every commit boundary exactly one callsite emits the transition — the inverse ordering (review-first) would leave implement.md §3 live alongside the verb for one commit and double-emit.
- **Depends on**: [9]
- **Complexity**: complex
- **Context**: Current emissions: `implement.md:54` (`batch_dispatch`), `:102` (§3 duplicate `phase_transition review→implement-rework`), `:114` (criticality read), `:119-121` (§4 `phase_transition implement→{review|complete} --tier`). FILE_EVENTS implement.md row (`:134-137`) is `{batch_dispatch:1, phase_transition:2}` — both retire to the zero-sweep after §3 removal + §4 verb-routing. review.md is UNTOUCHED here (its `{review_verdict:1, drift_protocol_breach:1, phase_transition:3}` row and its §5 rework emission stay live until Task 11). criticality-matrix.md keeps its own residual FILE_EVENTS row (`criticality_override:1`, `:145-147`) untouched — only its prose routing-rule line changes. Registry: `batch_dispatch` (`:14`), `phase_transition` (`:11`). Reuse the zero-sweep helper from Task 9. Serialized after Task 9 for the shared test/registry files. The implement→review boundary "is not a kept pause" — preserve that.
- **Verification**: (a) `just test` green with the implement.md zero-sweep; `grep -cE 'cortex-lifecycle-event|cortex_command\.lifecycle_event|log_event\(' skills/lifecycle/references/implement.md` = 0; (a) `bin/cortex-check-events-registry --audit` passes.
- **Status**: [ ] pending

### Task 11: Review cluster — review.md prose rewrite + FILE_EVENTS zero-sweep + registry re-point
- **Files**: `skills/lifecycle/references/review.md`, `tests/test_lifecycle_event_roundtrip.py`, `bin/.events-registry.md`
- **What**: Rewrite review.md §4/§4a/§5 to route on the review-verdict verb's discriminant; the drift auto-apply judgment loop stays prose and calls the verb afterward with post-hoc `--breach/--retries` args; add the command-not-found halt arm (names the verb, never the logger). Retire review.md's FILE_EVENTS entry to a per-file zero-sweep. Re-point the registry producer columns for `review_verdict`, `drift_protocol_breach`, and the review `phase_transition` arms to the verb and flip `scan_coverage` to `manual` (also correcting the stale `drift_protocol_breach` producer pin `review.md:166` → the verb). By this task, implement.md §3's duplicate rework emission is already gone (Task 10), so routing review.md §5 to the verb makes the review-verdict verb the clean single owner of `review→implement-rework` with no intervening double-emission.
- **Depends on**: [10]
- **Complexity**: complex
- **Context**: Current emissions: `review.md:80-82` (`review_verdict`), `:91` (`drift_protocol_breach`), `:99/100/101` (`phase_transition` complete/implement-rework/escalated). FILE_EVENTS review.md row (`:129-133`) is `{review_verdict:1, drift_protocol_breach:1, phase_transition:3}`. Registry: `review_verdict` (`:15`), `drift_protocol_breach` (`:116`, producer pin `review.md:166` is stale — actual emission is `review.md:91`), `phase_transition` (`:11`). Reuse the zero-sweep helper from Task 9. Serialized after Task 10 to avoid racing on the shared `test_lifecycle_event_roundtrip.py` and `bin/.events-registry.md` files.
- **Verification**: (a) `just test` green with the review.md zero-sweep; `grep -cE 'cortex-lifecycle-event|cortex_command\.lifecycle_event|log_event\(' skills/lifecycle/references/review.md` = 0; (a) `bin/cortex-check-events-registry --audit` passes; `cortex-check-skill-path` passes.
- **Status**: [ ] pending

### Task 12: Spec cluster — specify.md + refine SKILL.md §5 + refine-delegation.md rewrite + zero-sweep + registry re-point
- **Files**: `skills/refine/references/specify.md`, `skills/refine/SKILL.md`, `skills/lifecycle/references/refine-delegation.md`, `tests/test_lifecycle_event_roundtrip.py`, `bin/.events-registry.md`
- **What**: Rewrite specify.md §4/§5 and refine `SKILL.md` §5's write-back block to route on the spec-approve verb (the verb now owns `spec_approved`, the optional specify→plan transition, and the backend-gated write-back). Re-anchor refine-delegation.md: **edit the item-3 template line to drop `specify→plan` from its boundary enumeration** (so under lifecycle-wrapped refine only the verb — passed `--emit-transition` — emits that row, not the template AND the verb), while KEEPING the single template invocation for the clarify→research and research→specify boundaries; re-point its halt-before-Plan gate anchor to the verb call. Add command-not-found halt arms (name the verb, never the logger). Retire specify.md's FILE_EVENTS entry to a per-file zero-sweep. Re-point registry producer columns for `spec_approved`, the specify `phase_transition`, and `lifecycle_cancelled` to the verb and flip `scan_coverage` to `manual`.
- **Depends on**: [11]
- **Complexity**: complex
- **Context**: Current emissions: `specify.md:143` (`spec_approved`), `:145` (`lifecycle_cancelled`), `:149-151` (`phase_transition specify→plan`), `:153` (standalone carve-out); refine `SKILL.md:94-95` (`cortex-update-item --status refined --spec` / `--areas`, `:98` empty-areas clear); refine-delegation.md item 3 `:7` (ONE `cortex-lifecycle-event phase-transition` template line whose parenthetical enumerates all three boundaries `(clarify→research, research→specify, specify→plan)`) and halt-gate `:55`. FILE_EVENTS specify.md row (`:141-144`) is `{spec_approved:1, phase_transition:1}` → zero-sweep. **refine-delegation.md's FILE_EVENTS row stays `phase_transition:1`** — the single template invocation LINE remains (now enumerating only two boundaries), so the count is unchanged. **But that count is boundary-blind** — the roundtrip parser counts the line by literal occurrence and cannot see which boundaries it lists, so the count-1 pin is necessary-but-NOT-sufficient: it would pass identically whether `specify→plan` is dropped or left in, and leaving it in while the verb also emits under `--emit-transition` double-emits the specify→plan row invisibly. The task therefore adds a SEPARATE assertion that `specify→plan` no longer appears in that template's boundary enumeration (a targeted grep/parity check, not the FILE_EVENTS count). Registry: `spec_approved` (`:22`), `phase_transition` (`:11`), `lifecycle_cancelled` (`:24`). Consent enrichment: the verb passes `--decision approved` on the approve arm. Serialized after Task 11 for the shared test/registry files.
- **Verification**: (a) `just test` green with the specify.md zero-sweep; `grep -cE 'cortex-lifecycle-event|cortex_command\.lifecycle_event|log_event\(' skills/refine/references/specify.md` = 0; refine-delegation.md still contains exactly 1 `cortex-lifecycle-event phase-transition` template line AND that line's boundary enumeration no longer contains `specify` (the specify→plan boundary is verb-owned): a targeted check asserts the template's boundary list is `{clarify→research, research→specify}` only; (a) `bin/cortex-check-events-registry --audit`, `cortex-check-skill-path`, `cortex-check-bare-python-import` all pass.
- **Status**: [ ] pending

### Task 13: Overnight prompt reads criticality via the in-process shared reducer
- **Files**: `cortex_command/overnight/prompts/orchestrator-round.md`, `bin/.events-registry.md`
- **What**: Replace orchestrator-round.md's inline `_read_criticality` block (~lines 238-260) with an in-process import of `cortex_command.common.reduce_lifecycle_state` (the same reducer `cortex-lifecycle-state` wraps), matching the prompt's existing in-wheel-import execution model. Preserve the `gate_enabled` short-circuit (the read still executes only when `synthesizer_overnight_enabled`). Fallback semantics: (1) criticality truly unknowable (corrupted with no criticality, or reducer exception) → route the feature to the single-agent path (identical to today's default) and surface a morning-report warning line — never defer; (2) reduction reports corrupted BUT criticality present → use the present value AND **still surface the morning-report warning** (because `.corrupted` is tier-OR-criticality symmetric, a present criticality can be stale — the torn/vocab-rejected line may itself be the latest `criticality_override`, which is rejected per-value and never enters `state`, leaving the pre-override value; so the warning fires on ANY corrupted read, not only the unknowable arm). **Document the single-agent choice explicitly** (a comment in the prompt block): criticality-matrix.md:26's "run the critical-review / orchestrator-review gate" is the INTERACTIVE-context rule (mirrored by Task 7d's interactive verb); the overnight analog of "reviewed" is the morning-report surface (the warning line), and "never defer" is the overriding constraint per the project's failure-handling philosophy ("Surface failures in the morning report; keep working unless blocked"). Routing corrupted→critical_subset was considered and rejected: critical_subset features can be deferred (the synthesizer marks unselected variants `deferred` → `write_deferral`), which would reintroduce exactly the deferral this fallback forbids. This is also an intentional behavior fix: the inline block read `entry.get("criticality")` for `criticality_override` events, but overrides carry `from`/`to` only, so overnight has NEVER applied overrides.
- **Depends on**: [12]
- **Complexity**: complex
- **Context**: `synthesizer_overnight_enabled` short-circuit is Step 3b.1 prose (`orchestrator-round.md:224`); `gate_enabled = read_synthesizer_gate(...)` at `:236`; the `_read_criticality` block at `:238-260` (opens `events.log` at `:244`, defaults `"medium"` at `:246`, the `lifecycle_start`/`criticality_override` guard at `:256` — the `orchestrator-round.md:256` citation); partition loop at `:262-271` (critical → `critical_subset` gets the synthesizer dual-plan path; everything else → `single_agent_subset`, "no change to non-critical-tier behavior"). `reduce_lifecycle_state` returns a `LifecycleStateReduction` NamedTuple — read `.state.get("criticality")` and `.corrupted` (tier-OR-criticality symmetric; tier is not consumed here — criticality only, no verb-contract change). Deviation from the ticket's literal "state verb" wording is deliberate: the outcome is one reducer serving both modes, which the import satisfies; the CLI form remains the contract for skill prose (which cannot import Python, per L201). **Behavior-change framing (not "latent"):** the override fix is dormant only while `synthesizer_overnight_enabled` is off; the instant an operator flips that gate on — the feature's intended activation — newly-honored overrides change `critical_subset` membership in BOTH directions, and a downgrade override (e.g. critical→medium) now silently drops a feature OUT of the review-inclusive `critical_subset` into single-agent. Record this in the plan's Changes-to-Existing-Behavior expectation and the registry rationale as a bidirectional routing change gated on the switch this feature exists to let operators flip — not a dormant dead-code cleanup.
- **Verification**: (a) `grep -c '_read_criticality' cortex_command/overnight/prompts/orchestrator-round.md` = 0 and the replacement block imports `reduce_lifecycle_state`; the replacement contains the single-agent-rationale comment and emits the morning-report warning on any `.corrupted` read; (a) a prompt-render/citation test asserts the new text (`just test` green).
- **Status**: [ ] pending

### Task 14: Retire the orphaned heading-citation pin + registry citation cleanup
- **Files**: `tests/test_skill_section_citations.py`, `bin/.events-registry.md`
- **What**: Remove `test_plan_md_has_1a_heading` (its sole normative citer was the docstring inside the inline reduction block deleted in Task 13); keep the sibling 1b/§5 pins (they retain live citers). Update registry rows that cite `orchestrator-round.md:256` as consumer (the `lifecycle_start` and `criticality_override` rows) to reflect the new in-process read.
- **Depends on**: [13]
- **Complexity**: simple
- **Context**: `test_plan_md_has_1a_heading` at `test_skill_section_citations.py:40-54` (asserts `"### 1a. Check Criticality"`, cited by `orchestrator-round.md:242`); sibling `test_plan_md_has_1b_heading` (`:57-69`) and the §5 pin (`:72-86`) stay — they have live citers. Registry consumer columns citing `orchestrator-round.md:256`: the `lifecycle_start` row (`:13`) and `criticality_override` row (`:19`).
- **Verification**: (a) `just test` runs `test_skill_section_citations.py` green with the 1a function removed and no orphaned references; `grep -c 'test_plan_md_has_1a_heading' tests/test_skill_section_citations.py` = 0; (a) `bin/cortex-check-events-registry --audit` passes.
- **Status**: [ ] pending

## Risks

- **Consent field is a single-value enum this wave** — `spec_approved.decision` only ever carries `approved` (revise/cancel don't emit the event), so the field is degenerate until a future wave adds values. It is enum-typed (not free text) precisely to keep the additive door open; the alternative (no field) was rejected because it leaves spec approval without a self-describing consent record, which the problem statement calls out as a deliverable. Revisit if a reviewer prefers deferring the field entirely to Phase C.
- **Prose-rewrite serialization + ordering (Tasks 9→10→11→12)** — these are chained rather than parallel for two reasons: (1) they all edit two shared files (`test_lifecycle_event_roundtrip.py` FILE_EVENTS and `bin/.events-registry.md`), and same-file parallel writes would race; (2) the implement-cluster task (10) is ordered BEFORE the review-cluster task (11) so that `review→implement-rework`'s pre-existing duplicate (implement.md §3 + review.md §5) collapses to a single emitter at every commit boundary — remove implement.md §3 first (review.md §5 sole emitter), then route review.md §5 to the verb (verb sole emitter). The inverse ordering would leave implement.md §3 live alongside the verb for one commit and double-emit — the exact failure class this feature fixes. This trades wall-clock for safety.
- **Verb names locked at plan** — `cortex-lifecycle-plan-decision`, `-review-verdict`, `-spec-approve`, `-implement-transition` (modules `plan_decision.py`, `review_verdict.py`, `spec_approve.py`, `implement_transition.py`). Chosen to fit the existing 26-verb vocabulary (`-branch-decision`, `-picker-decision`, `-dispatch-choice`, `-complete-route`). No structural constraint forces these exact names.
- **Mid-verb partial-emission crash has no automatic resume repair** — by design (spec Non-Requirements): resume flows key on artifact existence and bypass the verbs; the `scan_lifecycle` mismatch detector surfaces a partial sequence and the idempotent verbs are the manual recovery tool. An automatic repair hook is Phase-C territory.
- **Marker line-shift window (Task 3 → Task 5)** — Task 3 inserts marker lines, shifting the old line-anchored parity test's targets; the old `LINE_TOLERANCE = 35` absorbs the local shifts (per-file shift is bounded by markers-per-file, ≤ ~4) until Task 5 retires the scheme. If any shift exceeds tolerance before Task 5 lands, the flip must land in the same commit as the markers.
- **Overnight corrupted-read routing is a deliberate interactive/overnight divergence** — for a corrupted/unknowable criticality read, Task 7d (interactive verb) routes to the review-requiring arm per criticality-matrix.md:26, while Task 13 (overnight) routes to single-agent + morning-report warning, never defer. This is intentional, not an inconsistency: criticality-matrix.md:26 is the interactive-context rule; overnight's analog of "reviewed" is the morning report, and "never defer" (project failure-handling philosophy) overrides — routing corrupted→critical_subset would reintroduce deferral (critical_subset can defer unselected variants). Task 13 documents this in the prompt. Revisit if a future wave gives overnight a never-deferring review path.
- **Verification nets are counts, not semantics — each pairs with a positive check** — the zero-sweeps, the refine-delegation count-1 pin, and the kept-pauses set-equality are coarse by construction and can go green while an invariant is violated. Mitigations are built in per net: the zero-sweep covers all three raw-emission forms (`cortex-lifecycle-event` / `cortex_command.lifecycle_event` / `log_event(`) with a negative control each and pairs with L201 for imports; refine-delegation's count-1 is backed by a separate boundary-enumeration assertion (Task 12); the kept-pauses set-equality is backed by per-kind semantic sub-checks including AskUserQuestion-proximity for the consent kinds (Task 5). A future regression through an un-covered form remains possible — the residual gap is documented rather than eliminated.

## Acceptance

`just test` is green with the lifecycle/backlog/`cortex_command` test trees now enforced (Task 1), and covers: the four verbs' write-side order tests, the verb deployment test, the marker-set-equality + freshness parity test with per-kind semantic sub-checks (also in CI), and the arm-f fixture tests. The four cluster files (plan/review/implement/specify) contain zero raw-emission occurrences of any form (`cortex-lifecycle-event` / `cortex_command.lifecycle_event` / `log_event(`); `kept-pauses.md` is generated output with a passing set-equality/freshness gate, its census exhaustively re-audited before the AskUserQuestion site-scan retired, and the requirements-doc supersession landed atomically with the flip (Task 5); `review→implement-rework` has exactly one emitter at every commit boundary (implement-before-review ordering); the overnight prompt reads criticality via the in-process shared reducer with documented single-agent fallbacks (morning-report warning on any corrupted read, never defer) and no `_read_criticality` remnant. All enforcement gates pass: `bin/cortex-check-events-registry --audit` (no cluster-event row still claims `gate-enforced` with a Python-only producer), `cortex-check-skill-path`, `cortex-check-bare-python-import`.
