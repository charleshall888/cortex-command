# Plan: improve-discovery-gate-presentation

## Overview
Add a `## Headline Finding` slot to the discovery research.md template, reorder the R4 gate prose in SKILL.md so the gate's first content section is the Headline Finding (decision-criterion encoding; empty-body fallback warns), reword `drop`'s gate-option description to neutral-terminus dual-use semantics, and add a structural test for Phase 1 acceptance. Runtime validation is the next applicable posture-check discovery run (subjective re-run signal, post-merge).

## Outline

### Phase 1: Headline-finding template + gate-presentation reorder + drop-prose tweak (tasks: 1, 2, 3)
**Goal**: Discovery's R4 gate leads with a Headline Finding before Architecture; `drop`'s wording supports neutral-terminus use; structural tests cover the new template slot, gate-prose ordering, and drop wording.
**Checkpoint**: `just test` exits 0 with the new structural test included; `grep` confirms `## Headline Finding`, gate-prose ordering, and `drop`'s reworded description.

## Tasks

### Task 1: Add `## Headline Finding` slot to discovery research.md template
- **Files**: `skills/discovery/references/research.md`
- **What**: Insert a new `## Headline Finding` section between `# Research: {topic}` (line 82 of the current template) and `## Research Questions` (line 84) in the template block. Section body is the authoring guidance directing the agent to populate the slot with one paragraph stating the verdict + the one or two key findings underlying it. Include a stable marker phrase in the authoring guidance (e.g., a verbatim sentence the structural test can grep) so R1(c)'s content-population obligation is verifiable.
- **Depends on**: none
- **Complexity**: simple
- **Context**: Current template structure at `skills/discovery/references/research.md:81-174` is enclosed in a fenced code block. The fenced block contains `# Research: {topic}` (line 82), `## Research Questions` (84), `## Codebase Analysis` (88), `## Web & Documentation Research` (98), `## Domain & Prior Art` (104), `## Feasibility Assessment` (110), `## Architecture` (115), `## Decision Records` (163), `## Open Questions` (172). New `## Headline Finding` inserts between lines 82 and 84. The authoring guidance reads in the same prose register as the existing template comments (HTML-comment guidance blocks like the one above `## Architecture` at line 116). Stable marker phrase candidates: "State the verdict and the one or two key findings supporting it" or similar — pick one and use it verbatim so the structural test in Task 3 can grep it.
- **Verification**: `grep -c '^## Headline Finding$' skills/discovery/references/research.md` ≥ 1; AND `awk '/^## Headline Finding$/{flag=1} /^## Architecture$/{if(flag){print "OK"; exit}}' skills/discovery/references/research.md` outputs `OK` (Headline Finding appears before Architecture in template ordering); AND `grep -c '<stable-marker-phrase>' skills/discovery/references/research.md` ≥ 1 where `<stable-marker-phrase>` is the verbatim sentence added in the authoring guidance.
- **Status**: [x] completed (commit 2d79b91e; marker phrase: "State the verdict and the one or two key findings supporting it")

### Task 2: Update R4 gate prose in SKILL.md (reorder + empty-body fallback + drop reword)
- **Files**: `skills/discovery/SKILL.md`
- **What**: Rewrite R4 gate prose (currently at `skills/discovery/SKILL.md:72-90`) so (a) the gate's first content section is named as `## Headline Finding` from research.md, followed by `## Architecture` sub-sections — encoded as a decision criterion, not a procedural sequence; (b) when the Headline Finding section is missing or its body is empty/whitespace-only, the gate falls back to Architecture-first presentation and the agent surfaces a warning naming the condition; (c) `drop`'s gate-option description rewords from abandon-only to dual-use (legitimate close when research is sufficient and no tickets are warranted, OR abandon outright) — include a stable marker phrase the structural test can grep.
- **Depends on**: none
- **Complexity**: simple
- **Context**: R4 gate prose begins at `skills/discovery/SKILL.md:72` (`### Research → Decompose approval gate (spec R4)`) and ends at line 90. The four current options are at lines 76-79 (`**approve**`, `**revise**`, `**drop**`, `**promote-sub-topic**`). The gate-prose change keeps all four options; `drop`'s description (line 78) gets reworded; a new decision-criterion sentence is added near the top of the section naming the Headline Finding as the first content section before Architecture. Empty-body fallback is a decision-criterion sentence (not procedural) — e.g., "When the Headline Finding section is missing or empty in research.md, the gate falls back to Architecture-first presentation and surfaces a warning." Stable marker phrase for `drop` reword (R3 acceptance) should explicitly name both legitimate uses — e.g., "close discovery when research is sufficient and no tickets are warranted, OR abandon outright." Follow CLAUDE.md `Design principle: prescribe What and Why, not How` (lines 64-70) — describe what the gate surfaces (decision criterion), not how the agent sequences output.
- **Verification**: `grep -c 'Headline Finding' skills/discovery/SKILL.md` ≥ 1; AND `awk` (or equivalent) confirms the gate-prose section names `## Headline Finding` before `## Architecture` in its description of what the gate surfaces; AND `grep -c '<drop-stable-marker-phrase>' skills/discovery/SKILL.md` ≥ 1 where the marker phrase names both legitimate uses of `drop`; AND `drop`'s description does not contain `abandon`/`failure`/`fail`/`discard` as the only descriptors (`grep -E 'drop.*(?:abandon|failure|fail|discard)' skills/discovery/SKILL.md` matches must coexist with the dual-use marker).
- **Status**: [x] completed (commit e84a2484; drop-marker phrase: "close discovery when research is sufficient and no tickets are warranted, OR abandon outright")

### Task 3: Add structural test for Phase 1 acceptance
- **Files**: `tests/test_discovery_gate_presentation.py`
- **What**: New pytest module that asserts the structural acceptance criteria for R1, R2, R3. Tests: (a) `skills/discovery/references/research.md` contains `## Headline Finding` above `## Architecture` AND contains the R1 stable marker phrase under the slot; (b) `skills/discovery/SKILL.md` R4 gate-prose section names `## Headline Finding` before `## Architecture` AND `drop`'s description contains the R3 dual-use marker phrase. Test names mirror the requirement IDs (e.g., `test_r1_headline_finding_template_slot`, `test_r2_gate_prose_orders_headline_finding_before_architecture`, `test_r3_drop_description_has_dual_use_marker`).
- **Depends on**: [1, 2]
- **Complexity**: simple
- **Context**: Existing discovery test file is `tests/test_discovery_module.py` (404 lines, covers the helper module subcommands). The new file is structural-prose-checks only — no helper module imports needed. Follow the pattern of other prose-structure tests in the repo for path discovery (use `Path(__file__).parent.parent` or `os.environ.get("CORTEX_ROOT")` per repo convention — check `tests/test_lifecycle_kept_pauses_parity.py` for the path-discovery pattern). The marker phrases used in Tasks 1 and 2 must be hardcoded as test constants — when those tasks land, the marker phrases are finalized and this task uses them verbatim. Test framework: pytest, consistent with the rest of `tests/`.
- **Verification**: `just test` exits 0 with the new test module included; AND `grep -c 'def test_r' tests/test_discovery_gate_presentation.py` ≥ 3 (one test per requirement); AND removing `## Headline Finding` from `skills/discovery/references/research.md` and re-running `just test` fails with a clear assertion error naming the R1 check (manual smoke test — not part of `just test`'s normal path).
- **Status**: [x] completed (commit 56810bcb; 3 tests pass; pre-existing unrelated `test_lifecycle_references_resolve.py` failure flagged not introduced)

## Risks
- **Structural tests don't fully verify runtime behavior**: Tasks 1-3 produce static-prose checks; runtime validation is deferred to the next applicable posture-check discovery run (subjective re-run signal post-merge). The dispatch-verification path was considered + dropped per Solution horizon reconciliation with simplicity. If the next posture-check run reveals the soft-form gate prose isn't routing correctly under a cold agent, escalation to MUST per CLAUDE.md `MUST-escalation policy` is filed as a follow-up with the failing run's events.log F-row citation.
- **Subjective re-run is the experiential validation, post-merge**: per the spec's `Evidence-thin acceptance` constraint, structural prose tests gate merge; the experiential signal (user's reaction on the next applicable posture-check topic) is post-merge. If the runtime behavior structurally conforms but the user's subjective reaction still fires, the gate-prose change would need a follow-up.

## Acceptance
The complete feature is acceptable when (a) `just test` exits 0 with `tests/test_discovery_gate_presentation.py` included, confirming structural conformance for R1, R2, R3; (b) the user's next applicable posture-check discovery run (post-merge) does not fire the `"summarize your findings"` reaction at the gate — the experiential validation per the spec's `Subjective re-run` acceptance signal.
