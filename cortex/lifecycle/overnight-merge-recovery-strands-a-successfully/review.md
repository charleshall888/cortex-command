# Review: overnight-merge-recovery-strands-a-successfully

## Stage 1: Spec Compliance

### Requirement 1: Home-repo merge operations target the integration worktree, not the home working tree
- **Expected**: For a home-repo feature (`repo_path=None`), merge-target resolution resolves to the session's home integration worktree (`state.worktree_path`), mirroring `_resolve_feature_integration_worktree`; the same resolution applies to the recovery and review-dispatch call sites; cross-repo resolution (`repo_path != None`) is byte-for-byte unchanged. Acceptance: a test that creates a real second git worktree checked out to `overnight/<id>` and drives a home-repo merge through the UN-mocked `merge_feature` asserts success with no `"already used by worktree"` error, plus a companion cross-repo-unchanged assertion. `pytest -k "home_repo_merge_worktree or cross_repo_resolution_unchanged"` exits 0.
- **Actual**: `_merge_target_repo_path(ctx, name)` (`outcome_router.py:250-267`) returns `ctx.home_worktree_path` for home features and delegates byte-for-byte to `_effective_merge_repo_path` for cross-repo — faithfully mirroring `runner.py:594-595`. `_effective_merge_repo_path` itself is unchanged. The four call sites (auto-merge `:646`/`:984`, review-dispatch `:1016`, recovery `:1229`) route through the helper. `grep -c '_effective_merge_repo_path('` = 2 (the `def` + the single cross-repo delegation). The on-disk test `test_home_repo_merge_worktree` builds a real home repo + a real second worktree owning `overnight/<id>`, patches only NON-merge helpers (`_get_changed_files`, `requires_review`, `cleanup_worktree`, `merge._check_ci_status`), and drives the UN-mocked `merge_feature`; it asserts no `"already used by worktree"` error, the feature merged, and the worktree's `overnight/<id>` HEAD advanced to include the feature commit. `test_cross_repo_resolution_unchanged` asserts `_merge_target_repo_path` equals `_effective_merge_repo_path` for a `repo_path != None` feature against a real cross-repo worktree. Both target tests pass (`2 passed`).
- **Verdict**: PASS
- **Notes**: Cross-repo resolution is provably unchanged (helper delegates to the untouched resolver; the companion test pins equality). The on-disk collision is genuinely reproduced against the real merge layer, satisfying the load-bearing constraint that the test must not stub the resolver/merge layer.

### Requirement 2: The repair-completed re-merge targets the integration worktree, not `Path.cwd()`
- **Expected**: The hard-coded `repo = Path.cwd()` in the `repair_completed` branch (feeding the checkout, ff-merge, and branch-delete) resolves to the integration worktree as in R1. Acceptance: no `Path.cwd()` used as the git `repo`/`cwd` in the `repair_completed` block; a test exercising the repair-completed re-merge for a home feature against a real second worktree asserts success with no `"already used by worktree"` error.
- **Actual**: The `repair_completed` block (`outcome_router.py:499-590`) now sets `repo = _merge_target_repo_path(ctx, name)` (`:501`) and uses `repo` as `cwd` for the checkout (`:526`), ff-merge (`:531`), and branch-delete (`:565`). `grep -n 'Path.cwd()'` shows the only remaining occurrence (`:505`) is inside a comment, not a code statement — no `repo = Path.cwd()` assignment survives. `test_repair_completed_home_remerge` drives the UN-mocked `outcome_router.subprocess.run` block against a real second worktree, asserts no collision, the feature merged, and the worktree's `overnight/<id>` HEAD fast-forwarded to the repair commit. Test passes.
- **Verdict**: PASS
- **Notes**: On the degraded `None`-resolution path the block surfaces the feature as paused and returns (`:502-523`) rather than falling back to `Path.cwd()`/the home tree — matching the spec's no-home-tree-fallback constraint.

### Requirement 3: The dirty-base precheck runs against the integration worktree
- **Expected**: With R1 landed, `recover_test_failure` receives the integration-worktree `repo_path`, so its `git rev-parse --show-toplevel` (`cwd=repo_path`) and `git status --porcelain --untracked-files=no` read the integration worktree. Acceptance: the two existing dirty-base tests are updated to assert the precheck's `git` commands run with `cwd` equal to the passed `repo_path`; `pytest test_merge_recovery.py` exits 0.
- **Actual**: The recovery call site passes `repo_path=merge_target` (`:1229`). The precheck still runs `git rev-parse --show-toplevel` with `cwd=str(repo_path) if repo_path else None` (`merge_recovery.py:234`) and `git status --porcelain --untracked-files=no` with `cwd=repo_root` (`:250`). Both dirty-base tests now thread a non-`None` `repo_path` (a `tempfile.TemporaryDirectory`) via the kwargs spread and assert `str(cwd) == str(repo_path)` for both the rev-parse and status calls (`test_merge_recovery.py:531/540-547`, `:572/581-583`). `pytest test_merge_recovery.py` = `14 passed`.
- **Verdict**: PASS
- **Notes**: The tests use the dict-spread form `"repo_path": repo_path` rather than the literal `repo_path=` keyword token the plan's verification grep anticipated; this is functionally equivalent and the substantive requirement (non-`None` path, `cwd` asserted equal to it) is fully met.

### Requirement 4: The misleading error string is corrected and pinned
- **Expected**: `error="dirty base branch after revert"` is replaced with a string that accurately describes a dirty integration worktree and makes no claim a revert occurred (must not contain "revert"); no destructive restore; the updated test pins the exact new string. Acceptance: `grep -c "dirty base branch after revert"` across source + test totals 0; the updated test asserts the exact new error string.
- **Actual**: `merge_recovery.py:259` now reads `error="dirty integration worktree blocks recovery"` — accurate, no "revert". `grep -c "dirty base branch after revert"` across `merge_recovery.py` + `test_merge_recovery.py` totals 0. The only "revert" occurrence in `merge_recovery.py` is in a docstring (`:201`), not the error string. `test_dirty_base_check_blocked_by_tracked_changes` pins the exact new string (`:577`). No `reset --hard`/`git clean` added in the diff range (verified via `git diff 5155f95f..HEAD`); the `attempts=0`/`paused=True` pause path is retained as specified.
- **Verdict**: PASS
- **Notes**: The genuinely-dirty integration worktree still surfaces via the existing pause path; recoverable surfacing is correctly left to #284.

### Phase A: Route home-repo merge/recovery/review to the integration worktree
- **Verdict**: PASS — R1 + R2 satisfied; the `None`-surface guard pauses (never home-tree-falls-back) at all four sites and the repair block; `test_outcome_router.py`, `test_lead_unit.py`, `test_orchestrator.py` all green.

### Phase B: Correct the dirty-base precheck error string
- **Verdict**: PASS — R3 + R4 satisfied; precheck scoped to the integration worktree, error string corrected and pinned, no destructive restore added.

## Requirements Drift
**State**: none
**Findings**:
- None. The change restores the already-documented behavior that per-feature merges land on the integration worktree / branch `overnight/{session_id}` (pipeline.md:17, 23) rather than the home working tree — it is a correctness fix to an existing contract, not new behavior. The retained `paused` disposition for a genuinely-dirty worktree and the deferral of recoverable surfacing to #284 are consistent with pipeline.md:38-40, 56. The degraded `None`-resolution pause is a new internal guard, but it surfaces as the already-documented `paused` status with no new feature status or event, so it is captured by the existing status lifecycle.
**Update needed**: None

## Stage 2: Code Quality
- **Naming conventions**: Consistent. `_merge_target_repo_path` parallels the existing `_effective_merge_repo_path` / `_effective_base_branch` private-helper naming and the `_resolve_feature_integration_worktree` resolver it mirrors. The new `home_worktree_path: Path | None = None` field follows the dataclass's typing style and is correctly appended last with a default (avoiding `non-default argument follows default argument`), keeping both test construction sites working unmodified.
- **Error handling**: Appropriate. The `None`-surface guard at each of the four sites (and the repair block) mirrors the existing pause pattern (`features_paused.append` + `FEATURE_PAUSED` event + `_write_back_to_backlog(..., "paused", ...)` + early return), using the consistent `"integration worktree unresolved"` error. It correctly distinguishes the home case (`ctx.repo_path_map.get(name) is None and merge_target is None`) so cross-repo `None` does not get mis-paused. The orchestrator pre-initializes `home_worktree_path` before the state-load `try` (commit 4c80b166), correctly avoiding an `UnboundLocalError` on the state-load-failure path where `overnight_state` is never bound.
- **Test coverage**: Strong. All four new on-disk tests run real git (init, worktree add, real `merge_feature`/`subprocess.run`) and assert observable post-merge git effects (worktree HEAD advance / ff to the repair SHA), genuinely reproducing the on-disk collision rather than stubbing it — satisfying the spec's explicit "must reproduce the on-disk collision, not stub the resolver" constraint. The unresolved-home test proves the home tree is never advanced. The dirty-base precheck tests pin both the new error string and the `cwd` against a non-`None` path. All plan verification commands executed and pass; full overnight + pipeline suite green (`777 passed, 1 skipped`).
- **Pattern consistency**: Strong. The helper faithfully encodes the home-vs-cross fork from `_resolve_feature_integration_worktree` (`runner.py:578-602`); the `None`-surface guard follows the established `outcome_router.py` pause-pattern; the cross-repo path is provably byte-for-byte unchanged (delegation to the untouched resolver, pinned by the companion test). The fix is confined to the targeted pipeline/overnight modules with no events-registry or SKILL.md-parity impact, as the spec anticipated.

## Verdict
```json
{"verdict": "APPROVED", "cycle": 1, "issues": [], "requirements_drift": "none"}
```
