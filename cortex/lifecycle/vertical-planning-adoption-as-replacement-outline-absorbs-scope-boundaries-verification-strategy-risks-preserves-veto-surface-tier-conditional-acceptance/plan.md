# Plan: Vertical-planning adoption as REPLACEMENT (#182)

## Overview

Ten-task vertical-slice landing of the audit-DR-1 REPLACEMENT: ship robustness work first (metrics.py alias-lookup + parser regression test), land the report.py compat shim BEFORE template changes so morning report never silently degrades, then restructure plan.md / specify.md / orchestrator-review.md in a coordinated middle commit (FM-10), and only at the end delete the legacy sections from the canonical plan.md template. Plugin mirrors auto-regenerate via the pre-commit hook on each canonical-touch commit; a defense-in-depth parity test (R14) lands independently.

## Tasks

### Task 1: Harden metrics.py:221 with closed-enum verdict alias-lookup
- **Files**: `cortex_command/pipeline/metrics.py`
- **What**: Replace `[e["verdict"] for e in review_events]` at line 221 with `[v for v in (_extract_verdict(e) for e in review_events) if v is not None]`. Add module-level constant `_VERDICT_FIELD_ALIASES: tuple[str, ...] = ("verdict", "review_verdict", "decision")` near the existing module constants. Add a named helper `_extract_verdict(event: dict) -> str | None` that iterates the tuple in order and returns the first present field's value, or `None` when no alias is present.
- **Depends on**: none
- **Complexity**: simple
- **Context**: Direct-access site is `cortex_command/pipeline/metrics.py:221` inside the `# ---- Review verdicts ----` block at lines 220–224. The closed enum is the explicit R11 scoping: do NOT generalize the helper across other field accesses in `metrics.py` (FM-6). Tuple ordering is the precedence contract — canonical `"verdict"` first; multi-alias events resolve to canonical (R11 Edge Case). Follow the existing pattern of module-level constants and underscore-prefixed private helpers in this file.
- **Verification**: `python -c "from cortex_command.pipeline.metrics import _VERDICT_FIELD_ALIASES, _extract_verdict; assert _VERDICT_FIELD_ALIASES == ('verdict', 'review_verdict', 'decision'); assert callable(_extract_verdict)"` exits 0.
- **Status**: [x] complete

### Task 2: Unit-test `_extract_verdict` against three event shapes
- **Files**: `cortex_command/pipeline/tests/test_metrics.py`
- **What**: Add a test class (or top-level test functions, matching the file's existing style) exercising `_extract_verdict` on (a) an event with canonical `"verdict": "APPROVED"`, (b) an event with alias `"review_verdict": "CHANGES_REQUESTED"` only, (c) an event missing all three aliases (returns `None`), and (d) an event with BOTH `"verdict"` and `"review_verdict"` present (tuple-ordering precedence — canonical wins).
- **Depends on**: [1]
- **Complexity**: simple
- **Context**: Test file already exists at `cortex_command/pipeline/tests/test_metrics.py`. Match the existing test-style conventions (pytest functions or unittest TestCase — whichever the file uses). The four cases satisfy spec R11 acceptance plus the multi-alias deterministic-precedence edge case from spec Edge Cases.
- **Verification**: `just test cortex_command/pipeline/tests/test_metrics.py` exits 0 with the new tests present and passing.
- **Status**: [x] complete

### Task 3: Plan parser regression test for `## Outline` + H3 phases
- **Files**: `cortex_command/pipeline/tests/test_parser.py`
- **What**: Append a regression-test class (after the current line 354) with three test methods covering: (a) a plan with `## Outline` ABOVE `## Tasks` parses successfully — all tasks discovered, all task-body fields extracted, the existing `_extract_section(text, "Overview")` extractor at `parser.py:258` finds `## Overview` and is NOT confused by `## Outline`; (b) a plan with `### Phase N:` H3 headings inside `## Outline` parses successfully — task bodies remain untruncated, all fields extracted on the subsequent `### Task N:` sections; (c) lock-in: an unrelated `## SomeOther` H2 section between two `### Task N:` blocks still truncates the prior task's body at the H2 boundary (preserves the current H2-anchor termination behavior at `parser.py:310`).
- **Depends on**: none
- **Complexity**: simple
- **Context**: Existing helpers are `_make_plan()` and `_task_block()` in the same file. Parser entry points: `_parse_tasks()` at `cortex_command/pipeline/parser.py:282-329`; H2 sweep at line 310 (`re.search(r"^##\s+", text[start:], re.MULTILINE)`); Overview extractor uses `_extract_section()` at line 115. Test fixtures should be constructed in-test via the existing `_make_plan()` helper rather than separate fixture files.
- **Verification**: `just test cortex_command/pipeline/tests/test_parser.py` exits 0 with the new tests present and passing.
- **Status**: [x] complete

### Task 4: report.py tier-conditional verification reader (compat shim ships first)
- **Files**: `cortex_command/overnight/report.py`
- **What**: Add three module-level helpers and branch `_render_feature_block` at line 534. (a) `_read_tier(feature: str) -> str` — reads `lifecycle/{feature}/events.log` for the most recent `lifecycle_start` or `complexity_override` event and returns its `tier` field (or `to` field for override events); returns `"simple"` when events.log is missing, malformed, or contains no relevant event. (b) `_read_acceptance(feature: str) -> str` — uses the existing section-reader regex pattern `r"^## SectionName\s*\n(.*?)(?=\n## |\Z)"` from `_read_verification_strategy` at line 717-731 against `## Acceptance`. (c) `_read_last_phase_checkpoint(feature: str) -> str` — parses `## Outline`, locates LAST `### Phase N:` heading, extracts `**Checkpoint**:` field; on heading-present-field-absent, walks backward through earlier phases to the most recent populated Checkpoint; returns `""` on no-Outline or no-Checkpoint-in-any-phase. Replace `verification = _read_verification_strategy(name)` at line 534 with: read tier; if `tier == "complex"`, try `_read_acceptance` then `_read_last_phase_checkpoint` then `_read_verification_strategy` (legacy); if `tier == "simple"`, try `_read_last_phase_checkpoint` then `_read_verification_strategy`. HYBRID policy: new-shape reader silently wins when both shapes present (spec R13e). This shim is what allows Task 10's deletion to be safe — in-flight plans authored under the old template continue rendering until they're migrated.
- **Depends on**: none
- **Complexity**: complex
- **Context**: Existing reader is `_read_verification_strategy(feature)` at `cortex_command/overnight/report.py:717-731`. Existing call site is line 534 in `_render_feature_block`. Pattern reference for the section-reader regex: lines 724-728. Tier extraction model: `cortex_command/pipeline/metrics.py:195-197` reads `lifecycle_start` events and pulls `tier` (NOT `complexity` — the persisted JSON key is `tier`). `lifecycle_start` event schema is defined in `skills/lifecycle/SKILL.md` (the canonical writer); `complexity_override` event schema in the same file uses `"from"` and `"to"` fields. Plan path convention: `Path(f"lifecycle/{feature}/plan.md")` per existing code in this file.
- **Verification**: `python -c "from cortex_command.overnight.report import _read_acceptance, _read_last_phase_checkpoint, _read_tier; print(callable(_read_acceptance), callable(_read_last_phase_checkpoint), callable(_read_tier))"` prints `True True True`.
- **Status**: [x] complete

### Task 5: Integration test — ten fixtures for report.py rendering
- **Files**: `cortex_command/overnight/tests/test_report.py`
- **What**: Add an integration test class exercising the ten fixtures: the nine from spec R13 acceptance plus a tenth covering corrupted-events.log on a complex plan. (1) complex plan with `## Acceptance`; (2) simple plan with `## Outline` + last-phase `**Checkpoint**`; (3) legacy plan with only `## Verification Strategy`; (4) degenerate plan (no Outline/Acceptance/Verification Strategy → asserts the rendered line is the generic fallback string `"See feature plan for verification steps."` — NOT empty); (5) HYBRID plan with both `## Outline` AND `## Verification Strategy` (asserts new-shape wins); (6) complex plan with `## Outline` but no `## Acceptance` (asserts last-phase Checkpoint via fallback); (7) simple plan with last phase heading-present field-absent (asserts walk-backward to most recent populated Checkpoint); (8) legacy plan with `## Verification Strategy` + manually-authored `## Acceptance` (asserts complex-tier prefers Acceptance — intentional going-forward stance); (9) `complexity_override`-escalated plan to `tier=complex` but still only `## Outline` / no `## Acceptance` (asserts fixture 6 behavior); (10) **complex-tier plan with corrupted/missing events.log and an `## Acceptance`-only verification source — asserts `_read_tier` returns `"simple"` per spec R13a default AND the rendered "How to try" line is the generic fallback string `"See feature plan for verification steps."` (loud, visible degradation — NOT silent empty)**. Also add **two key-name assertion tests** addressing the persistence-vs-user-facing distinction: (T-A) `_read_tier` on an events.log containing only a `complexity` field (no `tier` field) returns `"simple"` (the default — the wrong key is silently ignored); (T-B) `_read_tier` on an events.log containing BOTH `tier: "complex"` and a stray `complexity: "simple"` field returns `"complex"` (the canonical `tier` key wins).
- **Depends on**: [4]
- **Complexity**: simple
- **Context**: Test file exists at `cortex_command/overnight/tests/test_report.py`. Use `tmp_path` (pytest fixture) for filesystem isolation; construct `lifecycle/{feature}/plan.md` and `events.log` under tmp_path; monkey-patch CWD or pass paths into helpers (match the existing test file's pattern — read it first to find the convention). The events.log construction uses NDJSON one-event-per-line format; minimal `lifecycle_start` event: `{"event": "lifecycle_start", "feature": "<n>", "tier": "complex"}`; `complexity_override` event: `{"event": "complexity_override", "feature": "<n>", "from": "simple", "to": "complex"}`. Each fixture verifies the helper return value AND the rendered `_render_feature_block` "How to try" line. Mechanical fixture authoring against an existing test-file pattern; tier=simple per file-count rule (1 file, follow existing patterns).
- **Verification**: `just test cortex_command/overnight/tests/test_report.py` exits 0 with the new ten-fixture class and the two key-name assertions present and passing.
- **Status**: [x] complete

### Task 6: plan.md template restructure + Veto Surface→Risks rename in code consumers
- **Files**: `skills/lifecycle/references/plan.md`, `cortex_command/pipeline/dispatch.py`, `cortex_command/pipeline/tests/test_dispatch.py`
- **What**: In `skills/lifecycle/references/plan.md` §3 canonical template: (a) add a single prose paragraph at the top of §3 (before the template code-fence) covering R6 provenance — community-derived from CRISPY/QRSPI v2 (HumanLayer), practitioner-grade evidence quality (1/5), human-skim is the primary justification, do not quote the "more reliable than any prompt instruction" superlative as fact; (b) add `## Outline` section ABOVE `## Tasks` showing `### Phase N: <name> (tasks: 1, 2, ...)` H3 example with `**Goal**: <line>` and `**Checkpoint**: <observable state>` fields, with ≥1 phase for `complexity=simple` and ≥2 phases for `complexity=complex`; (c) rename `## Veto Surface` to `## Risks` in the template — preserve purpose, content shape, ~5–10 line cost; (d) immediately after the renamed `## Risks` block (or at top of §3), add the prose anchor: `Renamed from \`## Veto Surface\` 2026-05-11 — retro searches for the prior name still apply to plans authored before this date.`; (e) add a top-level `## Acceptance` section instructed to be populated ONLY on `complexity=complex` plans (~3 lines whole-feature acceptance criterion); template prose explicitly conditions on `complexity=complex` (the Clarify §4-resolved dimension) — this single template edit is what spec R3 calls "Section 1 template" AND "Verification bullets" AND "touch-point line" (sites 1, 4, 5 of the five-site list); all three are sub-elements of the §3 template-restructure scope and converge on the same `complexity=complex` token. Legacy `## Scope Boundaries` and `## Verification Strategy` REMAIN in the template at this step — Task 10 deletes them. In the SAME commit, update the 2 live downstream docstrings: `cortex_command/pipeline/dispatch.py:271` (rephrase "Per the Veto Surface" → "Per the Risks section") and `cortex_command/pipeline/tests/test_dispatch.py:943` (rephrase "the plan's Veto Surface" → "the plan's Risks section"). Plugin mirror at `plugins/cortex-core/skills/lifecycle/references/plan.md` auto-regenerates via the pre-commit hook on this commit.
- **Depends on**: none
- **Complexity**: complex
- **Context**: Canonical template at `skills/lifecycle/references/plan.md` §3 lives at lines 133-175 in the current file (template fence at 137-175). Heading H-level for phases inside Outline is H3 — the Clarify §4 / Spec §2 resolution; structurally avoids parser-truncation (FM-5) and `phase_durations` namespace collision (FM-4). Veto Surface live consumers: `cortex_command/pipeline/dispatch.py:271` (docstring inside the dispatch reference); `cortex_command/pipeline/tests/test_dispatch.py:943` (docstring inside the assert test). Plugin mirror regenerates byte-equal via `just build-plugin` (rsync -a --delete) invoked by `.githooks/pre-commit`. Both gates and report.py readers (Task 4) are already in place to consume the new shape; Task 8 will add the gates that enforce it.
- **Verification**: `grep -nE "^## Outline$" skills/lifecycle/references/plan.md` returns ≥1 match (exit 0); `grep -nE "^### Phase [0-9]+:" skills/lifecycle/references/plan.md` returns ≥1 match; `grep -nE "^## Risks$" skills/lifecycle/references/plan.md` returns ≥1 match; `grep -nE "^## Veto Surface$" skills/lifecycle/references/plan.md plugins/cortex-core/skills/lifecycle/references/plan.md` returns nothing (exit 1); `grep -nE "^## Acceptance$" skills/lifecycle/references/plan.md` returns ≥1 match; `grep -nE "complexity=complex" skills/lifecycle/references/plan.md` returns ≥1 match; `grep -nE "(CRISPY|QRSPI|community-derived|practitioner-grade)" skills/lifecycle/references/plan.md` returns ≥1 match; `grep -nE "Renamed from .Veto Surface" skills/lifecycle/references/plan.md` returns ≥1 match; `grep -n "Veto Surface" cortex_command/pipeline/dispatch.py cortex_command/pipeline/tests/test_dispatch.py` returns nothing.
- **Status**: [x] complete

### Task 7: specify.md template — `## Phases` section + per-requirement Phase tag
- **Files**: `skills/lifecycle/references/specify.md`
- **What**: In the canonical spec template (around lines 112-141, after `## Problem Statement` and before `## Requirements`), add a `## Phases` section with the example shape `- **Phase 1: <name>** — <one-line goal>` listing requirements-grouped phases. In `## Requirements`, modify each numbered example line to include an inline `**Phase**: <name>` tag matching one of the declared phases (e.g., `1. [Requirement]: [Acceptance criteria]. **Phase**: <name>`). Template instruction notes: ≥1 phase for `complexity=simple`, ≥2 phases for `complexity=complex` (matches plan.md Outline tolerance per R1). Plugin mirror at `plugins/cortex-core/skills/lifecycle/references/specify.md` auto-regenerates via pre-commit.
- **Depends on**: none
- **Complexity**: simple
- **Context**: Spec template lives at `skills/lifecycle/references/specify.md` lines 112-141 (current numbered structure starts at line 113 `# Specification: {feature}`). The `## Phases` insertion goes between `## Problem Statement` (line 115-116) and `## Requirements` (line 118). The `**Phase**` tag is inline after acceptance criteria on each requirement line, NOT a separate sub-bullet.
- **Verification**: `grep -nE "^## Phases$" skills/lifecycle/references/specify.md` returns ≥1 match; `grep -nE "\*\*Phase\*\*:" skills/lifecycle/references/specify.md` returns ≥1 match.
- **Status**: [x] complete

### Task 8: orchestrator-review.md — add gates P9 (plan outline), S7 (spec phases), P10 (complex Acceptance)
- **Files**: `skills/lifecycle/references/orchestrator-review.md`
- **What**: Insert three new table rows. (a) Gate **S7** after S6 (around line 150 in the Post-Specify Checklist table): `| S7 | Spec phases section present | Spec contains \`## Phases\` section with ≥1 phase; each requirement carries a \`**Phase**\` tag matching one of the declared phases. Skip on \`criticality=low AND tier=simple\` per the existing skip rule. |`. (b) Gate **P9** after P8 (around line 165 in the Post-Plan Checklist table): `| P9 | Plan outline section present | Plan contains \`## Outline\` section. For \`complexity=complex\` plans, ≥2 phases required; for \`complexity=simple\` plans, ≥1 phase acceptable. Each phase names its task IDs in the heading; each phase has \`**Goal**\` and \`**Checkpoint**\` fields. |`. (c) Gate **P10** after P9: `| P10 | Acceptance section present on complex plans | For \`complexity=complex\` plans, plan contains \`## Acceptance\` section with whole-feature acceptance criterion. Skip on \`complexity=simple\` plans — last-phase Checkpoint is the contract there. |`. All three gates inherit the existing `low+simple` skip rule from line 9. Use soft positive-routing phrasing per CLAUDE.md OQ3 — zero MUST/CRITICAL/REQUIRED in any of the three row texts. Plugin mirror at `plugins/cortex-core/skills/lifecycle/references/orchestrator-review.md` auto-regenerates via pre-commit.
- **Depends on**: [6, 7]
- **Complexity**: simple
- **Context**: Existing P-table ends at P8 (line 165 in `skills/lifecycle/references/orchestrator-review.md`); S-table ends at S6 (line 150). The skip rule at line 9 is `Skip orchestrator review when criticality is \`low\` AND tier is \`simple\`` — inherited by all gates structurally. P8 (existing) is the precedent for tier-conditional gating ("Gated on `criticality = critical` (when §1b ran); explicitly N/A for non-critical plans") — P10's complexity-gating follows the same prose shape but on a different dimension (`complexity=complex`, NOT `criticality=critical` — the Clarify §4 resolution).
- **Verification**: `grep -nE "^\| P9 \|" skills/lifecycle/references/orchestrator-review.md` returns ≥1 match; `grep -nE "^\| S7 \|" skills/lifecycle/references/orchestrator-review.md` returns ≥1 match; `grep -nE "^\| P10 \|" skills/lifecycle/references/orchestrator-review.md` returns ≥1 match; `awk '/^\| (P9|P10|S7) \|/' skills/lifecycle/references/orchestrator-review.md | grep -cE "MUST|CRITICAL|REQUIRED"` returns 0; **positive dimension check** — `grep -E "^\| P10 \|.*complexity=complex" skills/lifecycle/references/orchestrator-review.md` returns ≥1 match (asserts P10's inner condition uses the spec-R3-resolved dimension); **negative dimension check** — `grep -E "^\| P10 \|.*criticality=critical" skills/lifecycle/references/orchestrator-review.md` returns nothing (asserts P10 does NOT regress to the original-ticket bug pattern).
- **Status**: [x] complete

### Task 9: Plugin-mirror parity test (defense-in-depth)
- **Files**: `tests/test_plugin_mirror_parity.py`
- **What**: New pytest file that reads each canonical file in scope for this ticket (`skills/lifecycle/references/plan.md`, `skills/lifecycle/references/specify.md`, `skills/lifecycle/references/orchestrator-review.md`) and compares its bytes to the plugin mirror at `plugins/cortex-core/skills/lifecycle/references/<same-filename>.md`. The test fails on any drift with a clear message naming the drifted file. This is independent of the pre-commit hook (AS-3 defense-in-depth — if a developer skipped `just setup-githooks`, CI still catches drift).
- **Depends on**: [6, 7, 8]
- **Complexity**: simple
- **Context**: Plugin-mirror enforcement infrastructure is in `.githooks/pre-commit` and `bin/cortex-check-parity`; this test is the independent CI-level catch. Follow the conventions of existing root-level tests in `tests/` (pytest-style, the file `tests/test_check_parity.py` is the closest sibling — read it first to match the test discovery pattern and import conventions).
- **Verification**: `just test tests/test_plugin_mirror_parity.py` exits 0 when mirrors are in sync. Manual sanity-check: temporarily appending a stray byte to `plugins/cortex-core/skills/lifecycle/references/plan.md` and re-running the test causes failure with a message identifying that file; revert the manual byte after.
- **Status**: [x] complete

### Task 10: DELETE `## Scope Boundaries` + `## Verification Strategy` from plan.md template
- **Files**: `skills/lifecycle/references/plan.md`
- **What**: Hard-delete the `## Scope Boundaries` section template block and the `## Verification Strategy` section template block from `skills/lifecycle/references/plan.md` §3 canonical template (currently at lines 167-168 and 173-174 respectively per the existing template fence). No deprecation marker, no soft-delete comment — full removal per `requirements/project.md` Workflow trimming philosophy. The compat shim from Task 4 ensures the morning report continues rendering verification text for in-flight plans authored under the old template — and Task 5's ten-fixture behavioral suite is now a hard dependency, so this task cannot run until shim correctness is empirically verified, not just import-able. Plugin mirror auto-regenerates via pre-commit.
- **Depends on**: [4, 5, 6]
- **Complexity**: simple
- **Context**: This is the deletion step deliberately separated from Task 6 per research §"Sequencing hedge" (Adversarial FM-7) — the compat shim from Task 4 must ship first so in-flight plans remain readable in the morning report. Combining Task 6 (template) and Task 10 (deletion) into one commit removes the safety margin and is explicitly rejected by FM-7. The dependency on Task 5 (the nine-fixture behavioral suite, plus the corrupted-events.log fixture (10), plus the key-name assertions) gates this deletion on shim correctness rather than shim importability — without this edge, callable-existence smoke could authorize legacy-section deletion against a behaviorally-broken shim. The two sections to delete are the `## Scope Boundaries` and `## Verification Strategy` blocks INSIDE the template code-fence at §3 — not the template-prose-around-them. **Both the H2 heading AND the bracketed body prose under it must be removed** — a partial deletion (heading-only) would orphan the body content under the preceding section. After deletion, the §3 fence contains: `# Plan: {feature}`, `## Overview`, `## Tasks`, `## Outline` (added in Task 6), `## Risks` (renamed in Task 6), `## Acceptance` (added in Task 6).
- **Verification**: `grep -nE "^## Scope Boundaries$" skills/lifecycle/references/plan.md plugins/cortex-core/skills/lifecycle/references/plan.md` returns nothing (exit 1); `grep -nE "^## Verification Strategy$" skills/lifecycle/references/plan.md plugins/cortex-core/skills/lifecycle/references/plan.md` returns nothing (exit 1); **body-deletion checks** — `grep -F "What is explicitly excluded from this feature. Maps to the spec's Non-Requirements section." skills/lifecycle/references/plan.md plugins/cortex-core/skills/lifecycle/references/plan.md` returns nothing (asserts Scope Boundaries' template-body sentinel is gone, not just the heading); `grep -F "How to verify the complete feature works end-to-end after all tasks are done" skills/lifecycle/references/plan.md plugins/cortex-core/skills/lifecycle/references/plan.md` returns nothing (asserts Verification Strategy's template-body sentinel is gone, not just the heading).
- **Status**: [x] complete

## Verification Strategy

End-to-end the feature is verifiable through three composite checks:

1. **Canonical template shape**: `grep -nE "^## (Outline|Risks|Acceptance)$" skills/lifecycle/references/plan.md` returns 3 matches AND `grep -nE "^## (Scope Boundaries|Verification Strategy|Veto Surface)$" skills/lifecycle/references/plan.md plugins/cortex-core/skills/lifecycle/references/plan.md` returns 0 matches. `grep -nE "^## Phases$" skills/lifecycle/references/specify.md` returns ≥1 match.

2. **Gate enforcement live**: `grep -nE "^\| (P9|P10|S7) \|" skills/lifecycle/references/orchestrator-review.md` returns 3 matches with zero MUST/CRITICAL/REQUIRED escalations. **Plus dimension consistency**: `grep -E "^\| P10 \|.*complexity=complex" skills/lifecycle/references/orchestrator-review.md` returns ≥1 match AND `grep -E "^\| P10 \|.*criticality=critical" skills/lifecycle/references/orchestrator-review.md` returns nothing — asserts the P10 gate row encodes the spec-R3-resolved dimension and never regresses to the original-ticket bug.

3. **Test suite green**: `just test cortex_command/pipeline/tests/test_parser.py cortex_command/pipeline/tests/test_metrics.py cortex_command/overnight/tests/test_report.py tests/test_plugin_mirror_parity.py` exits 0 with all four new test surfaces passing.

A manual integration check by authoring a tiny new lifecycle (e.g., `lifecycle/{some-test-feature}/`) with the new template, running `_render_feature_block` against it, and confirming the rendered "How to try" line matches expectation is recommended but not required for sign-off.

## Veto Surface

- **H3 phases inside `## Outline` instead of H2 or renamed `## Slice N:`**: chosen because H3 structurally avoids parser-truncation (FM-5) AND `phase_durations` namespace collision (FM-4) without requiring a vocabulary fork from the CRISPY/QRSPI lineage. Reversible: a future ticket could rename to `## Slice N:` if vocabulary clarity becomes load-bearing. Worth revisiting if vertical-slice discourse diverges from "phase" in the cortex retro corpus.

- **Tier dimension for `## Acceptance` + P10 is `complexity=complex`, NOT `criticality=critical`**: chosen per Clarify §4 resolution. Trade-off: `critical+simple` plans (severe-risk small-surface changes like a one-line security predicate edit) bypass P10 enforcement. Escape hatch documented in spec Edge Cases: operators escalate via `complexity_override` event `simple` → `complex` to trigger P10. If retros surface frequent `critical+simple` gaps, revisit by adding criticality as a second axis to P10's inner condition.

- **Sequencing as 10 tasks with hard ordering (4+5 before 10, 6+7 before 8, 6+7+8 before 9)**: chosen per research §"Sequencing hedge" (Adversarial FM-7 + FM-10) to avoid the silent-degradation window (template ships before compat shim) and the false-positive window (gate text ships before templates). Task 10's dependency includes Task 5 (not just Task 4) — this gates legacy-section deletion on the shim's BEHAVIORAL verification (the ten-fixture suite + key-name assertions), not just its IMPORTABILITY (Task 4's callable smoke). Bundling Task 10 (deletion) with Task 6 (template restructure) was rejected explicitly by FM-7. Trade-off: 10 commits, more PR review surface than 3 large commits. If the PR-size-vs-incrementalism trade-off swings the other way, consider compressing Tasks 1+2, Tasks 4+5, and Tasks 6+10 into 3 super-commits — but keep the 4+5-before-10 ordering intact.

- **Archived-plan corpus testing is NOT in scope for Task 5**: spec Non-Requirements §9 explicitly excludes in-flight plan.md migration; the compat shim's correctness is gated by the ten synthetic fixtures, not by a corpus sweep across the ~108 archived `lifecycle/*/plan.md` files. Trade-off: real-world authoring variance (trailing colons on headings, CRLF line endings, leading BOM, double-spaces, HTML-comment blocks before the heading) is unmeasured. If the morning report degrades on a specific archived plan post-Task-10, the path forward is a targeted bug-fix ticket adding that variance to Task 5's fixture set — not blocking this ticket on a corpus expansion. The fail-mode for an unsupported variance is degraded-to-generic-fallback (`"See feature plan for verification steps."`), not silent-empty (fixtures 4 and 10 lock this in).

- **`_VERDICT_FIELD_ALIASES` as closed tuple, NOT generalized field-alias pattern**: chosen per spec R11 scoping (FM-6). Trade-off: future drift to a fourth verdict-name alias requires manual constant edit + unit test extension. Spec Technical Constraints documents this operational protocol. The structurally-strong defense (write-time schema validation at `cortex_command/overnight/events.py`) is recorded as future follow-up in spec Non-Requirements §48 — not in this ticket's scope.

- **HYBRID-plan policy: new-shape reader silently wins**: chosen per spec R13e as going-forward stance. Trade-off: operators with HYBRID plans (both `## Outline` AND `## Verification Strategy`) see only the new-shape content rendered. The legacy text in HYBRID plans is invisible to the morning report. Documented in spec Edge Cases; the migration path is to delete the legacy section from the in-flight plan.

- **No in-flight plan.md migration**: chosen per spec Non-Requirements. Trade-off: ~108 archived plan.md files in `lifecycle/` retain the old shape (`## Verification Strategy`, `## Veto Surface`, `## Scope Boundaries`). The R2 retro-anchor prose in the canonical template flags this. Compat shim (Task 4) keeps these renderable. Migration cost would be high relative to benefit since archived plans are rarely re-rendered.

## Scope Boundaries

- The optional `cortex plan-outline` topological-sort renderer (Mermaid or text-based) is NOT implemented — deferred per ticket DR-3 and spec Non-Requirements.
- Mermaid diagrams inside `## Outline` are NOT mandated in the template — allowed where they add value, but the template instructs text-outline as required shape.
- Support for `## Phase N:` H2 headings nested INSIDE `## Tasks` is NOT implemented or tested — the H3 convention (Task 6) structurally avoids the failure path, so the "hard-break behavior on nested H2" regression test from the original ticket text is intentionally omitted.
- Propagation of the provenance note (Task 6 §3 prose paragraph) into each authored `lifecycle/{feature}/plan.md` is NOT in scope — the note lives in the canonical template prose only.
- Shipping a specific "X-line per-feature reduction" claim in the canonical plan template or in the morning report is NOT in scope — the audit's 40–90-line estimate is research-internal context; not a shipped claim.
- Creating a separate `research/epic-172-audit/critical-review-residue.json` artifact is NOT in scope — Q-C citations rewrite to point to `research/epic-172-audit/research.md:266,274,277,278` directly.
- Soft-deletion / deprecation markers for `## Scope Boundaries` and `## Verification Strategy` are NOT in scope — hard deletion only.
- Write-time schema validation for `review_verdict` events at `cortex_command/overnight/events.py` is NOT in scope — `metrics.py:221`'s alias-lookup is defense-in-depth only; the upstream strict-schema work is a separate ticket.
- Migration of in-flight `lifecycle/{feature}/plan.md` files authored under the old template is NOT in scope — compat shim (Task 4) handles them transparently.
- Refactoring or generalization of the alias-lookup pattern across other `metrics.py` field accesses is NOT in scope — `_VERDICT_FIELD_ALIASES` is closed; new fields require explicit additions.
