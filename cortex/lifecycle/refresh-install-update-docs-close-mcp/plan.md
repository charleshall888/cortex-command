# Plan: refresh-install-update-docs-close-mcp

## Overview

Four-phase sequential execution matching the spec phases. Phase 1 housekeeping (file follow-up items + backfill #145) lands first to clear noise; Phase 2 docs refresh (lowest code-risk) lands next as a coherent single-PR; Phase 3 adds the dispatch preflight; Phase 4 lands the entire wontfix terminal-state convention as a single composite task that authors all six file changes and commits them atomically — collapsed from a six-task deferred-commit choreography into one composite task to avoid the prose-only-enforcement failure mode under autonomous execution.

## Outline

### Phase 1: Housekeeping (tasks: 1, 2, 3)
**Goal**: Clear pre-existing SessionStart noise (#145) and register follow-up backlog items for items 5 and 6 so they have IDs and can be picked up by future refines.
**Checkpoint**: `cortex/lifecycle/archive/lazy-apply-…/` exists; two new backlog items reference `discovery_source: 210-…`; `cortex/backlog/index.md` regenerated.

### Phase 2: Tier 1 docs refresh (tasks: 4, 5, 6, 7, 8)
**Goal**: Refresh user-facing install/upgrade docs to set the right MCP-tool-call-gated mental model and surface the `CORTEX_ALLOW_INSTALL_DURING_RUN=1` inline-only escape hatch; READMEs and setup.md reflect the two-layer auto-update chain accurately.
**Checkpoint**: `grep` acceptance gates on R5-R9 all pass; `install.sh:48` PATH hint unchanged.

### Phase 3: Tier 2 dispatch preflight (tasks: 9, 10)
**Goal**: Add fail-fast `command -v cortex-daytime-pipeline` preflight to `skills/lifecycle/references/implement.md §1a` Step 3 with positive-routing prose and dispatch-readiness-scoped rationale.
**Checkpoint**: Preflight in place; `bin/cortex-check-parity --audit` exits 0 with no new W003 for cortex-daytime-pipeline.

### Phase 4: Tier 3 wontfix terminal-state convention (tasks: 11)
**Goal**: Establish end-to-end terminal-wontfix recognition in a single composite task that authors all six coupled changes (workflow prose, Python detector, statusline bash mirror, parity fixture, parity test, events-registry row) and commits them atomically — preventing parity-test red interim states by construction rather than by prose-only enforcement.
**Checkpoint**: `pytest tests/test_lifecycle_phase_parity.py -k 'wontfix or feature_wontfix'` exits 0; `bin/cortex-check-events-registry --staged` exits 0; the resulting commit contains R12–R17 changes.

## Tasks

### Task 1: Backfill #145 to archive via `git mv` (R4)
- **Files**: `cortex/lifecycle/lazy-apply-cortex-cli-auto-update-via-sessionstart-probe-in-process-apply-on-invoke/` → `cortex/lifecycle/archive/lazy-apply-cortex-cli-auto-update-via-sessionstart-probe-in-process-apply-on-invoke/` (rename via `git mv`)
- **What**: Move the wontfix'd #145 lifecycle directory under `cortex/lifecycle/archive/` so `hooks/cortex-scan-lifecycle.sh:227`'s archive-skip drops it from SessionStart enumeration. The hand-edited `feature_wontfix` event row in `events.log:12` is preserved by `git mv`. R12 step (c) (backlog status update) is explicitly exempt per spec for this pre-workflow backfill.
- **Depends on**: none
- **Complexity**: simple
- **Context**: Use `git mv` (not `mv`) to preserve the file-rename history. The new path is `cortex/lifecycle/archive/lazy-apply-cortex-cli-auto-update-via-sessionstart-probe-in-process-apply-on-invoke/`. Pre-spec verification confirmed the source dir exists and contains an `events.log` with `feature_wontfix` at line 12.
- **Verification**: `test -d cortex/lifecycle/archive/lazy-apply-cortex-cli-auto-update-via-sessionstart-probe-in-process-apply-on-invoke && ! test -d cortex/lifecycle/lazy-apply-cortex-cli-auto-update-via-sessionstart-probe-in-process-apply-on-invoke && grep -q feature_wontfix cortex/lifecycle/archive/lazy-apply-cortex-cli-auto-update-via-sessionstart-probe-in-process-apply-on-invoke/events.log` exits 0.
- **Status**: [ ] pending

### Task 2: Run duplicate-check + create + enrich item 5 backlog item (R1 + R2)
- **Files**: `cortex/backlog/NNN-…installed-wheel-…md` (new file, NNN auto-assigned by `cortex-create-backlog-item`), `cortex/backlog/index.json` (auto-regenerated), `cortex/backlog/index.md` (auto-regenerated)
- **What**: Three sequential sub-steps:
  1. **R1 duplicate-check**: Run `ls cortex/backlog/ | grep -iE 'r8|cli_pin|cli-pin|wheel-commit|installed-wheel|maybe_check_upstream'`. If exit code is 0 (matches found), HALT the lifecycle's implement phase, surface the match list, and do NOT proceed to sub-steps 2–3. Pre-spec verification (2026-05-13) found zero matches; expected outcome is exit 1 (empty).
  2. **Create the item** via the real CLI: `cortex-create-backlog-item --title "R8 should track installed-wheel-commit, not CWD-working-tree HEAD (#146 follow-up)" --status backlog --type bug --priority medium`. The CLI auto-assigns the numeric ID, writes frontmatter (`schema_version`, `uuid`, `title`, `status`, `priority`, `type`, `created`, `updated`), and auto-runs `cortex_command.backlog.generate_index` to refresh `index.json` and `index.md` (unconditionally — not "may run"). Capture the new filename from `ls -1t cortex/backlog/[0-9]*.md | head -1`.
  3. **Enrich frontmatter and body**: Run `cortex-update-item <new-filename-stem> discovery_source=210-refresh-install-update-docs-close-mcp-only-auto-update-gaps.md "tags=[mcp, upgrade]"` (two arguments to one invocation; the quoted `"tags=..."` preserves the list format through shell parsing). Then use the Edit tool on the new file to append a body section after the closing `---` of the frontmatter — paraphrase the description from `cortex/backlog/210-…md` under "5. R8 cwd-vs-installed-wheel divergence" (the existing prose explaining `_maybe_check_upstream` resolves cortex_root via `git rev-parse --show-toplevel` from CWD, comparing working-tree HEAD to origin instead of installed-wheel pinned commit). Then re-run `just backlog-index` (or `cortex-generate-backlog-index`) to refresh the indexes with the new fields.
- **Depends on**: [1]
- **Complexity**: simple
- **Context**: The real CLI is `cortex-create-backlog-item` (not `cortex-backlog add` — there is no such command). Argparse contract: only `--title`, `--status`, `--type`, `--priority`, `--rework-of`, `--parent` are accepted; `--discovery-source`, `--tags`, and `--body` are NOT supported and must be added post-create via `cortex-update-item` and `Edit` respectively. `cortex-update-item` accepts arbitrary `key=value` pairs and writes them to the YAML frontmatter (proven by this lifecycle's own `cortex-update-item ... lifecycle_slug=refresh-install-update-docs-close-mcp` call). The Edit tool appends body content to the file after the `---` closing marker; do not edit the frontmatter via Edit (use `cortex-update-item` for frontmatter changes to preserve atomicity).
- **Verification**: `ls cortex/backlog/ | grep -iE 'r8|cli_pin|cli-pin|wheel-commit|installed-wheel|maybe_check_upstream'` (R1 pre-check) exits 1 AND, after sub-steps 2–3, the new file is present and well-formed: `python3 -c "import re, glob; files = glob.glob('cortex/backlog/*.md'); matches = [f for f in files if 'discovery_source: 210-refresh-install-update-docs-close-mcp-only-auto-update-gaps' in open(f).read() and 'installed-wheel' in open(f).read() and re.search(r'^uuid: [a-f0-9-]+', open(f).read(), re.M) and re.search(r'^status: backlog', open(f).read(), re.M) and re.search(r'^type: bug', open(f).read(), re.M) and re.search(r'^tags:', open(f).read(), re.M) and len(open(f).read()) > 800]; assert len(matches) == 1, f'expected 1 well-formed item 5 file, got {len(matches)}: {matches}'"` exits 0.
- **Status**: [ ] pending

### Task 3: Create + enrich item 6 backlog item (R3)
- **Files**: `cortex/backlog/NNN-cli-pin-drift-…md` (new file, NNN auto-assigned), `cortex/backlog/index.json` (auto-regenerated), `cortex/backlog/index.md` (auto-regenerated)
- **What**: Same shape as Task 2 sub-steps 2–3 (no separate R1 step — Task 2's duplicate check covers both items). Sub-steps: (i) run `cortex-create-backlog-item --title "CLI_PIN drift lint (#146 hygiene)" --status backlog --type chore --priority medium`; (ii) capture the new filename; (iii) run `cortex-update-item <new-filename-stem> discovery_source=210-refresh-install-update-docs-close-mcp-only-auto-update-gaps.md "tags=[mcp, upgrade]"`; (iv) Edit-append a body section paraphrasing "6. CLI_PIN drift lint" from `cortex/backlog/210-…md` (the existing prose about `plugins/cortex-overnight/server.py:105`'s hardcoded `CLI_PIN = ("v0.1.0", "1.0")` tuple silently staying stale when main advances).
- **Depends on**: [2]
- **Complexity**: simple
- **Context**: Same CLI semantics as Task 2. The discovery_source field is approximate (selected by elimination per spec Technical Constraints — `parent:` rejects UUID hyphens and ticket 210 is `chore` not `epic`); future refine of items 5/6 may revise.
- **Verification**: After sub-steps complete, `python3 -c "import re, glob; files = glob.glob('cortex/backlog/*.md'); matches = [f for f in files if 'discovery_source: 210-refresh-install-update-docs-close-mcp-only-auto-update-gaps' in open(f).read() and 'CLI_PIN' in open(f).read() and re.search(r'^uuid: [a-f0-9-]+', open(f).read(), re.M) and re.search(r'^status: backlog', open(f).read(), re.M) and re.search(r'^type: chore', open(f).read(), re.M) and re.search(r'^tags:', open(f).read(), re.M) and len(open(f).read()) > 600]; assert len(matches) == 1, f'expected 1 well-formed item 6 file, got {len(matches)}: {matches}'"` exits 0.
- **Status**: [ ] pending

### Task 4: Extend `docs/setup.md ## Upgrade & maintenance` with two-layer disambiguation (R6)
- **Files**: `docs/setup.md`
- **What**: Add a paragraph (or H3 subsection) inside `## Upgrade & maintenance` (existing H2 at line 188) that explicitly disambiguates the two upgrade layers: (a) marketplace auto-update at Claude Code startup refreshes the plugin file per Claude Code's plugin-marketplace mechanism (the cortex-overnight plugin's `CLI_PIN` is bumped here); (b) the next MCP tool call's pre-delegate auto-update orchestration in the cortex-overnight server runs R8 upstream-advance + R13 schema-floor checks and orchestrates `uv tool install --reinstall git+<url>@<tag>` synchronously on mismatch. Explicitly state the "MCP-tool-call-gated by design" framing, that Bash-tool subprocess dispatches don't trigger the upgrade check (per #145's wontfix), and that R10 (`implement.md §1a` preflight) is a fail-fast diagnostic for the intentional gap, not coverage. Cite `cortex/lifecycle/archive/decouple-mcp-server-from-cli-python-imports-own-auto-update-orchestration/spec.md`. **Do NOT name specific Python functions** (no `_ensure_cortex_installed` etc.).
- **Depends on**: none
- **Complexity**: simple
- **Context**: Existing `## Upgrade & maintenance` H2 starts at line 188 with content covering MCP auto-update mechanics + uv foot-guns + commands (lines 188-220). Place the new paragraph between the H2 intro and the existing `### uv foot-guns` H3 (~line 197). Spec R6 has the full required tokens for acceptance: `MCP-tool-call-gated`, `two-layer`/`two layers`, `#146` or `decouple-mcp-server`, `marketplace auto-update at Claude Code startup`, `pre-delegate`, `fail-fast preflight` or `R10`.
- **Verification**: `grep -q 'MCP-tool-call-gated' docs/setup.md && grep -q 'two-layer\|two layers' docs/setup.md && grep -q '#146\|decouple-mcp-server' docs/setup.md && grep -q 'marketplace auto-update at Claude Code startup' docs/setup.md && grep -q 'pre-delegate' docs/setup.md && grep -q 'fail-fast preflight\|R10' docs/setup.md && ! grep -q '_ensure_cortex_installed' docs/setup.md` exits 0.
- **Status**: [ ] pending

### Task 5: Add `CORTEX_ALLOW_INSTALL_DURING_RUN=1` callout to setup.md (R7)
- **Files**: `docs/setup.md`
- **What**: Add a boxed callout (or `### Carve-out` H3) inside `## Upgrade & maintenance` documenting the in-flight install guard and its inline-only bypass. Restate verbatim the `cortex/requirements/pipeline.md:154` contract — bypass is invoked inline as `CORTEX_ALLOW_INSTALL_DURING_RUN=1 uv tool install --reinstall git+<url>@<tag>`; **do NOT export**. List the four carve-outs (pytest, `CORTEX_RUNNER_CHILD=1`, dashboard, cancel-force). The callout MUST NOT show an `export` form of the variable.
- **Depends on**: [4]
- **Complexity**: simple
- **Context**: The canonical contract source is `cortex/requirements/pipeline.md:154`. Place the callout adjacent to (or inside) Task 4's two-layer paragraph for narrative continuity.
- **Verification**: `grep -q 'CORTEX_ALLOW_INSTALL_DURING_RUN' docs/setup.md && grep -q 'do NOT export' docs/setup.md && ! grep -E '^export CORTEX_ALLOW_INSTALL_DURING_RUN' docs/setup.md` exits 0.
- **Status**: [ ] pending

### Task 6: Extend `#### Verify install` H4 with PATH-not-found remediation (R8)
- **Files**: `docs/setup.md`
- **What**: Within the existing `#### Verify install` H4 (line 167, under `### 3. Per-repo setup`), add 1–2 sentences: if `cortex --print-root` returns `command not found`, run `uv tool update-shell` and reload your shell. Reference `install.sh:48` as the in-flight signal during install.
- **Depends on**: none
- **Complexity**: simple
- **Context**: The H4 at line 167 already uses `cortex --print-root` (no `--format json` flag). Add the remediation prose as a new paragraph at the end of the section, before the next heading.
- **Verification**: `awk '/^#### Verify install/,/^##? /' docs/setup.md | grep -q 'uv tool update-shell' && awk '/^#### Verify install/,/^##? /' docs/setup.md | grep -q 'command not found'` exits 0.
- **Status**: [ ] pending

### Task 7: Transform README line 24 to "Recommended:" markdown bullet (R5)
- **Files**: `README.md`
- **What**: Remove `README.md:24`'s code-fence comment (`# Recommended to turn on Auto-Update Marketplace Plugins (this will keep CLI auto updated as well)`) from inside the Quickstart code-fence. Add a normal-markdown "Recommended:" bullet immediately AFTER the Quickstart code-fence with two-layer mechanism wording (merged items 2+10): one bullet describing the marketplace startup refresh + next-MCP-tool-call CLI auto-update chain, cross-referencing `docs/setup.md#upgrade--maintenance`. **No Python function names in the bullet**.
- **Depends on**: [4, 5, 6]
- **Complexity**: simple
- **Context**: The cross-reference target (`docs/setup.md#upgrade--maintenance`) must have the disambiguated content (added by Task 4) before this bullet links to it. Bullet wording shape: "**Recommended:** turn on Auto-Update Marketplace Plugins. This refreshes the plugin file at Claude Code startup; the next MCP tool call triggers the cortex-overnight server's pre-delegate auto-update orchestration, which auto-updates the CLI via `uv tool install --reinstall`. See [docs/setup.md#upgrade--maintenance](docs/setup.md#upgrade--maintenance) for the two-layer mechanism."
- **Verification**: `! grep -q '# Recommended to turn on Auto-Update Marketplace Plugins' README.md && grep -q 'Recommended.*Auto-Update Marketplace Plugins' README.md && grep -q 'next MCP tool' README.md && grep -q 'upgrade--maintenance' README.md && ! grep -q '_ensure_cortex_installed' README.md` exits 0.
- **Status**: [ ] pending

### Task 8: Verify `install.sh:48` PATH hint stays intact (R9)
- **Files**: `install.sh` (read-only verification — no modification)
- **What**: Verify the explicit non-change requirement R9 — the trailing log line *"if 'cortex' is not on your PATH, run 'uv tool update-shell' and reload your shell."* still appears in `install.sh` at line 48 after all Phase 2 edits. This task is verification-only; no commit needed.
- **Depends on**: [4, 5, 6, 7]
- **Complexity**: trivial
- **Context**: Phase 2 docs work does not modify `install.sh`, but the hub-and-spoke trim discussion in research raised the possibility — this task makes the non-change explicit.
- **Verification**: `grep -qF "if 'cortex' is not on your PATH, run 'uv tool update-shell' and reload your shell." install.sh` exits 0.
- **Status**: [ ] pending

### Task 9: Add `command -v cortex-daytime-pipeline` preflight to `implement.md §1a` Step 3 (R10)
- **Files**: `skills/lifecycle/references/implement.md`
- **What**: Insert a Bash preflight check immediately before the `cortex-daytime-pipeline --feature {slug}` dispatch line at lines 88–91. The check shape: `command -v cortex-daytime-pipeline >/dev/null 2>&1 || { echo "cortex-daytime-pipeline not on PATH. Install or upgrade: uv tool install --reinstall git+https://github.com/charleshall888/cortex-command.git@v0.1.0" >&2; exit 1; }`. Surrounding prose uses positive-routing phrasing (no MUST/REQUIRED escalation). Describe as a separate Bash call (not chained with the dispatch line via `&&`). Add prose acknowledging this is the first fail-fast usage in skill prose, scoped to dispatch-readiness rationale (not as a general `command -v` precedent — `complete.md:39,42` remains warn-and-continue).
- **Depends on**: none
- **Complexity**: simple
- **Context**: `implement.md §1a` Step 3 sits at lines 76–100, with the dispatch line at 88–91. Surrounding control flow includes existing guards at lines 62–67 (double-dispatch) and 69–74 (overnight concurrent) — both use the "separate Bash call" pattern. The new preflight follows the same convention. Reference precedent: `skills/lifecycle/references/complete.md:39,42` for inline `command -v` usage (warn-and-continue posture there; here is fail-fast per dispatch-readiness criticality).
- **Verification**: `grep -q 'command -v cortex-daytime-pipeline' skills/lifecycle/references/implement.md && grep -q 'uv tool install --reinstall' skills/lifecycle/references/implement.md && awk '/§1a/,/§2/' skills/lifecycle/references/implement.md | grep -q 'command -v cortex-daytime-pipeline'` exits 0.
- **Status**: [ ] pending

### Task 10: Run parity audit + confirm no W003 for new preflight (R11)
- **Files**: none modified (audit-only)
- **What**: Run `bin/cortex-check-parity --audit` and confirm exit 0 with no W003 warning naming `cortex-daytime-pipeline` introduced by Task 9's change. Per spec, `bin/cortex-check-parity:431` already tracks the `command -v cortex-*` pattern — no new exception expected.
- **Depends on**: [9]
- **Complexity**: trivial
- **Context**: Parity check verifies that every `bin/cortex-*` reference in skill prose has a matching script (or vice versa) and that orphan references emit W003.
- **Verification**: `bin/cortex-check-parity --audit` exits 0 (or the project's `just`-recipe form — check `justfile` for the exact invocation).
- **Status**: [ ] pending

### Task 11: Phase 4 wontfix terminal-state convention — single composite task (R12 + R13 + R14 + R15 + R16 + R17)
- **Files**: `skills/lifecycle/references/wontfix.md` (new), `cortex_command/common.py`, `claude/statusline.sh`, `tests/fixtures/lifecycle_phase_parity/events-feature-wontfix/events.log` (new), `tests/test_lifecycle_phase_parity.py`, `bin/.events-registry.md`, plus optionally a 2-3 line cross-reference in `skills/lifecycle/SKILL.md`
- **What**: Author all six R12–R17 changes in a single composite task within one Claude session, then commit atomically via `/cortex-core:commit`. This collapses the previous deferred-commit choreography (Tasks 11–17) into one task to eliminate the prose-only-enforcement failure mode under autonomous execution. The six sub-edits, in this exact order:
  1. **(R12) Write the wontfix workflow prose** to a NEW file `skills/lifecycle/references/wontfix.md` (host file pinned — no SKILL.md vs wontfix.md branching). Document the three-step workflow IN ORDER: (a) `git mv cortex/lifecycle/{feature} cortex/lifecycle/archive/{feature}` FIRST (so the archive-skip at `hooks/cortex-scan-lifecycle.sh:227` immediately drops the lifecycle from SessionStart enumeration before any later step can fail); (b) append a `feature_wontfix` event to `cortex/lifecycle/archive/{feature}/events.log` (single-line JSONL with `ts`, `event`, `feature`, optional `reason` fields — include a literal example with `"event": "feature_wontfix"` so the events-registry scanner picks it up); (c) `cortex-update-item {backlog-slug} status=wontfix lifecycle_phase=wontfix session_id=null`. State explicitly that step order is load-bearing and that R13's detector patch is defense-in-depth for archive-internal phase queries. Also add a 2-3 line cross-reference in `skills/lifecycle/SKILL.md` to `references/wontfix.md` (per Wiring Co-Location convention — the new reference file needs a SKILL.md mention).
  2. **(R13) Patch `cortex_command/common.py:_detect_lifecycle_phase_inner`** JSON-parsing loop at lines 216–235. Add `elif event_type == "feature_wontfix":` branch alongside the existing `elif event_type == "phase_transition":` (lines 230–235). Declare a `feature_wontfix_seen = False` flag near the other flag inits (lines 218–221 region); set it to `True` in the new elif. After the loop completes, check `feature_wontfix_seen` first and return `phase=complete` if true — BEFORE the existing `feature_complete` substring scan at line 249, which stays as a fallback for legacy logs. Implementation uses exact-match `event_type ==`, not substring scan.
  3. **(R14) Mirror in `claude/statusline.sh:403`**. Add a sibling shell line that greps for `'"feature_wontfix"'` in `_lc_fdir/events.log` and sets `_lc_phase="complete"` on match. Place immediately adjacent to the existing line-403 `feature_complete` grep, inside the R12b structural-exception bash ladder (lines 376–389).
  4. **(R15) Add parity fixture** at `tests/fixtures/lifecycle_phase_parity/events-feature-wontfix/events.log` containing a single-line JSON `{"event": "feature_wontfix", "feature": "x"}`. Verify the directory layout against `tests/test_lifecycle_phase_parity.py`'s existing fixture-discovery convention — adjust path if needed.
  5. **(R16) Update `tests/test_lifecycle_phase_parity.py`** to add a fixture-based test case asserting both Python `detect_lifecycle_phase` and the statusline bash ladder return `phase=complete` for the new fixture. Cover the three parity layers (R12a/R12b/R12c equivalents) — match the existing parity test's pattern.
  6. **(R17) Register `feature_wontfix` in `bin/.events-registry.md`** with: scope `per-feature-events-log`; scan coverage `gate-enforced`; **producers** `skills/lifecycle/references/wontfix.md` (pinned to the wontfix.md host); consumers `cortex_command/common.py:_detect_lifecycle_phase_inner` and `claude/statusline.sh`; status `live`; rationale "Terminal-state marker for operator-decided wontfix; lifecycle moves to `cortex/lifecycle/archive/`, detector treats event as terminal for transient-state safety." After all six edits, verify `pytest tests/test_lifecycle_phase_parity.py` and `bin/cortex-check-events-registry --staged` both exit 0, then `git add` the seven file paths explicitly (no `-A`/`.`) and invoke `/cortex-core:commit` with subject "Land Phase 4 wontfix terminal-state convention (#210 R12–R17)" and a body summarizing the six-way coupled change set.
- **Depends on**: none (Phase 4 is intra-coupled; no external dependencies)
- **Complexity**: complex (4+ files, architectural change introducing new terminal-state convention)
- **Context**: This task supersedes the original Tasks 11–17 deferred-commit choreography flagged by critical-review as structurally broken under autonomous execution. Collapsing to one task makes the six-way atomicity a single-session property: the task spans one Claude session that authors all changes, runs the gates, and commits — eliminating the "fresh sub-agent per task" failure mode. The host file is pinned to `skills/lifecycle/references/wontfix.md` (a new file) to eliminate Task 15's contradictory size-budget criteria; the wontfix.md choice produces unambiguous producer-attribution in the registry row. Sources verified during critical-review: `cortex_command/common.py:216–235` JSON-parsing loop structure (review_verdict, spec_approved, plan_approved, phase_transition branches); `claude/statusline.sh:403` `feature_complete` grep line; `tests/test_lifecycle_phase_parity.py:300–347` parity test structure; `bin/.events-registry.md:11–12` row shape (lifecycle_cancelled at line 22 as a precedent for the producer/consumer column format).
- **Verification**: All six gates pass in sequence: (a) `test -f skills/lifecycle/references/wontfix.md` AND step-order check `python3 -c "import pathlib; src = pathlib.Path('skills/lifecycle/references/wontfix.md').read_text(); gm = src.find('git mv'); ev = src.find('feature_wontfix'); ui = src.find('cortex-update-item'); assert 0 <= gm < ev < ui, f'order check failed: git-mv={gm} event={ev} update={ui}'"` exits 0 AND `grep -q '\"event\": \"feature_wontfix\"' skills/lifecycle/references/wontfix.md` AND `grep -q 'references/wontfix' skills/lifecycle/SKILL.md`; (b) `python3 -c "import cortex_command.common as c; import tempfile, pathlib, json; d = pathlib.Path(tempfile.mkdtemp())/'feat'; d.mkdir(); (d/'events.log').write_text(json.dumps({'event': 'feature_wontfix', 'feature': 'x'}) + chr(10)); assert c.detect_lifecycle_phase(str(d))['phase'] == 'complete'"` exits 0 AND `grep -n 'event_type == \"feature_wontfix\"' cortex_command/common.py | wc -l` ≥ 1; (c) `grep -A2 '\"feature_wontfix\"' claude/statusline.sh | grep -q '_lc_phase=\"complete\"'`; (d) `test -f tests/fixtures/lifecycle_phase_parity/events-feature-wontfix/events.log && grep -q feature_wontfix tests/fixtures/lifecycle_phase_parity/events-feature-wontfix/events.log`; (e) `pytest tests/test_lifecycle_phase_parity.py` exits 0 AND `pytest tests/test_lifecycle_phase_parity.py -v -k 'wontfix or feature_wontfix'` reports ≥1 test passing; (f) `grep -q feature_wontfix bin/.events-registry.md` AND `grep -B2 -A4 'feature_wontfix' bin/.events-registry.md | grep -q 'references/wontfix'` (producer column references wontfix.md, not SKILL.md) AND `bin/cortex-check-events-registry --staged` exits 0; (g) commit hygiene — `git log -1 --name-only --pretty=format: HEAD | grep -cE '^(cortex_command/common\.py|claude/statusline\.sh|tests/fixtures/lifecycle_phase_parity/events-feature-wontfix/events\.log|tests/test_lifecycle_phase_parity\.py|skills/lifecycle/references/wontfix\.md|bin/\.events-registry\.md|skills/lifecycle/SKILL\.md)$'` returns ≥6 AND `git log -1 --pretty=%s HEAD | grep -qE 'wontfix|R12.*R17'`.
- **Status**: [ ] pending

## Risks

- **Task 11's seven-file scope exceeds the 1-5 file sizing target.** Justified by the spec's single-commit-landing Technical Constraint — splitting would re-introduce the deferred-commit fragility the rewrite explicitly eliminates. The complexity tier is `complex` (not `simple`) to match the multi-file architectural shape. Estimated duration: 45-60 min for a single composite session, vs the original 7-task choreography's ~75 min wall-clock with prose-only-enforcement risk.
- **Task 11's verification gates are extensive (seven sub-checks).** Acceptable because each gate validates a different artifact and the gates run quickly. If any sub-gate fails mid-task, the task halts and the partial work stays uncommitted (working-tree state, not committed history) — easy to recover by fixing the failing gate and re-running verification.
- **Task 2's R1 halt-on-duplicate branch defines no recovery path beyond "surface and resolve."** In autonomous overnight mode, R1 halts the lifecycle's implement phase and surfaces the match list in `daytime.log` for morning review. Tasks 3 and onward block per the dependency graph. The lifecycle resumes after the operator manually resolves the duplicate (either confirming all matches are false positives and re-running, or merging the new item into an existing one). This is the intended autonomous-mode semantics — humans handle duplicate resolution; the system does not auto-resolve.
- **Items 5/6's `discovery_source: 210-…` is approximate per spec.** A future refine of items 5 or 6 may revise the field. This plan does not preempt that future work.
- **R10 introduces the first fail-fast usage in skill prose.** Future skill authors copying the pattern uncritically to non-dispatch-readiness contexts would erode the precedent. Mitigation is documentation-only per spec R10 prose — no structural gate prevents misuse.
- **Task 11's "host file pinned to wontfix.md" decision** is the critical-review-driven simplification of the previous SKILL.md-vs-wontfix.md branch. Side effect: SKILL.md must gain a 2-3 line cross-reference per the Wiring Co-Location convention, which is included as part of sub-step 1.

## Acceptance

The lifecycle is complete when: (1) `cortex/lifecycle/archive/lazy-apply-…/` exists and the source path is gone (R4 via Task 1); (2) two new backlog items reference `discovery_source: 210-refresh-install-update-docs-close-mcp-only-auto-update-gaps.md` with well-formed frontmatter (`uuid`, `status: backlog`, `type: bug` for item 5 / `type: chore` for item 6, `tags:` populated) and substantive body content (>600 chars per Task 3, >800 per Task 2) — R2, R3 via Tasks 2, 3; (3) `docs/setup.md` contains the two-layer disambiguation paragraph + CORTEX_ALLOW_INSTALL_DURING_RUN callout + PATH-not-found remediation, and `README.md:24`'s code-fence comment is gone and replaced by a "Recommended:" markdown bullet cross-referencing setup.md (R5–R8 via Tasks 4–7); (4) `install.sh:48` PATH hint stays intact (R9 via Task 8); (5) `skills/lifecycle/references/implement.md §1a` Step 3 has the `command -v cortex-daytime-pipeline` preflight (R10 via Task 9) and `bin/cortex-check-parity --audit` passes (R11 via Task 10); (6) Phase 4's six-way coupled change set is in a single commit (R12–R17 via Task 11), with `pytest tests/test_lifecycle_phase_parity.py` passing and `bin/cortex-check-events-registry --staged` passing.
