# Plan: single-source-research-skill-fan-out

## Overview
Execute the seven verified epic-#347 audit verdicts (s3, s4, s6, s7, s13, s17, s18) as **pure deletions from `skills/research/SKILL.md`**, single-sourcing the fan-out prose against the canonical `skills/research/references/fanout.md` (which is **not** edited) and folding the terminal Step 5 routing into Step 1. Every cut target already resides in fanout.md, so the trim removes duplication without adding anything anywhere. Three phases map 1:1 to the spec; each task edits canonical + regenerates the cortex-core mirror + commits both trees, and every commit re-checks the cross-cutting invariants R10 (fanout.md byte-identical) and R11 (mirror parity + pointer URL form + frontmatter untouched).

## Outline

### Phase 1: Considerations hand-off single-sourcing (s3 + s6) (tasks: 1)
**Goal**: Merge the two duplicate considerations-contract statements (Step 1 L45 + Step 3 L61–64) into one canonical occurrence and drop the two derivable example-invocation bullets, keeping L45 verbatim.
**Checkpoint**: SKILL.md Steps 1+3 single-sourced; R1/R2/R3 greps pass; `just test` green; R10/R11 hold; committed.

### Phase 2: fanout.md-resident dedup (s4, s7, s13) (tasks: 2, 3, 4)
**Goal**: Cut the inline grid, angle-roster, and dispatch rationale that fanout.md already owns; keep the pointers, the upper-bound rider, the `(core)`/`(always last…)` h4 tags, and every ADR-0023 runnable mechanism line.
**Checkpoint**: grid/roster/rationale removed; R4/R5/R6/R7 greps pass; `just test` green; R10/R11 hold; committed.

### Phase 3: Output-structure strip + routing fold (s17, s18) (tasks: 5, 6)
**Goal**: Strip the duplicate bracketed annotations from the Output-structure skeleton and fold Step 5 "Route Output" into Step 1's mode-detection block.
**Checkpoint**: annotations stripped + Step 5 folded (Steps 1–4 contiguous); R8/R9 greps pass; `just test` green; R10/R11 hold; committed.

## Shared execution notes (all tasks)
- **Files edited per task**: `skills/research/SKILL.md` (canonical, the only hand-edit) + `plugins/cortex-core/skills/research/SKILL.md` (mirror, machine-regenerated). Sequence each task: edit canonical → `just build-plugin` (regenerates the mirror) → stage **both trees** → verify → commit via `/cortex-core:commit`.
- **Mirror-drift coupling**: the `.githooks/pre-commit` drift loop fails the commit if the mirror is stale and does **not** auto-stage. Never defer the mirror regen — `just build-plugin` and stage both trees inside the same task that edits canonical.
- **Concurrent-session commit scoping**: the working tree carries uncommitted work from a concurrent #349 session (`cortex/backlog/349-*.md` modified, `cortex/lifecycle/restructure-commit-skill-lazy-load-release/` untracked) plus this feature's own modified `cortex/backlog/350-*.md`. `/cortex-core:commit` runs a full-tree preflight, stages "relevant files" by its own judgment, and finalizes with `git commit -m` on the **whole staged index** — it does **not** apply a commit-scoped pathspec, so "stage only X" upstream is not self-enforcing. Discipline per task: (i) direct the commit step to stage **only** `skills/research/SKILL.md` and `plugins/cortex-core/skills/research/SKILL.md` — explicitly exclude both backlog files and every #349 path; (ii) before it finalizes, confirm `git diff --cached --name-only` lists exactly those two paths; (iii) after commit, confirm `git show --stat HEAD` names only those two files. The #350 backlog write-back and the lifecycle dir are committed separately at their own gates, never folded into a trim commit.
- **fanout.md is never edited** (R10). Every retained pointer keeps its full `](${CLAUDE_SKILL_DIR}/references/fanout.md)` URL-target form (R11d).
- **R10 baseline** = feature-base commit `f68b3b19` (HEAD at plan time; fanout.md's own last commit is `8b65e414`). R10 check: `git diff f68b3b19 -- skills/research/references/fanout.md` is empty (compares baseline↔working tree, so it catches an errant edit whether or not it was committed — a working-tree-only diff would not).
- These six tasks all edit the same file and are therefore a **strict sequential chain** (`Depends on [prev]`); no two may run in the same batch.

## Tasks

### Task 1: s3 + s6 — single-source the considerations hand-off (Phase 1)
- **Files**: `skills/research/SKILL.md`, `plugins/cortex-core/skills/research/SKILL.md`
- **What**: Delete Step 1's two example-invocation bullets (L31–33, derivable from `argument-hint`). Merge Step 1's reader-contract paragraph (L45) with Step 3's `### Considerations injection` paragraph (L61–63) into ONE canonical statement, keeping **L45 essentially verbatim** as the survivor; trim the Step-3 injection paragraph to per-angle applicability + a "see Step 1" pointer.
- **Depends on**: none
- **Complexity**: simple
- **Context**: Keep in Step 3's injection paragraph BOTH per-angle applicability arms with their *why* — core-only; **not Tradeoffs** (orthogonal/unnarrowed); **not Adversarial** (works on summaries). Cut from Step 3 the content-not-path / empty-file clauses (duplicated from Step 1) and the h3-nesting/placement *How* (structurally demonstrated by the three `### Considerations to investigate alongside the primary scope` headings already present in the fenced core templates at L87–88 / L111–112 / L131–132). Preserve the reader-contract halt clause "do not halt on a missing file" (R3) — the suite's `do\s+not\s+halt` regex is non-discriminating (also matches the s13-kept L192 fallback), so this is verified by grep, not the suite. Do **not** rewrite L45: Step 3's L63 wording ("injects its content" / "inject nothing") does NOT match the `substitut\w+ its literal content` / `no…injection` red-green regexes — keeping L63 as survivor flips two tests red (spec Edge Case 1). Do **not** touch the three `{research_considerations_bullets}` placeholders in the fenced templates (count must stay 3). Every surviving `research-considerations-file` mention stays exactly hyphenated — a space or `-bullets` variant trips `test_no_stale_bare_value_key`.
- **Verification**: (a) `just test` — pass if exit 0. (b) *Keeps survived* — `grep -c "substitutes its literal content" skills/research/SKILL.md` = 1 AND `grep -c "no considerations injection occurs" skills/research/SKILL.md` = 1 AND `grep -c "do not halt on a missing" skills/research/SKILL.md` = 1 AND `grep -c "not Tradeoffs" skills/research/SKILL.md` ≥ 1 AND `grep -c "not Adversarial" skills/research/SKILL.md` ≥ 1 AND `grep -c "{research_considerations_bullets}" skills/research/SKILL.md` = 3. (b2) *Cuts happened (discriminates done-vs-no-op; `just test` is green pre-edit and cannot)* — `grep -c "standalone, no lifecycle-slug" skills/research/SKILL.md` = 0 (both example bullets deleted, s3) AND `grep -c "nests below the agents" skills/research/SKILL.md` = 0 (Step-3 placement-How cut, s6) AND `grep -Fc "never the path" skills/research/SKILL.md` = 1 (was 2 — L45 survivor keeps it, the Step-3 content-not-path dup is cut, s6). (b3) *Frontmatter untouched* — `grep -c "^argument-hint:" skills/research/SKILL.md` = 1 (the field the deleted examples are "derivable from" must itself survive). (c) R10: `git diff f68b3b19 -- skills/research/references/fanout.md` empty. (d) R11: `diff skills/research/SKILL.md plugins/cortex-core/skills/research/SKILL.md` exit 0 AND `grep -Fc '](${CLAUDE_SKILL_DIR}/references/fanout.md)' skills/research/SKILL.md` = 3 AND `bin/cortex-measure-l1-surface research` = 379.
- **Status**: [ ] pending

### Task 2: s4 — collapse the Step 2 agent-count grid (Phase 2)
- **Files**: `skills/research/SKILL.md`, `plugins/cortex-core/skills/research/SKILL.md`
- **What**: Cut the inline floor(3)/corner(10)/monotonic restatement from Step 2 (L49); keep the fanout.md pointer sentence plus exactly one "upper bound on investigation breadth, not a quota — dispatch fewer…" rider.
- **Depends on**: [1]
- **Complexity**: simple
- **Context**: Mirror the condensed sibling `skills/discovery/references/research.md:41-43`, which already made this "pointer + one retained rider" reduction with no inline floor/corner numbers. Removing the inline 3/10 makes the Step-2 fanout.md Read effectively unconditional (a simple+low run can no longer read `3` inline and skip fanout.md) — this is the intended read-path delta and the residency backstop that keeps s7's cut safe. `test_research_fanout_matrix.py` pins fanout.md only (zero SKILL.md surface), so this verdict has no test pin — the greps below are the guard.
- **Verification**: (a) `just test` — pass if exit 0. (b) `grep -c "floor cell" skills/research/SKILL.md` = 0 AND `grep -c "upper bound on investigation breadth, not a quota" skills/research/SKILL.md` ≥ 1. (c) R10: `git diff f68b3b19 -- skills/research/references/fanout.md` empty. (d) R11: mirror `diff` exit 0 AND `grep -Fc '](${CLAUDE_SKILL_DIR}/references/fanout.md)' skills/research/SKILL.md` = 3 AND `bin/cortex-measure-l1-surface research` = 379.
- **Status**: [ ] pending

### Task 3: s7 — collapse the angle-selection restatement (Phase 2)
- **Files**: `skills/research/SKILL.md`, `plugins/cortex-core/skills/research/SKILL.md`
- **What**: Cut the restated mandatory-core-roster / adversarial-last / no-keyword-router rules from the `### Angle selection` block (L65–73); keep the count arithmetic, the template index, the fanout.md authority pointer, and the `(core)` / `(always last for high/critical)` h4 tags.
- **Depends on**: [2]
- **Complexity**: simple
- **Context**: The `(core)`/`(always last…)` h4 tags structurally re-encode the roster + ordering, so cutting the prose restatement loses nothing testable. The one-way risk: the negative "no topic→angle keyword router" rule has **no test pin and no structural echo** — after this cut it lives solely in fanout.md, reachable at dispatch only via the Step-2 fanout.md residency (Task 2's backstop). The retained L67 pointer ("the hybrid-angle-selection section of fanout.md") is the sole surviving in-repo reference (R6, a disclosed residual risk accepted under What/Why-not-How). Keep that pointer token exactly — a generic "Apply it." imperative is not region-discriminating and is not an acceptable proxy.
- **Verification**: (a) `just test` — pass if exit 0. (b) *Keeps survived* — `grep -c "hybrid-angle-selection" skills/research/SKILL.md` ≥ 1 (fanout.md authority pointer) AND `grep -Fc "(core)" skills/research/SKILL.md` = 3 (h4 core tags, all at L75/98/121 outside the cut region). (b2) *Cuts happened + ordering tag survives (discriminates done-vs-no-op)* — `grep -c "keyword router" skills/research/SKILL.md` = 0 (the no-keyword-router prose is cut; it now lives only in fanout.md, R6) AND `grep -Fc "always last for high/critical" skills/research/SKILL.md` = 1 (was 2: the L73 adversarial-last *prose* is cut, the L160 `#### Adversarial (always last for high/critical)` h4 tag survives — this single grep proves both the cut happened and the ordering encoding remains). (c) R10: `git diff f68b3b19 -- skills/research/references/fanout.md` empty. (d) R11: mirror `diff` exit 0 AND `grep -Fc '](${CLAUDE_SKILL_DIR}/references/fanout.md)' skills/research/SKILL.md` = 3 AND `bin/cortex-measure-l1-surface research` = 379.
- **Status**: [ ] pending

### Task 4: s13 — trim the dispatch-protocol rationale (Phase 2)
- **Files**: `skills/research/SKILL.md`, `plugins/cortex-core/skills/research/SKILL.md`
- **What**: In `### Dispatch protocol` (L182–195), cut BOTH the "error-correction layer" clause and the "judgment-inherit contract" rationale (L193) and the "no second wave" closer (L195); keep every ADR-0023 mechanism.
- **Depends on**: [3]
- **Complexity**: simple
- **Context**: ADR-0023 requires each consuming orchestrator body to carry its own runnable resolve+bind, so s13 keeps the mechanism inline and cuts **only** the restated rationale (both re-encoded in fanout.md L33–35). Must survive: the runnable `model=$(cortex-resolve-model --role searcher)` line, the core-wave `model:` bind, the read-only/no-worktree note, wave ordering, and the **full degrade-loud fallback** ("fall back to dispatching the core wave with no `model:`…"). No test pins the fallback, so its grep below is the sole guard. s13 is pure-rationale removal with no runtime dependency — it does **not** lean on fanout.md residency (unlike s7).
- **Verification**: (a) `just test` — pass if exit 0. (b) *All five must-survive ADR-0023 mechanisms are guarded, not just the resolve line* — `grep -c 'cortex-resolve-model --role searcher' skills/research/SKILL.md` ≥ 1 (resolve) AND `grep -Fc "as each core-wave Agent" skills/research/SKILL.md` ≥ 1 (core-wave `model:` bind) AND `grep -Fc "agents are read-only" skills/research/SKILL.md` ≥ 1 (read-only/no-worktree note) AND `grep -Fc "Adversarial wave (last)" skills/research/SKILL.md` ≥ 1 (wave ordering) AND `grep -c "fall back to dispatching the core wave" skills/research/SKILL.md` ≥ 1 (fallback head) AND `grep -Fc "gather wave is running on the inherited model" skills/research/SKILL.md` ≥ 1 (fallback tail — this guards the warning+do-not-halt clause that abuts the cut, so an over-cut into L192 fails here even though the head grep is only a prefix). (b2) *Rationale cut (both clauses, not one half)* — `grep -c "judgment-inherit contract" skills/research/SKILL.md` = 0 AND `grep -c "error-correction layer" skills/research/SKILL.md` = 0. (c) R10: `git diff f68b3b19 -- skills/research/references/fanout.md` empty. (d) R11: mirror `diff` exit 0 AND `grep -Fc '](${CLAUDE_SKILL_DIR}/references/fanout.md)' skills/research/SKILL.md` = 3 AND `bin/cortex-measure-l1-surface research` = 379.
- **Status**: [ ] pending

### Task 5: s17 — strip the Output-structure annotations (Phase 3)
- **Files**: `skills/research/SKILL.md`, `plugins/cortex-core/skills/research/SKILL.md`
- **What**: In the `### Output structure` skeleton (L216–237), strip the bracketed annotations that re-explain the angle-driven roster (dup of Step 4's opening), re-quote the empty-agent warning, and restate the escalator parse; keep the skeleton headings, the `## Considerations Addressed` definition body, and the `## Open Questions` omit-note.
- **Depends on**: [4]
- **Complexity**: simple
- **Context**: Keep the three skeleton heading lines (`## <Angle name>`, `## Open Questions`, `## Considerations Addressed`). Retain the `## Open Questions` one-line semantics note **including** "Omit this section if no open questions exist" (sole occurrence repo-wide, L233). Retain the whole `## Considerations Addressed` **definition body** (the non-empty-AND-lifecycle-mode condition, the `deferred — no relevant evidence found` fallback, and the "Appears after `## Open Questions`" placement) — the `= 1` grep below pins only the sentinel clause "One bullet per input consideration"; it is a done-vs-no-op discriminator, not full-body protection, so preserve the surrounding clauses by reading, not by grep. The empty-agent warning is deliberately de-duplicated here: after s17 the `⚠️ …returned no findings` warning exists only at its **Step-4 source** (L207, under `## Step 4: Synthesize Findings` → `### Empty/failed agent handling`), and s15's section is kept verbatim (see Non-Requirements) so that cross-reference still resolves.
- **Verification**: (a) `just test` — pass if exit 0. (b) *Keeps survived* — `grep -c "Omit this section if no open questions exist" skills/research/SKILL.md` = 1 AND `grep -c "One bullet per input consideration" skills/research/SKILL.md` = 1. (b2) *Annotations actually stripped (discriminates done-vs-no-op)* — `grep -Fc "this section may be incomplete" skills/research/SKILL.md` = 1 (was 2 — the Output-structure bracket re-quote of the warning is cut; the Step-4 L207 source keeps the sole surviving copy) AND `grep -Fc "Core always present" skills/research/SKILL.md` = 0 (the roster re-explanation bracket, a dup of Step 4's opening, is cut). (c) R10: `git diff f68b3b19 -- skills/research/references/fanout.md` empty. (d) R11: mirror `diff` exit 0 AND `grep -Fc '](${CLAUDE_SKILL_DIR}/references/fanout.md)' skills/research/SKILL.md` = 3 AND `bin/cortex-measure-l1-surface research` = 379.
- **Status**: [ ] pending

### Task 6: s18 — fold Step 5 into Step 1 (Phase 3)
- **Files**: `skills/research/SKILL.md`, `plugins/cortex-core/skills/research/SKILL.md`
- **What**: Fold `## Step 5: Route Output` (L239–251) into Step 1's mode-detection block, cutting the mkdir/write/announce narration. Remove the `## Step 5` heading; Steps 1–4 stay contiguous (Step 5 was terminal — no renumbering, no external "see Step 5" citation exists).
- **Depends on**: [5]
- **Complexity**: simple
- **Context**: Place the folded capital-S `**Standalone mode**` anchor (exact case, exactly one occurrence) **after** Step 1's considerations-file paragraph (the s3-preserved L45), because `test_standalone_reads_nothing` slices from the capital-S anchor to the next `## ` H2 (`###` does NOT terminate the slice — the regex needs a space in the 4th char) and asserts that span contains no `research-considerations-file` / `read (that|the) file`. Keep the folded Lifecycle-mode block **before** the Standalone-mode anchor so it falls outside the slice. Keep Step 1's existing lowercase `**standalone mode**` mode-detection bullet lowercase (do not merge it into the anchor). The folded Standalone routing text ("present in conversation / write no file / no directory") is already free of the pinned tokens. Confirmed no external file cites *research's* "Step 5": the repo's other `(Step 5)` cross-references all point at other skills' own Step 5 (chiefly `skills/refine/SKILL.md`, plus `skills/overnight/SKILL.md`), so removing research's heading dangles no cross-reference. (The `^## Step 5` = 0 grep below is itself a done-vs-no-op removal proof — it fails if the heading is not removed.)
- **Verification**: (a) `just test` — pass if exit 0 (`test_standalone_reads_nothing` passes). (b) `grep -c '^\*\*Standalone mode\*\*' skills/research/SKILL.md` = 1 AND `grep -c '^## Step 5' skills/research/SKILL.md` = 0. (c) R10: `git diff f68b3b19 -- skills/research/references/fanout.md` empty. (d) R11: mirror `diff` exit 0 AND `grep -Fc '](${CLAUDE_SKILL_DIR}/references/fanout.md)' skills/research/SKILL.md` = 3 AND `bin/cortex-measure-l1-surface research` = 379.
- **Status**: [ ] pending

## Risks
- **Spec R11(c) baseline is factually wrong** — it asserts the `research` L1 surface is "502"; the verified current value is **379** (post-#302 description revert). This plan pins 379 in every task's R11 check. Since the frontmatter is untouched, 379 must hold unchanged throughout. (Decision — corrected against `bin/cortex-measure-l1-surface research`.)
- **Verification-vacuity through-line** — the shared failure mode of these trim tickets, and the axis critical-review flagged hardest. `just test` is **green on the unedited file** (its regexes guard the prior #337 migration, not #350's deletions) and `bin/cortex-measure-l1-surface research` is a constant 379 regardless of body edits, so for the pure-deletion tasks *neither* backstops done-vs-no-op. This plan therefore pairs every task's *keep* greps with **removal proofs** — `= 0` / count-decrease assertions on tokens unique to the cut prose (`standalone, no lifecycle-slug`, `keyword router`, `Core always present`, `never the path` 2→1, `always last for high/critical` 2→1, `this section may be incomplete` 2→1, `^## Step 5` = 0) — and completes Task 4's mechanism set so all five ADR-0023 items are guarded, not just the resolve line. A skipped or half-done cut now fails its own task.
- **s15 is deliberately untouched** — its compression is declined and resolved (owned by #350, not deferred to #353); the Step-4 "Empty/failed agent handling" section (L203–210, in the gap between s13's L182–195 and s17's L216–237) ships verbatim. No task edits it.
- **Whole-plugin drift-block (conditional, not currently live)** — `just build-plugin` rsyncs the *entire* cortex-core skill set, not just `research`. If the concurrent #349 session (which edits `skills/commit/SKILL.md`) has an **uncommitted** `skills/commit/` change when a #350 task commits, the pre-commit hook rebuilds it into the mirror and the drift check fails on a file #350 never touched — blocking the commit. #349 is currently at Plan with a clean `skills/` tree, so this is dormant. Mitigation: run #350's implement to completion while #349 is idle, or have #349 commit its `skills/commit/` edits first; if a commit fails on `plugins/cortex-core/skills/commit/` drift, that is this coupling, not a #350 defect.

## Acceptance
With all six tasks complete: `skills/research/SKILL.md` is single-sourced against fanout.md (all seven verdicts applied, Steps 1–4 contiguous with Step 5 folded) and measurably shorter — the deliverable is weighted-token/byte reduction, so the file is smaller by bytes (several cuts are mid-line, so the whole-line delta is modest and is not gated by any grep); every task's removal proof (above) confirms its cut landed; `git diff f68b3b19 -- skills/research/references/fanout.md` is empty (fanout.md byte-identical); the cortex-core mirror is byte-identical to canonical; `bin/cortex-measure-l1-surface research` is unchanged at 379; all eleven requirement greps (R1–R11) pass; and `just test` exits 0.
