# Review: fix-pre-existing-scan-lifecycle-test-failures-in-tests-test-hookssh

## Stage 1: Spec Compliance

### Requirement 1: Hook emits canonical Claude SessionStart shape unconditionally when context is non-empty
- **Expected**: Single unconditional `jq -n` emitting `{"hookSpecificOutput":{"hookEventName":"SessionStart","additionalContext":"<ctx>"}}` with no env-var branching and no snake_case `additional_context`.
- **Actual**: `hooks/cortex-scan-lifecycle.sh` lines 457-466 contain exactly one `jq -n --arg ctx "$context"` invocation wrapped only by the pre-existing `if [[ -n "$context" ]]` guard. The `if [[ "${AGENT:-}" == "claude" ]]` branch and its `else` snake_case arm are gone. Acceptance command `AGENT= bash hooks/cortex-scan-lifecycle.sh <<<'{"hook_event_name":"SessionStart",...}' | jq -r 'has("hookSpecificOutput")'` returns `true` from the repo root (lifecycle/ has incomplete features).
- **Verdict**: PASS
- **Notes**: Diff confirms the dead `${AGENT:-}` reference is removed entirely, so the `set -euo pipefail` unbound-variable hazard noted in spec Technical Constraints is also eliminated.

### Requirement 2: Both failing scan-lifecycle tests pass without modification to assertions
- **Expected**: `bash tests/test_hooks.sh` exits 0 and reports `14 passed, 0 failed`.
- **Actual**: Suite reported `14 passed, 0 failed (out of 14)` including `scan-lifecycle/single-incomplete-feature` and `scan-lifecycle/claude-output-format` (PASS).
- **Verdict**: PASS
- **Notes**: `tests/test_hooks.sh` was not modified to add `AGENT=claude` invocations — the rejected Alternative B was correctly avoided.

### Requirement 3: Empty-context path preserves current behavior
- **Expected**: Empty lifecycle directory → hook emits 0 bytes, exit 0.
- **Actual**: With an empty `lifecycle/` tree in a tmpdir, `bash hooks/cortex-scan-lifecycle.sh <<< '...'` produced 0 bytes on stdout (stderr-only diagnostic messages about CLAUDE_ENV_FILE and cortex_command importability, which are pre-existing and unrelated). The outer `if [[ -n "$context" ]]` guard at line 459 is retained.
- **Verdict**: PASS
- **Notes**: stderr noise is pre-existing operator-facing diagnostics, not part of the hook output contract.

### Requirement 4: Plugin mirror regenerated by the pre-commit drift hook, not by hand
- **Expected**: (a) both files staged; (b) `jq -n` block byte-equal across canonical and mirror; (c) zero `additional_context` hits in mirror.
- **Actual**:
  - (a) `git show --stat a1d97c3` lists both `hooks/cortex-scan-lifecycle.sh` and `plugins/cortex-overnight/hooks/cortex-scan-lifecycle.sh` as modified in the commit.
  - (b) `diff` of the `awk '/jq -n/,/^}$/'` ranges from canonical and mirror produced no output (BYTE_EQUAL).
  - (c) `grep -c 'additional_context' plugins/cortex-overnight/hooks/cortex-scan-lifecycle.sh` returned `0`.
- **Verdict**: PASS
- **Notes**: Both files show the identical 8-line emission block and identical surrounding context; the mirror is a faithful regeneration.

### Requirement 5: Dead defensive code in `tests/test_lifecycle_phase_parity.py` removed
- **Expected**: (a) no `env["AGENT"] = ...` line; (b) no `payload.get("additional_context"...)` parser fallback; (c) docstring at line 490 references `additionalContext`, not `additional_context`.
- **Actual**:
  - (a) `grep -nE '^[[:space:]]*env\["AGENT"\][[:space:]]*='` returned exit 1 (no matches).
  - (b) `grep -nE 'payload\.get\("additional_context"'` returned exit 1 (no matches). Parser at line 541 reads only `payload["hookSpecificOutput"].get("additionalContext", "")`.
  - (c) Line 490 reads `the full additionalContext for diagnostic surfacing.`; line 540 comment reads `Claude Code SessionStart contract: hookSpecificOutput.additionalContext.` No remaining `additional_context` references in the file.
  - `pytest tests/test_lifecycle_phase_parity.py` → `40 passed`.
- **Verdict**: PASS
- **Notes**: Hard-deletion approach aligns with project.md's "Workflow trimming" principle as cited in the spec.

### Requirement 6: No regression in the targeted pytest module or the bash hook suite
- **Expected**: `bash tests/test_hooks.sh` exits 0; `pytest tests/test_lifecycle_phase_parity.py` exits 0. Pre-existing `test_lifecycle_references_resolve` failure is out of scope.
- **Actual**: `bash tests/test_hooks.sh` → `14 passed, 0 failed`. `pytest tests/test_lifecycle_phase_parity.py` → `40 passed in 3.86s`.
- **Verdict**: PASS
- **Notes**: Scoped verification per spec; broader sweep not gated.

## Requirements Drift

**State**: none
**Findings**:
- None — the implementation removes a vestigial env-var contract and aligns the hook with the canonical Claude Code SessionStart shape already documented in `docs/agentic-layer.md` and already used by sibling hooks (`cortex-validate-commit.sh`). Dual-source enforcement was respected: only the canonical hook was hand-edited; the plugin mirror is byte-equivalent in its `jq -n` block. No new behavior was introduced that the requirements docs don't already describe.
**Update needed**: None

## Stage 2: Code Quality

- **Naming conventions**: Consistent. The hook now uses the canonical Claude camelCase keys (`hookSpecificOutput`, `hookEventName`, `additionalContext`) matching the rest of the hooks in `hooks/*.sh` and the upstream Claude Code SessionStart schema.
- **Error handling**: Unchanged and appropriate. The `if [[ -n "$context" ]]` outer guard remains; `set -euo pipefail` at the top of the hook is preserved, and the removal of the `${AGENT:-}` reference eliminates one unbound-variable surface rather than introducing one. `jq -n --arg ctx "$context"` handles JSON-escaping of newlines/quotes/backslashes in the context payload as noted in the spec's Edge Cases.
- **Test coverage**: Plan verification commands executed in this review — the two previously red scan-lifecycle tests now pass, the parity pytest module (40 tests) is green, and the dual-source mirror is byte-equal in the emission block. Targeted scope per spec R6; pre-existing unrelated red test (`test_lifecycle_references_resolve`) correctly left out of scope.
- **Pattern consistency**: Strong. The hook now emits the same `hookSpecificOutput.additionalContext` shape used by `cortex-validate-commit.sh` and matches the schema enumerated in `docs/agentic-layer.md`. The commit message also follows repo conventions (imperative subject under 72 chars, body explains the regression provenance with commit hashes and the user-impact restoration).

## Verdict

```json
{"verdict": "APPROVED", "cycle": 1, "issues": [], "requirements_drift": "none"}
```
