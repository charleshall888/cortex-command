# Plan: offload-completemd-pr-state-routing-and (#331)

## Overview
Extract `complete.md`'s Step-7 PR-state router into a `cortex-lifecycle-complete-route <slug>` classifier verb (owning the ~10 terminal message strings) and the finalization staging shared by `complete.md` Step 11a + `post-refine-commit.md` into a `cortex-lifecycle-stage-artifacts --phase {complete|refine}` verb (dropping the bug-2 `git add -u cortex/backlog/` sweep). Both new modules model the read-only-classifier + thin-CLI precedent in `cortex_command/common.py` (`detect_lifecycle_phase` / `_cli_detect_phase`) and live in `cortex_command/lifecycle/`, console-script-only.
**Architectural Pattern**: layered
<!-- A deterministic CLI verb layer is extracted beneath the skill-prose orchestration layer; the prose collapses to "run it, act on `route`". -->

**Scope of this plan**: all three phases are buildable — **#330 is complete** (`status: complete`, merged to `main` at `c1971f70`), so its `cortex-lifecycle-event log --set/--set-json` extension exists and Phase 3 is unblocked. Two post-#330 corrections to the spec's Phase-3 assumptions (the spec was written when #330 was unbuilt; these are recorded reconciliations, like the spec's own Req-16 ticket reconciliation):

- **Interface is `--set k=v` (literal) / `--set-json k=v` (json-typed)**, not the spec's guessed `--field`. The verb emits `{ts, event, feature, <ordered fields>}` via default (spaced) `json.dumps` + `%Y-%m-%dT%H:%M:%SZ` timestamps, no auto `schema_version` (`cortex_command/lifecycle_event.py:121-152`).
- **`pr_opened` stays hand-written; only `feature_complete` migrates.** `ADR-0020-lifecycle-event-emission-contract.md` (shipped by #330 — the authority, superseding the spec's "Proposed ADR 0017", which is moot since 0017 is taken by #335) **explicitly exempts `pr_opened`** from the verb because its canonical shape places `schema_version` *before* `feature`, which the feature-first verb cannot produce. The ADR names "#331's `pr_opened`" as exempt. `feature_complete` (`{ts, event, feature, tasks_total, rework_cycles, merge_anchor}` — no `schema_version`) is fully verb-expressible and migrates. This narrows spec Reqs 16/17 to `feature_complete` only. See Risks R1.

## Outline

### Phase 1: complete-route classifier (tasks: 1, 2, 3, 4, 5, 6)
**Goal**: ship `cortex-lifecycle-complete-route` and collapse `complete.md` Step 7 prose to a verb call, with the routing test migrated to target the verb.
**Checkpoint**: verb registered + green golden-route-table test; `complete.md` Step 7 collapsed; migrated `test_lifecycle_complete_state_routing.py` green; per-phase gate (mirror in sync, parity no-E002, `just test`, audits) green and committed.

### Phase 2: stage-artifacts staging verb (tasks: 7, 8, 9, 10, 11)
**Goal**: ship `cortex-lifecycle-stage-artifacts`, collapse `complete.md` Step 11a + `post-refine-commit.md` staging, drop the bug-2 `-u` sweep, and migrate the two affected guard tests.
**Checkpoint**: verb registered + green staged-set test (incl. no-sweep control); both prose sections collapsed; finalization-region guard + `post-refine-commit` wiring test updated; per-phase gate green and committed.

### Phase 3: event-emission migration (tasks: 12, 13, 14)
**Goal**: migrate `feature_complete` (Step 11) emission to `cortex-lifecycle-event log --set/--set-json` **in place** (today's ordering preserved); `pr_opened` stays hand-written per ADR-0020. The bug-1 emission-ordering fix is split to #339 (the spec's prescribed fix regresses — see Risks R1).
**Checkpoint**: `feature_complete` emits via the verb with the field-set+values pinned by a real-consumer round-trip test; the committed `events.log` still contains the terminal row (today's behavior, no regression); events-registry green; per-phase gate green and committed.

## Tasks

### Task 1: Create the `complete-route` classifier module
- **Files**: `cortex_command/lifecycle/complete_route.py` (new); `pyproject.toml`
- **What**: A stateful recovery+routing verb `cortex-lifecycle-complete-route <slug>` that reproduces `complete.md` Step 7's strict-order branch state machine and emits a single compact-JSON verdict on stdout; its only writes are the Branch-3 single-match `pr.json` reconstruction. Register it as a console-script. (Reqs 1, 2, 3, 4, 5, 6, 7; 7a is structural — verb is consumed only by complete.md.)
- **Depends on**: none
- **Complexity**: complex
- **Context**:
  - Model on `_cli_detect_phase` (`cortex_command/common.py:1146`) — pure `classify(...) -> dict` + thin `main(argv) -> int` that does `sys.stdout.write(json.dumps(result, separators=(",", ":")) + "\n")` and returns 0. Return non-zero **only** on usage error (missing slug) or unresolvable root — gh failures route to 4a with exit 0, never a traceback.
  - Root + slug resolution: reuse `_resolve_user_project_root_from_cwd()` (`common.py:123`, imported the way `lifecycle_event.py:38` does) — it ignores `CORTEX_REPO_ROOT` and handles the `.git`-file worktree marker (Req 7). `lifecycle_dir = root / "cortex" / "lifecycle" / slug`; `pr_json = lifecycle_dir / "pr.json"`; `events_log = lifecycle_dir / "events.log"`.
  - events.log scan: the tolerant per-line `json.loads`-in-`try/except` loop from `_detect_lifecycle_phase_inner` (`common.py:283-311`). Branch 1 = any `event == "feature_wontfix"` (precedes all PR checks) → route `wontfix`. Branch 2 = any `event == "feature_complete"` — **shape-agnostic** (matches both the 6-key merge row and the 3-key close row; match on the `event` key only, never on field count) → route `already_complete`.
  - **Every route carries an explicit non-terminal continuation target, not just a `terminal` boolean** (the boolean alone collapses three distinct destinations — see Task 5). Output a `continue_to` field on non-terminal routes naming the next complete.md step: `already_complete` → `"step12"` (short-circuit; NO re-cleanup, NO duplicate event, NO second pr.json), `on_main` → `"step9"`, `first_run` → `"step1"` (re-run first-run path Steps 1-6), `merged_clean_ancestor` (4e) → `"step8"`. Terminal routes (`wontfix`, `pr_unknown`/4a, `pr_not_found`/4b, `pr_open`/4c, `merged_dirty`/4d, `merged_not_ancestor`/4f, `pr_closed`/4g) carry `continue_to: null` + a `message`. `orphan_ambiguous` is a **stop-for-user-pick** route (see below), neither a print-and-exit terminal nor an auto-continue.
  - Branch detection first: `git rev-parse --abbrev-ref HEAD` via `subprocess.run(..., timeout=..., capture_output=True, text=True)` mapping failure to a safe default, per `lifecycle_staleness` (`common.py:1105-1133`). On `main`/`master` → route `on_main`, `terminal=false`, **no** `gh pr list` orphan probe (Req 5 — preserves `complete.md:21`).
  - gh availability + degradation bands (Req 4): detect binary with `shutil.which("gh")`; distinguish (a) missing → `unknown`, (b) `gh auth status` non-zero → `unknown`, (c) `gh pr view` non-zero whose stderr matches network/auth patterns → `unknown` (Branch 4a) vs. `Could not resolve to a PullRequest`/`GraphQL: not found` → Branch 4b. All of 4a route `pr_state: "unknown"`, exit 0.
  - Branch 3 (`pr.json` absent): `gh pr list --head "interactive/{slug}" --state all --json number,state,mergedAt --limit 5`. 0 matches → route `first_run`, `terminal=false`, `continue_to: "step1"`. 1 match → atomically reconstruct `pr.json` (tempfile in the lifecycle dir + `os.replace`, per `complete.md:46`) then fall through to Branch 4. >1 → route `orphan_ambiguous`, payload `candidates: [...]`, **no** `pr.json` write (Req 6). This route is the kept multi-match user-pick affordance (Req 20): the verb stops without selecting; the prose surfaces the candidates, the user picks, then the prose writes `pr.json` for the chosen PR and **re-runs the verb** (which now finds `pr.json` present → Branch 4) — see Task 5. The verb itself never auto-selects and never writes `pr.json` here.
  - Branch 4 routes + the **verbatim terminal message strings** (Req 3): reproduce the `## Byte-Identical Behavioral Contract` table in `research.md` exactly — 4a `pr_unknown`, 4b `pr_not_found`, 4c `pr_open`, 4d `merged_dirty`, 4e `merged_clean_ancestor` (`terminal=false`, `continue_to: "step8"`), 4f `merged_not_ancestor`, 4g `pr_closed`.
  - **Worktree-path resolution must cover all three completion paths** (trunk, worktree-interactive, feature-branch — `project.md` multi-step-complete invariant), not only `interactive/{slug}`: resolve the worktree path via `git worktree list --porcelain` matching `interactive/{slug}`; **if no interactive worktree exists** (feature-branch / on-main path, the same case Step 8's prefix-check skips), fall back to the current checkout root (`git rev-parse --show-toplevel`). The 4d dirty-check (`git status --porcelain`) and the 4e/4f `git merge-base --is-ancestor <head_branch> origin/main` run against that resolved path — so on the feature-branch path the `<path>` placeholder is non-empty and the 4d "uncommitted changes; resolve first" guard still fires (it must not silently default to clean → 4e).
  - Output dict keys: `{route, terminal, continue_to, message, pr_state, pr_url, pr_number, head_branch}` plus `candidates` on the `orphan_ambiguous` route. `message` is `""` on non-terminal routes; `continue_to ∈ {null, "step1", "step8", "step9", "step12"}`; `pr_state ∈ {"", "unknown", "OPEN", "MERGED", "CLOSED"}`. The full route enumeration (`wontfix, already_complete, on_main, first_run, orphan_ambiguous, pr_unknown, pr_not_found, pr_open, merged_dirty, merged_clean_ancestor, merged_not_ancestor, pr_closed`) and its `{terminal, continue_to, pr_state}` triple per route is the golden table Task 3 pins.
  - pyproject: add `cortex-lifecycle-complete-route = "cortex_command.lifecycle.complete_route:main"` to `[project.scripts]` (alpha order, after `cortex-lifecycle-branch-mode` at line 52). Console-script-only — no `bin/` wrapper (the `cortex-lifecycle-event` precedent; registration alone satisfies parity `gather_deployed`, `parity_check.py:279`).
- **Verification**: `python3 -c "import cortex_command.lifecycle.complete_route"` exits 0 AND `pip show -f cortex-command >/dev/null 2>&1; cortex-lifecycle-complete-route --help` after `just python-setup` exits 0 (console-script resolves). Pass if both exit 0.
- **Status**: [ ] pending

### Task 2: Extend the shared gh-stub fixture for the complete-route gh surface (backward-compatible)
- **Files**: `tests/fixtures/gh-stub.sh`
- **What**: Add handling for `gh pr list --head ... --json number,state,mergedAt` (controllable match count: 0/1/N), `mergedAt` on `gh pr view --json state,mergedAt`, **`gh pr view` non-zero-exit scenarios with controllable stderr** (network/auth-pattern stderr for Branch 4a vs. `Could not resolve to a PullRequest`/`GraphQL: not found` stderr for Branch 4b — the existing no-scenario path exits 1 with EMPTY stderr and cannot discriminate them), and `gh auth status`, keyed by new `$GH_STUB_*` env vars — **without changing any existing scenario** (`open-ready-mismatch`, `open-draft-mismatch`, `merged`, `closed`, the `gh pr ready` modes, `gh pr create`), so `tests/test_runner_pr_gating.py` stays green.
- **Depends on**: none
- **Complexity**: simple
- **Context**:
  - Current dispatch (`tests/fixtures/gh-stub.sh`) switches on `cmd="${1:-}"`/`sub="${2:-}"`; `gh pr view` reads `$GH_STUB_SCENARIO`; `gh pr ready` reads `$GH_STUB_READY_MODE`; unknown `gh` commands exit 0 silently. Existing `gh pr view` scenarios emit `{url,isDraft,state}` with **no `mergedAt`** — Branch 4d/4e/4g need `mergedAt`, so the new `pr view` scenarios must add it (e.g. a `merged-anchored` scenario emitting `mergedAt` non-null; a `closed-unmerged` scenario emitting `mergedAt:null`).
  - Add `gh pr list` branch reading e.g. `$GH_STUB_PR_LIST_COUNT` (0 → `[]`; 1 → one object; N → N objects) emitting `[{"number":..,"state":..,"mergedAt":..}]`.
  - Add a new `gh pr view` failure dimension (e.g. `$GH_STUB_VIEW_FAIL`): `network` → exit 1 with a network/auth stderr line (drives 4a); `notfound` → exit 1 with `Could not resolve to a PullRequest with the number of <n>` / `GraphQL: not found` stderr (drives 4b). Keep the existing `$GH_STUB_SCENARIO` success dimension untouched; the new success scenarios that carry `mergedAt` are additive.
  - Add `gh auth status` branch reading e.g. `$GH_STUB_AUTH` (`ok` → exit 0; `fail` → exit 1 with an auth-error stderr line).
  - `set -u` is set in the stub — every new branch must read its env via `${GH_STUB_*:-default}` so an unset var doesn't crash; runner-safe since `test_runner_pr_gating.py` never reaches the new branches.
  - PATH-injection precedent for tests (`tests/test_runner_pr_gating.py:110` `_install_gh_stub` → copy to `tmp/bin/gh`, chmod 0o755, prepend to PATH; pop the `GH_STUB_*` envs before `env.update(extra)`).
- **Verification**: `.venv/bin/pytest tests/test_runner_pr_gating.py -q` exits 0 (no regression in existing consumers) AND `GH_STUB_PR_LIST_COUNT=2 bash tests/fixtures/gh-stub.sh pr list --head x --state all --json number,state,mergedAt --limit 5` prints a 2-element JSON array AND `GH_STUB_VIEW_FAIL=notfound bash tests/fixtures/gh-stub.sh pr view 7 --json state,mergedAt; echo $?` exits non-zero with `Could not resolve` on stderr. Pass if the pytest exits 0 and both stub probes behave as stated.
- **Status**: [ ] pending

### Task 3: Golden-route-table test for the complete-route verb
- **Files**: `tests/test_complete_route.py` (new)
- **What**: Parametrized golden-route-table test asserting the exact `{route, terminal, continue_to, pr_state}` tuple (and terminal `message` substring) for each of the 12 routes, plus: 4a/4b discrimination driven by the Task-2 `$GH_STUB_VIEW_FAIL` failure dimension (network→`pr_unknown`, notfound→`pr_not_found`); gh-absent → `pr_state:"unknown"`/exit 0; the `already_complete` route asserts `continue_to=="step12"` (the idempotency short-circuit); zero-events-written on every non-Branch-3 route; Branch-3 real-fs `pr.json` reconstruction (single-match) and `orphan_ambiguous` (multi-match, NO write, non-empty `candidates`); the feature-branch (no-interactive-worktree) 4d path resolves `<path>` to the checkout root and still routes `merged_dirty` on a dirty tree; worktree-CWD resolution ignoring `CORTEX_REPO_ROOT`; and the Req-7a speculative-caller grep guard. (Reqs 1, 2, 4, 5, 6, 7, 7a.)
- **Depends on**: [1, 2]
- **Complexity**: complex
- **Context**:
  - Top-level `tests/` (so `just test` picks it up; `justfile` runs `pytest tests/`, not `cortex_command/lifecycle/tests/`). Direct-import the classifier; build fixtures with `tmp_path` + `monkeypatch.chdir(root)` + `monkeypatch.delenv("CORTEX_REPO_ROOT", raising=False)` (the `cortex_command/tests/test_lifecycle_event.py` idiom).
  - gh states via the Task-2 stub PATH-injected onto `PATH`; the 12 branches map to the `research.md` contract table. Branch-3 reconstruction + `orphan_ambiguous` need a real on-disk lifecycle dir (assert `pr.json` written-then-routed for single-match; assert NOT written + non-empty `candidates` for multi-match).
  - Zero-events assertion (Req 1b): snapshot the fixture's `events.log` line count, run the verb on each non-Branch-3 route, assert the count is unchanged (the verb appends nothing).
  - Worktree-CWD test (Req 7): model on `test_lifecycle_event.py:234` `_setup_worktree` — `.git` **file** with a `gitdir:` pointer + a `cortex/lifecycle/` dir; `chdir(inside-worktree)`; `setenv("CORTEX_REPO_ROOT", <unrelated-main>)`; assert the verb reads the worktree-rooted `events.log`/`pr.json` and the main-repo path is untouched.
  - Req-7a grep guard: assert `grep -rEc "cortex-lifecycle-complete-route|complete_route" claude/statusline.sh cortex_command/dashboard/ hooks/` == 0 (no render/observability surface references the verb).
- **Verification**: `.venv/bin/pytest tests/test_complete_route.py -q` exits 0 (all 12 branch cases + gh-absent + zero-events + worktree-CWD + Req-7a grep pass). Pass if exit 0.
- **Status**: [ ] pending

### Task 4: Migrate `test_lifecycle_complete_state_routing.py` to target the verb
- **Files**: `tests/test_lifecycle_complete_state_routing.py`
- **What**: Re-point the prose-grep assertions (25 of 27 cases) in `test_lifecycle_complete_state_routing.py` to assert against the verb instead — the 12 terminal-message substrings and the command/order tokens — per spec Req 3 ("every assertion that previously read complete.md text now targets the verb"). Three assertion *shapes* resist a 1:1 swap and need explicit handling (see Context). (Req 3.)
- **Depends on**: [1, 2]
- **Complexity**: complex
- **Context**:
  - Current shape: `COMPLETE_MD.read_text()` (`_complete_text()`) + `substring in text` across a 12-tuple `ROUTING_BRANCHES` param list + 15 named tests (full inventory: terminal-message substrings like `"lifecycle was wontfix'd at"`, `"The PR may have been deleted"`, `"refusing cleanup until verified"`, and command/order tokens).
  - Message/branch assertions → assert the verb's `{route, message, pr_state}` output (drive gh states via the Task-2 stub — this is why **Depends on [1, 2]**, not [1]: the migrated assertions need the stub scenarios and `$GH_STUB_*` vars Task 2 adds).
  - **Three resistant assertion shapes** (do not naively 1:1 these):
    - `test_worktree_retained_language_in_terminal_branches` asserts `text.count("Worktree retained") >= 3` — a *cross-message repetition invariant* over three terminal routes (4a, 4b, 4g). A single verb invocation emits ONE `message`, so re-express it as **three separate per-route `message`-contains checks** (`pr_unknown`, `pr_not_found`, `pr_closed` each contain "Worktree retained"), not one count over one message.
    - The orphan-probe prose substrings (`"Zero matches"`/`"Exactly one match"`/`"Multiple matches"`) and `"first-run path"`/`"retroactively"` are prose *descriptions* with no verb-output equivalent → become **behavioral route assertions**: `first_run` (zero), `pr.json`-reconstructed-then-Branch-4 (one), `orphan_ambiguous` (multi).
    - `test_step_7_heading_present` is a structural heading guard with no verb equivalent → keep it (it's the "intentional residual" Req 3 allows).
  - Command/order/precedence assertions → assert the verb's observable `gh`/`git` invocations under the stub or the module source — not the prose. Strict-order/precedence: assert Branch-1 `wontfix` route wins even when a `feature_complete` and an OPEN PR coexist (behavioral, not string).
  - **Persisted wiring guard** (closes the asymmetry R4-C3/excluded-R3 flagged): keep ONE structural assertion in this file that `complete.md`'s Step-7 region invokes `cortex-lifecycle-complete-route` AND no longer contains the Branch 4a-4g terminal-message strings (so narration-removal is regression-guarded by a persisted test, not only Task 5's one-shot grep). This is the migrated counterpart to the positive `stage-artifacts`-invocation token that `test_complete_md_finalization_commit.py` keeps on the Phase-2 side.
  - Acceptance (Req 3): after migration `grep -c "complete.md" tests/test_lifecycle_complete_state_routing.py` reflects only intentional residual references (the structural heading + wiring guards), not a hard 0.
- **Verification**: `.venv/bin/pytest tests/test_lifecycle_complete_state_routing.py -q` exits 0 AND the routing/message assertions assert verb output (spot-check: `grep -c "_complete_text()" tests/test_lifecycle_complete_state_routing.py` reflects only the retained structural heading + narration-removal wiring guards, no routing-message reads). Pass if pytest exits 0 and the only surviving `complete.md` reads are the two named structural guards.
- **Status**: [ ] pending

### Task 5: Collapse `complete.md` Step 7 prose to the verb call
- **Files**: `skills/lifecycle/references/complete.md`; `plugins/cortex-core/skills/lifecycle/references/complete.md` (regenerated mirror)
- **What**: Replace Step 7's Branch 1-4 / 4a-4g narration with a verb call that **dispatches on `route`/`continue_to`, not a binary `terminal` switch** (the binary switch would drive the `already_complete` route into Step 8 and re-emit `feature_complete` — a fix-invalidating idempotency break flagged in critical review). Preserve the on-main short-circuit and the `wontfix`/`feature_complete` precedence; keep the Branch-3 multi-match user-pick as prose ("ask the user", no `AskUserQuestion` literal) WITH its post-pick continuation. Regenerate the mirror with `just build-plugin`. (Reqs 8, 20.)
- **Depends on**: [1, 4]
- **Complexity**: simple
- **Context**:
  - Step 7 region is `complete.md:82-149`. Soft positive-routing, What/Why phrasing ("run it, act on `route`"). The collapsed prose: "run `cortex-lifecycle-complete-route <slug>`; if `route` is terminal (`message` non-empty, `continue_to: null`), print `message` and exit; else continue at the step named by `continue_to` — `already_complete` → **Step 12** (short-circuit; no re-cleanup, no duplicate `feature_complete`, no second pr.json), `on_main` → Step 9, `first_run` → Steps 1-6, `merged_clean_ancestor` → Step 8."
  - **`orphan_ambiguous` continuation (do not drop it)**: the original (`complete.md:112`) is continuous — "ask the user which to use … **On selection, write pr.json and proceed to Branch 4.**" The collapsed prose must preserve the full tail: surface `candidates`, ask the user, **write `pr.json` for the chosen PR (atomic tempfile + `os.replace`), then re-run `cortex-lifecycle-complete-route <slug>`** (now `pr.json` is present → Branch 4 state). Without the write+re-run, the pick is never persisted and re-invocation loops on the same multi-match. Kept affordance (Req 6), no `AskUserQuestion` literal (Req 20).
  - The on-main short-circuit narration at `complete.md:21` stays (it gates whether Step 7 / the verb is reached at all).
  - Kept-pauses (Req 20): `complete.md` currently has `grep -c "AskUserQuestion"` == 0 and the only Complete kept-pause is the merge-wait at `complete.md:73` (`kept-pauses.md:21`). Do NOT introduce an `AskUserQuestion` literal — keep the multi-match pick phrased as "ask the user" so `test_lifecycle_kept_pauses_parity.py` direction-2 stays dormant. The merge-wait anchor must remain within ±35 lines of the Step-6 heading after the reflow.
  - Mirror: `just build-plugin` regenerates `plugins/cortex-core/...`; commit canonical + mirror together (drift hook) at the Task-6 gate.
  - Narration-removal is regression-guarded by Task 4's persisted wiring assertion (Step-7 region contains the verb call and NOT the Branch 4a-4g message strings), so it is a durable test, not only this one-shot grep.
- **Verification**: `grep -c "complete-route" skills/lifecycle/references/complete.md` >= 1 AND the Step-7 region no longer contains the routing narration: `awk '/^### Step 7/,/^### Step 8/' skills/lifecycle/references/complete.md | grep -Ec "gh pr view --json|merge-base --is-ancestor|Branch 4[a-g]"` == 0 AND `grep -c "AskUserQuestion" skills/lifecycle/references/complete.md` == 0. Pass if first >=1 and the other two == 0.
- **Status**: [ ] pending

### Task 6: Phase-1 gate + commit
- **Files**: (verification/commit only — no new edits) `cortex_command/lifecycle/complete_route.py`, `pyproject.toml`, `tests/fixtures/gh-stub.sh`, `tests/test_complete_route.py`, `tests/test_lifecycle_complete_state_routing.py`, `skills/lifecycle/references/complete.md`, `plugins/cortex-core/skills/lifecycle/references/complete.md`
- **What**: Run the per-phase completion gate (Req 19) and commit Phase-1 work with an explicit pathspec. This gate is not deferred to the end of the feature.
- **Depends on**: [3, 4, 5]
- **Complexity**: simple
- **Context**:
  - Trunk discipline: implementation runs on `main` sequentially (editing `cortex_command/` → `just test` exercises the editable install, so worktree verification would test stale code — `project_lifecycle_implement_dispatch_mode`). Commit with explicit `git commit -- <files>` pathspec given concurrent offload-epic sessions on trunk (`feedback_trunk_shared_index_concurrent_session`).
  - Commit canonical + regenerated mirror together (`feedback_drift_hook_shared_checkout_coupling`): run `just build-plugin` before/with the commit. Use `/cortex-core:commit`.
- **Verification**: `just build-plugin && git diff --quiet plugins/cortex-core/ && python3 -m cortex_command.parity_check --json` reports no E002 AND `just check-events-registry && just check-events-registry-audit && just test` exit 0 AND the bare-python-import, skill-path, contract, and prescriptive-prose audits exit 0. Pass if all exit 0.
- **Status**: [ ] pending

### Task 7: Create the `stage-artifacts` staging verb
- **Files**: `cortex_command/lifecycle/stage_artifacts.py` (new); `pyproject.toml`
- **What**: `cortex-lifecycle-stage-artifacts --phase {complete|refine} --feature {slug}` over a single shared staging engine owning the "enumerate explicit paths, never directory-glob" discipline; stages the exact per-phase path set, auto-detects refine approval/cancel, narrows the complete backlog write-back to two explicit paths (dropping the bug-2 `-u` sweep), and reports a stage-first signal. Register as a console-script. (Reqs 9, 10, 11, 12.)
- **Depends on**: [6]
- **Complexity**: complex
- **Context**:
  - Same root resolution as Task 1 (`_resolve_user_project_root_from_cwd()`); `lifecycle_dir = root / "cortex" / "lifecycle" / slug`. Runs `git add -- <explicit paths>` then `git diff --cached --quiet`; output `{signal: "staged"|"nothing_staged", staged_paths: [...]}` (Req 12 — the caller acts on `signal`).
  - `--phase complete` staged set (Req 9): `cortex/lifecycle/{slug}/{research,spec,plan,review,index}.md` + `events.log` (those present) + the review-drift `**File**:` path iff `review.md` has a `## Suggested Requirements Update` section (parse that section, extract the `File` value; no dir-add on `cortex/requirements/`) + the **narrowed** backlog write-back.
  - Backlog write-back, narrowed (Req 11, bug-2): stage exactly (a) the resolved ticket file — `cortex-resolve-backlog-item {slug}` exit-0 stdout `filename` field (`resolve_item.py:272`; exit 3 → silently skip; never a `{slug}.md` glob, which matches zero files since the lifecycle slug is a truncated prefix), and (b) `cortex/backlog/index.md`. **Drop** today's `git add -u cortex/backlog/` sweep. Both explicit-path adds; on `none`/external the resolver exits 3 and `index.md` is unmodified, so neither stages (emergent — no backend branch; per Non-Requirements, the verb is NOT backend-aware).
  - `--phase refine` staged set (Reqs 9, 10): scan `events.log` bottom-up for the most recent transition — `phase_transition specify→plan` (approval) or `lifecycle_cancelled` (cancel); stage `research.md, spec.md, index.md, events.log` (+ the resolver-filename backlog file on Context A), with `spec.md` **omitted** on the cancel path. Use the same tolerant per-line `json.loads` reader.
  - Explicit-add discipline: never a directory-scoped `git add` on `cortex/lifecycle/`, `cortex/backlog/`, or `cortex/requirements/`; never the `-u` tracked-modified form on those dirs.
  - pyproject: add `cortex-lifecycle-stage-artifacts = "cortex_command.lifecycle.stage_artifacts:main"` (alpha order, after `cortex-lifecycle-picker-decision` at line 57). Console-script-only.
- **Verification**: `python3 -c "import cortex_command.lifecycle.stage_artifacts"` exits 0 AND after `just python-setup`, `cortex-lifecycle-stage-artifacts --help` exits 0 and shows `--phase` + `--feature`. Pass if both exit 0.
- **Status**: [ ] pending

### Task 8: Real-git staged-set test for stage-artifacts (incl. negative + no-sweep controls)
- **Files**: `tests/test_stage_artifacts.py` (new)
- **What**: Real-git test asserting sorted `git diff --cached --name-only` equals the expected set for each `--phase`/sub-mode, plus the ported negative-token controls (1:1 from `test_complete_md_finalization_commit.py:21-35`) as staged-set assertions, the bug-2 no-sweep control, and the `nothing_staged`/`staged` signal. (Reqs 9, 10, 11, 12, 13.)
- **Depends on**: [7]
- **Complexity**: complex
- **Context**:
  - Real-git harness: the `_git(*args, cwd, check)` helper + persisted repo config (`git init -b main`, `core.hooksPath /dev/null`, gpgsign off, pinned identity) from `tests/test_revert_merge_real_git.py:51-106`. Build a fixture repo with a `cortex/lifecycle/{slug}/` artifact set and a `cortex/backlog/` with a resolvable ticket + `index.md`.
  - Cases: `--phase complete` (full set incl. resolver-filename + `index.md`); `--phase complete` with a `## Suggested Requirements Update` review.md (the drift `File` staged) and without (skipped); `--phase refine` approval (incl. `spec.md`) and cancel (excludes `spec.md`); resolver exit-3 fixture (no backlog file staged, verb still succeeds).
  - Negative controls (Req 13): fixtures with **unrelated dirty tracked** files under `cortex/lifecycle/` (residue), `cortex/requirements/`, and `cortex/backlog/OTHER.md`; assert `git diff --cached --name-only` does NOT include them — behaviorally exercising the dropped `-u` sweep and the no-directory-glob discipline (not string-checking the source).
  - Signal (Req 12): assert `nothing_staged` on a no-op re-run vs `staged` on a real stage, matching `git diff --cached --quiet`'s exit.
- **Verification**: `.venv/bin/pytest tests/test_stage_artifacts.py -q` exits 0 (all sub-modes + negative + no-sweep + signal cases pass). Pass if exit 0.
- **Status**: [ ] pending

### Task 9: Collapse `complete.md` Step 11a + `post-refine-commit.md` staging prose
- **Files**: `skills/lifecycle/references/complete.md`; `skills/lifecycle/references/post-refine-commit.md`; `plugins/cortex-core/skills/lifecycle/references/complete.md` + `.../post-refine-commit.md` (regenerated mirrors)
- **What**: Replace `complete.md` Step 11a staging (the enumerated `git add`, the resolver lookup, the `-u` sweep, the repeated "never directory-glob" warnings) and `post-refine-commit.md`'s Staging + No-Op Short-Circuit sections with a `cortex-lifecycle-stage-artifacts --phase {complete|refine} --feature {slug}` invocation, leaving the residual prose (the `/cortex-core:commit` call, the stage-first/halt framing as control flow) intact. Regenerate mirrors. (Req 14.)
- **Depends on**: [5, 7]
- **Complexity**: simple
- **Context**:
  - `complete.md` Step 11a region is `complete.md:209-266` (between the `<!-- finalization-commit-step -->` / `<!-- /finalization-commit-step -->` markers — keep the markers; Task 10's region guard depends on them). Replace staging with the verb call; the verb's `signal` drives the stage-first control flow (`nothing_staged` → skip `/cortex-core:commit` + continue to Step 12; `staged` → commit; commit failure → halt before Step 12).
  - `post-refine-commit.md` Staging (`:21-40`) + No-Op Short-Circuit (`:42-51`) collapse to the verb call; keep Flag Check, Commit Subject, and the Halt-Before-Plan gate as prose control flow. The verb auto-detects approval/cancel (Req 10), so the prose no longer narrates the bottom-up scan.
  - Keep the structural gate (stage-first guard + halt-on-failure) as **control flow, not prose-only** (research authoring note). The bug-2 `git add -u cortex/backlog/` line is removed from Step 11a (now inside the verb, narrowed).
  - **Scope the edits to Steps 7 (Task 5) + 11a only** — `complete.md` is read by ~10 tests (`test_complete_cleanup_gates.py`, `test_complete_md_hard_guard_snapshot.py`, `test_complete_index_sync_gate.py`, `test_complete_pr_routing.py`, `test_complete_pr_json_schema.py`, `test_skill_section_citations.py`, …). The collapse is safe because their regions (Steps 2/3/4/8/10 + headings) are untouched, and shared tokens (`git status --porcelain`, `git merge-base --is-ancestor`) survive in the **untouched Step-8 cleanup gate** (`complete.md:164-165`) — so those whole-file/region guards don't break. Do NOT edit outside the Step-7 and Step-11a regions.
  - Mirrors: `just build-plugin`; commit canonical + mirror together at the Task-11 gate.
- **Verification**: `grep -c "stage-artifacts" skills/lifecycle/references/complete.md skills/lifecycle/references/post-refine-commit.md` shows >=1 in each file AND `grep -c "git add -u cortex/backlog/" skills/lifecycle/references/complete.md` == 0 (bug-2 sweep removed from prose). Pass if both hold.
- **Status**: [ ] pending

### Task 10: Update the finalization-region guard + the post-refine wiring test
- **Files**: `tests/test_complete_md_finalization_commit.py`; `tests/test_post_refine_commit_wired.py`
- **What**: Update `test_complete_md_finalization_commit.py` (the `<!-- finalization-commit-step -->` region guard) to assert the collapsed prose — positive token = the `stage-artifacts` verb invocation; the `git add -u cortex/backlog/` positive token flips to **absent** (bug-2); the negative-token intent now lives in Task 8's behavioral test (Req 13). Also update `test_post_refine_commit_wired.py` so its content-token assertions match the collapsed `post-refine-commit.md` (the cancel-path/staging tokens that moved into the verb). (Req 15 + spec-gap, see Risks R2.)
- **Depends on**: [7, 9]
- **Complexity**: simple
- **Context**:
  - `test_complete_md_finalization_commit.py` slices the `finalization-commit-step` region (markers at `complete.md:209/266`) via `_extract_region` and asserts positive tokens (`cortex-read-commit-artifacts`, `/cortex-core:commit`, `git diff --cached --quiet`, the enumerated lifecycle files, `Suggested Requirements Update`, `cortex-resolve-backlog-item`, `git add -u cortex/backlog/`, halt clause, `main`/`master`) and negative tokens (`git push`, `gh pr create`, `git add cortex/lifecycle/`, `git add -u cortex/requirements/`, bare `git add cortex/backlog/`, `cortex/backlog/*-`). **Distinguish staging-mechanics tokens from residual control-flow tokens** (the verb absorbs only the former):
    - **Move into the verb → flip to ABSENT/drop**: the enumerated lifecycle files, `cortex-resolve-backlog-item`, `Suggested Requirements Update`, and `git add -u cortex/backlog/` (the last is the bug-2 flip: it is currently asserted *present* at line 126 and must now be asserted *absent*).
    - **Stay as prose control flow → KEEP asserting present**: `cortex-read-commit-artifacts` (the commit-artifacts flag gate — Task 9 keeps Flag Check as prose), `/cortex-core:commit`, `git diff --cached --quiet` (the stage-first guard stays prose control flow), the halt clause, and the `main`/`master` post-commit branch advisory. Add a positive `stage-artifacts`-invocation token. Keep all negative tokens.
  - `test_post_refine_commit_wired.py` (the actual `post-refine-commit.md` guard — the spec named the wrong file): `test_post_refine_commit_contains_required_tokens` asserts `cortex-read-commit-artifacts`, `/cortex-core:commit`, halt tokens, since-last-commit tokens, and cancel-path tokens (`"cancelled"/"cancel"`). Handle each precisely: `cortex-read-commit-artifacts`/`/cortex-core:commit`/halt **stay** (still prose); the cancel-path token is satisfied by the **kept Commit-Subject prose** `Refine {feature}: cancelled at spec approval` (Task 9 preserves Commit Subject) so it needs **no change**; the since-last-commit narration (`"most recent"` in the removed Preconditions/No-Op scan) is **dropped** (no verb-output equivalent); add a positive `stage-artifacts`-invocation token. The two cross-file wiring tests (`refine-delegation.md` 50-line distance at `refine-delegation.md:25`; `SKILL.md` >=2 refs) and the mirror-existence test survive unchanged because Task 9 edits neither `refine-delegation.md` nor `SKILL.md` — verify they still pass.
- **Verification**: `.venv/bin/pytest tests/test_complete_md_finalization_commit.py tests/test_post_refine_commit_wired.py -q` exits 0. Pass if exit 0.
- **Status**: [ ] pending

### Task 11: Phase-2 gate + commit
- **Files**: (verification/commit only) `cortex_command/lifecycle/stage_artifacts.py`, `pyproject.toml`, `tests/test_stage_artifacts.py`, `tests/test_complete_md_finalization_commit.py`, `tests/test_post_refine_commit_wired.py`, `skills/lifecycle/references/complete.md`, `skills/lifecycle/references/post-refine-commit.md`, and their regenerated mirrors
- **What**: Run the per-phase completion gate (Req 19) and commit Phase-2 work with an explicit pathspec.
- **Depends on**: [8, 9, 10]
- **Complexity**: simple
- **Context**: Same trunk + explicit-pathspec + commit-canonical+mirror-together discipline as Task 6. `just build-plugin` before/with the commit; `/cortex-core:commit`.
- **Verification**: `just build-plugin && git diff --quiet plugins/cortex-core/ && python3 -m cortex_command.parity_check --json` reports no E002 AND `just check-events-registry && just check-events-registry-audit && just test && .venv/bin/pytest tests/test_lifecycle_kept_pauses_parity.py tests/test_lifecycle_phase_parity.py -q` exit 0 AND the bare-python-import, skill-path, contract, and prescriptive-prose audits exit 0. Pass if all exit 0.
- **Status**: [ ] pending

### Task 12: Migrate `feature_complete` emission to the verb (in place — no ordering change)
- **Files**: `skills/lifecycle/references/complete.md`; `plugins/cortex-core/skills/lifecycle/references/complete.md` (regenerated mirror)
- **What**: Replace `complete.md` Step 11's raw-JSON `feature_complete` block with a `cortex-lifecycle-event log` invocation **at today's position** (Step 11, before the Step-11a stage+commit) — so the emitted row is still staged and committed by Step 11a exactly as today (no ordering change, no regression). **`pr_opened` (Step 5) is left untouched** — ADR-0020 exempts it (schema_version-first shape). **The bug-1 emission-ordering fix is deliberately NOT done here** — it is split to [[339-fix-feature-complete-emission-ordering-strand-on-step-11a-commit-failure]] because critical review proved the spec's "emit after the commit" prescription regresses (events.log is in Step 11a's staged set, so emitting after the commit leaves the terminal row permanently uncommitted), and the correct structural fix lives in `complete-route`'s Branch 2 — a design effort that earns its own ticket. (Reqs 16, 17 — narrowed to `feature_complete` emission migration per ADR-0020; the OD-1 ordering fix moves to #339.)
- **Depends on**: [11]
- **Complexity**: simple
- **Context**:
  - Migrated invocation (mirrors #330's established pattern, e.g. `implement.md:176` `batch_dispatch`): `cortex-lifecycle-event log --event feature_complete --feature {slug} --set-json tasks_total={N} --set-json rework_cycles={N} --set merge_anchor=merge`. `tasks_total`/`rework_cycles` are ints → `--set-json`; `merge_anchor` is a string → `--set`. `{N}` values come from `cortex-lifecycle-counters --feature {slug}` (as today). The verb emits `{ts, event, feature, tasks_total, rework_cycles, merge_anchor}` in ADR-0020 canonical form (spaced `json.dumps`, `…Z` ts, no `schema_version`) — exactly the Req-17 field-set+values pin.
  - **Keep the emission at Step 11 (before Step 11a)** — `complete.md:197-207` stays the emission site; Step 11a (Task 9's collapsed staging) then stages `events.log` (now containing the row) + commits it, preserving today's committed-row behavior. Do NOT relocate the emission past the commit (that is the regressing #339 territory). Region overlaps Task 9's Step-11a collapse → `Depends on [11]` serializes after the Phase-2 gate.
  - Branch-2 detection in `complete-route` is shape-agnostic (Task 1) and the close-form 3-key `feature_complete` (`backlog-writeback.md`) is **out of scope** (Non-Requirements) — do not touch it.
  - Mirror: `just build-plugin`; commit canonical + mirror together at the Task-14 gate.
- **Verification**: `grep -c '"event": "feature_complete"' skills/lifecycle/references/complete.md` == 0 (raw-JSON literal removed) AND `grep -c "cortex-lifecycle-event log --event feature_complete" skills/lifecycle/references/complete.md` >= 1 AND `grep -c '"event": "pr_opened"' skills/lifecycle/references/complete.md` == 1 (pr_opened raw JSON deliberately retained per ADR-0020) AND the `feature_complete` verb call sits before the `<!-- finalization-commit-step -->` marker (`awk '/Step 11 —/,/finalization-commit-step/'` contains the verb call — emission stays pre-11a). Pass if all hold.
- **Status**: [ ] pending

### Task 13: Round-trip field-set + real-consumer test for `feature_complete`
- **Files**: `tests/test_complete_feature_complete_emission.py` (new)
- **What**: A frozen-`ts` round-trip test asserting the migrated `feature_complete` row's parsed object has the exact key-set `{ts, event, feature, tasks_total, rework_cycles, merge_anchor}` and value-types (`tasks_total`/`rework_cycles` ints, `merge_anchor=="merge"`, NO `schema_version`/`worktree_path`), classified through real consumers. (Reqs 16, 17.)
- **Depends on**: [12]
- **Complexity**: complex
- **Context**:
  - Drive the emission by invoking `cortex-lifecycle-event log --event feature_complete --feature <slug> --set-json tasks_total=3 --set-json rework_cycles=1 --set merge_anchor=merge` against a `tmp_path` fixture (the `test_lifecycle_event.py` idiom: `monkeypatch.chdir(root)` + `delenv CORTEX_REPO_ROOT`); parse the emitted row and assert the key-set + types. **Real-consumer classification** (Req 17): feed the row to `detect_lifecycle_phase` (asserts route `complete`); to `pipeline/metrics.py` — assert `extract_feature_metrics` carries `merge_anchor=="merge"` through and that it reads `feature`/`ts` without KeyError (it hard-indexes both), and (for the regime-partition claim) feed a `"merge"`-anchored row through `compute_aggregates` (`metrics.py:1041-1061` `avg_phase_durations_by_anchor`) and assert it buckets under the interactive anchor; and to `common.py:198`. A `pr_opened` row is NOT migrated, so no `pr_opened` round-trip is needed here (it stays hand-written) — but add a guard asserting `pr_opened`'s hand-written shape still carries `schema_version` (regression guard for the ADR-0020 exemption).
  - No bug-1 commit-failure test here: the emission/commit ordering is unchanged (Task 12 keeps today's order), and the ordering *fix* (with its discriminating structural test) is #339's scope. A behavioral "no feature_complete on commit failure" test would be self-sealing against unchanged prose, so it is deliberately omitted.
  - Top-level `tests/` so `just test` runs it.
- **Verification**: `.venv/bin/pytest tests/test_complete_feature_complete_emission.py -q` exits 0 (round-trip field-set+values + real-consumer classification pass). Pass if exit 0.
- **Status**: [ ] pending

### Task 14: Events-registry reconciliation + Phase-3 gate + commit
- **Files**: `bin/.events-registry.md` (only if the `feature_complete` producer row needs reconciliation); verification/commit of Tasks 12–13
- **What**: Verify (and update only if needed) the `feature_complete` producer row now that `complete.md` emits it via `cortex-lifecycle-event log --event feature_complete` (the registry scanner recognizes `--event <name>`, so `complete.md` stays a registered producer). `pr_opened`'s row is unchanged (still hand-written in `complete.md`). Run the per-phase gate (Req 19) and commit Phase-3 work. (Reqs 18, 19.)
- **Depends on**: [12, 13]
- **Complexity**: simple
- **Context**:
  - `bin/.events-registry.md:12` (`feature_complete`) already lists `skills/lifecycle/references/complete.md` as a producer; after migration `complete.md` still produces it (via the verb), so the row likely needs no change — but verify `just check-events-registry && just check-events-registry-audit` stay green and edit the row only if the gate flags drift. `pr_opened` row (`:137`) is untouched.
  - Trunk + explicit-pathspec + commit-canonical+mirror-together discipline (as Task 6/11). `just build-plugin` before/with the commit; `/cortex-core:commit`.
- **Verification**: `just build-plugin && git diff --quiet plugins/cortex-core/ && python3 -m cortex_command.parity_check --json` reports no E002 AND `just check-events-registry && just check-events-registry-audit && just test` exit 0 AND the bare-python-import, skill-path, contract, and prescriptive-prose audits exit 0. Pass if all exit 0.
- **Status**: [ ] pending

## Risks

- **R1 — Phase 3 is now in-pass (#330 complete); three spec reconciliations apply.** #330 landed on `main` (`c1971f70`), so `cortex-lifecycle-event log --set/--set-json` exists and the whole feature builds in one lifecycle. Three recorded reconciliations of the (pre-#330) spec: (a) **`pr_opened` is NOT migrated** — `ADR-0020` exempts it (schema_version-first shape); only `feature_complete` migrates (narrows Reqs 16/17). (b) **The interface is `--set/--set-json`** (not the spec's `--field`), and the governing ADR is the shipped **0020**, not the spec's "Proposed ADR 0017" (0017 was taken by #335). (c) **The bug-1 emission-ordering fix (OD-1) is split to [[339-fix-feature-complete-emission-ordering-strand-on-step-11a-commit-failure]]** — critical review proved the spec's prescribed fix ("emit `feature_complete` after the Step-11a commit") *regresses*: `events.log` is in Step 11a's staged set, so emitting after the commit leaves the terminal row permanently uncommitted (a dirty working-tree line after every completion). The correct fix is structural in `complete-route`'s Branch 2 (short-circuit only on a *committed* `feature_complete` + idempotent emission) — a real design effort, so #331 migrates the emission *in place* (no regression, no fix) and #339 owns the ordering correctness. bug-2 (the `-u` sweep, Phase 2) is unaffected and still ships in #331. **These deviations from the approved spec are surfaced for operator awareness at §4**; amending the spec text is a separate optional edit.
- **R2 — spec named the wrong test for the post-refine collapse.** Req 15 cites `test_complete_md_finalization_commit.py`, but that file guards `complete.md`'s finalization region, not `post-refine-commit.md`. The real `post-refine-commit.md` guard is `tests/test_post_refine_commit_wired.py` (a third file the spec never enumerates), whose content-token assertions break when the Req-14 collapse moves cancel/approval detection into the verb. Task 10 migrates **both** tests; flagged so the gate isn't surprised.
- **R3 — bug-2 flips a required-present token to absent.** `test_complete_md_finalization_commit.py` currently asserts `git add -u cortex/backlog/` is **present** in the finalization region (line 126). Dropping the sweep (Req 11) makes that assertion wrong; Task 10 must flip it, not just add tokens.
- **R4 — shared gh-stub extension must stay backward-compatible.** `tests/fixtures/gh-stub.sh` is consumed by `test_runner_pr_gating.py`; Task 2 adds scenarios without touching existing ones, and Task 6's gate runs that suite to prove no regression.
- **R5 — trunk + concurrent offload-epic sessions.** Editing `cortex_command/` requires sequential/trunk dispatch (the editable install makes worktree verification test stale code). Commit with explicit `git commit -- <files>` pathspecs so a sibling session's staged files don't leak in; run `just build-plugin` with each prose-edit commit (drift hook).

## Acceptance

The whole feature ships in one lifecycle (#330 is complete): `cortex-lifecycle-complete-route <slug>` and `cortex-lifecycle-stage-artifacts --phase {complete|refine} --feature {slug}` are registered console-scripts modeled on `detect-phase`; `complete.md` Step 7 + Step 11a and `post-refine-commit.md` staging collapse to verb calls (`grep` shows the verb invocations, the routing/staging narration and the bug-2 `-u` sweep gone), with Step 7 dispatched on `route`/`continue_to` (Branch 2 → Step 12, no duplicate `feature_complete`); the bug-2 backlog staging is narrowed to the resolver-filename + `index.md` (no unrelated dirty `cortex/backlog/` ticket is swept); `feature_complete` (Step 11) emits via `cortex-lifecycle-event log --set/--set-json` **in place** (today's ordering — row stays committed), with `pr_opened` left hand-written per ADR-0020; the golden-route-table, staged-set, and `feature_complete` round-trip tests pass; the migrated guard tests (`test_lifecycle_complete_state_routing.py`, `test_complete_md_finalization_commit.py`, `test_post_refine_commit_wired.py`) target the verbs; and the per-phase gate (mirror in sync, parity no-E002, events-registry green, `just test`, kept-pauses + phase parity, audits) is green at each of the three phase boundaries. bug-2 (`-u` sweep) ships in #331; the bug-1 emission-ordering fix is tracked in #339 (its spec-prescribed form regresses — see R1).
