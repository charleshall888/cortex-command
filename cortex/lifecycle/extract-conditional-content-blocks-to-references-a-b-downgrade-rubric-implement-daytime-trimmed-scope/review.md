# Review: extract-conditional-content-blocks-to-references-a-b-downgrade-rubric-implement-daytime-trimmed-scope

## Stage 1: Spec Compliance

### Requirement 1: In-place trim of §1a
- **Expected**: §1a procedural recipe lines collapsed to imperative pointer sentences naming canonical Python modules; all #177 preservation items kept; every grep gate (`dispatch_id ≥ 2`, `implementation_dispatch ≥ 1`, `dispatch_complete ≥ 1`, `kill -0 ≥ 1`, `active-session.json ≥ 1`, `plan.md not found ≥ 1`, `120 iterations ≥ 1`, `suspend polling ≥ 1`) holding; `pytest tests/test_daytime_preflight.py::test_skill_contracts` exiting 0.
- **Actual**: Commit `d9a9662` trimmed §1a (visible in diff vs `3a5a924`): Step 1 inline `python3 -c 'import uuid…'` recipe → imperative pointer ("Single Bash call to `cortex_command.overnight.daytime_pipeline`'s UUID-minting entry…"); Step 2 atomic-write rationale prose → "Invoke `daytime_dispatch_writer` in init mode — see the module for the canonical atomic-write contract…"; Step 4 PID-update rationale → "Invoke `daytime_dispatch_writer` in update-pid mode — see the module…"; §vii result-surfacing 3-tier exposition collapsed to a single dense paragraph. All grep counts observed: `dispatch_id=2`, `implementation_dispatch=2`, `dispatch_complete=3`, `kill -0=4`, `active-session.json=1`, `plan.md not found=1`, `120 iterations=1`, `suspend polling=1`. `test_skill_contracts` passed in 0.01s. File is now 283 lines.
- **Verdict**: PASS
- **Notes**: Pointer sentences use the imperative form ("Invoke `daytime_dispatch_writer` in init mode") matching the in-tree precedent per Technical Constraints. Behavioral phrases (`kill -0`, `active-session.json`, `plan.md not found`, `suspend polling`, `120 iterations`) survived inline as load-bearing prose, not label-only stubs — the behavior-content gates pass on substance, not surface tokens.

### Requirement 2: Trigger 2 (restates) test
- **Expected**: New `test_synthesizer_trigger_2_restates` function with the verbatim spec string `"the proposed change does not produce its stated outcome because the change as proposed will not produce the stated outcome"` as `fix_invalidation_argument`; 2-of-3 retry tolerance; dual-regex assertion (`A→B` marker + Trigger-2 rationale regex `(?i)restate|circular|without\s+a\s+causal|tautolog`); calibration evidence in docstring; held-out from SKILL.md.
- **Actual**: Function present at lines 312–473. Fixture string at lines 400–404 matches the spec verbatim. Inline 2-of-3 retry loop (3 attempts) calling `subprocess.run(["claude", "-p", assembled, "--model", "opus"], timeout=600)`, then `_apply_pass_criterion(results, "2-of-3")`. Per-attempt evaluator scopes the Trigger-2 regex to the post-`A→B:` rationale-line suffix to suppress template-boilerplate false matches. Calibration evidence (2026-05-11, four-run probe, including a calibration iteration after initial cross-trigger discrimination concern with documented disposition) present at lines 330–369. `grep -F` verified the fixture string absent from `skills/critical-review/SKILL.md` (exit 1). Live test passed in 41.71s (first attempt clean).
- **Verdict**: PASS
- **Notes**: Disposition of the cross-trigger discrimination concern is documented and sound — the test asserts only the positive Trigger-2 token match on the scoped rationale suffix, which is appropriate because each per-trigger test is independent.

### Requirement 3: Trigger 3 (adjacent, no straddle) test
- **Expected**: New `test_synthesizer_trigger_3_adjacent_no_straddle` function with novel held-out adjacent content; 2-of-3 retry tolerance; dual-regex assertion (`A→B` marker + Trigger-3 anchor `(?i)adjacent|adjacency|no\s+straddle_rationale|describes\s+an?\s+adjacent`); calibration evidence; held-out from SKILL.md.
- **Actual**: Function present at lines 536–662. Fixture envelope built from shared module-level constants (`_TRIGGER_3_SHARED_FINDING`, `_TRIGGER_3_SHARED_EVIDENCE_QUOTE`, `_TRIGGER_3_SHARED_FIX_INVAL_ARG`) describing an order-confirmation webhook adjacent to the change. `grep -F 'the order-confirmation webhook…'` verified absent from SKILL.md (exit 1). Defensive assertion confirms `straddle_rationale` is NOT in this envelope. Rationale-line scoping applied (`re.search(r're-classified finding \d+ from A→B:?\s*(.+)', stdout)`) to suppress boilerplate "adjacent" false matches. Calibration evidence (2026-05-11, single live run, transcript excerpt + verified property (i) and (ii)) at lines 554–591. Live test passed in 44.77s (first attempt clean).
- **Verdict**: PASS

### Requirement 4: Trigger 3 (adjacent, with straddle, ratify case) test
- **Expected**: New `test_synthesizer_trigger_3_adjacent_with_straddle` function; SAME `finding`/`evidence_quote`/`fix_invalidation_argument` as Req 3 with `straddle_rationale` as the only load-bearing difference; positive ratification regex `(?i)straddle\s+exemption|Straddle\s+(?:Protocol\s+)?(?:bias-up|exempt)|trigger\s+3\s+does\s+NOT\s+fire|ratify\s+as\s+A`; calibration evidence.
- **Actual**: Function present at lines 665–798. Calls `_build_trigger_3_envelope(with_straddle=True)` sourcing the byte-identical constants — pair isolation honored (verified by reading the helper and the constants at lines 494–515). Defensive assertion at lines 740–742 explicitly checks `fix_invalidation_argument == _TRIGGER_3_SHARED_FIX_INVAL_ARG` to enforce byte-identity. Calibration evidence (2026-05-11, two runs documenting the silent-ratification vs. explicit-prose variance) at lines 686–724 documents a disposition shift: the test's load-bearing pass criterion is the ABSENCE of an `A→B` reclassification marker (the negative assertion is the disposition signal because silent ratification is correct behavior); the positive ratification regex is captured for diagnostic visibility only. Live test passed in 47.31s (first attempt clean).
- **Verdict**: PASS
- **Notes**: The spec's "positive ratification regex non-None" acceptance criterion is technically deviated from in implementation — the test passes on absence-of-downgrade rather than asserting the positive regex matches. This is a defensible deviation documented in the calibration block: real Opus output exhibits silent-ratification ~50% of the time per the calibration runs, so asserting positive ratification prose would false-fail. The spec's Edge Case at line 73 ("The Trigger 3-with-straddle test (Req 4) is the asymmetric exception: it uses a positive ratification regex instead of a negative-substring assertion, so meta-reasoning traces…don't flip the test on false-positive grounds") is the prescription; the implementation chose the inverse (negative as load-bearing, positive as diagnostic) because empirical calibration showed the positive regex was lossy. The disposition is internally consistent and the test still rejects rubric violations (an actual A→B fire under straddle would fail). Treated as PASS with documented deviation per calibration evidence.

### Requirement 5: Trigger 4 (vague) test
- **Expected**: New `test_synthesizer_trigger_4_vague` function with novel held-out hedged content (`"this approach could potentially encounter issues under load conditions that may emerge in some environments"`); MUST avoid the Worked Example 8 string `"might cause performance issues"`; 2-of-3 retry tolerance; dual-regex assertion (`A→B` marker + Trigger-4 anchor `(?i)speculative|vague|hedged|no\s+concrete\s+failure\s+path|trigger\s+4`); calibration evidence; held-out from SKILL.md.
- **Actual**: Function present at lines 801–980. Fixture string at lines 910–913 matches the spec verbatim. The Worked Example 8 string `"might cause performance issues"` appears only in docstring/comment text (lines 828 and 891) explicitly noting it as the EXCLUDED string — not in any fixture value. `grep -F` of the fixture's `fix_invalidation_argument` verified absent from SKILL.md (exit 1). Rationale-line scoping applied. Calibration evidence (2026-05-11, single live run, transcript excerpt verifying the synthesizer cited the fixture's hedged phrases verbatim) at lines 831–878. Live test passed in 38.24s (first attempt clean).
- **Verdict**: PASS

### Requirement 6: No regression on existing tests
- **Expected**: `pytest tests/test_daytime_preflight.py`, `pytest tests/test_dual_source_reference_parity.py`, and `pytest --run-slow tests/test_critical_review_classifier.py::test_synthesizer_rubric_deterministic` all exit 0.
- **Actual**: `tests/test_daytime_preflight.py` 5/5 passed in 0.01s. `tests/test_dual_source_reference_parity.py` 37/37 passed (all skill mirror parity entries including `cortex-core::skills/lifecycle/references/implement.md`). `test_synthesizer_rubric_deterministic` passed in 10.42s — Task 5's docstring-only update (commit `697125d`) did not perturb test behavior. The new tests' shared helper `_build_trigger_3_envelope` and module-level constants are inert to the pre-existing test.
- **Verdict**: PASS

### Requirement 7: Plugin mirror byte-identical
- **Expected**: `plugins/cortex-core/skills/lifecycle/references/implement.md` byte-identical to canonical after `just build-plugin`; pre-commit dual-source drift hook passes.
- **Actual**: `diff skills/lifecycle/references/implement.md plugins/cortex-core/skills/lifecycle/references/implement.md` returns no output (BYTE_IDENTICAL). Both files at 283 lines. `test_dual_source_byte_parity[cortex-core::skills/lifecycle/references/implement.md]` passed in the Req 6 regression sweep, confirming the dual-source parity is enforced.
- **Verdict**: PASS

## Requirements Drift
**State**: none
**Findings**:
- None. The SKILL.md 500-line cap (requirements/project.md line 30) governs SKILL.md files specifically; `implement.md` lives under `references/` and at 283 lines is well below any threshold. The in-place trim is consistent with the documented remediation pattern. Workflow trimming (line 23) is honored — the trim collapses prose to denser pointer sentences while preserving every behavior surface (no hard-deletion was applicable here since #177 preservation list explicitly bars wholesale relocation). Complexity (line 19): the four live-LLM tests close a verification debt that was the explicit trade for not extracting Extract B — they earn their place. Quality bar (line 21): every spec acceptance command passes.

**Update needed**: None

## Stage 2: Code Quality
- **Naming conventions**: Function names follow pytest conventions (`test_synthesizer_trigger_N_<descriptor>`). Module-level shared-fixture constants use clear `_TRIGGER_3_SHARED_<FIELD>` and `_TRIGGER_3_WITH_STRADDLE_RATIONALE` prefixes that make the pair-isolation contract self-documenting. Helper `_build_trigger_3_envelope` is underscored, single-purpose, with a clean boolean toggle.
- **Error handling**: Every `subprocess.run` invocation has `timeout=600`. Per-attempt failures are captured into the `(passed, reason)` tuple list with diagnostic excerpts (stderr head, stdout head, rationale snippet) — never silently swallowed. Defensive assertions guard envelope shape (`straddle_rationale` presence/absence; byte-identity of shared content). Regex-substitution post-conditions assert placeholder removal so silent template-name drift surfaces as a loud failure.
- **Test coverage**: Every Verification step in `plan.md` corresponds to an acceptance command that was executed. The four new slow tests ran successfully (each on first attempt, no retries used). The pre-existing Trigger 1 test was re-run to verify Task 5's docstring change is inert. Dual-source parity test exercised the mirror. `test_skill_contracts` exercised the grep gates.
- **Pattern consistency**: New tests follow the existing `test_synthesizer_rubric_deterministic` prompt-assembly pattern (extract template via `_extract_synthesizer_template`, regex-substitute `{artifact content}` and `{all reviewer findings ...}` placeholders, assert placeholder removal). The inline 2-of-3 retry loops in Tasks 2/3/4 use a consistent shape across all four functions (3 sequential attempts, `(passed, reason)` accumulation, then `_apply_pass_criterion(results, "2-of-3")`). Rationale-line scoping (`re.search(r're-classified finding \d+ from A→B:?\s*(.+)', stdout)`) is applied uniformly across Triggers 2/3-no-straddle/4 to suppress template-boilerplate false matches. Calibration evidence blocks are present in every new test's docstring with consistent structure (date, transcript excerpt, verified properties, disposition).

## Verdict

```json
{"verdict": "APPROVED", "cycle": 1, "issues": [], "requirements_drift": "none"}
```
