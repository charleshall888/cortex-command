# Plan: fix-selected-path-interactive-worktree-orphan

## Overview
Fix the selected-path interactive-worktree orphan-lock by reordering the lock acquire from §1 Step B into §1a.ii (after the §1a.i overnight guard), unifying both entry modes — extending #348's guard-then-acquire pattern. Driven test-first: the discriminating structural test is authored and observed RED on the pre-fix tree, then the reorder turns it GREEN, and test + reorder + mirror land in one commit. The deferred §1a.iii-failure orphan is filed as a follow-up.

## Outline

### Phase 1: TDD fix (tasks: 1)
**Goal**: Author the discriminating test (red on current), apply the reorder + prose scrub, regenerate the mirror, land one green commit.
**Checkpoint**: `just test` exits 0 with the new test passing; the pre-fix red was observed before the reorder; canonical/mirror byte-identical.

### Phase 2: File the deferred follow-up (tasks: 2)
**Goal**: File the deferred §1a.iii owner-checked-release follow-up ticket (whole-feature acceptance is established by Phase 1's `just test`).
**Checkpoint**: the follow-up backlog ticket exists and is committed.

## Tasks

### Task 1: Reorder the selected-path acquire into §1a.ii (test-first, one green commit)
- **Files**: `tests/test_implement_worktree_interactive_contract.py`, `skills/lifecycle/references/implement.md`, `plugins/cortex-core/skills/lifecycle/references/implement.md`
- **What**: Add a discriminating structural regression test, observe it RED against the current `implement.md`, then reorder the acquire and scrub the dangling prose so the test goes GREEN; regenerate the mirror; commit all three files together.
- **Depends on**: none
- **Complexity**: complex
- **Context**:
  - **Test** (spec R1): mirror the section-extraction style of `test_gate_and_gated_path_use_same_binary` in the same file. Extract §1 via `re.search(r"### 1\. Pre-Flight Check.*?(?=### 1a\.)", text, re.DOTALL)`; §1a via `r"### 1a\..*?(?=### 2\.)"` (bounded at the next real heading `### 2. Task Dispatch`, **not** `\Z`/EOF); the §1a.ii step by narrowing §1a between the `**ii.` and `**iii.` markers. Three assertions: (i) `"cortex-interactive-lock acquire" not in section_1`; (ii) within §1a, earliest `cortex-interactive-lock acquire` index > last `_interactive_overnight_check.sh` index (forward-guard); (iii) **[unconditionality discriminator]** the §1a.ii step contains `cortex-interactive-lock acquire` **and** contains neither the substring `selected` nor `suppressed` (case-insensitive, within the `**ii.`→`**iii.` step). Rationale: the acquire-present check alone is satisfied by the *suppressed* arm and says nothing about the selected path; the real invariant is that §1a.ii acquires **unconditionally** (no per-entry-mode branch). Any dead-arm variant — including a paraphrase like "for picker-selected … no acquire needed here; for suppressed … acquire" (the B2 half-fix) — must name at least one mode and is therefore caught, whereas the correct unified step names neither mode. This replaces an exact-`` Entry mode `selected` `` label check, which "picker-**selected**" evades. Indices in (ii) are §1a-relative; (i) is §1-relative; (iii) is measured within the §1a.ii step substring — separate origins, asserted independently. The `find("**ii.")`/`find("**iii.")` extraction depends on those markers surviving the reorder (see Reorder: keep the `**ii.` heading).
  - **Reorder** (spec R2): delete the §1 Step B block (current lines ~75–81, the `**Step B — Interactive lock acquisition**` fenced `cortex-interactive-lock acquire {slug}`); rewrite §1a.ii (current lines ~102–105) so the acquire is **unconditional** for both entry modes — remove the `` Entry mode `selected` ``/`` Entry mode `suppressed` `` split and the `selected`-arm skip ("§1 Step B already acquired… do **not** acquire again"). **Keep the `**ii.` step-marker heading** (the test extraction and `test_gate_and_gated_path_use_same_binary` depend on it) and the "only after the overnight guard (i) has passed" rationale and the exit-0→iii / non-zero→surface-stderr-and-exit handling. **The unified §1a.ii must name neither entry mode** ("selected"/"suppressed") — phrase it as one unconditional acquire (e.g. "acquire the per-feature lock … Exit 0 → proceed to iii; non-zero → surface stderr and exit §1a") so assertion (iii) is satisfied and no dead arm can hide.
  - **Scrub** (spec R3): fix every dangling reference so §1 reads as **one** preflight guard — line ~19 ("run Step A … and Step B … below" → drop the Step B clause), line ~63 ("run the two interactive preflight guards below (Steps A and B)… If either guard rejects" → one guard, "Step A", "If it rejects"), line ~73 (**both** clauses — the exit-`0` "proceed to Step B" **and** the exit-`2` stale-runner "proceed to Step B" → "proceed to §1a"), line ~104 (removed with the split). The `grep -niEc "…step b…"` = 0 acceptance backstops any missed clause.
  - **Preserve** (spec R4): keep §1's overnight pre-check labeled **`Step A`** (do NOT relabel) so line ~100's back-ref "semantics as §1 Step A" still resolves; keep the marker recording ("record entry mode `selected`"/"`suppressed`") and Step v's marker branch untouched (ADR-0008). Do not touch `cortex_command/lifecycle_implement.py`.
  - **Mirror** (spec R5): after editing canonical, run `just build-plugin` to regenerate `plugins/cortex-core/skills/lifecycle/references/implement.md`; stage canonical + mirror + test in ONE commit (dual-source drift pre-commit gate).
  - **TDD ordering** (spec R7, critical-review A2): run the new test against `implement.md` BEFORE the reorder edit and capture the failing (red) output; then apply the reorder and confirm green. Do not commit a red tree — the single commit is green.
  - Commit with explicit pathspec (concurrent session active in the repo — see Risks). Use `/cortex-core:commit`.
- **Verification**:
  - (a) Discriminating power comes from assertion (iii): it reds on the pre-fix tree **and** on the B2 half-fix (a §1a.ii that still names an entry mode / skips the selected acquire), so a green (iii) means the acquire is genuinely unconditional. The TDD red-before-green run is a *sanity check that the test actually fires* (a no-op or mis-extracting test would not red on the pre-fix tree) — run the new test against `implement.md` before the reorder, confirm red, then confirm green after; report both. The red is reconstructable at review time via `git checkout HEAD~1 -- skills/lifecycle/references/implement.md` + re-run, so it is not a bare self-report.
  - (b) `just test` exits 0 (full suite, incl. `test_sidecar_invocation_form_bash_s_count`, `test_overnight_guard_sidecar_called_at_least_twice`, `test_gate_and_gated_path_use_same_binary`, `test_lifecycle_step_v_ordering`, `test_dual_source_reference_parity`).
  - (c) independent whole-file greps on `skills/lifecycle/references/implement.md` (the §1-scoped and §1a.ii-scoped checks live in the pytest test, which regex-scopes; shell grep cannot scope to a section): `grep -niEc "step b|steps a and b|two interactive preflight guards|either guard rejects"` = 0; `grep -c "§1 Step A"` ≥ 1; `diff skills/lifecycle/references/implement.md plugins/cortex-core/skills/lifecycle/references/implement.md` exits 0.
- **Status**: [x] complete

### Task 2: File the §1a.iii owner-checked-release follow-up ticket
- **Files**: `cortex/backlog/{NNN}-owner-checked-release-on-abort-for-1a-iii-worktree-create-failure-orphan.md` (new; NNN = next backlog id)
- **What**: File the deferred follow-up for the §1a.iii `cortex-worktree-create`-failure orphan — a release-on-abort that must be **owner-checked** to be safe.
- **Depends on**: [1]
- **Complexity**: simple
- **Context**: create via the backlog tooling (`/cortex-backlog:backlog new` or `cortex-backlog-author`). The body must record: the §1a.iii post-acquire abort leaks the lock on BOTH entry modes (pre-existing, unchanged by #355); the fix is a release-on-abort at the §1a.iii failure exit; it **must be owner-checked** because `release_lock(feature_slug)` in `cortex_command/interactive_lock.py` unlinks unconditionally (no owner arg), so a naive release would delete another session's lock under the non-atomic `acquire_lock` double-pass (the clobber from #355's Edge Cases); scope covers both entry modes symmetrically (post-#355 the two paths are unified). Reference #355 as the parent context. Do not implement the fix here — only file the ticket. Commit the new ticket file with explicit pathspec via `/cortex-core:commit`.
- **Verification**: (b) the new backlog file exists under `cortex/backlog/` with a title referencing the §1a.iii orphan (`grep -rl "1a.iii\|worktree-create.*orphan\|owner-checked release" cortex/backlog/` returns the new file) and is committed (`git log --oneline -1 -- cortex/backlog/` shows it); (a) `just backlog-index` exits 0.
- **Status**: [x] complete

## Risks
- **Concurrent session in the repo**: the working tree currently carries another session's uncommitted work (`compress-projectmd-sections-that-restate-adrs`). Both tasks MUST commit with **explicit pathspec** (never `git add -A`) so that work is not captured. This also argues for **trunk sequential dispatch** over an interactive worktree (shared-index + interactive-worktree sub-agent instability). Note: `just build-plugin` regenerates the **whole** mirror tree — if the concurrent session has uncommitted canonical edits, build-plugin may produce mirror changes for *their* files too; stage/commit **only** `plugins/cortex-core/skills/lifecycle/references/implement.md` (the #355 mirror) by explicit path and leave the rest, and verify the drift gate passes on the committed set before finalizing.
- **§1a.iii deferral**: the adjacent §1a.iii-failure orphan is deferred to the Task-2 follow-up. Two critical-review reviewers argued for including it now; it is deferred because the safe fix needs an owner-checked release (a real design task), and because #355 stays a clean single-mechanism structural change. The user may override at plan approval to fold a scoped §1a.iii release into #355 — if so, add an owner-checked-release requirement + its own discriminating test before implementing.
- **Dispatch-mode recommendation**: trunk (current branch), sequential, explicit-pathspec commits — surfaced at the merged plan-approval/branch surface.

## Acceptance
On the picker-`selected` interactive-worktree path, the lock acquire occurs only at §1a.ii (after the §1a.i overnight guard), identical to the `suppressed` path: `skills/lifecycle/references/implement.md` §1 contains no `cortex-interactive-lock acquire`, §1a.ii acquires **unconditionally** with no per-entry-mode branch (names neither "selected" nor "suppressed"), no dangling Step B / two-guard prose remains, the mirror is byte-identical, and `just test` is green with a new discriminating test whose unconditionality assertion reds on both the pre-fix tree and a dead-arm half-fix. The residual §1a.iii-failure orphan is tracked by a filed follow-up ticket.
