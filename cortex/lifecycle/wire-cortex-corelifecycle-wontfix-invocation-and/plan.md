# Plan: wire-cortex-corelifecycle-wontfix-invocation-and (#329)

## Overview
Structuralize the `/cortex-core:lifecycle` invocation grammar behind a tested `cortex-lifecycle-parse-args` console-script (parse → `{mode,feature,phase}`), rewrite SKILL.md Step 1 into a thin act-on-`mode` routing table (preserving a phase-override-honoring route) guarded by a docs-derived drift-guard + mode-coverage test, then add an order-enforcing `cortex-lifecycle-wontfix` verb (archive → append → terminalize, fail-forward/idempotent) and thin `references/wontfix.md` to a single verb call. The parser/verb live as console-script layers beneath the prose skill; the skill shells out rather than narrating procedure.
**Architectural Pattern**: layered
<!-- A structural CLI layer (parser + verb) sits beneath the model-executed SKILL.md prose, which is reduced to shelling out and acting on returned data. -->

**Gate-sequencing invariant (load-bearing — see Risks).** `cortex-check-parity` enforces drift in BOTH directions: W003 (deployed-but-unreferenced) AND **E002 (referenced-but-undeployed, an error/exit-1 — `parity_check.py:775-790`)**. The pre-commit hook runs `check-parity --staged` on any staged `skills/*`. Therefore each new `cortex-*` console-script's `pyproject` deployment MUST land in the **same commit** as its first SKILL.md/reference mention — neither direction may straddle a commit boundary. This supersedes the lifecycle `plan.md` reference's "wiring-first" hint, which is wrong about E002.

## Outline

### Phase 1: Structural invocation parser + reserved-sub-command routing (tasks: 1, 2)
**Goal**: Replace Step 1's prose first-word parse with a tested grammar CLI deployed-and-referenced atomically, reconcile every advertised slash-command form (preserving explicit-phase-override routing), and add a docs-derived drift-guard + mode-coverage test that catches the doc↔parser drift class for the current closed grammar.
**Checkpoint**: `cortex-lifecycle-parse-args` unit tests green; SKILL.md Step 1 routes via the helper with a per-`mode` handler table and an explicit-phase-override consumer (so `complete <slug>` and `<feature> <phase>` reach their named phase); `tests/test_lifecycle_invocation_grammar_parity.py` green (robust scrape/normalize, both negative controls, mode coverage). The `wontfix` route is described by mode-name only (no `cortex-lifecycle-wontfix` token yet → no E002); the verb deploys in Phase 2.

### Phase 2: Wontfix order-enforcing verb + reference thinning (tasks: 3, 4, 5, 6)
**Goal**: Deploy `cortex-lifecycle-wontfix` (with its SKILL.md token citation in the same commit), prove its archive/append/terminalize contract with discriminating tests, thin `wontfix.md`, and close registry/ADR/mirror hygiene.
**Checkpoint**: `cortex-lifecycle-wontfix` deployed + tested (untracked-dir archive incl. `archive/` auto-create, all four guard branches, worktree-without-env refusal, fail-forward recovery with the correct monkeypatch binding); `wontfix.md` carries no `git mv`; ADR claimed at the next-free number; `just test` green with `check-parity`/`check-contract`/`check-events-registry` clean on the staged diff.

## Tasks

### Task 1: Deploy `cortex-lifecycle-parse-args` + rewrite SKILL.md Step 1 into an act-on-`mode` routing table (atomic deploy+wire)
- **Files**: `cortex_command/lifecycle/parse_args.py` (new), `cortex_command/lifecycle/tests/test_parse_args.py` (new), `pyproject.toml`, `skills/lifecycle/SKILL.md`, `plugins/cortex-core/skills/lifecycle/SKILL.md` (mirror, via `just build-plugin`)
- **What**: Create the grammar CLI emitting `{"mode","feature","phase"}`, deploy it as `cortex-lifecycle-parse-args`, AND rewrite Step 1 to invoke it and act on `mode` — all in one commit so the deploy and the first reference co-locate (E002+W003 both clean). The `wontfix` route is named by mode only (the `cortex-lifecycle-wontfix` token arrives with the verb in Task 3).
- **Depends on**: none
- **Complexity**: complex
- **Context**:
  - **Module shape** (follow `branch_mode_cli.py`/`dispatch_choice_cli.py`): `_build_parser() -> argparse.ArgumentParser` with a single positional `arguments` (the raw `$ARGUMENTS`; `nargs="?"`, default `""`); `main(argv: Optional[List[str]] = None) -> int` whose first line is `_telemetry.log_invocation("cortex-lifecycle-parse-args")` (`from cortex_command.backlog import _telemetry`); print `json.dumps({...})`; `return 0`. Expose a pure `parse(arguments: str) -> dict` helper so tests bypass argv. `if __name__ == "__main__": sys.exit(main())`.
  - **Closed mode set as a module constant** (Req 2/10): `KNOWN_MODES = ("wontfix","resume","complete","phase","feature","needs-derivation","empty","error")` — the constant Task 2's mode-coverage test enumerates.
  - **Canonical parse order** (Req 3): empty-check (`mode=empty`) → `#`-sigil handling → reserved-word match (post-sigil, lowercased word #1) → slug/prose-derivation → resolve & route.
  - **`#`-sigil** (spec Edge Cases / OQ-A): a leading `#` on word #1 **suppresses reserved-matching** — `#wontfix` → `mode=feature, feature=wontfix` (not the verb).
  - **Reserved first-words** {`wontfix`,`resume`,`complete`} **invert** the default order (word #2 = target): `wontfix <slug>` → `mode=wontfix, feature=slug`; `wontfix` alone → `mode=error`. `resume <slug>` → `mode=resume, feature=slug`; `resume` alone → `mode=empty` (scan, Req 5). `complete <slug>` → `mode=complete, feature=slug, phase=complete` (Req 6); `complete` alone → `mode=phase, phase=complete`.
  - **Bare phase tokens** {`research`,`specify`,`plan`,`implement`,`review`} sole word → `mode=phase, phase=<token>` (Req 7). Default `<feature> [phase]`: valid-slug word #1 → `mode=feature, feature=word1, phase=word2-or-""`; non-slug word #1 → `mode=needs-derivation`. Valid-slug pattern `^[a-z0-9]+(-[a-z0-9]+)*$`.
  - **SKILL.md Step 1 rewrite**: invoke `cortex-lifecycle-parse-args "$ARGUMENTS"`; act on `{mode,feature,phase}` via a routing-table region naming every `KNOWN_MODES` literal (so Task 2's mode-coverage passes). Per-mode: `wontfix` (invoke the **wontfix verb** with the slug, report, **halt** — terminal/short-circuit; named by mode only here, token added in Task 3), `resume` (feature=slug → Step 2, but refuse "no such lifecycle to resume" when `cortex/lifecycle/<feature>/` absent — do not create), `complete` (enter the Complete phase for feature=slug via the explicit-override route below), `phase` (bare token — surface "specify a feature, e.g. `/cortex-core:lifecycle <feature> plan`"), `feature` (resolver + Step 2), `needs-derivation` (model derives a kebab slug from `$ARGUMENTS`, then proceeds as `feature`), `empty` (incomplete-lifecycle scan), `error` (reserved word missing its slug — report and halt).
  - **Preserve the explicit-phase-override consumer (load-bearing — fixes the `complete`/`<feature> <phase>` mis-route)**: relocate SKILL.md:169's honor behavior into Step 1 — **when the parser returns a non-empty `phase` (the `complete` mode, or `feature` mode with a phase token in word #2), enter that phase directly (warn if prerequisites missing) rather than deferring to Step 2's artifact detector** (`detect-phase` takes only a feature dir and ignores `phase`). Reword the bare-`<phase>` promise (Req 7/9) but **keep** explicit-feature+phase honoring. Remove the old "first word = feature name … second word … phase override" prose (Req 4). Reword `:25` ("active feature") and `:169` (replace "honor the request" wording while preserving the resolved-feature+phase behavior). Add `/cortex-core:lifecycle wontfix {{slug}}` to the Invocation block. **Do not edit the frontmatter** `argument-hint`/`inputs` — the preserved override keeps `inputs.phase` accurate (L1 surface untouched).
  - **Caller enumeration (run live, do not trust hardcoded line numbers)**: `grep -rn '/cortex-core:lifecycle' skills/lifecycle/` to enumerate the forms Task 2 will sweep. Note: `parallel-execution.md` lines DO carry args (`120`, `{feature}`) — they are forms, not bare mentions; `implement.md`/`post-refine-commit.md`/`criticality-matrix.md` carry bare no-arg mentions. `complete.md:72/80/148` advertise `complete <slug>`/`wontfix <slug>` which the new grammar+override-route now deliver — no `complete.md` edit needed once the override route exists (verify in Task 2's drift-guard).
  - **Binstub materialization**: adding a `[project.scripts]` entry does not create the PATH binstub until the editable install is refreshed. After editing `pyproject.toml`, re-run the editable install (or verify via `python3 -m cortex_command.lifecycle.parse_args …` against the working tree). `_telemetry`/tests run in-process regardless.
  - Run `just build-plugin`; stage canonical + mirror together (dual-source hook).
- **Verification**: `python3 -m pytest cortex_command/lifecycle/tests/test_parse_args.py` exits 0 AND `python3 -m cortex_command.lifecycle.parse_args "wontfix add-foo" | python3 -c "import json,sys; d=json.load(sys.stdin); assert d['feature']=='add-foo' and d['mode']=='wontfix'"` exits 0 AND `grep -c 'cortex-lifecycle-parse-args' skills/lifecycle/SKILL.md` ≥ 1 AND `grep -c 'first word = feature name' skills/lifecycle/SKILL.md` = 0 AND `grep -c 'honor the request' skills/lifecycle/SKILL.md` = 0 AND `python3 -m pytest tests/test_dual_source_reference_parity.py -q` exits 0 — pass if all hold.
- **Status**: [x] done — c5643846 (17 parser tests pass; smoke OK; greps 1/0/0/4; dual-source 59 pass; parity+contract clean)

### Task 2: Add the docs-derived drift-guard + mode-coverage parity test
- **Files**: `tests/test_lifecycle_invocation_grammar_parity.py` (new)
- **What**: Scrape advertised `/cortex-core:lifecycle <form>` occurrences from the live doc bytes, normalize placeholders, feed each through the parser, and assert each classifies to its expected `mode` via a shape→mode oracle — with two negative controls — plus a mode-coverage assertion that every `KNOWN_MODES` literal appears in SKILL.md's Step 1 routing region.
- **Depends on**: [1]
- **Complexity**: complex
- **Context**:
  - Follow `tests/test_lifecycle_config_parity.py`'s shape: module-level `REPO_ROOT = Path(__file__).resolve().parent.parent`, named path constants, a self-proving mutation sentinel.
  - **Scrape — robust capture (fixes the single-token degradation)**: match `/cortex-core:lifecycle` followed by its argument span up to the closing backtick/end-of-inline-code (capture the WHOLE argument span, e.g. `wontfix <slug>` as one unit, not just `wontfix`), then **strip surrounding markdown punctuation** (trailing/leading `` ` ``, `"`, `'`) and trailing prose (e.g. `parallel-execution.md`'s "and 121 in parallel"). **Require a non-empty argument span** so bare `/cortex-core:lifecycle` mentions (implement.md/post-refine-commit.md/criticality-matrix.md) are dropped. Verify the capture yields the two-token reserved forms intact (`wontfix <slug>`, `resume <feature>`, `complete <slug>`), not their lone first word.
  - **Normalize** (Req 8b): map placeholders (`<slug>`, `<feature>`, `{{feature}}`, `{feature}`, `<phase>`, `{{phase}}`, **and `{{slug}}`** — include the brace-slug form Task 1 adds) to canonical test tokens via a defined dict, then parse each.
  - **Oracle** (Req 8c): a shape→`mode` map keyed by first-word class (reserved word `wontfix`/`resume`/`complete` + presence of a second token; lone phase token; bare feature placeholder). Assert each scraped+normalized form's parser `mode` matches the oracle.
  - **Two negative controls** (Req 8): (i) removing a form's handling from the parser (in-memory mutated parse fn) fails the test; (ii) injecting a doc-surface fixture form whose oracle-expected mode differs from parser output fails it. **Scope honesty (B-finding)**: a *novel reserved verb* added to docs-only is NOT auto-caught (oracle and parser share the closed grammar and would agree on bare-feature) — document this in a test comment; the recurrence guarantee is scoped to the current closed grammar's forms, not new-verb additions.
  - **Mode coverage — discriminating** (Req 10, `-k mode_coverage`): read `KNOWN_MODES` from `cortex_command.lifecycle.parse_args`; slice SKILL.md's Step 1 routing-table region (between heading anchors) and assert each literal appears **as a routing-table entry**, not merely as an incidental English word (e.g. require it in a table row / backticked code span), so a missing handler for common-word modes (`complete`/`resume`/`empty`/`error`) is actually caught.
- **Verification**: `python3 -m pytest tests/test_lifecycle_invocation_grammar_parity.py` exits 0 (covers `-k grammar`/`-k mode_coverage`/`-k edge` and both negative controls, incl. the reserved-two-token-form captures) — pass if exit 0.
- **Status**: [x] done — 03edc3d6 (5 tests pass; mode_coverage selector 1 pass; 14 forms scraped; both negative controls fire)

### Task 3: Deploy the `cortex-lifecycle-wontfix` order-enforcing verb + cite it in SKILL.md (atomic deploy+wire)
- **Files**: `cortex_command/lifecycle/wontfix_cli.py` (new), `pyproject.toml`, `skills/lifecycle/SKILL.md`, `plugins/cortex-core/skills/lifecycle/SKILL.md` (mirror, via `just build-plugin`)
- **What**: A console-script verb performing, as sequential gated statements in one function, (a) archive-move, (b) byte-faithful newline-terminated `feature_wontfix` append, (c) backlog terminalization — fail-forward and idempotent. Deploy `cortex-lifecycle-wontfix` AND update SKILL.md's `wontfix` route to name the console-script token in the same commit (E002+W003 clean).
- **Depends on**: [1]
- **Complexity**: complex
- **Context**:
  - **Module shape**: `_build_parser()` with a **positional `slug`** + `--reason` + optional `--backlog-slug` (no required flags → contract-lint clean); `main(argv=None) -> int`, first line `_telemetry.log_invocation("cortex-lifecycle-wontfix")`. stderr prefix `cortex-lifecycle-wontfix:`. Exit codes: `0` success; `2` ambiguous backlog (propagated) + argparse usage; `1` move/IO failure, refusal, or zero-match-where-a-parent-was-expected.
  - **Slug validation first** (Req 14): reject `slug` not matching `^[a-z0-9]+(-[a-z0-9]+)*$` before any filesystem op.
  - **Main-root anchor + worktree refusal (fixes the silent-wrong-target foot-gun)**: resolve via `cortex_command.common._resolve_user_project_root()` (returns `CORTEX_REPO_ROOT` verbatim when set; else walks cwd, stopping at the first dir containing `cortex/` — which for a worktree is the worktree root). **Guard**: if `CORTEX_REPO_ROOT` is unset AND the resolved root's `.git` is a worktree pointer (a file, not a directory), **refuse** with `cortex-lifecycle-wontfix: refusing destructive archive from a worktree without CORTEX_REPO_ROOT set` (exit 1) rather than archiving a worktree-local copy. The lifecycle/overnight harness sets the env, so the common path is unaffected; this only bites direct manual worktree calls.
  - **Step (a) archive-move** (Req 12, OQ-E): compute `archive_root = root/"cortex"/"lifecycle"/"archive"`; **`archive_root.mkdir(parents=True, exist_ok=True)` first** (init never scaffolds `archive/`; `os.rename`/`shutil.move` require the dest parent to exist). Then `os.rename` (fallback `shutil.move`) `cortex/lifecycle/<slug>` → `archive/<slug>`. **4-case pre-flight guard** before the move: dst-exists-not-src → skip (no-op-if-archived); src-not-dst → move; both-exist → error (exit 1, no nesting); neither → unknown-slug error (exit 1). `os.rename` over `git mv` (exit-128 on untracked dirs; archive-skip is name-based at `cortex_command/hooks/scan_lifecycle.py:907`; target depth-2 `archive/<slug>/` matches the `clean.py` glob).
  - **Read backlog parent before the move** (Req 14): parse `cortex/lifecycle/<slug>/index.md` frontmatter for `parent_backlog_uuid`/`parent_backlog_id` (capture before step (a)). `--backlog-slug` overrides. Absent/null parent → step (c) documented no-op (not error).
  - **Step (b) append** (Req 13, OQ-F): build `row = json.dumps({"ts": <Z-ts>, "event": "feature_wontfix", "feature": <slug>, "reason": <reason>})` with **default** separators `(", ", ": ")`, insertion-ordered keys, `Z`-timestamp via `datetime.now(tz=timezone.utc).strftime("%Y-%m-%dT%H:%M:%SZ")` (NOT `+00:00`, NO `schema_version`). **Append `row + "\n"`** (the trailing newline is the CALLER's responsibility — `_append_event_atomic` writes its arg verbatim with no `\n`, unlike `log_event` which adds its own; omitting it concatenates the next append into one unparseable line). Call `_append_event_atomic(archived_log_path, row + "\n")` imported from `cortex_command.lifecycle_event` (reuses flock+tempfile+`os.replace`). **Coupling note**: this targets a private (`_`-prefixed) cross-module helper; Task 4's row-template regression test pins the behavior so a future refactor fails loudly. **Idempotent**: skip the append iff a prior row in the archived log has a parsed `json.loads(line).get("event") == "feature_wontfix"` (a parsed event-field match — NOT a substring grep, which a `reason` containing the token would false-positive). The check-then-append is not atomic; acceptable for a rare manual terminal op (document inline).
  - **Step (c) terminalization** (Req 14, OQ-C): subprocess `cortex-update-item <target> --status wontfix --lifecycle-phase wontfix --session-id null` (`update_item.main()` reads `sys.argv` directly — no `argv` param — so a library call can't pass args; shelling preserves exit-2 + backend routing and keeps the subpackage decoupled). `<target>` = `--backlog-slug` else index.md parent (prefer `parent_backlog_uuid`; confirm `cortex-update-item` resolves it during implementation). Propagate exit 2. **Verify** terminalization occurred (or was a deliberate no-op); zero-match where a parent was expected → exit 1. Skip if already terminal.
  - **Fail-forward / idempotent** (Req 15): never roll back the move; each step independently re-asserts its postcondition on re-invocation.
  - **ADR back-pointer**: a code comment on the ordering cites **ADR-0004** (no new ADR for the verb; the parser/grammar ADR is Task 6).
  - **SKILL.md citation**: update the `wontfix` route prose to name `cortex-lifecycle-wontfix <slug> --reason "…"` (no required flags → contract-clean). Add `cortex-lifecycle-wontfix = "cortex_command.lifecycle.wontfix_cli:main"` to `pyproject.toml`. Wheel-only — no plugin mirror for the module; run `just build-plugin` for the SKILL.md mirror and stage canonical+mirror together.
- **Verification**: `python3 -c "import cortex_command.lifecycle.wontfix_cli"` exits 0 AND `python3 -m cortex_command.lifecycle.wontfix_cli --help` exits 0 AND `grep -c 'cortex-lifecycle-wontfix' skills/lifecycle/SKILL.md` ≥ 1 AND `python3 -m pytest tests/test_dual_source_reference_parity.py -q` exits 0 — pass if all hold (behavioral coverage lands in Task 4).
- **Status**: [x] done — 6b857013 (import ok; --help exit 0; SKILL token=1; dual-source 59; parity+contract exit 0)

### Task 4: Unit-test the wontfix verb (all four guard branches, worktree refusal, discriminating row, fail-forward)
- **Files**: `cortex_command/lifecycle/tests/test_wontfix_cli.py` (new)
- **What**: Cover the verb's archive/append/terminalize contract with discriminating assertions, including the cases the previous decomposition missed.
- **Depends on**: [3]
- **Complexity**: complex
- **Context**:
  - Harness per `test_init_ensure.py`: temp git repo (`git init`, `commit.gpgsign false`), set `CORTEX_REPO_ROOT`/cwd, invoke `main(argv=[...])` in-process (and/or the module via `python3 -m`); stub `cortex-update-item` by monkeypatching the verb's subprocess call or seeding a fake backlog item the real producer resolves. The temp repo has **no pre-existing `archive/`** — so case (i) exercises the `mkdir(archive_root)` fix; a verb missing it fails here.
  - **Archive — all four guard branches** (Req 12): (i) an **untracked** lifecycle dir archives successfully into an auto-created `archive/`; (ii) re-running on an already-archived slug is a clean no-op (no nesting, no duplicate row); (iii) both-exist errors without nesting; (iv) **neither-exists → unknown-slug exit 1** (the previously-untested branch).
  - **Worktree resolution** (Req 12): (v) `CORTEX_REPO_ROOT`=main + cwd in a `git worktree add` checkout → archives the main-resident dir; (vi) **`CORTEX_REPO_ROOT` UNSET + cwd in a worktree → refusal (exit 1, no move)** — this is the discriminating worktree assertion (case (v) alone only proves the env short-circuit).
  - **Row (Req 13)**: assert (a) the emitted row feeds through `cortex_command.common.detect_lifecycle_phase` to `phase=complete` AND matches the `claude/statusline.sh` `"feature_wontfix"` grep; and (b) — the **discriminating** check — the row equals the documented template for the current four-field set with default separators, `Z`-timestamp, and a **trailing newline**, and that a SECOND append after it parses cleanly (catches the newline-omission regression). This template-equality is the pin for the private-import coupling.
  - **Terminalization (Req 14)**: (i) terminalizes via index.md parent id; (ii) a `..`/`/`-bearing slug rejected pre-move; (iii) ambiguous-slug exit-2 propagates; (iv) absent/null `index.md` no-ops step (c) cleanly; (v) zero-match where a parent was expected → non-zero.
  - **Fail-forward (Req 15)**: simulate a step-(b) failure after a successful move by monkeypatching **`cortex_command.lifecycle.wontfix_cli._append_event_atomic`** (the verb's imported binding — patching the source module would NOT affect the already-imported reference and the test would pass vacuously) to raise once, then re-invoke, and assert the `feature_wontfix` row is present and the backlog item terminal afterward.
- **Verification**: `python3 -m pytest cortex_command/lifecycle/tests/test_wontfix_cli.py` exits 0 — pass if exit 0.
- **Status**: [x] done — 1782db60 (13 tests pass; fixed test-only subprocess-patch scope so telemetry's git call isn't intercepted)

### Task 5: Thin `references/wontfix.md` to one verb call + fix the stale citation + mirror
- **Files**: `skills/lifecycle/references/wontfix.md`, `plugins/cortex-core/skills/lifecycle/references/wontfix.md` (mirror, via `just build-plugin`)
- **What**: Replace the three bash HOW-snippets and the prose ordering-gate with a single `cortex-lifecycle-wontfix <slug> --reason "…"` call noting the move→append→terminalize order is now a code invariant; keep the WHEN/WHY content; correct the stale citation.
- **Depends on**: [3]
- **Complexity**: simple
- **Context**:
  - Keep WHEN/WHY prose (B2): use-case triage, the `cortex-update-item` exit-2 ambiguity handling, a one-line detector-belt note (`scan_lifecycle.py:907` name-based archive-skip + `common.py` `feature_wontfix` terminal detection).
  - Remove the `git mv` step (`:9–15`), the `printf` append step (`:17–39`), and the "Step order is load-bearing" / "Why the step order matters" prose-gate paragraphs (`:5`, `:49–53`); the ordering rationale now lives as a code comment in the verb (Task 3).
  - **Fix the stale citation** (both `:5`, `:15`): `hooks/cortex-scan-lifecycle.sh:227` → `cortex_command/hooks/scan_lifecycle.py:907`.
  - This adds a SECOND in-scope reference to the already-deployed verb (gate-neutral). Run `just build-plugin`; stage canonical + mirror together.
- **Verification**: `grep -c 'git mv' skills/lifecycle/references/wontfix.md` = 0 AND `grep -c 'cortex-lifecycle-wontfix' skills/lifecycle/references/wontfix.md` ≥ 1 AND `grep -c 'scan_lifecycle.py:907' skills/lifecycle/references/wontfix.md` ≥ 1 AND `python3 -m pytest tests/test_dual_source_reference_parity.py -q` exits 0 — pass if all hold.
- **Status**: [x] done — 818ab5e9 (git mv 0; verb ref 1; citation 1; gate paragraphs removed; dual-source 59; parity+contract+events-registry exit 0)

### Task 6: Registry hygiene, ADR at next-free number, final mirror/test sweep
- **Files**: `bin/.events-registry.md`, `cortex/adr/00NN-structural-lifecycle-invocation-grammar.md` (new; `NN` = next-free, see Context), `cortex/lifecycle/wire-cortex-corelifecycle-wontfix-invocation-and/spec.md` (update the Proposed-ADR number reference to match)
- **What**: Update the `feature_wontfix` registry row to name the new Python producer (Should: flip `scan_coverage` to `manual`), author the parser/grammar/drift-guard ADR at the next-free number, and run the final gate sweep.
- **Depends on**: [2, 4, 5]
  <!-- transitively covers 1/3; the final `just test` sweep must run after every test-adding task (2, 4) and the thinning (5). -->
- **Complexity**: simple
- **Context**:
  - **ADR number collision (load-bearing — see Risks)**: the spec proposes "ADR-0017", but `cortex/adr/0017-reconcile-and-gate-lifecycle-config-sources.md` already exists (#335). Re-scan `cortex/adr/` at implement time (`ls cortex/adr/ | grep -E '^0[0-9]{3}-' | sort | tail`) and claim the **lowest free number** (currently `0018`; a concurrent #330 session may also be eyeing it — re-check immediately before writing). Name the file `00NN-structural-lifecycle-invocation-grammar.md` and update spec.md's "Proposed ADR: 0017…"/"captured as ADR-0017" references to the claimed `NN`. Author from the spec's Proposed-ADR block (Context/Decision/Trade-off/Scope-honesty).
  - **Registry** (Req 18): in `bin/.events-registry.md`, update the `feature_wontfix` row (`:26`) producers column from `skills/lifecycle/references/wontfix.md` to `cortex_command/lifecycle/wontfix_cli.py`; optionally flip `scan_coverage` `gate-enforced` → `manual` (Should — the literal now emits from Python, out of skill-scan scope). Preserve the R5 column order.
  - **Final sweep**: `just build-plugin` (no-op if Tasks 1/3/5 already regenerated mirrors), then run the gate suite on the staged diff.
- **Verification**: `ls cortex/adr/ | grep -cE '^00[0-9]{2}-structural-lifecycle-invocation-grammar\.md$'` = 1 (the ADR file exists at the claimed number) AND `just test` exits 0 (incl. `tests/test_dual_source_reference_parity.py`) AND `just check-parity --staged && just check-contract --staged && just check-events-registry --staged` exits 0 — pass if all hold.
- **Status**: [x] done — ADR claimed 0018 (0017 taken by #335); registry producer→wontfix_cli.py + scan_coverage→manual; spec ADR refs updated; just test 7/7; staged gates clean

## Risks
- **Gate-sequencing — E002 is symmetric.** `cortex-check-parity` errors on referenced-but-undeployed (E002, `parity_check.py:775-790`), not only deployed-but-unreferenced (W003); the pre-commit hook runs it on any staged `skills/*`. Every new console-script's `pyproject` deploy and its first skill reference are therefore co-located in one task/commit (Task 1 for `parse-args`, Task 3 for `wontfix`). The lifecycle `plan.md` reference's "wiring-first / mention-before-deploy is fine" hint is wrong about E002 — do not follow it here.
- **ADR-0017 is already taken** (`0017-reconcile-and-gate-lifecycle-config-sources.md`, #335). #329 claims the next-free number (0018 today). A concurrent #330 lifecycle (also unbuilt) may race for 0018 — Task 6 re-scans immediately before writing; second-to-commit renumbers.
- **`complete <slug>` and `<feature> <phase>` route through the preserved explicit-override consumer**, not the artifact detector. If Task 1's rewrite drops that consumer (the trap the original plan fell into), both forms are recognized-but-mis-routed and the drift-guard (parse-only) won't catch it — Task 2's mode-coverage is a structural proxy, not a routing test. Keep the explicit-phase-override route.
- **Drift-guard scope honesty**: the docs-derived guard catches the doc↔parser drift class for the **current closed grammar** (a doc form the parser mis-handles). A genuinely **new reserved verb** added to docs-only would be classified bare-feature by both the shape-keyed oracle and the parser (they agree → green), so adding a reserved verb requires updating the oracle alongside the grammar. This is the parse↔dispatch-prose residual the spec ADR already acknowledges — not eliminated.
- **Worktree destructive-call guard**: the verb refuses to archive from a worktree when `CORTEX_REPO_ROOT` is unset (rather than silently archiving a worktree-local copy). The harness/overnight paths set the env, so this only affects direct manual worktree invocations. Documented and tested (Task 4 case vi).
- **Sequential/trunk dispatch is mandatory** (spec Technical Constraints): editing the editable-installed `cortex_command` package means a worktree would verify stale code (`just test` runs the editable install). Do not dispatch these tasks into worktrees. Adding a `[project.scripts]` entry needs an editable-install refresh before the binstub is on PATH; verify via `python3 -m` against the working tree meanwhile.
- **Residual prose-dispatch risk** (acknowledged, not eliminated): SKILL.md's act-on-`mode` branches are model-executed; no Python test asserts the model runs the right branch. The parser unit tests, mode-coverage proxy, drift-guard, and the verb's own idempotent/no-op safety net bound the failure surface.

## Acceptance
All four advertised invocation forms — `wontfix <slug>`, `resume <feature>` (require-existing), `complete <slug>`, and bare `<phase>` — resolve correctly through `cortex-lifecycle-parse-args`, and SKILL.md Step 1 routes each `mode` to a documented handler; `complete <slug>` and `<feature> <phase>` reach their named phase via the preserved explicit-phase-override route. `/cortex-core:lifecycle wontfix <slug>` archives the lifecycle (untracked dirs and archive-less trees included, via `mkdir(archive/)`), appends a detector-compatible newline-terminated `feature_wontfix` row, and terminalizes the linked backlog item — idempotently and fail-forward, refusing rather than silently mis-targeting on an unguarded worktree call. The docs-derived drift-guard + mode-coverage tests are green (both negative controls fire; reserved two-token forms capture intact), and `just test` passes with `check-parity`/`check-contract`/`check-events-registry` clean on the staged diff. Each commit is gate-green: every new console-script deploys in the same commit as its first skill reference.
