# Session Retro: 2026-04-22 08:48

## Problems

**Problem**: Created the lifecycle directory using the backlog filename stub (`remove-single-agent-worktree-dispatch-and-flip-recommended-default`) instead of the canonical slugified title (`...-to-current-branch`). **Consequence**: Had to rename the directory mid-flow, rewrite the `.session` file, re-run the `lifecycle_slug` backlog write-back, and regenerate the `discovery_reference` event in events.log with the corrected feature slug — extra tool cycles and transient inconsistent state.

**Problem**: Wrote the initial `discovery_reference` event with placeholder timestamp `2026-04-21T00:00:00Z` instead of a real UTC timestamp. **Consequence**: events.log had a non-truthful timestamp for that event until I rewrote the file during the slug-rename cleanup; would have remained wrong if the slug mismatch hadn't forced a rewrite.

**Problem**: Research phase's "no tests reference §1a" conclusion did not check for `### 1b\.` as a test-file anchor pattern — only scanned for §1a, `.dispatching`, `worktree/agent-*`, `implementation_dispatch`, `dispatch_complete`. **Consequence**: Spec v1 shipped with a false "No test-suite changes required" non-requirement; critical-review caught `tests/test_daytime_preflight.py:334`'s hard-coded `### 1b\.` regex that R5's renumber would break; had to add R18 and revise the non-requirement.

**Problem**: Research phase's "no live path creates worktree/agent-*" framing treated only *code writers*, not *historical on-disk events.log entries*. **Consequence**: Spec v1 missed that `lifecycle/devils-advocate-smart-feedback-application/events.log` has terminal `dispatch_complete (mode: "worktree")` without `feature_complete` — `detect_lifecycle_phase()` would misclassify it as `implement 0/4` after R10 deletes Worktree-Aware Phase Detection. Critical-review caught it; had to add R19 backfill requirement.

**Problem**: Codebase research agent reported `.claude/worktrees/outer-probe/research/agent-output-efficiency` and `...agent-driven-demoability-...` as "remnant `worktree/agent-*` directories." They are actually nested subdirectories inside the `outer-probe` live worktree — not `agent-*` prefix siblings; the cleanup hook regex would never have matched them. **Consequence**: Weakened the codebase agent's (already wrong) recommendation to keep the cleanup hook; adversarial review had to override.

**Problem**: Spec v1's R15/R16 specified "body-level note" without pinning location or format, unlike R14 which pinned a dated blockquote. **Consequence**: Critical-review flagged that /refine's Clarify step has no recognition for unshaped amendment notes; had to revise R15/R16 to pin dated-blockquote shape and require `## Out of scope` section placement.

**Problem**: Spec v1 did not address the symlink-deployment substrate (repo symlinks into `~/.claude/`) — claimed "single-PR atomic rollout" without reasoning about when the user's live filesystem actually updates (git pull, not PR merge). **Consequence**: Critical-review flagged that mid-session `git pull` can reach an intermediate state the spec explicitly denies; had to add an Edge Case and a Technical Constraint.

**Problem**: Did not verify the `events.log` format convention (NDJSON vs YAML block) until after writing the first event; had to stop and sample existing lifecycle events.log files to confirm NDJSON is dominant. **Consequence**: Minor delay; one existing events.log uses YAML block format (inconsistent precedent). Chose NDJSON without flagging the inconsistency for future cleanup.
