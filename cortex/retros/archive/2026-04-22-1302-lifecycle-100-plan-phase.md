# Session Retro: 2026-04-22 13:02

## Problems

**Problem**: Designed the plan's branch-gating mechanism around `[A-branch]` / `[E-branch]` task tags and a `Gate predicate` field without first verifying how the overnight parser at `claude/pipeline/parser.py` actually consumes plan.md. **Consequence**: Critical review had to surface that the parser extracts only Files/Depends-on/Complexity/Status and that all four Phase-2 branch roots would dispatch in the same batch regardless of §Decision. Required a full §Branch-Gating Protocol section and gate-check amendments to 13 task titles to retrofit builder-level mutual exclusion. The initial design read as "this works" but was dead prose the runtime would never see.

**Problem**: Wrote R6 verification using `grep -oE '"[^"]{10,}"'` to harvest removed phrases from the rail diff, on the assumption that the phrases would be literal-quoted. **Consequence**: The Iron Law phrase `NO COMPLETION CLAIMS WITHOUT FRESH VERIFICATION EVIDENCE` lives unquoted inside a code fence, the Red Flags bullets are unquoted imperatives, and short rationalizations like `"should"` fall below the 10-char floor. The HIGH-risk target the plan names most prominently would have been invisible to its own verification. Had to rewrite the extraction to take every removed diff line as a candidate.

**Problem**: Narrowed the R6 retention check scope to HIGH-risk / MEDIUM-risk file lists instead of using spec R6's verbatim "only probe-log.md and handoff.md" rule. **Consequence**: Phrases leaking into the lifecycle dir's own spec.md, research.md, plan.md, or index.md would have silently passed the verification even though spec R6 explicitly calls them out as disallowed retention locations. Surfaced during adversarial review; had to add the full-repo spec-R6 grep to Task 16 and reorder its dependency to run after Task 15.

**Problem**: First draft of Task 15's broadened phrase-extraction verification ran a full-repo grep that would have failed because Task 16 (MEDIUM-risk patch) had not yet cleaned those files. **Consequence**: Caught mid-application before writing — but reveals that the full-repo scope was not correctly apportioned between sibling tasks on first pass. Required restructuring Task 15 to HIGH-only scope and moving the full-repo gate into Task 16.

**Problem**: Placed rail-hash stability checks only at end-of-battery (Tasks 6 and 14) in the initial plan, leaving a 40-trial exposure window for mid-battery drift. **Consequence**: A concurrent edit to `claude/reference/verification-mindset.md` between sub-batches would corrupt data for ~30 trials before detection. Post-review, had to fold per-trial hash and mtime checks into `probe-apparatus.sh`'s contract.

**Problem**: Did not specify platform for `probe-apparatus.sh` script; used `stat -f '%m %N'` which is macOS-specific. **Consequence**: Not a runtime issue since this is a macOS machine, but the script is not portable and the plan does not note this constraint — a future port to Linux execution would silently break the mtime check.

**Problem**: Task 2 baseline capture initially had only `git cat-file -e` (SHA exists) as its integrity check — no assertion that reference files at baseline_sha match the current working tree. **Consequence**: Critical review surfaced that baseline_sha could be recorded against a stale HEAD with an intervening rail-touching commit, and the ring-fence byte-identity check in Task 9 would silently validate against the wrong snapshot. Had to add a working-tree diff assertion to Task 2 and a `git log --oneline "$BASE..HEAD" -- <rail>` check to Task 9 and Task 10.

**Problem**: Task 7 verification checked that §Decision cited `rf_vote=N/7` and `cf_vote=N/7` counts but did not assert the declared branch's predicate was actually satisfied by those counts. **Consequence**: A Task 7 writing `## Decision: A` with cited counts that do not meet the A predicate would pass verification. Had to add a case-statement predicate-correctness check to Task 7's verification.

**Problem**: Task 14's Post-Rewrite Comparison row-format grep checked columns through `P_new` only, omitting the `regression` column shape. **Consequence**: A row with a malformed `regression` cell would pass the row count check but fail the regression count check elsewhere, producing inconsistent verification outcomes. Fixed by extending the row grep to include `(true|false)` on the regression column.

**Problem**: The plan's branch-gating depends on the builder (running on Opus 4.7) correctly interpreting a declarative "if §Decision != X, empty-commit and exit" instruction embedded in the task description — the exact class of literalism-sensitive instruction this ticket exists to remediate. **Consequence**: The gating mechanism shares failure mode with its target. Acknowledged in Veto Surface item 8 but not structurally resolved.

**Problem**: The user had to pause the deep technical discussion twice (`"What is it we are actually doing in this ticket?"` and `"Go ahead and make the changes"`) to redirect from verbose comparison toward execution. **Consequence**: Burned time on plan-comparison prose the user had already synthesized; revealed the response cadence was not matched to their intent.

**Problem**: When discussing concurrency risk earlier in the session, I built up a thorough model of how concurrent-session edits to the rail could corrupt probe data — then did not apply that model to my own plan's gating mechanism when I wrote it immediately afterward. **Consequence**: The concurrency model identified the rail-hash gap but did not identify the branch-tag-ignoring parser, even though both are rooted in the same failure class (runtime does not read what the plan asserts). Critical review caught it; I should have caught it in my own pre-review pass.
