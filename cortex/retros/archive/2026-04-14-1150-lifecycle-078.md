# Session Retro: 2026-04-14 11:50

## Problems

**Problem**: Wrote `sys.exit(1)` as the orphan guard's termination mechanism in plan Task 4 Context. **Consequence**: `sys.exit()` inside an asyncio coroutine raises `SystemExit` only within the task — the main coroutine continues unaffected; the plan described non-functional behavior. Caught by critical review; required a fix pass.

**Problem**: Wrote `name in features_paused` and `name in features_deferred` for return-code inspection in plan step 13 without verifying the `BatchResult` field types. **Consequence**: Both fields are `list[dict]` not `list[str]`; the membership checks would always return False and collapse paused/deferred outcomes into the failure branch. Caught by critical review; required a fix pass.

**Problem**: Used ambiguous "try: ... try: ... finally:" notation in plan step 12 Context. **Consequence**: Not valid Python as written; the intent (single `try` body) was unclear and omitted the `except` clause needed to handle `apply_feature_result`'s real `raise RuntimeError` paths. Caught by critical review; required a fix pass.

**Problem**: Created `asyncio.create_task(_orphan_guard(...))` without storing the reference in the plan. **Consequence**: No way to cancel the task in `finally`; every clean run would emit "Task was destroyed but it is pending!" to stderr. Caught by critical review; required a fix pass.

**Problem**: Did not include backlog write-back side effects in plan Veto Surface or Files list. **Consequence**: `backlog_ids={feature: None}` triggers slug-based write-backs on all outcomes (deferred demotes item to `backlog`); this is a meaningful side effect that belongs in Veto Surface but was absent. Caught by critical review; added in fix pass.
