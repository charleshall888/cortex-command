# Session Retro: 2026-04-14 11:46

## Problems

**Problem**: The initial plan.md wrote the `transition` patch target rule backwards — the Veto Surface stated "patching the definition site (`claude.overnight.state.transition`) is the correct pattern when a function is imported with `from`," which inverts Python's binding semantics. When `orchestrator.py` does `from cortex_command.overnight.state import transition`, the name is bound in `orchestrator.__dict__`; patching the definition site is invisible to calls inside `orchestrator.py`. **Consequence**: If implemented as written, all `transition` patches in `test_lead_unit.py` would silently no-op, leaving session-phase writes untested with no test failure signal. Required critical review to catch.

**Problem**: Task 9 Context enumerated patch.object call sites "at lines 168, 184, ..., 503 etc." but did not surface the `_install_common_patches` helper block in `TestAccumulateResultViaBatch` (lines ~1472–1530), which contains ~10 string-form patches including `save_batch_result` — a symbol missing from every migration list in the plan. **Consequence**: Implementer would have missed 10+ string-form patches and failed the grep verification, with no guidance on what to change.

**Problem**: Line 1467 in `test_lead_unit.py` was cited in the plan as `from cortex_command.overnight.batch_runner import run_batch` with destination `claude.overnight.orchestrator`, but the actual line is `from cortex_command.overnight.batch_runner import FeatureResult as _FR` and the correct destination is `claude.overnight.types`. **Consequence**: Implementer would have written a wrong import (`orchestrator.FeatureResult`) that the test would silently accept if `FeatureResult` happens to be re-exported, or fail at runtime with a harder-to-diagnose error.

**Problem**: Task 10 heartbeat scenario instructed "patch `asyncio.create_task` to capture the task" — patching the global `asyncio.create_task` inside `IsolatedAsyncioTestCase` corrupts the test framework's own event loop management. **Consequence**: Test would produce framework-level errors unrelated to heartbeat behavior, making the scenario unimplementable as written.

**Problem**: Task 10 circuit breaker scenario instructed constructing `CircuitBreakerState` "pre-loaded into the `OutcomeContext` via a side_effect" — `side_effect` on a mock replaces return values, not `__init__` of an internally constructed object. **Consequence**: Implementer would produce a test that does not actually exercise the circuit breaker code path; the scenario is not a valid Python mocking pattern.

**Problem**: The initial plan omitted inline imports at lines 1775 and 1811 in `test_lead_unit.py` from the Task 9 Context enumeration. **Consequence**: Implementer following the explicit line list would miss two `run_batch` imports in the tail of `TestAccumulateResultViaBatch`, causing the grep verification to fail.
