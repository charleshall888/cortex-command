# Session Retro: 2026-04-22 08:47

## Problems

**Problem**: Created the lifecycle directory and wrote `lifecycle_slug` to the backlog item using the filename-based slug (`gate-overnight-pr-creation-on-merged-over-zero`) before verifying against the canonical `slugify(title)` algorithm, which would have produced `gate-overnight-pr-creation-on-merged0-draft-on-zero-merge`. **Consequence**: had to pause mid-protocol and run a repo-wide analysis of lifecycle_slug vs slugify(title) to confirm the filename-based choice matched 12 other historical tickets before committing. State was already on disk at that point; any deviation would have required cleanup. Future invocations should verify slug convention as a first action, not after directory creation.

**Problem**: Wrote research.md's Open Questions using numbered-list markers (`1.`, `2.`, ...) instead of `-`/`*` bullets. **Consequence**: the lifecycle complexity-escalation heuristic literally counts "`-` or `*` prefixed lines directly under that heading" — my formatting would have silently failed the count. Caught it in self-review but required an extra Edit pass to convert markers. Should default to `-` for research open-questions by convention.

**Problem**: First pass of spec.md had Req 5 specifying a state-file write (`integration_pr_flipped_once: true`) while Req 7 said state writes are "skipped silently" under `--dry-run`, and Req 8's test asserted on fixture state-file mutation. **Consequence**: inconsistency wasn't caught during the initial Apply pass after critical review; only surfaced when I re-read the spec during the Ask resolution. Required a second round of edits (adding `DRY-RUN state-write <field>: <value>` emit pattern) just before presenting the approval surface. Cross-requirement consistency check belongs in §2b Pre-Write Checks, not in a post-hoc sweep.

**Problem**: Did not push back on scope escalation during the Expanded-bundle choice + critical-review Apply fixes. The ticket started at 3 requirements; grew to 8 via user's Expanded scope; grew to 10 after critical review. **Consequence**: the final spec covers substantially more surface than the original ~30-minute ticket. User accepted both scope choices so not strictly an error, but the "complexity must earn its place" project principle was not actively defended — I aggregated reviewer findings into Apply fixes without evaluating whether each was truly load-bearing for this ticket vs. deferrable.

**Problem**: Recalled `feedback_explain_value_first.md` memory only after preparing the first AskUserQuestion batch for the spec interview. **Consequence**: added a scrambling step ("wait, I need to frame this first") instead of front-loading the value/history framing by default. Memory check should be a gate before any AskUserQuestion batch when research has materially reshaped a ticket, not a reactive look-up.

**Problem**: Trusted research.md's approximate line numbers for `skills/morning-review/references/walkthrough.md` (lines "~489-530") when drafting Req 6 without re-verifying exact lines. **Consequence**: had to re-verify during §2b Pre-Write Checks (walkthrough lines 495, 520, 589). Minor but indicates a pattern of treating research-produced line numbers as authoritative when they're approximate by construction (subagents use relative terms). Specs citing line numbers should verify at spec time, not inherit from research.

**Problem**: Initial orchestrator-review pass didn't flag the missing (merge>0, DEGRADED=true) 2×2 matrix cell in Req 8's test inventory. **Consequence**: the gap was surfaced by critical-review Agent 4 instead, requiring an Apply fix. The orchestrator-review S2 "edge cases" check should explicitly enumerate a test-coverage matrix for any requirement that spans ≥2 independent axes, not rely on prose edge-case listing.
