# Session Retro: 2026-04-17 17:37

## Problems

**Problem**: Created the lifecycle directory using the backlog filename slug (`restructure-critical-review-step4-suppress-dismiss-output`) instead of the canonical `slugify(title)` result (`restructure-critical-review-step-4-to-suppress-dismiss-output`). **Consequence**: Had to rename the directory mid-setup, update the `feature:` field in `index.md`, rewrite the `feature` field in the existing `discovery_reference` event in `events.log`, and re-run `update-item lifecycle_slug=...` to correct the backlog write-back.

**Problem**: Wrote Task 1's Verification in the initial plan as a single grep check for R1 alone (`grep -c "what was dismissed and why" = 0`) while Task 1's actual scope covered R1+R2+R3+R4+R6+R9+R10. **Consequence**: Critical review Angle 1 + Angle 4 both identified the under-gating; had to rewrite Task 1's Verification to the full R1–R10 battery (17 sub-checks) and collapse the 3-task chain to 2 tasks.

**Problem**: Built Task 2's "combined acceptance bundle" in the initial plan as 5 witnesses and claimed they were "sufficient witnesses for the full R1-R10 pass." **Consequence**: Critical review Angle 2 produced a concrete malformed implementation (`"Report results. Dismiss: N objections. Compliant: yes. Compliant: also yes. Non-compliant: no."`) that passes all 5 witnesses while failing 7 of 10 R-requirements. The "sufficient witnesses" claim was unsound; had to replace with full battery.

**Problem**: Used `git log -1 --name-only --pretty=format:""` and `HEAD^..HEAD` in Task 3 and Verification Strategy instead of spec R7's literal `git diff --name-only main.. -- skills/`. **Consequence**: Critical review Angle 3 identified false-pass paths (integration branches with prior lifecycle commits touching other `skills/` files) and false-fail paths (`integration_branch = main` → empty diff output). Had to align both verifications with R7's exact literal and surface the `main` vacuity as a Veto Surface item.

**Problem**: Wrote initial spec's R2, R3, R4-first, and R6 as whole-file greps rather than Step-4-scoped `awk` ranges. **Consequence**: Critical review synthesis identified this as the root false-pass vector — any phrase placed anywhere in SKILL.md (footnote, appendix, separate "Step 4b" section) would pass. Had to rewrite R2/R3/R4/R6/R10 with `awk '/^## Step 4:/,/^## Step [^4]/'` scoping and replace whole-file greps with block-scoped ones.

**Problem**: Initial spec's R4 required `grep -c "strengthened from" ≥ 1` as the sole polarity check while Edge Cases asserted the worked example "must cover" semantic inversion (MUST→SHOULD, narrowing, polarity reversal). "Strengthened" is the opposite polarity from the semantic-inversion case. **Consequence**: Critical review angle identified that a worked example demonstrating only tightening passes R4 while failing the Edge Cases MUST. Had to add R4 checks requiring both `strengthened from` AND `(inverted|reversed|relaxed|narrowed) from`, plus `Compliant:` ≥ 2 to force two-polarity coverage.

**Problem**: Initial spec's R9 used "reviewed by a human or orchestrator review" as Interactive/session-dependent escape — form (c) from the S1 criteria — even though form (a) was mechanically available via `awk` range extraction (which R4 already used). **Consequence**: R9 was the only acceptance criterion offloaded to human judgment despite the boundary being structurally identical to R4's. Had to mechanize R9 to `diff <(git show main:...awk)> <(awk...)>` byte-identity check plus a Step 4 heading-preservation grep.

**Problem**: Initial spec omitted explicit MoSCoW classification; all requirements were implicitly Must-have. **Consequence**: Orchestrator review cycle 1 flagged S3; had to dispatch a fresh subagent fix to add the MoSCoW preamble stating all R1–R9 (later R1–R10) are Must-have and enumerate the Won't-have non-requirements.

**Problem**: Initial spec R4's second acceptance criterion used prose "Verified by: inspect the block between the line containing 'strengthened from' and the nearest non-compliant marker" — form (c) Interactive/session-dependent — when form (a) via `awk + grep -c "Non-compliant:"` was available. **Consequence**: Orchestrator review cycle 1 flagged S1; the same subagent fix had to replace the prose with a binary-checkable `awk` range check.

**Problem**: Codebase research agent 1 asserted `claude/overnight/report.py` and `claude/pipeline/metrics.py` "consume the schema" for `critical_review` events.log entries. **Consequence**: The Tradeoffs research agent's Alt C recommendation (Dismiss-to-events.log capture) built on this false premise as "follows clarify-critic precedent and has existing consumers." Adversarial review Agent 5 disconfirmed via direct grep — zero consumers. Alt C was downgraded from recommendation to rejected; the spec explicitly skips events.log capture with R8.

**Problem**: Initial sequencing question ("Ship now, no gating on #068" vs. "Land #068 first, monitor, then ship") was offered as a terse two-option prompt without sufficient context. **Consequence**: User responded "can you expound on this?" — had to add a context block explaining #068's status, the epic-wide silence concern, and the practical trade-offs before re-asking. Would have been faster to lead with the expanded context in the first question.

**Problem**: Session ran full research (5 parallel agents), adversarial critical review on spec (4 angles + Opus synthesis, 10 objections applied), and adversarial critical review on plan (4 angles + Opus synthesis, 10 objections applied) for a ticket that ultimately produces a single-file edit to one paragraph in `skills/critical-review/SKILL.md`. **Consequence**: Substantial conversational ceremony relative to the implementation surface. The same adversarial-review machinery that tightened acceptance criteria also consumed a large context window — specifically the kind of verbose internal work the ticket itself aims to suppress in Step 4.
