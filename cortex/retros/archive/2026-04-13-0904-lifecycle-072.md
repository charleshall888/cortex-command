# Session Retro: 2026-04-13 09:04

## Problems

**Problem**: Initial plan writing missed the prose-execution gap for the Section 2a rewrite — designed a two-path routing system (Guard 1 → "active path" → path-conditional sections) without specifying how a linearly-reading agent navigates the fork. Walkthrough.md has no runtime branching; this is a well-known property of skill text, but wasn't applied during plan authoring. **Consequence**: Critical review required 5 of 7 Apply fixes to address the same root cause (routing state, conditional framing, demo offer structure, print template placement, Guard 2 position).

**Problem**: Task 2 ACs were proposed without cross-checking current file content — 4 of 7 grep patterns (`after the first`, `control character`, `inline.*#`, `whitespace-only`) already existed in walkthrough.md before Task 2 runs, making them non-verifying. The file had been read and was in context. **Consequence**: The plan would have let an implementer ship a `demo-commands:` list path with missing parsing rules while all 7 ACs passed; thresholds had to be revised to ≥ 2 or ≥ 4 post-critical-review.
