# Session Retro: 2026-04-20 07:55

## Problems

**Problem**: Plan v1 wired main() as "concatenate across log files → sort by ts → pair_dispatch_events", which silently violates the spec's explicit "Pairing is file-local" invariant (spec line 84). **Consequence**: A `dispatch_complete` in session B could false-pair to a `dispatch_start` in session A across file boundaries; would have shipped a measurement-integrity bug that only surfaced because critical-review read spec line 84 against the plan's own Task 6 wiring.

**Problem**: Plan v1's walker design used a per-feature "most-recent-start" map ("nearest preceding start with same feature") without checking real dispatch log patterns. **Consequence**: Same-feature retry storms in actual logs (6 `dispatch_start` events within 33ms for one feature) would have mapped every subsequent complete to the last start, silently over-attributing cost/turns to one tier bucket. Only discovered because a reviewer spot-checked a real session log.

**Problem**: Plan v1 introduced a SECOND new top-level JSON key (`escalation_frequency`) beyond what spec Req 6 sanctioned (singular: "a new top-level key `model_tier_dispatch_aggregates`"). I surfaced the drift as a Veto Surface item rather than treating it as a spec-level decision requiring escalation. **Consequence**: Scope drift was being framed as a plan-level veto invitation rather than "spec ambiguity that needs resolution" — risked being approved silently through the default approval flow.

**Problem**: Plan v1 Task 5 scoped `--since` to the new aggregation pipeline only, leaving `features`/`aggregates`/`calibration` all-time, and called this "backwards compatibility" — without adding window metadata to the JSON output or a CLI header. **Consequence**: metrics.json produced with `--since 2026-04-18` would have been silently mixed-time-scope; the motivating downstream consumer (#088 snapshot-diff) would have computed wrong deltas by construction. Reviewer correctly called "backwards compatibility" a post-hoc rationalization.

**Problem**: Plan v1 specified `warnings.warn` for orphan (untiered) surfacing without verifying that Python's default warnings filter would actually emit per-call messages. **Consequence**: Default filter deduplicates identical UserWarnings to a single stderr line regardless of call count — 50 orphans would produce 1 message, on a stream Task 7's own verification command (`... | grep`) discards. Spec's "explicit warning" requirement would not have been met in practice.

**Problem**: Plan v1 Task 2 listed 6 files (4 fixture JSONL files plus metrics.py and test_metrics.py), exceeding P1's 5-file cap. **Consequence**: Orchestrator review cycle-1 flagged it; required a cycle-2 same-context fix to inline the test events.

**Problem**: Orchestrator review (P1–P7 rubric) passed plan v1 on cycle 2 — none of the structural defects above were in the checklist, and critical-review alone (gated on tier=complex) caught all five. **Consequence**: If this ticket had been tagged tier=simple, orchestrator review would have been the only gate and the plan would have shipped with the defects. The P1–P7 checklist is insufficient on its own for non-trivial algorithmic work.
