# Session Retro: 2026-04-11 15:12

## Problems

**Problem**: The research adversarial agent (Agent 5) flagged that the repair/ff-merge path in `_apply_feature_result` has no `dispatch_review` call and therefore no path to `FEATURE_DEFERRED` — but this finding wasn't elevated clearly enough in the research synthesis to influence the spec interview. It was re-discovered by the critical review as Requirement 3 being dead weight. **Consequence**: Requirement 3 (repair path event write) was included in the spec, passed through interview and orchestrator review, and only removed after the full critical review cycle — wasting review cycles.

**Problem**: The "other severities" branch in `render_deferred_questions()` (non-blocking deferrals annotated with a note) was dead code from the start — non-blocking deferrals complete and don't appear in that render function. The spec interview asked the right question about the deferred case but didn't probe whether non-blocking deferrals actually appear in `render_deferred_questions()`. **Consequence**: Dead code branch entered the spec and required the critical review (Opus synthesis) to catch and remove it.

**Problem**: Technical Constraints initially stated "config... no new parameters needed" for the integration branch name in the event details dict, without verifying what fields `BatchConfig` actually contains. The correct value is `effective_branch` (a local variable computed just before the insertion point), not `config.base_branch`. **Consequence**: The spec would have guided an implementer toward `config.base_branch`, which diverges from the per-feature integration branch in multi-repo sessions. Caught by the integration-branch-access critical review angle.

**Problem**: Requirement 6 (annotation wording) was initially marked "Interactive/session-dependent" despite the exact wording strings being fully defined in the spec body. This is a weak AC discipline — "Interactive/session-dependent" is for content that cannot be verified with a command, not for literal constants the spec author defined. **Consequence**: The wording content would have had no CI regression protection; caught and corrected by the acceptance-criteria-testability angle in critical review.
