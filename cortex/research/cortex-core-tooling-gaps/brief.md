All ten candidate gaps are confirmed absent and will move forward as tickets across three epics, with the ADR citation auditor constrained to report-only, the stale-status work split into two sequenced tickets, C1 implemented as a transient per-run CLI flag, and the next-free-ADR-number helper dropped as speculative.

Making the ADR auditor blocking was weighed and rejected because a ratified decision document carries a three-ground argument for prose-only enforcement, and a blocking gate would require an explicit maintainer overturn rather than a tooling ticket. Persistent frontmatter for the overnight-exclude need was considered but set aside because the codebase already separates permanent parking from view-only deferral, and a transient flag fits without adding a new field. Stale-status detection was originally a single candidate; existing prior art shows semantic status-versus-reality drift is not solved deterministically by any tooling, and the detector has nothing to compare until a machine-checkable convention exists and is populated — so the two pieces were separated with a hard dependency. The consumer always-loaded ratchet template carries a maintainer-confirm flag rather than a clear verdict because a ratified ADR prohibits cortex from writing into consumer root documents and the right destination requires an explicit call before the ticket can be scoped.

The accepted tradeoff is that the stale-status detector — the capability most directly motivated by the original gap report — is now gated behind a status convention that must be established and backfilled across dozens of existing research documents before any automated check is possible, with no date scheduled for that precondition work.
