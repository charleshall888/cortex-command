# Research: cli-served-lifecycle-state-machine

Replace model-as-router skill navigation with a CLI-served lifecycle state machine: `cortex-lifecycle-next --feature X` emits {state, instruction fragment, pause spec, advance contract}; `cortex-lifecycle-advance` records completion and emits events. Sized complex/critical; 9 core research agents + 1 adversarial agent + a 4-angle critical review (7 ratified fix-invalidating objections applied) + a 5-angle holistic design wave (delivery mechanism, operator experience, whole-system coherence, minimal alternative, STPA hazard analysis) whose findings **reshape the funding recommendation** — see §Holistic Design Review.

## Research Questions

1. **Fragment distribution — plugin markdown + caller-passed `--skill-dir` vs wheel-embedded, with ADR-0002 skew implications?** → **Answered, with a mandatory prerequisite**: *neither flavor is safe unaided*. cortex-core has zero version-skew protection today — `CLI_PIN`, the MCP-gated reinstall, and the SessionStart reinstall hooks all live only in cortex-overnight, by twice-reaffirmed scope decision [cortex/lifecycle/trigger-cortex-cli-reinstall-at-sessionstart/spec.md:60; cortex/lifecycle/extend-cortex-cli-version-sync-hook/spec.md:21,113], and cortex-core-only installs have no automatic CLI install path at all [docs/setup.md:28]. No `--skill-dir`-style argv precedent exists anywhere; the SP001/SP002 lint covers Read/prompt paths only, not argv or Python string literals [cortex_command/lint/skill_path.py:32-48]. A **protocol handshake ("step 0", design in RQ5)** is a prerequisite for either flavor — and it *guards* the skew surface (detect-and-halt with remediation), it does not *close* it: the payload flavor has no auto-remediation and cortex-core-only installs have no automatic reinstall path. The selector-vs-server choice itself remains open — see Open Questions — because it is really the structural-guarantee question, not a token-cost question.
2. **What does `cortex-lifecycle-next` emit, and do one-line next-state previews suffice?** → **Answered**: the envelope generalizes proven shapes — `resolve.py`'s `{state, next}` directive [cortex_command/lifecycle/resolve.py:59-74,192-209], `complete_route.py`'s 12-route `{route, terminal, continue_to}` [cortex_command/lifecycle/complete_route.py:34-48], and `branch_decision.py`'s `{skip|resolved|prompt}` pause-vs-advance split [cortex_command/lifecycle/branch_decision.py:99-132]. No fragment in the corpus needs more than one-hop lookahead — every cross-phase dependency is either a state-read at current-phase entry or the SKILL.md-mandated "Next" transition line [skills/lifecycle/SKILL.md:74; skills/lifecycle/references/implement.md:111,117]. **Caveat (adversarial)**: that evidence comes from a world where the model holds the whole map; the machine should carry an optional path-overview field for resume reporting and "what's left?" questions [skills/lifecycle/SKILL.md:48].
3. **Overnight `--mode` pause-resolution policy?** → **Answered, re-scoped**: overnight's pause substitutes are not pause *responses* but *different transitions* — refined-status precondition instead of spec approval [cortex_command/overnight/backlog.py:42], deferral files instead of questions [cortex_command/overnight/deferral.py:1-4], brain triage instead of failure prompts, auto-merge instead of merge-wait. The headless orchestrator can never reach a pausing state, so every pause spec needs an overnight policy arm. The honest claim is **"shared state+event core, two policy layers"**, not "orchestrator-round.md retires." A written overnight transition table — quantifying what fraction of orchestrator-round.md is machine-ownable sequencing (round filter, dependency gate, criticality lookup, envelope-extraction mechanics, event emission) vs judgment prose that stays (plan format contract, synthesis criteria — must-stay-prose either way) — is a prerequisite for funding the unification step, and its go/no-go is a separate decision from steps 0–2 (see RQ5).
4. **Golden-replay verification harness design?** → **Answered, redesigned after critical review**. The originally-recommended arms — (a) terminal-state replay over the 325-log / 10,534-line historical corpus, (c) forward decision-capture — cover the **pure-reducer slice, which the extraction keeps unchanged and which already has an 8-case golden-replay parity suite** [tests/fixtures/cortex-lifecycle-state/README.md]. They are worth having (S effort each; they catch split-brain-reader divergence, the class of the fixed `cortex-lifecycle-state` null-collapse [cortex/lifecycle/cortex-lifecycle-state-collapses-to-null/research.md:11-35]) but they are **not the extraction's safety net**: historical logs were written by the old system, so read-side replay never executes the new write path, and decision-capture's goldens are emitted by the code under suspicion — a wrong day-one decision is recorded as correct. Day-one confidence must come from arms that exercise the new surfaces directly: **(d) write-side verb tests** that run `advance` in a sandbox and assert emitted event order against the gate check (the wave-2 `test_finalize.py` retarget pattern — this, not historical order-assertions, is the F1 detector); **(e) a CI-diffed golden of the `describe` verb's rendered transition table plus per-state envelope tests** pinning the state→fragment and state→pause-spec mappings the extraction creates (no historical event records fragment reads; `feature_paused` carries zero payload fields [cortex_command/lifecycle_event.py:231-279]); **(f) environment-fixture guard tests** staging lock files and probe states against `next`'s envelope (guards evaluate live state that never appears in events.log); **(g) a status-projection differential sweep** running the retained scan_lifecycle mismatch detector pre/post cutover and requiring the new projector to reproduce or shrink the existing mismatch set (historical frontmatter is a known-inconsistent oracle). The complete-route slice's gh/git inputs were never recorded — historical replay is structurally impossible there [cortex_command/lifecycle/complete_route.py:79-243]; decision-capture covers it from version N onward only. Per-event diff (b) stays M–L, scoped to pure-reducer transitions.
5. **Migration sequence with standalone value per step?** → **Answered, amended three times** (adversarial pass, critical review, then the holistic wave — which re-shaped the sequence into Phase A/B/C, see §Holistic Design Review and Feasibility; the step 0–5 design below is preserved as Phase C's internal sequence): step 0 (protocol handshake) → 1 (selector verb) → 2 (events-to-advance, with the pause-kind discriminant in the schema from day one) → 3 (pauses-as-data runtime) → 4 (overnight core-sharing) → 5 (entry-point configs). Step-2-as-foundation matches the #330→#331/#332 precedent [cortex/backlog/336-offload-deterministic-lifecycle-mechanics-to-cli-verbs.md:17-25]. Critical-review amendments to step 0's design, all binding: **(i) carrier** — the protocol version is stamped into the *existing* verbs' payloads (`resolve`, `enter`, `complete-route`) at step 0, before the new verbs exist, and the SKILL.md loop treats a missing field *and* verb-invocation failure (command-not-found / wrapper exit 2) as skew with a remediation message — otherwise the fresh-plugin + stale-wheel pairing at the step-1 boundary fails in the shell before any assertion can run; **(ii) bidirectionality** — the loop passes its own expected protocol as an argv flag (`--protocol N`) that the wheel asserts; a legacy plugin passes no flag and the wheel treats absence as legacy-protocol, keeping legacy verbs serviceable — because the stale-plugin + fresh-wheel direction is the one the repo's own machinery manufactures (auto-release bumps the wheel per merge [.github/workflows/auto-release.yml]; the cortex-core plugin waits for a manual `/plugin` update); **(iii) tolerance semantics** — the protocol version is a small integer decoupled from release versions, bumped only on breaking contract changes, asserted as a compatibility range — exact-equality against per-merge release tags would halt every consumer on every wheel-only release; **(iv) coexistence policy** — absorbed verbs (`cortex-lifecycle-resolve`, per-event `cortex-lifecycle-event` subcommands) stay callable until a protocol-floor bump, following the events-registry 30-day-grandfather discipline, and `advance` carries idempotency/dedup keys so a skewed consumer running both paths cannot double-emit (schema-additive but count-corrupting, which decision-capture would then pin as golden). Each step must additionally be additive-only against events.log or it forces a major bump + coordinated plugin release.
6. **In-flight lifecycle compatibility?** → **Answered**: the migration-sentinel pattern is established (derive as-if-migrated state from historical shapes, validate against the real corpus [cortex/lifecycle/auto-progress-lifecycle-phases-when-no-blockers/spec.md:25]); readers-tolerate-all-historical-shapes is a codified constraint [cortex/requirements/project.md:37]. Only 2 in-flight lifecycles exist locally — but consumer repos have uninspectable corpora and receive partial step sequences via auto-release, so **"safe to land in an arbitrary consumer repo mid-lifecycle" is a per-step acceptance criterion**, discharged per step by the RQ5 handshake + coexistence machinery, not assumed. The tolerant-reader discipline extends to **consumer config files**, not just events.log: step 5's axis rename must alias old `lifecycle.config.md` field names with a deprecation warning — a silent reinterpretation under opposite floors yields a wrong default, not an error.
7. **Fold in #370?** → **Answered**: bug confirmed at [cortex_command/lifecycle/resolve.py:183] — the `new` branch checks `dir_exists` on the raw token and never consults the `backlog["lifecycle_slug"]` it already holds; the `mode=="resume"` branch has the same blindness [resolve.py:161-169]. "Resolver becomes machine core" = slug as the canonical identity key, with numeric ID/title/UUID as input normalization before state lookup. **But the fix + a verb-level regression test should land now, independent of this epic** — porting an untested fix is how dropped-literal bugs happened last wave.

## Codebase Analysis

- **The proposal is a generalization of a proven, mid-transition pattern, not greenfield.** 26 lifecycle verbs exist [pyproject.toml:43-94]; SKILL.md is already a resolve→enter→phase-table loop [skills/lifecycle/SKILL.md:26-70]; `complete.md` routes almost entirely off `complete_route`'s JSON [skills/lifecycle/references/complete.md:19]. Wave-2's own closeout deferred exactly this next lever: "an operator decision at the gate" beyond clause-parity trimming [cortex/lifecycle/lifecycle-corpus-trim-wave-2/route-table-after.md:103-111].
- **Fragment corpus**: 21 files, 65,938 bytes in skills/lifecycle/references/ plus ~39K in skills/refine/references/. Grading uses a **provenance criterion** (sharpened by critical review): a gate is *machine-verifiable* only when its evidence is CLI-produced or CLI-checkable without model authorship — prior advance-emitted events under flock, guard-verb exit codes, file-existence facts the machine itself established. Model-authored artifacts (plan.md checkboxes, review.md verdicts) and model-relayed human utterances (spec approval) are **attestations in substance even when a file exists to read** — CLI verification of them confirms shape, not truth. Machine-ownable *sequencing* (routing tables, gate matrices, guard composition): backlog-writeback.md, wontfix.md, critical-review-gate skip matrix, criticality-matrix behavior table, refine-delegation's event/gate sequencing. Recite-only routing (served, never verified — gh/git/tool-call outcomes): complete.md's PR-state routing, worktree-entry.md's EnterWorktree call (ADR-0008 requires the structural two-mode split; a runtime soft-gate was explicitly rejected [cortex/adr/0008:20,39]). Must-stay-prose: plan.md authoring rules, orchestrator checklists, reviewer/builder prompt templates, failure-handling judgment.
- **Pause inventory**: 10 test-enforced pauses [skills/lifecycle/references/kept-pauses.md:5-14] + 3 sites the parity regex misses (implement.md:76 retry/skip/abort; complete-first-run.md:11 test-command ask; concurrent-sessions.md:5 resume pick). Three kinds need a discriminant: AskUserQuestion-site, phase-exit wait, config-conditional (suppression predicate already exposed by `cortex-lifecycle-branch-decision`), **plus a fourth class critical review surfaced: relayed-consent gates (spec approval, merge consent) — the judgment is the human's, not the model's, and they are the highest-stakes pauses**. kept-pauses.md is tests-only with zero runtime consumers [kept-pauses.md:3] — the parity test *is* the current enforcement, so retiring the pin class requires a successor contract, not just a generated registry: (a) a generated parity check asserting every pause-requiring state's envelope carries its pause spec, and (b) a runtime tooth — `advance` for a pause-gated transition **refuses unless a recorded pause event exists** (the `approval_checkpoint_responded` closed-vocabulary pattern from discovery [cortex_command/discovery.py:419-430] is the in-tree precedent). Residual trust remains: a model could fabricate the pause event; the event is at least durable and auditable. The parity net must be fixed on the current corpus before it's lifted into pause specs.
- **Events substrate**: append-only per-feature events.log under flock+O_APPEND [cortex_command/lifecycle_event.py:75-119]; `_EVENT_SUBCOMMANDS` table owns field contracts [lifecycle_event.py:231-279]; ADR-0020 closes the hand-written exception class at 3. `detect_lifecycle_phase` is a **hybrid** artifact-presence + event machine [cortex_command/common.py:239-393] — pure event replay cannot reproduce it without point-in-time artifact snapshots.
- **Status projection**: three independent writers (refine SKILL.md prose → `refined` [skills/refine/SKILL.md:94]; finalize.py:159 → `complete`; wontfix_cli.py:139-146 → `wontfix`); `update_item` accepts arbitrary `--status` with no enum validation; scan_lifecycle.py:166-200's mismatch detector exists because they disagree in practice. **Adversarial correction**: the machine cannot "own" a projection into github-issues/jira — external write-back is deliberately best-effort (ADR-0016) and a human closing an issue in the GitHub UI is legitimate and invisible. Owned projection scopes to the cortex-backlog backend; the mismatch detector is retained as the reconciliation surface **and enrolled in the verification harness as the pre/post-cutover differential sweep (RQ4 arm g)**.
- **Guards**: interactive lock (8-row liveness table, interactive_lock.py:387-448), overnight-active probe (bash sidecar + deliberately-duplicated Python reimplementation with KEEP-IN-SYNC comments [cortex_command/lifecycle/prepare_worktree.py:16-29]), branch decision — all already verbs; prose only sequences them, and `prepare_worktree.py` already composes guard→lock→create. Guard evaluation reads live environment state that never appears in events.log — guard misfire is invisible to event replay, hence RQ4 arm (f).
- **Overnight duplication surface**: orchestrator-round.md re-implements plan.md §1a and competing-plans.md §1b semantics by explicit citation [cortex_command/overnight/prompts/orchestrator-round.md:241-303,412-413], pinned only by the hand-maintained heading-existence test tests/test_skill_section_citations.py. The scheduler (launchd/lock/spawn) vs per-feature sequencing (orchestrator.py/feature_executor.py/outcome_router.py) boundary is already docstring-enforced. Note the cited §1a/§1b duplication is judgment prose that stays prose either way — the machine-ownable content in orchestrator-round.md is its sequencing scaffolding (round filter, dependency gate, criticality lookup, envelope extraction, event emission), whose actual volume the RQ3 transition table must quantify before step 4 is funded.
- **Point fixes that don't need the machine** (solution-horizon discipline — credit them separately): plan.py:145-151 `_TERMINAL` missing the wont-do variants (one-line shared-import fix); stale common.py:175-176 comment (backlog.py already imports the shared set [cortex_command/overnight/backlog.py:28]); vestigial `SYNTHESIZER_VALIDATED_{INTERACTIVE,OVERNIGHT}` event names defined but never emitted [cortex_command/overnight/events.py:85-86].
- **Test lattice**: FILE_EVENTS pins exact per-file event-invocation counts across 8 files [tests/test_lifecycle_event_roundtrip.py:123-154]; ~5 markdown-anchored tests (kept-pauses, invocation-grammar, step-v-ordering, EnterWorktree-callsites, dual-source cases) retire-and-retarget to verb-level tests per the wave-2 precedent (test_complete_index_sync_gate.py → test_finalize.py). **W003 parity-orphan fires for every verb whose only wiring is deleted reference prose** [cortex_command/parity_check.py:70-84] — needs allowlist rows or scanner extension. SP001/SP002 cannot see Python string literals feeding served fragments — a real enforcement gap to close or accept explicitly.

## Web & Documentation Research

- **Pause-as-data reference shapes**: AG-UI's Interrupt type (`id`, `reason`, `message`, `responseSchema`, `expiresAt`, resume-payload validation, idempotent resubmission) is the closest external analog to the pause spec (https://docs.ag-ui.com/concepts/interrupts); Mastra pairs a `suspendSchema` with a `resumeSchema` per step. LangGraph's `interrupt()`/`Command(resume=...)` warns: code before a pause re-executes on resume, so pre-pause side effects must be idempotent.
- **Idempotency is the recurring universal constraint**: LangGraph, Temporal, Google ADK ("tools run at least once and may run more than once"), and event-stream analyses all converge — a retried `advance` with real side effects needs dedup/idempotency keys; separate the append-only decision log from derived state. (Now load-bearing in RQ5's coexistence policy.)
- **Schema evolution**: Temporal's `GetVersion()` gates are the battle-tested precedent for evolving the machine definition while old logs stay replayable; event-sourcing best practice is additive-only fields, prefer new event types over mutating shapes — matching the repo's own tolerant-reader doctrine.
- **Replay verification**: deterministic-replay literature requires structured (parseable) decision emission, "fail loudly" on out-of-trace operations, and captured metadata; free-prose decisions are not replay-verifiable (https://www.sakurasky.com/blog/missing-primitives-for-trustworthy-ai-part-8/).
- **The forcing constraint is real and platform-level**: anthropics/claude-code#34592 (primary source) — AskUserQuestion is absent from all sub-agent tool contexts across versions, with the community workaround being exactly a file-relay-to-main-loop pattern.
- **Progressive disclosure caution**: SkillRouter (arXiv 2603.22455) — routing on name/description alone dropped Hit@1 by 31–44pp; the router needs body content. cortex's design sidesteps this (code, not an LLM, routes) **only if** the state table encodes enough precondition logic that the model never has to re-infer "what state am I really in" from prose.
- **No prior art found for the exact next/advance CLI-served pattern** in aider/goose/opencode/BMAD/Spec Kit — flagged as not-found rather than confirmed-absent. MCP's resource_link-vs-embedded-content guidance maps onto the selector/server tradeoff.

## Domain & Prior Art

- In-tree: `cortex-lifecycle-parse-args` + ADR-0018's docs-scrape drift guard is the selector-verb precedent that already shipped standalone; `cortex-generate-backlog-index` is the generate-markdown-from-code precedent for a `cortex-lifecycle-describe` verb [cortex_command/backlog/generate_index.py:220-240]; `plan-synthesizer.md` is the one fragment already unified across interactive and overnight via importlib.resources — and its interactive consumption is under-specified (no concrete invocation snippet, no end-to-end test) [skills/lifecycle/references/competing-plans.md:35].
- "The Log is the Agent" (arXiv 2605.21997) and OpenHands' event-sourced ConversationState are the architectural cousins of events.log-as-source-of-truth; Burr is the closest lightweight FSM-owns-transitions library shape.
- Prior-wave lessons (the threat model): emission-before-commit ordering, `git add -u` cross-session contamination, dash/slash path mismatch, F1 side-effects-before-decision-gate, dropped `discovery_source` literal [commit 7b9e03c3; cortex/lifecycle/offload-completemd-pr-state-routing-and/spec.md:59,87-96]. The wave-2 F1 fix is the structural template the advance verb generalizes: `enter()` returns a `needs-decision` state *before composing any mutating step* [cortex/lifecycle/lifecycle-corpus-trim-wave-2/compression-diff-review.md:121-136], and `wontfix_cli.py`'s function body *is* the ordering invariant [cortex_command/lifecycle/wontfix_cli.py:196-198].

## Feasibility Assessment

Re-shaped by the holistic wave into three phases: A (machine-independent point fixes, land now), B (the minimal composition — a strict prefix of the machine), C (the served loop — a separately-gated decision on B's evidence). Steps 3–5 of the original sequence remain gated behind C.

| Phase / item | Effort | Risks | Prerequisites |
|----------|--------|-------|---------------|
| **A1**: #370 fix + verb-level regression test in cortex_command/lifecycle/tests/test_resolve.py | S | none — ticket-specified | none |
| **A2**: route review_dispatch.py's three per-feature emissions through `lifecycle_event.log_event` (today an UNLOCKED bare append [cortex_command/pipeline/state.py:288-304] duplicating `_EVENT_SUBCOMMANDS` shapes) | S | none — lock+schema parity fix | none |
| **A3**: point fixes — plan.py `_TERMINAL` shared import; `interactive_lock._emit_event` → `log_event`; vestigial `SYNTHESIZER_VALIDATED_*` constants; prune/fence dormant pipeline FSM (`pipeline/state.py:20-28` + pipeline-state.json) | S | none | none |
| **A4**: dormant-config audit (skip-specify/skip-review/default-tier/-criticality documented-but-never-parsed, yet set in live configs) + unknown-workflow-key warning in the config reader | S | none | none |
| **B1**: four decision-cluster wrapper verbs — plan-decision (plan_approved→feature_paused→phase_transition in fixed order), review-verdict routing (`{route, terminal, continue_to}` shape), spec-approve (absorbs `status: refined` write-back), implement transitions — each with a write-side order test (harness arm d); FILE_EVENTS retires via the finalize precedent [cortex/lifecycle/lifecycle-corpus-trim-wave-2/plan.md:72]; W003 via tests-surface assertions | M | none new — inherits the accepted skew posture (enter/finalize/branch_decision shipped with zero skew protection); no handshake, no flavor decision, no bounding ADR needed | A1 |
| **B2**: marker-generated kept-pauses — stable pause-ID markers at each site + repo-data kind/rationale file + generator (generate_index.py pattern); parity becomes exact marker-set equality; closes the 3 regex-missed sites; carries the four-kind discriminant | S–M | none — repo data is hot-editable, no wheel release, no skew; a wheel table cannot bind to prose lines anyway | none |
| **B3**: overnight consumes `cortex-lifecycle-state --field criticality` instead of orchestrator-round.md's inline ~25-line reduction reimplementation; §1a citation pin loses its consumer | S | none | none |
| **B4**: environment-fixture guard tests (harness arm f) against existing guard verbs | S | none | none |
| **C**: the served loop — step 0 protocol handshake (version stamped into existing verbs, bidirectional `--protocol N`, compat-range, verb-failure-as-skew) + next/advance composing the B1 verbs — **go/no-go decided AFTER B on B's evidence**, carrying the operator-experience, coherence, and hazard-register requirements (§Holistic Design Review) as spec obligations, plus the bounding ADR for ADR-0019, the ADR-0020 advance-schema definition, coexistence policy, and the flavor decision | M–L | Residual unique value is a centralized graph + uniform envelope + path overview + cross-phase order refusal — weigh against ~2-2.5x cost delta and the zero-incident record for the misrouting class it guards | Phases A+B complete; go/no-go criteria met (see Open Questions) |
| **C+1 (was step 3)**: pauses-as-data runtime tooth (advance refuses pause-gated transitions without a recorded pause event) — B1's consent verbs already deliver the relayed-consent audit for the two highest-stakes pauses | M | recorded-event tooth is fabricatable — auditable, not unforgeable | C; parity net closed on current corpus (B2 delivers this) |
| **C+2 (was step 4)**: overnight core-sharing (shared state+event core, two policy layers) | L | #190 multi-writer re-litigation; every pause needs an overnight arm; §1a/§1b duplication is must-stay-prose — machine-ownable volume unquantified | C; written overnight transition table **including pipeline writers** (review_dispatch sequencing), not just orchestrator-round.md |
| **C+3 (was step 5)**: entry-point configs (refine stop-at:spec; discovery branch) | M–L | greenfield; clarify-fork axis collision; consumer configs hold old field names — rename must alias with deprecation warnings | C; axis rename/split with config aliasing |
| Harness, remaining arms: (e) describe-verb golden + per-state envelope tests, (g) status-projection differential sweep, **(h) reverse-direction golden (old reader over machine-written logs — doubles as the statusline-divergence detector)**, (a) historical terminal-state replay, (c) forward decision-capture, (b) per-event diff | S–M each | (a)/(c) cover the unchanged reducer and version-N+1 only; (e)/(g)/(h) are cutover instruments — pay only at C | (d)/(f) land in B; the rest with C |

## Holistic Design Review

Five-angle wave run before decompose at the user's direction; each finding below is binding on the phased shape in Feasibility.

### Delivery mechanism: CLI confirmed; MCP and hooks-only rejected

Scored comparison across skew, fragments/ADR-0009, pauses, enforcement, and headless consumption: **pure CLI 16/21, CLI+hook-tooth hybrid 16.5/21, MCP server 11/21, hooks-only 8.5/21.** The decisive criterion is headless consumption: SDK workers run with `_ALLOWED_TOOLS = [Read, Write, Edit, Bash, Glob, Grep]`, no `setting_sources`, no `mcp_servers` [cortex_command/pipeline/dispatch.py:293,788-805] — plugin MCP servers and plugin hooks structurally cannot reach them, while CLI verbs are consumable by all three execution contexts (interactive loop, `claude -p` orchestrator, SDK workers) and by model-free Python. CLI-first is also non-foreclosing: the overnight server's R1 invariant (subprocess + versioned JSON only, zero `cortex_command` imports [plugins/cortex-overnight/server.py:10-15]) proves an MCP wrapper composes over CLI verbs later. Two accessories extracted from the rejected flavors: (1) the **skew-closure vehicle is a SessionStart background-install hook** — proven in-tree by `cortex-cli-background-install.sh` (detached uv-reinstall on drift), so closure never requires an MCP server, only reversing #235/#263; (2) one **PreToolUse deny hook keyed on Write/Edit `file_path` → `cortex/lifecycle/*/events.log`** (never Bash-text regex — the commit validator's three-regex extractor demonstrates that sieve) upgrades enforcement interactively, sequenced only after orchestrator-round.md's direct events.log appends [cortex_command/overnight/prompts/orchestrator-round.md:334] migrate to verbs.

### The incident record: the motivating failure class has never occurred

Full sweep of 56 archived retros (the complete corpus, 2026-04-01→04-23), cortex/debug, lifecycle reviews, and ~200 commits: **zero recorded incidents of the model misrouting the lifecycle phase sequence from prose routing** — no skipped kept-pause, no unauthorized auto-advance, no wrong-phase reference read. The two adjacent runtime incidents are on different surfaces: the /pr+/commit rail experiment (14/40 trials bypassed a conditional-load trigger [cortex/retros/archive/2026-04-23-0400-lifecycle-100-implement.md:48-49] — fixed by a PreToolUse hook, a surface the machine doesn't touch) and the refine research-skip [cortex/debug/2026-04-02-refine-spec-without-research.md] whose root cause was prose→prose drift. The failure classes with real incident records are **(b) hand-maintained prose drifting from code** (stale registry line numbers, a phantom research schema, tolerance-constant drift, an explicit "no durable prose↔Python route-consistency gate" admission) and **(c) event-emission field/ordering mistakes** (emit-before-commit strand, NDJSON spacing, invented event schema). Phase B targets exactly (b) and (c); the loop's structural-sequencing guarantee targets (a), which has no occurrences — and the funded selector flavor is conceded "convention, not guarantee" regardless.

### The minimal composition is a strict prefix — hence the phased funding shape

Phase B (wrapper verbs + marker-generated pauses + overnight reducer dedup + arms d/f) delivers motivating outcomes 1 (FILE_EVENTS), 2 (kept-pauses generated), 4-mechanical (overnight reducer dedup), 6 (#370), plus recorded relayed-consent events for the two highest-stakes pauses — at ~2-2.5x less cost than steps 0–2, with **no protocol handshake** (plain wrapper verbs inherit the accepted skew posture that enter/finalize/branch_decision shipped under; it is the per-step next/advance loop that raises skew stakes enough to require step 0), no flavor decision, no bounding ADR, no coexistence policy. Strict-prefix property: the machine's own architecture has `advance` compose exactly these verb bodies [see Architecture], and step 0's design stamps the protocol into existing verbs anyway — nothing built in B is rework if C funds. The solution-horizon triggers do not fire against B-first: steps 3–5 are already not committed, the multi-place-patch concern is answered by shared imports and the B2 generator, and the skew constraint C mitigates is one C itself creates. C's residual unique value: one centralized graph representation (whose absence has produced one one-line drift bug in repo history), a uniform envelope with path overview, and cross-phase order refusal (each B1 verb can carry the same local precondition at less machinery).

### Operator-experience requirements (ranked; binding on C if it funds)

1. **Explainable derivation**: `next` emits (or supports `--explain` for) the evidence trace — events consumed, artifact facts, guard results, table row fired. `describe` covers statics; #370-class debugging needs dynamics.
2. **Every advance refusal names the missing evidence and the sanctioned override** — including an explicit verb for recording out-of-band resolutions, else event fabrication becomes the de facto recovery path.
3. **The force-source hatch must be deliberately extended**: `cortex-lifecycle-resolve` is a console script with **no bin wrapper today** — no `CORTEX_COMMAND_FORCE_SOURCE` path exists for it, and Layer-3 background reinstalls clobber venv edits. New verbs ship behind 4-branch wrappers or honor force-source in the console scripts, with a documented consumer-repo recipe.
4. **Evidence redundancy stays in the envelope**: the contradiction that caught #370 (backlog status vs emitted state) rode in one payload — redundant cross-facts are the model's error-detection surface, not bloat.
5. **Authoring locality**: measured churn is ~255 prose commits vs ~40 wheel commits over the same window — gate conditions and route tweaks are the high-frequency edit surface and must stay in the fast (repo/plugin) layer; this also weighs the flavor decision and favors B2's repo-data table over wheel code.
6. **One-command local-dev mode** — working-tree Python is shadowed by the installed wheel in this very repo (bin wrapper branch order).
7. **events.log hand-append is a documented recovery contract** — dedup/idempotency machinery must tolerate operator-authored rows.
8. **Path overview default-on at resume**; skew remediation messages copy-pasteable (the bin-wrapper exit-2 message is the template).

### System-coherence requirements (binding on C)

1. **The envelope carries the legacy display-phase projection as a field**; every machine state declares one — a single mechanism serving reader migration, the permanent statusline mirror pin, and rollback. Without it the machine adds a state-vs-phase vocabulary schism instead of collapsing the parity problem (which is really 2 derivations + thin wrappers, not 3).
2. **Writer census as a step-2/C acceptance criterion**: every per-feature events.log writer enumerated and classified advance-routed / sanctioned-sibling / forbidden. Named today: review_dispatch.py (decides transitions itself, unlocked append — A2 fixes the lock; C must fold or sanction it) and morning-review advance_lifecycle.py (synthesizes phase_transition/review_verdict/feature_complete rows).
3. **Reversibility at the step-2 cliff**: a dual-emission window (advance emits the exact legacy vocabulary + machine events as additive extras), a reverse-direction golden (arm h), FILE_EVENTS quarantined one 30-day registry cycle rather than deleted, and a written roll-forward exit procedure (consumer repos cannot roll back — auto-release + no CLI_PIN).
4. **Dormant-config activation guard**: the machine must not silently honor previously-unparsed fields (`skip-specify`, `skip-review`, `default-tier/-criticality` are documented-but-never-implemented yet set in live configs) — activation requires opt-in or a loud first-read warning; unknown workflow-shaping keys warn instead of silent-ignore.
5. **The transition table is closed and wheel-owned** — consumer config supplies enum-validated parameters selecting among wheel-defined transitions and can never define states or reorder edges. Refused (not deferred) per project.md's complexity/trimming/out-of-scope philosophy; the bounding ADR also bounds the new **skill-invocation directive class** (the envelope may name only skills whose invocation condition is machine-readable state — this is new vocabulary, no CLI-served skill invocation exists today) and acknowledges the release-cadence coupling cost of moving the critical-review gate matrix into the wheel.
6. Statusline keeps its own derivation permanently (latency exception structural); dashboard timeline/metrics stay on raw events under registry governance; the pipeline-run FSM and external-backend status ownership are declared out of the machine's scope by name.

### Hazard register (machine-introduced, ranked; each with cheapest structural mitigation)

1. **Dual events.log authority across worktrees**: `lifecycle_event.py` resolves the log by physical CWD (worktree-local copy) while `interactive_lock` anchors to the main root — one feature, two logs, two lock files; per-file flock excludes nothing on the other copy. The machine promotes this file to gate authority without choosing a resolver. Mitigation: pin one resolver for machine verbs, or the envelope records the physical log identity (path+inode) it reduced and advance asserts it.
2. **Unspecified idempotency keys + check-then-act advance**: two sessions can both pass the gate and double-execute side effects (the interactive lock covers worktree entry only, and its own acquire is documented non-atomic). Mitigation: extend the existing sibling flock's critical section in advance to reduce+gate+append — a compare-and-swap asserting expected `from_state` inside the lock. Also discharges stale-envelope fencing (envelope carries expected-state/log-hash; advance refuses with "state moved, re-run next").
3. **Legacy payload-less `feature_paused` rows under the new discriminant**: an overnight policy arm mapping unknown-kind to defer/auto-resolve self-approves consent gates. Mitigation: reducer defaults kind-absent pauses to the most restrictive kind (relayed-consent, operator-resume-only).
4. **Migration-window projector demotion**: prose sets `refined` (event-less today); the sole-writer projector re-derives and "corrects" it backward. Mitigation: status writes monotonic on the lattice — never demote without a demoting event; keep the scan_lifecycle mismatch detector running continuously through the window.
5. **Side-effect amplification of fabricated attestations**: one fabricated pause-response now triggers the full mechanical side-effect chain. Mitigation: merge-consent pairs with the one available machine-verifiable cross-check (gh PR state); pause-response events carry a quoted-utterance payload so fabrication is a specific falsifiable lie.
6. **Serve-time guard TOCTOU widened by the serve→act hop**: guards gating mutations re-evaluate inside advance/prepare-worktree at act time; `next`'s guard output is labeled advisory in the schema, never a permission.
7. **Zombie-resurrection amplification** (#329 class): every transition funnels through the `mkdir(exist_ok=True)` writer and the projector can convert a ghost dir into status corruption. Mitigation: archive-shadow check on the append path (the gate #329 declined, now justified) and the projector never writes status for archive-shadowed features.
Retry safety: two-row transitions (`advance_started {invocation_id}` before side effects, `advance_committed` after) + per-side-effect existence probes (the finalize pattern) — specified at step 2, else decision-capture pins double-execution as golden. For balance, hazards the machine genuinely improves: duplicate rows become detectable, F1 ordering gets structural composition, pause-skips become auditable, and post-wontfix un-terminaling is fixed by any expected-state precondition.

## Architecture

The target architecture if Phase C funds. Phase B builds the identity resolver (in resolve.py), the B1 transition-executor verb bodies that `advance` would later compose, the pause-spec registry's repo-data substrate (B2), and harness arms d/f — all strict-prefix pieces of this design.

### Pieces

- **Transition table** — the single wheel-side source of truth for states, gates, guard preconditions, pause specs, and parameters (criticality × tier × backend × config overrides); the thing `describe` renders, parity tests generate from, and the harness's envelope tests pin.
- **Identity resolver** — slug-canonical resolution absorbing #370; numeric ID/title/UUID normalize to the lifecycle slug before any state lookup.
- **next verb** — read-only server: resolves identity, reduces events.log, evaluates guard preconditions, and emits {state, fragment ref-or-content, pause spec, advance contract, optional path overview, protocol version}.
- **advance verb** — the write-side transition executor. Its F1 defense is **structural composition**: side effects execute inside the verb, behind its own gate check, in fixed source order (the `wontfix_cli`/`enter` needs-decision pattern) — not advance-time verification of externally-performed work. It carries idempotency keys, verifies machine-verifiable gates (CLI-produced evidence only), records attestation-mode and relayed-consent transitions as durable events with rationale, refuses pause-gated transitions without a recorded pause event, emits additive-only events through the ADR-0020 contract, and owns the in-repo status projection (cortex-backlog backend only).
- **Pause-spec registry** — the four-kind discriminant (question / phase-exit / conditional / relayed-consent) with per-kind data (options, resume semantics, suppression predicate), replacing kept-pauses.md as generated output, with the successor enforcement contract (generated parity + advance's recorded-pause-event requirement).
- **Fragment corpus** — the judgment-dense What/Why prose that stays prose; distribution flavor (plugin-path selector vs wheel-served content) is the open structural decision.
- **Protocol handshake** — a small protocol integer, decoupled from release versions, stamped into existing-verb payloads at step 0 and passed bidirectionally (`--protocol N` argv from the loop; payload field from the wheel), compat-range asserted on both sides, with verb-invocation failure treated as skew. **Guards** cortex-core's unprotected skew surface (detect-and-halt with remediation); closing it (auto-remediation) requires reversing #235/#263 — an open decision.
- **Interactive policy layer** — the thin SKILL.md next/advance loop in the main conversation, the only place pauses render (realistic size 2.5–4K with a line-item budget, not ~1K).
- **Overnight policy layer** — the pause-resolution arms (defer/precondition/triage/auto-merge) consuming the same core; replaces orchestrator-round.md's sequencing scaffolding (volume to be quantified by the RQ3 transition table) but not its judgment content or policy role.
- **Verification harness** — day-one arms: write-side advance order tests, describe-verb transition-table golden + per-state envelope tests, environment-fixture guard tests, status-projection differential sweep. Supplementary arms: historical terminal-state replay (reducer slice), forward decision-capture (version-N+1 regressions), per-event diff (deferred).
- **describe verb** — generated markdown rendering of the transition table for authoring ergonomics *and* the harness's CI-diffed golden (generate_index.py pattern, not the hand-authored events-registry pattern).
- **Escape hatches** — phase override, criticality/complexity override, wontfix, cancel: kept as sibling verbs outside the transition table, as today.

### How they connect

The transition table is the machine; everything else is a view or an actor on it. `next` is the pure read path: identity resolver → events.log reduction (the existing `reduce_lifecycle_state`/`detect_lifecycle_phase` primitives remain the derivation layer) → guard evaluation → envelope emission, always exit 0, protocol version stamped. The interactive loop acts on the envelope in the main conversation — reading the fragment (selector) or receiving it inline (server), rendering any pause spec via AskUserQuestion, then calling `advance`, which executes the transition's side effects inside its own gate-checked body, requires recorded pause events for pause-gated transitions, emits the transition event, and updates the in-repo status projection. events.log remains the sole durable state (no state.json — #190's multi-writer rejection and ADR-0004's frontmatter-sub-state rejection both bind); the overnight layer consumes the identical next/advance core but resolves each pause spec through its policy arms instead of rendering it, and keeps the fleet scheduler untouched above. The harness closes the loop from both sides: write-side verb tests and envelope goldens pin the new surfaces at cutover; historical replay pins the reducer; decision-capture makes future transitions (including the gh/git-dependent complete-route slice) regression-detectable from version N onward. Boundaries: the machine owns sequencing and machine-verifiable gates; attestation and relayed-consent gates are recorded, auditable, and honestly labeled as trust-carrying; fragments own judgment; escape-hatch verbs mutate state outside the table; external backlog backends receive best-effort write-back, never ownership.

## Decision Records

**events.log stays the only durable state.** #190 rejected a state.json because the single-writer assumption is empirically false (morning-review is a third writer class) [cortex/lifecycle/promote-lifecycle-state-out-of-eventslog-full-reads/spec.md:26]; ADR-0004 rejected frontmatter sub-state and log rewrites. The machine is a pure transition function over file-derived state (ADR-0001-compatible), never a daemon or side-store.

**The machine owns sequencing and machine-verifiable gates, never thinking — with the trust boundary stated honestly.** Machine-verifiable means CLI-produced or CLI-checkable evidence (prior advance-emitted events, guard exit codes, machine-established file facts). Model-authored artifacts (plan.md checkboxes, review verdicts) and relayed human utterances (spec approval) are attestations in substance regardless of file shape — CLI-reading them confirms shape, not truth, and the design does not claim otherwise. Routing tables, gate matrices, and guard sequencing are machine-ownable; plan-authoring rules, checklists, prompt templates, and failure judgment stay judgment-dense prose.

**The F1 defense is structural composition plus write-side tests — not gate verification.** F1 is an ordering bug; verification at advance-time checks truth, not ordering, and cannot police side effects performed before advance was invoked. The defense: transitions with side effects execute those side effects *inside* the advance verb, behind its gate check, in fixed source order (generalizing `enter()`'s needs-decision short-circuit and `wontfix_cli`'s function-body-as-invariant), pinned by write-side verb tests that run advance and assert emitted order. Historical order-assertions cannot catch this class — old logs were written by the old system.

**Status projection scopes to the cortex-backlog backend.** The machine becomes the sole lifecycle-driven in-repo status writer (replacing three independent write sites); external backends stay ADR-0016 best-effort; scan_lifecycle's mismatch detector is retained as the reconciliation surface and doubles as the harness's pre/post-cutover differential oracle.

**Overnight is re-scoped from "retires" to "shared core, two policy layers" — and step 4 is a separate go/no-go.** The forcing constraint means the headless orchestrator can never pause; its four substitutes are different transitions, not pause responses. The cited orchestrator-round.md duplication (§1a/§1b) is judgment prose that stays prose either way; the machine-ownable sequencing scaffolding is unquantified until the RQ3 transition table exists. What certainly retires is the heading-citation pin class.

**Funding shape: Phase A point-fixes now, Phase B composition next, Phase C loop as a gated decision.** (Supersedes the earlier "fundable core is steps 0–2" record after the holistic wave.) The incident record shows the loop's guarded failure class has zero occurrences while Phase B fixes the classes with real records (drift, emission) at ~2-2.5x less cost and is a strict prefix of the machine — `advance` would compose the B1 verb bodies, and step 0 stamps existing verbs anyway. The loop (C) is decided after B on B's evidence, carrying the operator-experience, coherence, and hazard-register requirements as spec obligations; C+1..C+3 (pause runtime, overnight core-sharing, entry-point configs) gate behind C. This is the solution-horizon test applied in the converse direction: pre-committing the loop when the record doesn't yet indict prose routing would be prediction, not knowledge.

**Delivery mechanism is CLI verbs.** MCP-server and hooks-only flavors were evaluated and rejected — SDK workers structurally exclude plugin MCP servers and hooks [cortex_command/pipeline/dispatch.py:293,788-805], CLI is the one surface all three execution contexts share, and the overnight server's R1 invariant proves an MCP wrapper composes over CLI later (non-foreclosing). Two accessories adopted: the SessionStart background-install hook pattern as the named skew-closure vehicle (if #235/#263 is ever reversed), and an optional PreToolUse deny on Write/Edit `file_path` → events.log as the interactive enforcement tooth, sequenced after overnight's direct appends migrate.

**The value claim is drift-proof sequencing, netted honestly.** No per-lifecycle token total favors the machine; selector flavor keeps all fragment reads and adds JSON payloads; the leanification campaign already closed byte surfaces at audited floors. Benefits: structurally-guaranteed sequencing *for machine-verifiable gates* (recite-only gates change who displays them; the selector flavor is convention, not guarantee — see Open Questions), event emission in code, generated parity surfaces **net of** the new hand-maintained surfaces they create (W003 allowlist rows, the SP001/SP002 literal gap, and — server flavor — a third wheel↔plugin sync surface in a repo where dual-source drift already blocks all commits), one shared core under both execution modes (if step 4 funds), and retirement of the FILE_EVENTS pin class plus the kept-pauses pin class *with its successor enforcement contract in place*.

**Escape hatches stay sibling verbs.** Phase override (`resolve.py` already honors it with a warning), criticality/complexity overrides, wontfix, and cancel remain out-of-band mutations, keeping the transition table honest about the happy path.

**New router verbs need their own ADR bounding ADR-0019.** A next verb that reads config, resolves identity, evaluates guards, and serves instructions is categorically not a dumb arg-actor; sanctioning it silently under 0019 is the scope-creep the ADR itself warns about.

**Absorbed verb surfaces get a coexistence policy, not a cliff.** Additive-only events.log discipline does not cover the verb surface: absorbed verbs stay callable until a protocol-floor bump (events-registry 30-day-grandfather discipline as the template), and advance's idempotency keys prevent double-emission from skewed consumers running both paths.

## Open Questions

- **Phase-C go/no-go criteria — the new load-bearing question.** What evidence from living with Phase B would justify funding the loop? Candidate signals: a recorded class-(a) misrouting incident post-B; the distributed verb graph producing cross-verb drift the shared imports and B2 generator don't catch; resume/"what's left?" friction the Next-line convention doesn't cover; or steps C+2/C+3 independently justifying a shared core. Decompose should encode these as the gate ticket's decision criteria rather than leaving "later" unbounded.
- **Flavor (selector vs server) — deferred to C.** Selector = convention, not guarantee; server = structural but carries pre-resolution obligations (ADR-0009), 1–2.3K tokens per call, and a third sync surface with no pre-commit trigger. Operator requirement 5 (authoring locality: 255 prose vs 40 wheel commits) now weighs alongside token math; a hybrid (server for sequencing fragments, selector for judgment prose) and the PostToolUse-injection variant are recorded options. Decided at C's spec time, as a C prerequisite.
- **Handshake closure vs guarding — vehicle now named, timing open.** Closure = the SessionStart background-install hook pattern extended to cortex-core, which reverses #235/#263 and needs a decision record. Only relevant if C funds; B needs neither.
- **Overnight transition table**: what fraction of orchestrator-round.md is machine-ownable sequencing vs judgment prose — **and the table must enumerate pipeline writers (review_dispatch's verdict/rework/re-merge sequencing), not just orchestrator prose.** Determines whether C+2 funds as core-sharing or shrinks to "shared reducer only" (B3 already delivers the shared-reducer floor).
- **SKILL.md line-item budget (C)**: which specific current blocks does the loop absorb, and where does each land? Target is honest 2.5–4K, not ~1K.
- **Axis rename before C+3**: discovery's research-sizing complexity/criticality vs lifecycle's implementation axes share field names with different semantics and opposite floors — rename/split design, consumer-config aliasing, and additive-only event implications unresolved.
- **Lint-gap closure (C, server flavor only)**: extend SP001/SP002 scanning to Python string literals feeding served fragments, or accept as documented convention?
- **Pause honoring — residual trust (C+1)**: the recorded-pause-event tooth is fabricatable. Partially hardened by the hazard-register mitigations (quoted-utterance payloads; gh cross-check for merge consent); is the durable-audit posture then sufficient for the remaining consent gates?
- *(Resolved by the holistic wave and recorded above: delivery mechanism → CLI; W003 wiring → B1 uses the finalize tests-surface-assertion precedent, and C's absorbed-verb W003 only arises if C funds; idempotency-key semantics → flock-wrapped CAS on (feature, expected from_state) plus two-row transitions.)*
