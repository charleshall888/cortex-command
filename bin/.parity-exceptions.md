# Parity exceptions allowlist

Allowlist for `bin/cortex-check-parity` (lifecycle 102 / DR-5). Entries here suppress the W003 "orphan" warning for `cortex-*` scripts that are intentionally not wired through the in-scope SKILL.md / requirements / docs / hooks / justfile / tests scan surface.

The linter parses this file at load time. Schema (markdown table, 5 columns):

| Column | Constraint |
| --- | --- |
| `script` | Matches regex `cortex-[a-z][a-z0-9-]*`. Bare token, no `bin/` prefix. |
| `category` | One of `maintainer-only-tool`, `library-internal`, `deprecated-pending-removal`. |
| `rationale` | ≥30 characters after trim. Forbidden literals (case-insensitive): `internal`, `misc`, `tbd`, `n/a`, `pending`, `temporary`. Be specific. |
| `lifecycle_id` | Lifecycle ticket / backlog id that introduced the entry. |
| `added_date` | `YYYY-MM-DD`. |

Adding an entry is a small architectural decision. Reviewers: confirm `category` enum is correct and `rationale` is specific (not 'internal' or 'misc' — those literals are linter-rejected). The W005 warning surfaces when an allowlisted script also has a wiring signal in scope; prune those rows.

| script | category | rationale | lifecycle_id | added_date |
| --- | --- | --- | --- | --- |
| `cortex-archive-sample-select` | `maintainer-only-tool` | Lifecycle archive sampling — invoked manually by the maintainer when archiving a session; no agent flow references it and no wiring path is appropriate today. | `102` | `2026-04-27` |
| `cortex-batch-runner` | `library-internal` | Entry-point binary spawned by `cortex_command/overnight/runner.py` via `subprocess.Popen` — wiring is in Python source, which is outside the scan-glob surface (`*.md`, `*.sh`, justfile, tests). | `151` | `2026-04-29` |
| `cortex-pipeline-metrics` | `library-internal` | Console-script entry point now invoked in-process by `cortex_command/hooks/scan_lifecycle.py` (it calls `cortex_command.pipeline.metrics.main` directly after the bash→Python hook refactor). Wiring is Python source, outside the scan-glob surface. | `resolve-cortex-interpreter-via-cli` | `2026-05-18` |
| `cortex-common` | `library-internal` | The detect-phase and staleness subcommands are consumed via direct `cortex_command.common` function imports by the cortex-lifecycle-resolve façade verb; the module is imported broadly across the codebase and no SKILL.md or shell path invokes the CLI wrapper after the Step 1+2 offload. | `offload-lifecycle-resolution-to-facade-verbs` | `2026-07-03` |
| `cortex-lifecycle-parse-args` | `deprecated-pending-removal` | Superseded by the cortex-lifecycle-resolve façade verb, which imports the parse() grammar classifier directly from parse_args.py; the console wrapper is no longer invoked from any SKILL.md or shell path and is retained only until the module entry point is dropped. | `offload-lifecycle-resolution-to-facade-verbs` | `2026-07-03` |
| `cortex-lifecycle-dispatch-choice` | `deprecated-pending-removal` | Superseded by the cortex-lifecycle-branch-decision façade verb, which imports read_dispatch_choice directly; the console wrapper is no longer invoked from any SKILL.md or shell path after the Implement §1 offload. | `offload-lifecycle-resolution-to-facade-verbs` | `2026-07-03` |
| `cortex-generate-kept-pauses` | `maintainer-only-tool` | Source-tree generator that renders kept-pauses.md from kept-pauses-data.toml; invoked by the maintainer via `just kept-pauses` or the console script. Its consuming freshness gate lands in the epic-371 Phase-2 marker-equality flip, so no SKILL.md, docs, or test references it yet. | `build-the-verb-completion-composition-wrapper` | `2026-07-10` |
