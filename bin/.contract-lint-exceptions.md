# Contract lint exception ledger

Allowlist for `cortex-check-contract` (lifecycle 253). Entries here suppress contract-lint violations for `cortex-*` binaries whose argv parsing is not AST-extractable. Categorical whole-binary exemptions use `*` for both `flag-or-subcommand` and `path-glob`.

The linter parses this file at load time. Schema (markdown table, 7 columns):

| Column | Constraint |
| --- | --- |
| `binary` | Matches regex `cortex-[a-z][a-z0-9-]*`. Bare token, no `bin/` prefix. |
| `flag-or-subcommand` | Flag token (e.g. `--status`), subcommand name, or `*` for whole-binary categorical exemption. |
| `path-glob` | `fnmatch`-style glob against the violation's file path, or `*` for all paths. |
| `category` | Closed enum: `migration-note`, `template-fragment`, `intentional-omission`, or the no-argparse variant (see categories in ledger rows below). |
| `rationale` | ≥30 characters after trim. Forbidden literals (case-insensitive): `internal`, `misc`, `tbd`, `n/a`, `pending`, `temporary`. Be specific. |
| `lifecycle_id` | Lifecycle ticket / backlog id that introduced the entry. |
| `added_date` | `YYYY-MM-DD`. |

Adding an entry is a small architectural decision. Reviewers: confirm `category` enum is correct and `rationale` is specific.

| binary | flag-or-subcommand | path-glob | category | rationale | lifecycle_id | added_date |
| --- | --- | --- | --- | --- | --- | --- |
| cortex-update-item | * | * | non-argparse-module | uses key=value positional pairs instead of argparse flags; backlog #257 tracks the syntax refactor to flag form | 253-skill-prose-to-cli-argparse-contract-lint | 2026-05-25 |
| cortex-lifecycle-state | * | * | non-argparse-module | hand-rolled argv loop in state_cli.py iterates sys.argv directly — no argparse.ArgumentParser construction to introspect | 253-skill-prose-to-cli-argparse-contract-lint | 2026-05-25 |
| cortex-worktree-resolve | * | * | non-argparse-module | accepts a single positional argument via sys.argv without constructing an argparse.ArgumentParser — no introspectable surface | 253-skill-prose-to-cli-argparse-contract-lint | 2026-05-25 |
| cortex-interrupt | * | * | non-argparse-module | reads the PID argument via direct sys.argv access in overnight/interrupt.py — no argparse.ArgumentParser construction to extract a surface from | 253-skill-prose-to-cli-argparse-contract-lint | 2026-05-25 |
| cortex-log-invocation | * | * | non-argparse-module | reads all arguments via direct sys.argv access — no argparse.ArgumentParser construction to extract a surface from | 253-skill-prose-to-cli-argparse-contract-lint | 2026-05-25 |
| cortex-common | * | * | non-argparse-module | dispatch shim in cortex_command/common.py uses dispatch table over sys.argv subcommand name; no argparse.ArgumentParser is constructed at module scope | 253-skill-prose-to-cli-argparse-contract-lint | 2026-05-25 |
| cortex-generate-backlog-index | * | * | non-argparse-module | regenerates the backlog index unconditionally with no flag-driven options; constructs no argparse.ArgumentParser to introspect | 253-skill-prose-to-cli-argparse-contract-lint | 2026-05-25 |
| cortex-commit-preflight | * | * | non-argparse-module | emits the preflight JSON unconditionally with no flag-driven options; constructs no argparse.ArgumentParser to introspect | 253-skill-prose-to-cli-argparse-contract-lint | 2026-05-25 |
| cortex-morning-review-gc-demo-worktrees | * | * | non-argparse-module | demo-worktree GC tool reads optional flags via direct sys.argv inspection rather than argparse.ArgumentParser construction | 253-skill-prose-to-cli-argparse-contract-lint | 2026-05-25 |
| cortex-create-backlog-item | * | *skills/backlog-author/SKILL.md | intentional-omission | backlog-author only outputs the body and describes passing it via --body; title/status/type are the caller's responsibility to supply — these are pipe-pattern descriptions, not standalone invocations | 253-skill-prose-to-cli-argparse-contract-lint | 2026-05-25 |
| cortex-create-backlog-item | * | *fixtures/backlog_author/valid_five_section.md | intentional-omission | fixture body describes delegating file creation to cortex-create-backlog-item --body; the --body pipe pattern is the point being illustrated, not a complete invocation | 253-skill-prose-to-cli-argparse-contract-lint | 2026-05-25 |
| cortex-critical-review | * | *skills/critical-review/SKILL.md | intentional-omission | subcommand names appear as prose references in a sentence describing the gate protocol; the ellipsis form signals incomplete flags intentionally — the full invocation contract is defined in references/verification-gates.md | 253-skill-prose-to-cli-argparse-contract-lint | 2026-05-25 |
