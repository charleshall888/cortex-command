# Bare `python3 -m cortex_command.*` allowlist

Callsites retained on the `python3 -m cortex_command.*` form post-R14 promotion, with rationale.
Each entry must name the file, line range, module, and one of the accepted categories below.

**Categories**:
- `module-execution-pathway-test` — the callsite is specifically exercising the module-execution
  path (i.e. `python3 -m`) as a distinct dispatch vector under test. Replacing it with the
  console-script name would change what the test measures.
- `document-content-invariant` — the callsite is a string literal or docstring that pins
  the invocation text found in a companion document (`implement.md`, skill reference, etc.).
  Updating the string here without updating the companion document would break the invariant
  the test is enforcing; companion document updates are tracked separately.
- `wrapper-script-r14-pending` — the callsite is inside a bin/ shell wrapper that serves as
  the console-script entry point itself. Branch (a) logic (`python3 -m`) becomes redundant once
  `[project.scripts]` entries are installed; cleanup deferred to R14.

---

## Entries

### tests/test_critical_review_path_validation.py:12, :171

| field | value |
|---|---|
| file | `tests/test_critical_review_path_validation.py` |
| lines | 12, 171 |
| module | `cortex_command.critical_review` |
| category | `module-execution-pathway-test` |
| rationale | Line 12 is a module docstring enumerating the two test layers, including "CLI subprocess — `python3 -m cortex_command.critical_review`". Line 171 is the docstring of `_run_cli`, describing what it invokes. The actual subprocess call at lines 172–183 uses `sys.executable, "-m", "cortex_command.critical_review"` — which intentionally tests the module-execution pathway (not the console-script PATH-resolution pathway). Both docstrings accurately describe the test intent. Updating the docstrings to reference the console-script name would misrepresent what the subprocess tests. |
| r14_ticket | `208` |
| added_date | `2026-05-12` |

---

### bin/cortex-backlog-ready:7

| field | value |
|---|---|
| file | `bin/cortex-backlog-ready` |
| line | 7 |
| module | `cortex_command.backlog.ready` |
| category | `wrapper-script-r14-pending` |
| rationale | Branch (a) of the shell wrapper falls back to `python3 -m cortex_command.backlog.ready` when the package is importable but the console-script is not yet on PATH (e.g. during `uv tool install` transitions). Once the `[project.scripts]` console-script `cortex-backlog-ready` is consistently installed, this wrapper's branch (a) becomes a no-op and the wrapper can be simplified or removed. Tracked under R14 wrapper-cleanup. |
| r14_ticket | `208` |
| added_date | `2026-05-12` |

---

### skills/overnight/references/new-session-flow.md:3

| field | value |
|---|---|
| file | `skills/overnight/references/new-session-flow.md` |
| line | 3 |
| module | `cortex_command.<module>` |
| category | `document-content-invariant` |
| rationale | Callout box documenting the general invocation idiom for planning helpers. Uses a generic `<module>` placeholder rather than a concrete module name — it describes the pattern, not a specific dispatch. The companion document (`new-session-flow.md`) owns this text; updating it in isolation without revising the companion doc would break the documented contract. Tracked under R14 doc-update. |
| r14_ticket | `208` |
| added_date | `2026-05-12` |

---

### bin/cortex-morning-review-complete-session:9, :16

| field | value |
|---|---|
| file | `bin/cortex-morning-review-complete-session` |
| lines | 9, 16 |
| module | `cortex_command.overnight.complete_morning_review_session` |
| category | `wrapper-script-r14-pending` |
| rationale | Branch (a) at line 9 invokes `python3 -m cortex_command.overnight.complete_morning_review_session` when the package is importable but the console-script may not yet be on PATH. Branch (b) at line 16 handles the `CORTEX_COMMAND_ROOT` dev-checkout path with an explicit PYTHONPATH. Both branches become redundant once `cortex-morning-review-complete-session` is consistently installed as a `[project.scripts]` console-script and is reliably on PATH. Tracked under R14 wrapper-cleanup. |
| r14_ticket | `208` |
| added_date | `2026-05-12` |

---

### bin/cortex-log-invocation:6, :14, :19, :25

| field | value |
|---|---|
| file | `bin/cortex-log-invocation` |
| lines | 6, 14, 19, 25 |
| module | `cortex_command.log_invocation` |
| category | `wrapper-script-r14-pending` |
| rationale | Lines 6, 14, 19, 25 are the dual-channel wrapper for `cortex-log-invocation` (promoted in Phase 1 of installation-integrity-layer-bash-to-entry). Line 6 is the comment header documenting the branch (b) dispatch path. Lines 14, 19, 25 are branches (a), (b), and (c) respectively: force-source, wheel-import, and working-tree fallback. All three execution branches use `python3 -m cortex_command.log_invocation` because the wrapper itself IS the console-script shim; once the wheel binstub is resolved, the wrapper is invoked only in pre-wheel contexts. Cleanup to bare-name dispatch deferred to R14. |
| r14_ticket | `208` |
| added_date | `2026-05-20` |

---

### bin/cortex-resolve-backlog-item:6, :17, :22, :28

| field | value |
|---|---|
| file | `bin/cortex-resolve-backlog-item` |
| lines | 6, 17, 22, 28 |
| module | `cortex_command.backlog.resolve_item` |
| category | `wrapper-script-r14-pending` |
| rationale | Dual-channel wrapper for `cortex-resolve-backlog-item` promoted in Phase 2 (DR4). Line 6 is the comment header documenting branch (b). Lines 17, 22, 28 are branches (a) force-source, (b) wheel-import, and (c) working-tree fallback. Each branch dispatches via `python3 -m cortex_command.backlog.resolve_item` because the wrapper is the console-script shim and must reach the module through multiple environments. Cleanup deferred to R14 wrapper-cleanup. |
| r14_ticket | `208` |
| added_date | `2026-05-20` |

---

### bin/cortex-auto-bump-version:6, :17, :22, :28

| field | value |
|---|---|
| file | `bin/cortex-auto-bump-version` |
| lines | 6, 17, 22, 28 |
| module | `cortex_command.auto_bump_version` |
| category | `wrapper-script-r14-pending` |
| rationale | Dual-channel wrapper for `cortex-auto-bump-version` promoted in Phase 3 bulk migration. Line 6 is the comment header. Lines 17, 22, 28 are branches (a) force-source, (b) wheel-import, and (c) working-tree fallback, each invoking `python3 -m cortex_command.auto_bump_version`. The wrapper is the console-script shim; branch (b) becomes redundant once the wheel binstub is reliably installed. Cleanup deferred to R14. |
| r14_ticket | `208` |
| added_date | `2026-05-20` |

---

### bin/cortex-check-prescriptive-prose:6, :18, :23, :29

| field | value |
|---|---|
| file | `bin/cortex-check-prescriptive-prose` |
| lines | 6, 18, 23, 29 |
| module | `cortex_command.lint.prescriptive_prose` |
| category | `wrapper-script-r14-pending` |
| rationale | Dual-channel wrapper for `cortex-check-prescriptive-prose` promoted in Phase 3 bulk migration. Line 6 is the comment header. Lines 18, 23, 29 are branches (a) force-source, (b) wheel-import, and (c) working-tree fallback, each dispatching via `python3 -m cortex_command.lint.prescriptive_prose`. The wrapper is the entry-point shim; cleanup deferred to R14 once the console-script is consistently on PATH. |
| r14_ticket | `208` |
| added_date | `2026-05-20` |

---

### bin/cortex-commit-preflight:6, :18, :23, :29

| field | value |
|---|---|
| file | `bin/cortex-commit-preflight` |
| lines | 6, 18, 23, 29 |
| module | `cortex_command.commit.preflight` |
| category | `wrapper-script-r14-pending` |
| rationale | Dual-channel wrapper for `cortex-commit-preflight` promoted in Phase 3 bulk migration. Line 6 is the comment header documenting branch (b). Lines 18, 23, 29 are branches (a) force-source, (b) wheel-import, and (c) working-tree fallback, each invoking `python3 -m cortex_command.commit.preflight`. Cleanup deferred to R14. |
| r14_ticket | `208` |
| added_date | `2026-05-20` |

---

### bin/cortex-complexity-escalator:7, :20, :25, :31

| field | value |
|---|---|
| file | `bin/cortex-complexity-escalator` |
| lines | 7, 20, 25, 31 |
| module | `cortex_command.lifecycle.complexity_escalator` |
| category | `wrapper-script-r14-pending` |
| rationale | Dual-channel wrapper for `cortex-complexity-escalator` promoted in Phase 3 bulk migration. Line 7 is the comment header. Lines 20, 25, 31 are branches (a) force-source, (b) wheel-import, and (c) working-tree fallback, each dispatching via `python3 -m cortex_command.lifecycle.complexity_escalator`. Cleanup deferred to R14 once the console-script is reliably installed. |
| r14_ticket | `208` |
| added_date | `2026-05-20` |

---

### bin/cortex-git-sync-rebase:6, :17, :22, :28

| field | value |
|---|---|
| file | `bin/cortex-git-sync-rebase` |
| lines | 6, 17, 22, 28 |
| module | `cortex_command.git.sync_rebase` |
| category | `wrapper-script-r14-pending` |
| rationale | Dual-channel wrapper for `cortex-git-sync-rebase` promoted in Phase 3 bulk migration. Line 6 is the comment header. Lines 17, 22, 28 are branches (a) force-source, (b) wheel-import, and (c) working-tree fallback, each invoking `python3 -m cortex_command.git.sync_rebase`. Cleanup deferred to R14. |
| r14_ticket | `208` |
| added_date | `2026-05-20` |

---

### bin/cortex-lifecycle-counters:7, :20, :25, :31

| field | value |
|---|---|
| file | `bin/cortex-lifecycle-counters` |
| lines | 7, 20, 25, 31 |
| module | `cortex_command.lifecycle.counters` |
| category | `wrapper-script-r14-pending` |
| rationale | Dual-channel wrapper for `cortex-lifecycle-counters` promoted in Phase 3 bulk migration. Line 7 is the comment header. Lines 20, 25, 31 are branches (a) force-source, (b) wheel-import, and (c) working-tree fallback, each dispatching via `python3 -m cortex_command.lifecycle.counters`. Cleanup deferred to R14 once the console-script is consistently installed. |
| r14_ticket | `208` |
| added_date | `2026-05-20` |

---

### bin/cortex-lifecycle-state:6, :16, :21, :27

| field | value |
|---|---|
| file | `bin/cortex-lifecycle-state` |
| lines | 6, 16, 21, 27 |
| module | `cortex_command.lifecycle.state_cli` |
| category | `wrapper-script-r14-pending` |
| rationale | Dual-channel wrapper for `cortex-lifecycle-state` promoted in Phase 3 bulk migration. Line 6 is the comment header. Lines 16, 21, 27 are branches (a) force-source, (b) wheel-import, and (c) working-tree fallback, each invoking `python3 -m cortex_command.lifecycle.state_cli`. Cleanup deferred to R14. |
| r14_ticket | `208` |
| added_date | `2026-05-20` |

---

### bin/cortex-load-parent-epic:6, :18, :23, :29

| field | value |
|---|---|
| file | `bin/cortex-load-parent-epic` |
| lines | 6, 18, 23, 29 |
| module | `cortex_command.backlog.load_parent_epic` |
| category | `wrapper-script-r14-pending` |
| rationale | Dual-channel wrapper for `cortex-load-parent-epic` promoted in Phase 3 bulk migration. Line 6 is the comment header. Lines 18, 23, 29 are branches (a) force-source, (b) wheel-import, and (c) working-tree fallback, each dispatching via `python3 -m cortex_command.backlog.load_parent_epic`. Cleanup deferred to R14 once the console-script is reliably on PATH for all users. |
| r14_ticket | `208` |
| added_date | `2026-05-20` |

---

### bin/cortex-morning-review-gc-demo-worktrees:9, :16

| field | value |
|---|---|
| file | `bin/cortex-morning-review-gc-demo-worktrees` |
| lines | 9, 16 |
| module | `cortex_command.overnight.gc_demo_worktrees` |
| category | `wrapper-script-r14-pending` |
| rationale | Branch (a) at line 9 invokes `python3 -m cortex_command.overnight.gc_demo_worktrees` when the package is importable but the console-script may not yet be on PATH. Branch (b) at line 16 handles the `CORTEX_COMMAND_ROOT` dev-checkout path with explicit PYTHONPATH. Both branches become redundant once `cortex-morning-review-gc-demo-worktrees` is consistently installed as a console-script. Cleanup deferred to R14 wrapper-cleanup. |
| r14_ticket | `208` |
| added_date | `2026-05-20` |

---

### bin/cortex-backlog-ready:9, :16

| field | value |
|---|---|
| file | `bin/cortex-backlog-ready` |
| lines | 9, 16 |
| module | `cortex_command.backlog.ready` |
| category | `wrapper-script-r14-pending` |
| rationale | Branch (a) at line 9 invokes `python3 -m cortex_command.backlog.ready` when the package is importable but the console-script is not yet on PATH. Branch (b) at line 16 handles the `CORTEX_COMMAND_ROOT` dev-checkout fallback with explicit PYTHONPATH. Both branches become redundant once `cortex-backlog-ready` is consistently installed as a `[project.scripts]` console-script. Cleanup deferred to R14. |
| r14_ticket | `208` |
| added_date | `2026-05-20` |

---

### claude/hooks/cortex-session-start-path-bootstrap.sh:31

| field | value |
|---|---|
| file | `claude/hooks/cortex-session-start-path-bootstrap.sh` |
| line | 31 |
| module | `cortex_command.doctor.path_self_test` |
| category | `wrapper-script-r14-pending` |
| rationale | The PATH self-test added by Task 27 of installation-integrity-layer-bash-to-entry invokes `python3 -m cortex_command.doctor.path_self_test` directly because `cortex_command.doctor.path_self_test` has no promoted `[project.scripts]` console-script entry in the current release. The hook runs in the SessionStart context where `cortex-doctor` (if promoted) would be on the augmented PATH; once a console-script is promoted for the doctor module, this line becomes a wrapper-script-r14 cleanup candidate. |
| r14_ticket | `208` |
| added_date | `2026-05-20` |

---

### justfile:350, :381

| field | value |
|---|---|
| file | `justfile` |
| lines | 350, 381 |
| module | `cortex_command.parity_check` |
| category | `wrapper-script-r14-pending` |
| rationale | Line 350 is the `check-parity` recipe: `uv run python3 -m cortex_command.parity_check`. The comment above it explains this is intentional — invoking the module directly avoids shell-in-Python confusion when the pre-commit hook calls `just check-parity` from inside an already-running Python subprocess. Line 381 is the `check-bare-python-callsites` recipe invoking the same module with `--audit-bare-python-m-callsites`. Both use `uv run python3 -m` rather than the console-script because `uv run` ensures the correct venv is active for PyYAML availability; cleanup deferred to R14 once uv's console-script resolution is validated in this context. |
| r14_ticket | `208` |
| added_date | `2026-05-20` |

---

### tests/fixtures/cortex-check-parity/README.md:66

| field | value |
|---|---|
| file | `tests/fixtures/cortex-check-parity/README.md` |
| line | 66 |
| module | `cortex_command.parity_check` |
| category | `document-content-invariant` |
| rationale | The README documents the fixture recapture procedure using `LC_ALL=C TZ=UTC python3 -m cortex_command.parity_check --self-test`. This is the determinism harness per spec requirement 17: capture commands use the `python3 -m` form to guarantee the working-tree module is exercised (not the wheel binstub, whose PATH resolution could differ between capture and replay). Updating to the console-script name would change what the README documents and break the determinism-harness invariant. Companion document update tracked under R14. |
| r14_ticket | `208` |
| added_date | `2026-05-20` |

---

### tests/fixtures/cortex-check-prescriptive-prose/README.md:112, :118, :124

| field | value |
|---|---|
| file | `tests/fixtures/cortex-check-prescriptive-prose/README.md` |
| lines | 112, 118, 124 |
| module | `cortex_command.lint.prescriptive_prose` |
| category | `document-content-invariant` |
| rationale | The README documents fixture recapture commands using `LC_ALL=C TZ=UTC python3 -m cortex_command.lint.prescriptive_prose`. These lines pin the determinism-harness invocation (LC_ALL, TZ, python3 -m form) per spec requirement 17. The `python3 -m` form guarantees working-tree module execution during recapture regardless of installed-wheel state. Updating to the console-script would misrepresent the harness and risk PATH-resolution nondeterminism in capture. Companion document updates tracked under R14. |
| r14_ticket | `208` |
| added_date | `2026-05-20` |

---

### tests/fixtures/cortex-commit-preflight/README.md:112, :123, :131

| field | value |
|---|---|
| file | `tests/fixtures/cortex-commit-preflight/README.md` |
| lines | 112, 123, 131 |
| module | `cortex_command.commit.preflight` |
| category | `document-content-invariant` |
| rationale | The README documents fixture recapture commands using `LC_ALL=C TZ=UTC python3 -m cortex_command.commit.preflight`. These lines pin the determinism harness per spec requirement 17: `python3 -m` ensures the working-tree module is exercised during recapture, avoiding PATH-resolution nondeterminism from the installed wheel binstub. Updating the README to use the console-script would misrepresent the documented capture procedure. Companion document updates tracked under R14. |
| r14_ticket | `208` |
| added_date | `2026-05-20` |

---

### tests/test_check_parity.py:19

| field | value |
|---|---|
| file | `tests/test_check_parity.py` |
| line | 19 |
| module | `cortex_command.parity_check` |
| category | `document-content-invariant` |
| rationale | Line 19 is in the module docstring: "The test now invokes the module directly via `python3 -m cortex_command.parity_check` to remain runnable in both wheel-installed and working-tree contexts." The docstring explicitly documents the rationale for using the module-execution form; replacing the reference would misrepresent the test's dispatch strategy and break the documented invariant. |
| r14_ticket | `208` |
| added_date | `2026-05-20` |

---

### tests/test_check_parity_first_run_green.sh:16

| field | value |
|---|---|
| file | `tests/test_check_parity_first_run_green.sh` |
| line | 16 |
| module | `cortex_command.parity_check` |
| category | `module-execution-pathway-test` |
| rationale | This shell test (Task 12 of lifecycle 102) verifies that the parity linter exits 0 on a clean working tree. It invokes `python3 -m cortex_command.parity_check` directly — not via the `bin/` wrapper or the console-script — to test the module-execution pathway independently of PATH resolution. Replacing with the console-script name would change what the test validates. |
| r14_ticket | `208` |
| added_date | `2026-05-20` |

---

### tests/test_cortex_auto_bump_version_parity.py:225

| field | value |
|---|---|
| file | `tests/test_cortex_auto_bump_version_parity.py` |
| line | 225 |
| module | `cortex_command.auto_bump_version` |
| category | `module-execution-pathway-test` |
| rationale | Line 225 is the docstring of `_invoke_case`, which drives a `subprocess.run([sys.executable, "-m", "cortex_command.auto_bump_version"])` call at lines 247–252. The parity test intentionally exercises the module-execution path to validate byte-identical output between the promoted Python module and the captured bash fixtures. Using the console-script would test PATH resolution rather than the module's behavior, defeating the parity test's purpose. |
| r14_ticket | `208` |
| added_date | `2026-05-20` |

---

### tests/test_cortex_backlog_ready_parity.py:189

| field | value |
|---|---|
| file | `tests/test_cortex_backlog_ready_parity.py` |
| line | 189 |
| module | `cortex_command.backlog.ready` |
| category | `module-execution-pathway-test` |
| rationale | Line 189 is the docstring of `_invoke_case`, which drives a `subprocess.run([sys.executable, "-m", "cortex_command.backlog.ready"])` call in the function body. The parity test intentionally exercises the module-execution path to validate byte-identical output against captured bash fixtures. Replacing with the console-script would change what the test measures (PATH resolution vs module behavior). |
| r14_ticket | `208` |
| added_date | `2026-05-20` |

---

### tests/test_cortex_check_parity_parity.py:194

| field | value |
|---|---|
| file | `tests/test_cortex_check_parity_parity.py` |
| line | 194 |
| module | `cortex_command.parity_check` |
| category | `module-execution-pathway-test` |
| rationale | Line 194 is the docstring of `_invoke_case`, which drives `subprocess.run([sys.executable, "-m", "cortex_command.parity_check"])` in the function body. The parity test intentionally tests the module-execution path to validate stdout/stderr/exitcode against captured bash fixtures. Replacing with the console-script name would change what the test validates. |
| r14_ticket | `208` |
| added_date | `2026-05-20` |

---

### tests/test_cortex_check_prescriptive_prose_parity.py:99

| field | value |
|---|---|
| file | `tests/test_cortex_check_prescriptive_prose_parity.py` |
| line | 99 |
| module | `cortex_command.lint.prescriptive_prose` |
| category | `module-execution-pathway-test` |
| rationale | Line 99 is the docstring of `_invoke_case`, which drives `subprocess.run([sys.executable, "-m", "cortex_command.lint.prescriptive_prose"])` in the function body. The parity test intentionally exercises the module-execution path to validate byte-identical output against captured bash fixtures. Replacing with the console-script would change what the test measures. |
| r14_ticket | `208` |
| added_date | `2026-05-20` |

---

### tests/test_cortex_complexity_escalator_parity.py:162

| field | value |
|---|---|
| file | `tests/test_cortex_complexity_escalator_parity.py` |
| line | 162 |
| module | `cortex_command.lifecycle.complexity_escalator` |
| category | `module-execution-pathway-test` |
| rationale | Line 162 is in the docstring of the `_invoke_case` helper, which drives `subprocess.run([sys.executable, "-m", "cortex_command.lifecycle.complexity_escalator"])` in the function body. The parity test intentionally tests the module-execution path to validate byte-identical output against captured bash fixtures. |
| r14_ticket | `208` |
| added_date | `2026-05-20` |

---

### tests/test_cortex_lifecycle_counters_parity.py:20, :162

| field | value |
|---|---|
| file | `tests/test_cortex_lifecycle_counters_parity.py` |
| lines | 20, 162 |
| module | `cortex_command.lifecycle.counters` |
| category | `module-execution-pathway-test` |
| rationale | Line 20 is in the module docstring describing that the test "runs the module via `python3 -m cortex_command.lifecycle.counters`". Line 162 is in the docstring of `_invoke_case`, which drives `subprocess.run([sys.executable, "-m", "cortex_command.lifecycle.counters"])`. Both docstrings accurately describe the module-execution dispatch used by the parity test; replacing with the console-script name would misrepresent what the test validates. |
| r14_ticket | `208` |
| added_date | `2026-05-20` |

---

### tests/test_cortex_lifecycle_state_parity.py:150

| field | value |
|---|---|
| file | `tests/test_cortex_lifecycle_state_parity.py` |
| line | 150 |
| module | `cortex_command.lifecycle.state_cli` |
| category | `module-execution-pathway-test` |
| rationale | Line 150 is the docstring of `_invoke_case`, which drives `subprocess.run([sys.executable, "-m", "cortex_command.lifecycle.state_cli"])` in the function body. The parity test intentionally tests the module-execution path to validate byte-identical output against captured bash fixtures, independent of PATH-resolution. |
| r14_ticket | `208` |
| added_date | `2026-05-20` |

---

### tests/test_cortex_load_parent_epic_parity.py:170

| field | value |
|---|---|
| file | `tests/test_cortex_load_parent_epic_parity.py` |
| line | 170 |
| module | `cortex_command.backlog.load_parent_epic` |
| category | `module-execution-pathway-test` |
| rationale | Line 170 is the docstring of `_invoke_case`, which drives `subprocess.run([sys.executable, "-m", "cortex_command.backlog.load_parent_epic"])` in the function body. The parity test intentionally exercises the module-execution path to validate byte-identical output against captured bash fixtures. |
| r14_ticket | `208` |
| added_date | `2026-05-20` |

---

### tests/test_cortex_log_invocation_parity.py:364

| field | value |
|---|---|
| file | `tests/test_cortex_log_invocation_parity.py` |
| line | 364 |
| module | `cortex_command.log_invocation` |
| category | `module-execution-pathway-test` |
| rationale | Line 364 is the docstring of `_invoke_case`, which drives `subprocess.run([sys.executable, "-m", "cortex_command.log_invocation"])` in the function body. The parity test intentionally tests the module-execution path to validate stdout/stderr/exitcode against captured bash fixtures for the foundational `cortex-log-invocation` promotion (Phase 1). |
| r14_ticket | `208` |
| added_date | `2026-05-20` |

---

### tests/test_cortex_resolve_backlog_item_parity.py:121

| field | value |
|---|---|
| file | `tests/test_cortex_resolve_backlog_item_parity.py` |
| line | 121 |
| module | `cortex_command.backlog.resolve_item` |
| category | `module-execution-pathway-test` |
| rationale | Line 121 is the docstring of `_invoke_case`, which drives `subprocess.run([sys.executable, "-m", "cortex_command.backlog.resolve_item"])` in the function body. The parity test intentionally exercises the module-execution path to validate byte-identical output against captured bash fixtures for the DR4 promotion (Phase 2). |
| r14_ticket | `208` |
| added_date | `2026-05-20` |

---

### tests/test_critical_review_path_validation.py:18, :225

| field | value |
|---|---|
| file | `tests/test_critical_review_path_validation.py` |
| lines | 18, 225 |
| module | `cortex_command.critical_review` |
| category | `module-execution-pathway-test` |
| rationale | Line 18 is in the module docstring enumerating the two test layers, including "CLI subprocess — `python3 -m cortex_command.critical_review`". Line 225 is the docstring of `_run_cli`, describing what it invokes. The actual subprocess call uses `sys.executable, "-m", "cortex_command.critical_review"` — intentionally testing the module-execution pathway (not PATH resolution). These lines extend the existing allowlist entry (lines 12, 171) for the same file and rationale. |
| r14_ticket | `208` |
| added_date | `2026-05-20` |

---

### tests/test_path_self_test_hook_integration.py:63, :211

| field | value |
|---|---|
| file | `tests/test_path_self_test_hook_integration.py` |
| lines | 63, 211 |
| module | `cortex_command.doctor.path_self_test` |
| category | `document-content-invariant` |
| rationale | Both lines are documentation references describing the bash hook's invocation, not actual subprocess callsites. Line 63 is a `#` comment narrating what `cortex-session-start-path-bootstrap.sh` runs; line 211 is inside a Python docstring describing what the integration test exercises. The hook itself owns the `python3 -m cortex_command.doctor.path_self_test` invocation (allowlisted separately at `claude/hooks/cortex-session-start-path-bootstrap.sh:31`); rewriting these documentation strings would break the test's narrative correspondence with the hook source. |
| r14_ticket | `208` |
| added_date | `2026-05-20` |

---

### tests/test_morning_review_status_close_ordering.py:3, :55, :60, :131

| field | value |
|---|---|
| file | `tests/test_morning_review_status_close_ordering.py` |
| lines | 3, 55, 60, 131 |
| module | `cortex_command.backlog.update_item` |
| category | `document-content-invariant` |
| rationale | All four lines are documentation references (module docstring at line 3, section comment at line 55, test docstring at line 60, and helper docstring at line 131) that narrate the invocation form the test searches for in `skills/morning-review/references/walkthrough.md`. The actual literal asserted in the skill prose (`CLOSE_ARG = "--status complete"` at line 22) is the console-script form. Using the `python3 -m cortex_command.backlog.update_item` module-path form in these docstrings is deliberate per the lifecycle 257 spec (Task 9 Context): it keeps the test file out of the Phase 2 verification grep's match set (`^[[:space:]]*cortex-update-item`), while still narrating the operation accurately. Updating these docstrings to the console-script form would re-introduce the lifecycle's Phase 2 grep false-match. |
| r14_ticket | `208` |
| added_date | `2026-05-25` |
