---
name: hmm
description: Stop — that did not land. Re-pitch the last stretch in plain words.
disable-model-invocation: true
---

# Hmm

I don't follow where you've got to. Re-pitch this explanation — back up and explain, rather than just shorten.

Lead with the context I'm missing. Write in ASD-STE100 Simplified Technical English: plain approved words, active voice. Name things with the project's own vocabulary from `cortex/requirements/glossary.md`, and define any term that isn't in there on first use.

Use an example if it helps. Sketch a low-fidelity visual only when it carries the idea better than prose does.
